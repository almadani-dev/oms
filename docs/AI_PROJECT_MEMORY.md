# AI Project Memory

## Current Context

## Recent Changes

- **2026-07-05** — Added Phase 1 of "ميزان المراجعة" (Trial Balance) report: `TrialBalanceReportService`, `TrialBalancePage`, and its Blade view. Read-only, single-currency-scoped, period-movement-only (no opening/closing balance, no exports yet).
- **2026-07-05** — Added Phase 2: Excel/Word export buttons for Trial Balance (`TrialBalanceExcelExportService`, `TrialBalanceWordExportService`). Exports only ever read the applied/displayed report snapshot on `TrialBalancePage`, never live filter state.

- **2026-07-06** — Added Phase 1 of “تقرير الحركات المالية الشامل” (Comprehensive Financial Transactions report): `ComprehensiveFinancialTransactionsReportService`, `ComprehensiveFinancialTransactionsPage` (slug `comprehensive-financial-transactions`, nav sort 4 after Trial Balance), and its Blade view. Read-only journal listing, one row per transaction line, mixed currencies allowed in the detail table but every summary grouped per currency. Category = TransactionSuperType, type = TransactionType, project via transaction_lines.project_cost_id → projects_costs.project_id. No exports yet.

- **2026-07-06** — Enhanced the Comprehensive Financial Transactions summary section: category/type sections retitled to إحصائيات and now show distinct-transaction counts (`transaction_count` per bucket in `finalizeGrouped()`, tracked via transaction-id sets — never line count ÷ 2); general summary labels renamed to عدد المعاملات / عدد بنود القيود. Debit/credit totals stayed line-based and per-currency.

- **2026-07-06** — Added Phase 2: Excel/Word export buttons for the Comprehensive Financial Transactions report (`ComprehensiveFinancialTransactionsExcelExportService`, `ComprehensiveFinancialTransactionsWordExportService`). Same snapshot-only pattern as Trial Balance exports: they render the applied/displayed result only, gated by `canExport()` on `hasSubmitted`. Category/type stats export one row per bucket+currency with the distinct-transaction count written once per bucket.

- **2026-07-06** — Data reset: purged all rows (TRUNCATE, AUTO_INCREMENT reset) from 16 operational/business tables so the user can start entering clean real data — `accounts`, `attachments`, `bank_accounts`, `general_exchanges`, `general_expenses`, `partners`, `project_cost_budgets`, `project_cost_budgets_payments`, `project_cost_receipts`, `project_financial_alerts`, `project_financial_snapshot_currency_totals`, `project_financial_snapshots`, `projects`, `projects_costs`, `transaction_lines`, `transactions`. All settings/lookup/auth/system tables (`settings`, `roles`, `permissions`, `users`, `currencies`, `fiscal_years`, `bank_types`, `accounts_type`, `partners_types`, `projects_super`, `projects_status`, `transaction_super_types`, `transactions_types`, etc.) were left untouched. No migrations, no schema changes, no code changes. Full pre-purge backup at `storage/app/private/backups/oms_backup_2026-07-06.sql`. Storage files (attachments, receipts) were **not** deleted — only the DB rows referencing them.

- **2026-07-06** — Added optional opening balance support on account creation (`AccountForm` + `CreateAccount::handleRecordCreation`). If opening_balance > 0, one `DB::transaction()` creates the account, an `OPB-YYYY-XXXX` transaction of type "قيد افتتاحي" (under new super type "قيود افتتاحية", both firstOrCreate/restore-if-trashed), and 2 balanced lines: debit the new account, credit a per-currency clearing account "أرصدة افتتاحية" (account_code `OPB-{CUR}`, own AccountType "أرصدة افتتاحية", auto-created). Lines have `project_cost_id = null` so project reports/snapshots (which read only receipts/budgets/payments tables anyway) are untouched. `current_balance` is now `disabled()->dehydrated(false)` on the account form — display-only on create and edit. Balance updates reuse the standard increment(debit)/decrement(credit) pattern.

- **2026-07-06** — Removed the obsolete "المبالغ المرصودة" resource (`ProjectCostBudgetResource`, was auto-discovered at `admin/project-cost-budgets`). Deleted the whole `app/Filament/Resources/ProjectCostBudgets/` directory (resource class, its 4 Pages, `PaymentsRelationManager`, `ProjectCostBudgetForm`, `ProjectCostBudgetInfolist`, `ProjectCostBudgetsTable`) — all exclusive to this resource, confirmed via grep with no external references. This resource was a plain raw CRUD directly on the `project_cost_budgets` table (create/edit/delete with no transaction lines, no account-balance updates, no accounting entries at all) — a legacy bypass of the real business flow. The proper flow, `ProjectCostBudgetsPaymentResource` ("صرف مبلغ المشروع", `admin/project-cost-budgets-disbursements`), already creates every `project_cost_budgets` row itself (as part of a balanced `DB::transaction()` with 4 transaction lines and account balance updates) and was left completely untouched, along with the `ProjectCostBudget` model, its migration/table, and `ExecutionPayments` ("صرف مبالغ التنفيذ"). Confirmed model class, table, and existing rows all still intact after removal. Ran `optimize:clear`, `filament:optimize`, `icons:cache`, and `graphify update .` afterward.

- **2026-07-06** — Removed the obsolete "تقارير المشاريع" page (`ProjectsReportPage`, was auto-discovered at `admin/projects-report-page`). Deleted the page class and its dedicated Blade view (`projects-report-page.blade.php`) — both were used exclusively by this page, no custom route, no owned DB table (it only read existing `projects`/`projects_costs`/`project_cost_budgets` tables read-only), no other code referenced it. All other report pages (Trial Balance, Comprehensive Financial Transactions, Donor Financial Report, Account Statement, Projects General Financial, Project Financial Details) are unaffected and still registered. Ran `optimize:clear`, `filament:optimize`, `icons:cache`, and `graphify update .` afterward.

- **2026-07-06** — Fixed broken "صورة الإشعار" image on Project Cost Receipt view (`/admin/project-cost-receipts/{id}`). Root cause was environmental, not code: `public/storage` was a plain empty directory instead of a symlink, so `php artisan storage:link` refused to run ("link already exists") and every `Storage::disk('public')->url(...)` attachment URL 404'd. Confirmed empty, removed it, and re-ran `storage:link` to create the correct symlink to `storage/app/public`. No application code changed — `ProjectCostReceiptForm`'s `FileUpload` (disk `public`, directory `receipts`), the create/edit pages' file-move logic, and `ViewProjectCostReceipt`'s `Storage::disk('public')->url($attachment->file_path)` were all already correct per Filament/Laravel convention.

- **2026-07-06** — Added "تقرير الجهات المانحة" (Donor Financial Report): `DonorFinancialReportService`, `DonorFinancialReportPage` (nav group التقارير, slug `donor-financial-report`), and Excel/Word export services + Blade view — all in one pass, audited before commit. Read-only, live-query (no snapshot table), required `donor_id` (only `partners.is_donor = true`), optional date_from/date_to (filter `projects.approval_date` only), project_status_id, project_super_id, project_id (scoped to the selected donor's projects via `projects.donor_id`), and a display-only `currency_id` that hides non-matching currency rows/groups without ever excluding a project. Report only loads on "عرض"; any filter change calls `clearResults()` and re-blocks both exports until "عرض" is pressed again; exports render the already-computed on-screen snapshot, never live filter state — same pattern as Trial Balance / Comprehensive Financial Transactions. Three currency grains kept strictly separate, never summed together: (1) cost-side — `projects_costs`/`project_cost_receipts`, surplus = received − planned; (2) source/disbursement grain — `project_cost_budgets.source_currency_id` + admin/transfer deductions read from `transaction_lines` tagged `ProjectCostBudget::LINE_ADMIN`/`LINE_TRANSFER` (the authoritative tags written by `CreateProjectCostBudgetsPayment`, the real disbursement-creation flow); (3) execution grain — `project_cost_budgets.disbursement_currency_id` + `project_cost_budgets_payments`, remaining = final_amount − execution_paid. "Real" budgets = `transaction_id IS NOT NULL`, matching the existing `ProjectsReportPage` convention. Audit (code review + tinker smoke test) passed with no correctness bugs; see the 2026-07-06 Donor Financial Report entries in TASKS_LOG.md and NEXT_STEPS.md for the two non-blocking assumptions worth revisiting later.

- **2026-07-14** — Added automatic Arabic `transactions.description` generation across all 6 financial write flows (receipts, disbursement, execution payments, general expenses, general exchanges, opening balance). New shared service `app/Services/Transactions/TransactionDescriptionBuilder.php::buildAndSave()` reads a transaction's final, active (non-soft-deleted) lines — eager-loading `account` (`withTrashed()`, so a soft-deleted account still renders its historical name) and `currency` — groups credit (`credit_base > 0`) before debit (`debit_base > 0`) preserving line-id order (already the authoritative creation order in every flow: source/admin/transfer/destination etc.), formats each entry `"حساب {name} ({line's currency_id → code}) — {amount, 2dp, thousands-separated}"` (never duplicating an already-`حساب`-prefixed name, never using `account.currency` — the **line's own** `currency_id` is authoritative, critical for cross-currency disbursement/exchange lines), joins multiple entries per side with `؛ `, and normalizes the caller-supplied summary (trim/collapse whitespace/strip repeated trailing periods/append exactly one). Throws if either side has zero entries (rolls back the flow's existing `DB::transaction()` — no second transaction introduced). Each Create/Edit page builds its own short Arabic summary from final saved relations (e.g. receipt: `"استلام مبلغ من {partner} لتمويل مشروع {project}"` with graceful fallbacks when partner/project is absent) and calls the builder as the last step before its success notification, inside the existing transaction boundary; edit flows call it again after lines are reversed/replaced and the domain row updated, so the description always reflects final state. `transactions.notes` and `transaction_lines.notes` (including all `LINE_*` tags) are completely untouched — description is display/search/audit-only, never parsed for accounting logic. No accounting formula, balance-update, or currency-handling code was touched. No migration needed (`description` column already existed). Historical transactions were **not** backfilled (out of scope for this task). Verified via 15 unit tests (`tests/Unit/Services/TransactionDescriptionBuilderTest.php`, covering formatting/ordering/soft-delete/validation edge cases against a minimal hand-migrated SQLite schema) plus a tinker script that ran all 6 real create flows + 5 real edit-regeneration flows against real dev-DB data inside a rolled-back `DB::transaction()` (zero residue; transaction count unchanged at 10 before/after).
- **2026-07-14** — Fixed two pre-existing migration bugs discovered while adding `RefreshDatabase`-based tests (unrelated to the description feature; present since the single squashed initial commit `c40e0d8`, 2026-06-10): (1) `2026_06_09_130009_create_exchange_rate_history_table.php` created the table directly as `exchange_rate_histories` (plural) instead of `exchange_rate_history` (singular, matching its filename), which made the later `2026_06_09_174625_rename_exchange_rate_history_to_exchange_rate_histories.php` fail on any fresh migrate (`no such table: exchange_rate_history`); (2) `2026_06_09_130012_create_accounts_table.php` already defined `current_balance`/`iban` directly, duplicating what `2026_06_09_193018_add_columns_to_accounts_table.php` also adds, causing `duplicate column name` on fresh migrate. Both were invisible on the live dev DB because both migration pairs are already recorded `Ran` there from their real historical batches (create-then-rename / create-then-add-columns), so they never re-execute — only a genuinely fresh `migrate`/`RefreshDatabase` run hits them. Fixed by restoring create-migration `up()`/`down()` to their original singular/without-the-later-columns form, and making the rename migration idempotent (`Schema::hasTable()` guards in both directions). Confirmed via `migrate:status` before and after that the dev DB's recorded batches (3, 4, 5, 14, 19) were completely unaffected — no live migration was executed, only the migration **files** changed. A third, unrelated incompatibility remains and was deliberately **not** touched: `2026_06_24_000005_backfill_denormalized_currency_and_amounts.php` (the real "Currency denormalization Batch 1" backfill) uses MySQL-only `UPDATE ... JOIN` raw SQL that SQLite cannot execute at all — this is real financial backfill logic, out of scope to rewrite here. `TransactionDescriptionBuilderTest` therefore does not use `RefreshDatabase`; it migrates only the ~8 structural migrations for the tables it needs and runs with `PRAGMA foreign_keys = OFF` for that connection (see the test file's class docblock).

- **2026-07-14** — Added automatic per-line Arabic `transaction_lines.description` + structured `transaction_lines.line_role` across all 6 financial write flows (the line-level counterpart of the same-day `transactions.description` feature). New migration `2026_07_14_120000_add_description_and_line_role_to_transaction_lines_table.php` adds `description` (nullable text, after `notes`) and `line_role` (nullable string(50), after `description`) — no DB enum, no index, no backfill; historical rows stay NULL. New string-backed enum `app/Enums/TransactionLineRole.php` is the single role vocabulary (11 roles: `funding_source`/`receipt_destination` for receipts, shared `source`/`administrative_deduction`/`transfer_fee`/`destination` for disbursement+exchange (+`source` also for expense credit), `beneficiary`/`execution_source` for execution payments, `expense` for expense debit, `opening_balance_target`/`opening_balance_counterpart` for opening balances) with `arabicLabel()` + null-safe `labelFor()`. Roles are assigned explicitly at every `TransactionLine::create()` call site (and on the receipt edit flow's in-place `->update()` calls, so pre-feature receipts self-heal their NULL roles when edited) — never inferred from account/side/order/notes. New service `app/Services/Transactions/TransactionLineDescriptionBuilder.php::buildAndSaveForTransaction(Transaction, array $purposesByRole)` formats each active line as `"{مدين|دائن}: حساب {name} ({line currency code}) — {debit_base|credit_base, 2dp, thousands} | الغرض: {purpose}."` — purposes are supplied per-call by each flow (the same role wording differs by flow), amounts come from the posted side only (never `amount_currency`), currency from the line's own `currency_id`. Validation throws inside the caller's existing `DB::transaction()` (whole flow rolls back) on: missing/unknown `line_role`, missing purpose mapping, both-sides-positive, negative side, missing account/currency. **Zero-amount lines (0% admin/transfer deduction placeholders) are skipped with `description = NULL`** — user-approved deviation from the strict "both zero throws" spec, matching how `transactions.description` already omits them (throwing would have broken every 0%-deduction disbursement/exchange). Shared formatting (account-name `حساب`-prefix dedup, `number_format(2,'.',',')`, whitespace-collapse + exactly-one-final-period) extracted into trait `app/Services/Transactions/Support/FormatsTransactionText.php`, used by both builders — `TransactionDescriptionBuilder`'s public API and output stayed byte-identical (its 15 exact-string tests still pass). Also hardened the standalone `TransactionLineResource` (`admin/transaction-lines`, "سطور المعاملات") into a read-only audit resource — it was a raw line-level CRUD bypass (create/edit/delete with no DB::transaction, no balance updates) missed by the earlier `TransactionResource` hardening: routes reduced to index/view, `CreateTransactionLine`/`EditTransactionLine` deleted, 8 `can*` → `false`, table gets `defaultSort('id','desc')` + new `line_role` (Arabic label badge) and searchable `description` columns; `LinesRelationManager` (already read-only) also gained both display columns. Reports/exports/snapshots deliberately unchanged — nothing reads the new fields yet. Verified: 18 new unit tests + 15 existing (33/33), plus rolled-back tinker run of all 6 real create flows + 5 real edit flows against dev data (zero residue; balances/notes/descriptions byte-identical after rollback).

- **2026-07-14** — Converted the raw `TransactionResource` (`admin/transactions`, "المعاملات المالية") into a **strictly read-only audit resource**. It previously let users create a bare `Transaction` with zero lines (no `DB::transaction()`, no balance/currency logic, no double-entry enforcement) and edit any transaction's header fields (incl. `partner_id`, `fiscal_year_id`, `transaction_type_id`) independently of its owning financial-source record — a direct bypass of the same class of integrity guarantee the `ProjectCostBudgetResource` removal fixed on 2026-07-06. Changes: `getPages()` now only registers `index`/`view` (`create`/`edit` routes no longer exist — `admin/transactions/create` and `admin/transactions/{record}/edit` return 404); `CreateTransaction.php`/`EditTransaction.php` page classes deleted (confirmed unreferenced anywhere else); `TransactionResource` now overrides `canCreate()`/`canEdit()`/`canDelete()`/`canDeleteAny()`/`canRestore()`/`canRestoreAny()`/`canForceDelete()`/`canForceDeleteAny()` to all return `false` (authorization-layer hardening, not just hidden buttons); `ListTransactions`/`ViewTransaction` no longer have any header actions; `TransactionsTable` keeps only `ViewAction` (no `EditAction`/`DeleteBulkAction`); `LinesRelationManager` keeps the lines table visible (columns/search/sort) but has zero header/record actions and the same 8 `can*` overrides (as `protected` instance methods per `InteractsWithRelationshipTable`). Both `TransactionsTable` and `LinesRelationManager` now use `->defaultSort('id', 'desc')` (newest first; users can still click column headers to sort). Navigation entry "المعاملات المالية" stays visible. `TransactionForm`'s `description` field (already `disabled()->dehydrated(false)` from the same-day earlier fix) is untouched. Verified headlessly (no full Livewire/HTTP mount needed): built the real `Table` objects via reflection and confirmed `getFlatActions()` returns only `ViewAction` for both tables, `getDefaultSortColumn()`/`Direction()` return `id`/`desc` for both, and all 8 `can*` calls return `false`. Re-ran the 2026-07-14 six-flow tinker verification (rolled back) — byte-identical descriptions to before this change, confirming the 6 legitimate flows and `TransactionDescriptionBuilder` are completely unaffected.

- **2026-07-15** — Backfilled the historical (pre-2026-07-14) `transactions.description`/`transaction_lines.line_role`/`transaction_lines.description` gap left by the previous two entries, and surfaced all three fields in the detailed financial-movement sections of 4 reports. New idempotent `php artisan transactions:backfill-descriptions --dry-run|--apply` (new `App\Services\Transactions\Backfill\TransactionFlowClassifier` + `ClassifiedTransaction` DTO under a new `app/Services/Transactions/Backfill/` namespace) classifies a historical transaction only via a direct `transaction_id` FK on its flow's domain record, or `transactions_types.name = 'قيد افتتاحي'` for opening balance — `notes` `LINE_*` tags are used only to map lines to roles once the flow is already known, never to identify it. Reuses the exact live builders (`TransactionDescriptionBuilder::build()` + a new public `TransactionLineDescriptionBuilder::describeLine()`, an additive extraction with unchanged `buildAndSaveForTransaction()` behavior) so backfilled text is byte-identical to what a fresh create would produce, and is naturally idempotent (only calls `->save()` when a value differs — Eloquent's dirty-check means a canonical row issues no UPDATE). Applied against the dev DB: 10/10 transactions classified, 24/24 lines backfilled, 0 unclassified, verified idempotent on a second `--apply` run, and verified byte-identical amounts/notes/balances/record-counts before vs. after via a full field snapshot. Then added `وصف العملية المالية`/`دور سطر القيد`/`وصف سطر القيد` — additively, never replacing an existing field — to Comprehensive Financial Transactions (screen + Excel + a restructured per-transaction Word layout), Account Statement (screen + Excel + Word; its pre-existing `description` column was only relabeled, not changed), Project Financial Details (parent description at the existing receipt/budget/payment grain + a nested expandable line-detail widget, since this report is not line-grained), and Donor Financial Report (same nested-widget pattern on its `movements` array). Trial Balance and every summary-card/dashboard/percentage/collection-rate calculation were untouched. Also standardized the three approved labels on `TransactionResource`/`TransactionLineResource` (`الوصف`/`دور السطر`/`وصف السطر` → `وصف العملية المالية`/`دور سطر القيد`/`وصف سطر القيد`), which both remain strictly read-only. See DECISIONS_LOG.md (2026-07-15 entries) for the classification-priority and grain-preservation reasoning.

- **2026-07-15** — Implemented (dry-run only, apply NOT executed) a reusable operational-data cleanup tool: `App\Services\Maintenance\OperationalDataCleanupService` + `OperationalCleanupReport` DTO, and `php artisan oms:clean-operational-data {--dry-run|--apply --confirmation= --backup-file= [--skip-files]}`. Refuses to run outside `local`/`development`/`testing`; `--apply` additionally refuses without the exact token `DELETE-OMS-OPERATIONAL-DATA` and a verified non-empty `--backup-file`. Deletes (active + soft-deleted, force) in FK-safe child-before-parent order: `project_financial_alerts` → `project_financial_snapshot_currency_totals` → `project_financial_snapshots` → `transaction_lines` → `project_cost_budgets_payments` → `project_cost_receipts` → `project_cost_budgets` → `general_expenses` → `general_exchanges` → `transactions` → `projects_costs` → `projects` → attachments linked to those 5 attachable types → resets every `accounts.current_balance` to 0 (accounts themselves never deleted), all inside one `DB::transaction()`. Bulk deletes use `DB::table()->delete()` instead of Eloquent specifically so (a) one statement covers both active and trashed rows (query builder ignores the SoftDeletes scope) and (b) the `Project`/`ProjectCost`/`ProjectCostBudget`/`ProjectCostBudgetsPayment`/`ProjectCostReceipt` observers (which only flip `project_financial_snapshots.is_dirty`) never fire during the wipe — harmless but pointless since those snapshot rows are deleted in the same operation. Preserves users/roles/permissions, accounts + all accounting config, donors (`partners.is_donor = true`), currencies, full exchange-rate history, fiscal years, and all reference/type tables untouched. Donor classification uses **only** the authoritative `is_donor` boolean — there is no association/beneficiary/vendor flag distinct from it anywhere in the schema or the `PartnerForm`, so every non-donor partner is reported as `unknown_unclassified` and preserved; none are ever deletion candidates in this phase (see DECISIONS_LOG.md). Attachment file deletion is two-phase: DB rows deleted inside the transaction, physical files (on the `public` disk, under `payments/`, `general-expenses/`, `receipts/`, `general-exchanges/`, `execution-payments/`) deleted only after commit, matched by the exact `file_path` of a deleted attachment row — never by directory sweep. Files in those directories with no matching attachment row are reported as orphans and never touched. Discovered (report-only, untouched) an unexpected legacy table `bank_accounts` (0 rows, no Eloquent model, not created by any current migration) and confirmed the dev MySQL connection can see unrelated sibling databases (`masterdb`, `mydb`, `smart_ecommerce_db`) on the same shared server — the unexpected-table scan filters strictly to the `oms` schema to avoid false positives. Numbering (`transactions.transaction_number`, `projects.code`/`projects_super.code`) is entirely derived (`MAX()`/`count()` over existing rows, including trashed) — not DB AUTO_INCREMENT, not a stored sequence table — so it naturally restarts at 001 per prefix once all rows are gone; no explicit reset was implemented, per instructions. 20 new Feature tests (`tests/Feature/Commands/CleanOperationalDataCommandTest.php`) run against a real, selectively-migrated SQLite `:memory:` schema (same pattern as `BackfillTransactionDescriptionsCommandTest`, excluding the two MySQL-only migrations: the raw-SQL currency backfill and the `SHOW INDEX FROM`-guarded supporting-indexes migration) — all pass, plus the full existing suite (71/72; the 1 failure is the pre-existing unrelated `ExampleTest` hitting `/`, a route this Filament app never defines). `--dry-run` was run against the real dev DB and confirmed zero writes (all row counts/balances identical before and after). **`--apply` has not been run anywhere except the SQLite test suite** — awaiting explicit user approval per task instructions.

- **2026-07-15** — **Executed** the operational data cleanup approved above: `php artisan oms:clean-operational-data --apply --confirmation=DELETE-OMS-OPERATIONAL-DATA --backup-file="C:\laragon\www\oms\storage\app\backups\oms_before_operational_cleanup.sql"` (run at 12:21:16, then re-run at 12:22:27 to verify idempotence). The required backup file did not exist yet at approval time; created it first via `mysqldump --routines --triggers --single-transaction` against the live `oms` database (password passed via `MYSQL_PWD` env var, never on the command line or in any output) — 120,266 bytes, verified non-empty before proceeding. **Deleted** (active+trashed, all now 0 rows): 2 projects, 3 projects_costs, 1 project_cost_budget, 1 project_cost_budgets_payment, 3 project_cost_receipts, 2 general_expenses, 1 general_exchange, 12 transactions, 28 transaction_lines, 8 attachments (DB rows), 1 project_financial_snapshot, 2 project_financial_snapshot_currency_totals. **Files deleted:** exactly 6 (the linked attachments whose physical files existed — 1,474,023 bytes), 0 failures. **Preserved exactly as verified:** 2 donors, 15 accounts (all balances now 0.00, names/codes/types/currencies/relationships byte-identical), 2 users, 5 roles, 24 permissions, 3 currencies, 0 exchange-rate-history rows (unchanged), 1 fiscal year, 11 transaction types, 7 transaction super types, all other reference tables, 61 migrations, and the untouched `bank_accounts` table (still 0 rows). The 6 pre-existing orphan files (706,133 bytes, under `payments/`/`execution-payments/`) were **not** deleted — confirmed present and byte-identical after both apply runs; `receipts/`/`general-expenses/` directories are now empty but were not removed; the `public/storage` symlink was untouched (unchanged mtime). Second `--apply` run made **zero** additional database or file changes (idempotent) — 0 files deleted on the second pass, all counts identical. Post-apply verification (the service's own `verify()` against pre/post fingerprints) **PASSED** on both runs. Full 21-point manual verification checklist also confirmed independently via tinker + filesystem inspection. Not committed — neither this data-only operation nor the command/service/test code from the prior implementation phase; no commit was requested.

- **2026-07-16** — Made the Execution Payment (`صرف مبالغ التنفيذ`, `admin/execution-payments/create`/`/edit`) credit account **editable**, per the same-day audit's approved recommendation. Moved the "الحساب الدائن" section above "الحساب المدين (المستفيد)" in `ExecutionPaymentForm.php` and replaced the old display-only `credit_account_display` field with a real cascade (`credit_account_type_id` → `credit_bank_type_id` → `credit_currency` (display) → `credit_account_id`), an exact copy of the existing beneficiary-account cascade pattern. The credit account still **defaults automatically** from the selected budget's `ProjectCostBudget::LINE_DESTINATION` line (new protected helper `ExecutionPaymentForm::applyCreditDefaults()`, called from the `project_cost_budget_id` Select's `afterStateUpdated` — i.e. only on a genuine user-triggered budget selection/change, never during Edit hydration) but the user can now pick any other account in the **same currency** before saving — cross-currency replacement accounts are explicitly out of scope (no fx fields/logic added). **No migration was needed**: the actually-selected credit account is persisted exactly as before, as `transaction_lines.account_id` on the line tagged `notes = ProjectCostBudgetsPayment::LINE_CREDIT` / `line_role = TransactionLineRole::ExecutionSource` — `project_cost_budgets_payments` still has no account column. New server-side guard `ExecutionPaymentForm::validateCreditAccount(array $data): Account` (throws `Illuminate\Validation\ValidationException` with Arabic messages) re-checks, independent of the form's Select options, that: the submitted `credit_account_id` exists (default SoftDeletes scope), its `account_type_id`/`bank_type_id` match the submitted cascade, its `currency_id` equals `ExecutionPaymentForm::budgetCurrencyId($budgetId)`, and `beneficiary_account_id` is in that same currency too; called in both `CreateExecutionPayment::handleRecordCreation()` and `EditExecutionPayment::handleRecordUpdate()` **before** `DB::transaction()` opens, so a rejected submission leaves zero mutations. Both handlers now use the validated `$creditAccount->id` for the credit `TransactionLine`, the balance `decrement`, and (via the unchanged `TransactionLineDescriptionBuilder`/`TransactionDescriptionBuilder` calls reading final saved lines) the line/parent descriptions automatically reflect whichever account was actually used — neither builder was touched. **Also fixed a pre-existing drift bug** found during the audit: `EditExecutionPayment::mutateFormDataBeforeFill()` previously recomputed the credit-account display from the budget's *current* destination line, and `handleRecordUpdate()` previously recomputed `$creditAccountId` the same way — so editing an execution payment for any reason (even just the date) after the underlying budget's destination account had been changed elsewhere would silently move the historical credit line onto the new account. Hydration now reads `credit_account_id`/`credit_account_type_id`/`credit_bank_type_id`/`credit_currency` from the payment's **own saved `LINE_CREDIT` line** instead, and `handleRecordUpdate()` no longer recomputes from the budget at all — it trusts the validated submitted `credit_account_id`, which the reactive default only touches on a genuine budget change during that Edit session. `TransactionLineRole` enum, `TransactionDescriptionBuilder`, `TransactionLineDescriptionBuilder`, the disbursement flow (`ProjectCostBudgetsPaymentForm`/`Create`/`EditProjectCostBudgetsPayment`), reports/exports, and financial snapshots were all left untouched. New `tests/Feature/ExecutionPayments/ExecutionPaymentCreditAccountTest.php` (7 tests, selectively-migrated SQLite `:memory:` schema, same pattern as `CleanOperationalDataCommandTest`) covers: untouched-default create, manual-override create, cross-currency rejection (zero mutation), Edit hydration reading the saved line (not the drifted budget destination), Edit-with-unrelated-field-change preserving the historical account, Edit manual account change (exactly one active `LINE_CREDIT` line, correct balance reversal/reapplication), and the reactive default firing on a genuine budget change (tested via a mocked `Filament\Schemas\Components\Utilities\Set`). Full suite: 78/79 (the 1 failure is the pre-existing unrelated `ExampleTest`).

- **2026-07-16** — Applied an approved responsive UI layout standard (UI-only, no accounting logic changed) to the credit/debit account sections of `GeneralExpenseForm.php`, `ProjectCostReceiptForm.php`, and `ExecutionPaymentForm.php`: both account sections are now wrapped in a shared `Grid::make(['default' => 1, 'lg' => 2])`, with the credit section placed first in source order. Desktop/wide (≥lg): both sections render side by side with equal widths — credit on the right, debit on the left — via native CSS Grid RTL auto-placement (the app already renders `dir="rtl"`; no custom CSS added, per instructions). Narrow/mobile/tablet (<lg): stacks to one column, credit above debit. `GeneralExpenseForm.php` and `ProjectCostReceiptForm.php` previously had the debit section before the credit section as two separately-stacked full-width `Section`s (the actual bug being fixed — vertically separated, unbalanced); both were reordered (credit first) and merged into one `Grid`. `ExecutionPaymentForm.php` already had the credit section first (from the same-day credit-account-editable task) so only needed the `Grid` wrap for side-by-side desktop rendering — its editable credit-account cascade, auto-default-from-budget behavior, and Edit hydration-from-saved-line logic are all completely unchanged (verified by the existing 7-test `ExecutionPaymentCreditAccountTest`, all still passing). No field names, options queries, `afterStateUpdated`/`live()` callbacks, `dehydrated()` flags, validation, Create/Edit page handlers, models, or migrations were touched — only the schema's top-level component tree (each account section's *internal* `->columns(2)` field layout is untouched). Multi-account forms `ProjectCostBudgetsPaymentForm.php` (صرف مبلغ المشروع) and `GeneralExchangeForm.php` (التحويلات العامة) were deliberately left unchanged — see DECISIONS_LOG.md (2026-07-16) for why. Verified: `php -l` clean on all 3 files; `view:cache`/`view:clear` compiled with no Blade errors; `ExecutionPaymentCreditAccountTest` + `TransactionDescriptionBuilderTest` + `TransactionLineDescriptionBuilderTest` (40/40); full suite 78/79 (same 1 pre-existing unrelated `ExampleTest` failure, unchanged).

- **2026-07-16** — Rearranged the General Exchange form (`التحويلات العامة`, `admin/general-exchanges/create`/`/edit`) layout only, in `GeneralExchangeForm.php`: the two transaction-info sections ("تفاصيل المعاملة" and "النسب والمبالغ") are now wrapped in a top `Grid::make(['default' => 1, 'lg' => 2])` row, credit-section-analogous "تفاصيل المعاملة" placed first in source order so it renders on the **right** on desktop (≥lg, native CSS Grid RTL auto-placement, no custom CSS) with "النسب والمبالغ" on the **left**; on narrow/mobile screens (<lg) they stack with "تفاصيل المعاملة" first/above. The full-width "الحسابات" section (4 accounts: مصدر دائن، نسبة إدارية مدين، تحويل مدين، وجهة مدين) now sits underneath that row, unchanged internally and still full width; "المرفقات" stays last. This form was previously ordered "النسب والمبالغ" → "الحسابات" → "تفاصيل المعاملة" → "المرفقات" (the "الحسابات" section sitting beside/between the two transaction sections, unbalanced) — the two transaction sections were moved together to the top and "الحسابات" moved down, with the internal 16-field account block copy-pasted byte-identical (same field order, options queries, `afterStateUpdated`/`live()` callbacks, `dehydrated()` flags, helper methods `calculate()`/`deriveAmounts()`/`clearAmounts()`/`currencyName()`/`accountOptions()` — all untouched). No field names, validation, calculations, currency logic, account filtering, or the `CreateGeneralExchange.php`/`EditGeneralExchange.php` handlers were touched. Verified: `php -l` clean; `view:cache`/`view:clear` compiled with no Blade errors; `CleanOperationalDataCommandTest` + `BackfillTransactionDescriptionsCommandTest` + `TransactionDescriptionBuilderTest` + `TransactionLineDescriptionBuilderTest` (70/70, the closest existing coverage since no dedicated `GeneralExchangeForm` test exists).

- **2026-07-18** — Implemented positive-amount validation across all 5 financial workflows (project cost receipts, project cost budget disbursements, execution payments, general expenses, general exchanges), following the same-day read-only audit that found zero server-side protection against zero/negative amounts, FX rates, or deduction percentages anywhere in these flows. New reusable static guard `App\Services\Validation\FinancialAmountGuard` (Arabic `ValidationException` messages, matching the `ExecutionPaymentForm::validateCreditAccount()` convention already in the codebase): `assertSimpleAmount()`/`assertPositiveAmount()` (amount ≥ 0.01), `assertPositiveFxRate()` (fx_rate ≥ 0.000001, matching the `decimal(15,6)` column precision), `assertValidPercentage()` (0–100 inclusive per field), `assertCombinedPercentagesBelowFull()` (administrative + transfer percentage strictly < 100), `assertPositiveDerivedAmount()` (amount_after_deductions / final_amount > 0), and a composed `assertDisbursementInputs()` for the two deduction/FX flows. Also added matching Filament UI constraints (`->minValue(0.01)` on every amount field, `->minValue(0)->maxValue(100)` on both percentage fields, `->minValue(0.000001)` on both `fx_rate` fields) to `ProjectCostReceiptForm`, `ExecutionPaymentForm`, `GeneralExpenseForm`, `ProjectCostBudgetsPaymentForm`, `GeneralExchangeForm` — UI rules alone were explicitly treated as bypassable (per the audit's finding that Livewire form state can be submitted directly), so the guard is called independently in all 10 Create/Edit page handlers (`handleRecordCreation`/`handleRecordUpdate`), always **before** `DB::transaction()` opens — including on Edit paths, so an invalid submission can never partially reverse an existing valid record's balances/lines before failing. Removed the silent `(float) ($data['fx_rate'] ?: 1)` fallback (which converted an explicitly-submitted `0` into `1` without any error) from the 4 submitted-data sites in `CreateProjectCostBudgetsPayment`/`EditProjectCostBudgetsPayment`/`CreateGeneralExchange`/`EditGeneralExchange`, replaced with `?? 1` (missing-key-only default; an explicit `0` now reaches the guard and is rejected) — the *display-only* `?: 1` fallbacks in `mutateFormDataBeforeFill()` hydration and the forms' "احسب" preview `calculate()` helpers were deliberately left untouched (they read already-saved historical records or are non-authoritative live previews, not submitted mutation input). `GeneralExchangeForm::deriveAmounts()`/`ProjectCostBudgetsPaymentForm`'s inline equivalent — the authoritative accounting formula — were not altered, only guarded before being called. Execution-payment over-budget behavior (non-blocking warning notification) was explicitly left unchanged, per instructions. Server-side account/currency/type revalidation for the 4 workflows other than execution payments (a separate gap the same audit found) is explicitly out of scope for this task.

- **2026-07-18** — Implemented server-side financial account validation for the 4 workflows that had none (project cost receipts, project cost budget disbursements, general expenses, general exchanges), following the same-day read-only audit that found these flows relied entirely on Filament Select filtering — `account_type_id`/`bank_type_id` are Livewire component state only, never persisted, so a directly-submitted account_id could bypass the rendered options with no server check. New `App\Services\Validation\FinancialAccountGuard`: `assertAccountMatches()` (existence via `Account::find()` — SoftDeletes-scoped so a trashed account is already "not found" — plus account-type, bank-type, currency, and an optional `$requireActive` check), a batch `assertAccounts(array $specs): array` returning verified `Account` models keyed by role (used by all 4 workflows to validate every debit/credit/source/admin/transfer/destination account in one call), and `requireActiveOnChange(?int $original, $submitted): bool`. Called in all 8 Create/Edit page handlers before `DB::transaction()` opens — Edit pages first re-fetch the record's *pre-mutation* saved lines (moved out of the transaction closure specifically for this) to get each role's original account_id, then pass `require_active` = `requireActiveOnChange($original, $submitted)` per role: an account unchanged from the historical saved value may remain inactive (approved rule — historical records may reference an account active at creation time but since deactivated), but a *newly selected* replacement must be active; on Create every account must be active. Every `Account::find($data['xxx_account_id'])?->increment/decrement(...)` balance-update call (the same "silent skip on bad account_id" pattern flagged in the audit) was replaced with the already-guard-verified `Account` model instance (`$accounts['xxx']->increment(...)`, no `?->` needed — existence is already proven). **Approved rule: the same account may legally be used for both debit and credit (or any combination of roles)** — no distinct-account check exists or was added anywhere, including execution payments. `ExecutionPaymentForm::validateCreditAccount()` (execution payments were otherwise out of scope, already having its own guard) gained the same active-account behavior via two new optional named parameters `$requireActiveCredit`/`$requireActiveBeneficiary` (default `true`), purely additive — its 4 existing existence/type/bank/currency checks and their exact messages are untouched; `CreateExecutionPayment.php` needed no change (defaults already match Create semantics), `EditExecutionPayment.php` now fetches old lines before calling it and passes the same `requireActiveOnChange()` computation. No migration, no accounting-formula change, no transaction-number-generation change, no historical data touched. Verified: 60 new/targeted tests (`FinancialAccountGuardTest` unit + 4 new `*AccountValidationTest` Feature files covering all 4 workflows + 3 new tests added to `ExecutionPaymentCreditAccountTest`), full suite 150/151 (1 pre-existing unrelated `ExampleTest` failure).

- **2026-07-18** — Implemented (design → implementation → automated verification → fx_rate correction, four passes) a centralized `App\Services\Validation\FinancialTransactionBalanceGuard`, the final layer of server-side financial defense: it validates the *exact* `TransactionLine` payload array about to be written — built in memory from the already-guarded inputs, but re-verified independently — across all 10 Create/Edit page handlers (5 workflows), before `DB::transaction()` opens, after `FinancialAmountGuard`/`FinancialAccountGuard` have already run. Confirmed from the actual code (not assumed) that `debit_base`/`credit_base` are **not** normalized to a common base currency: in the two multi-currency workflows (project cost budget disbursements, general exchanges), the source/admin/transfer lines carry the source currency and the destination line carries the disbursement currency in the *same* `debit_base`/`credit_base` columns — so a naive `SUM(debit_base) == SUM(credit_base)` silently mixes two currencies whenever `fx_rate != 1` and was never implemented anywhere; the guard instead checks two same-currency equations independently (source-currency conservation, derived — never trusted from a stored field — as `source.credit_base − admin.debit_base − transfer.debit_base`; and the destination FX equation `destination.debit_base == amount_after_deductions × fx_rate`), plus a same-currency `SUM(debit_base) == SUM(credit_base)` for the 3 truly single-currency workflows (receipts, execution payments, general expenses). All monetary comparisons convert to **integer minor units** (`(int) round(value * 100)`) and compare for **exact equality** — no `abs($a-$b) <= 0.01` tolerance anywhere, per explicit instruction; a one-cent discrepancy is treated as a real bug, not rounding noise. The guard also independently validates, per line: exactly one side (debit xor credit) is positive, never both/neither/negative; `amount_currency` is positive and exactly matches whichever side is active; `fx_rate` is a **structurally required key with no default and no fallback anywhere in the guard** — a payload that omits it is rejected outright, in both `assertValidLinePayload()` and, independently, `assertBalancedSingleCurrencyLines()` — and its value must be positive, and must equal exactly 1 for single-currency lines; `line_role` is present and unique within the payload; the line's actual debit/credit side matches its role's expected direction (an internal role→side map covering the 9 roles used by these 5 workflows); and, when a caller opts in via the optional `$expectedNotesByRole` parameter, the `notes` tag matches the expected authoritative value for that role (workflow-agnostic duplicate-notes detection always runs; the exact-match check is opt-in and — deliberately, to keep this task's diff scoped to the guard only — not yet wired into any of the 10 production call sites, only exercised directly by unit tests). Refactored all 10 Create/Edit handlers to a **build → validate → insert-unchanged** shape: each workflow's line-construction helper (`buildLines()`/`createLines()`/`buildReceiptLines()`) now *returns* the line array(s) instead of calling `TransactionLine::create()` directly; the guard validates that returned array; only then does `DB::transaction()` open and insert the identical array (`TransactionLine::create($line + ['transaction_id' => $id])`) with zero recalculation. `EditProjectCostReceipt` is the one structural exception — it `->update()`s its two existing `TransactionLine` rows in place (never `forceDelete()` + recreate, unlike the other four workflows) — so its helper returns the exact `->update()` payload arrays instead, validated the same way before being applied unchanged; that update payload now **explicitly writes `fx_rate => 1`** on both its debit and credit arrays (a same-day correction — the first version omitted this key and let the guard assume `1.0`, which both violated "no silently assumed financial value" and meant a historically-corrupted stored `fx_rate` would never actually be checked or corrected). One consequence, now covered by a dedicated test: a **valid** receipt Edit normalizes any historically-corrupted stored `fx_rate` back to `1`, since the payload always writes it explicitly; a **rejected** Edit still leaves the old line — corrupted `fx_rate` included — completely untouched. Zero changes to any accounting formula, any delete/reversal method (`deleteReceipt`/`deletePayment`/`deleteExchange`/`deleteExpense` in the 5 `*Table.php` classes), or any of the 10 files' calculation lines — confirmed by targeted grep both before and after the correction, not just by memory. Verified: 36 guard unit tests (`FinancialTransactionBalanceGuardTest`, hand-built line arrays, no database) covering every rejection branch (both-sides-positive, both-zero, negative, bad `amount_currency`, bad/missing `fx_rate`, missing key, missing/duplicate role, wrong notes tag, wrong role direction, wrong currency, 1-cent imbalance, 1-cent FX error, reversed FX direction, 6-decimal FX rate); 23 integration tests (`BalanceGuardIntegrationTest`, one per workflow, real reflection-invoked Create/Edit against a selectively-migrated SQLite schema) proving valid submissions still complete end-to-end and any rejection leaves transaction/line counts and account balances completely untouched, a dedicated regression pair for `EditProjectCostReceipt` proving the exact validated update payload lands on the same row IDs and that a rejected edit partially-updates nothing, plus the fx_rate-normalization regression above; all 151 pre-existing tests for the 5 workflows and both earlier guards (`FinancialAmountGuardTest`/`FinancialAccountGuardTest` + every `*AmountValidationTest`/`*AccountValidationTest`) still pass unmodified; full suite 209/210 (the 1 failure is the same pre-existing unrelated `ExampleTest` 404-on-`/` seen in every prior entry above). Committed as `validate transaction balance with multi-currency support`. See `docs/DECISIONS_LOG.md` (2026-07-18 entries) for the minor-units-exact-equality, guard-scope-vs-workflow-files, and fx_rate-required-key reasoning, and `docs/TASKS_LOG.md` for the full file list.

- **2026-07-20 (OMS Permissions Task 2B — custom report page and export authorization)**: All 6 custom Filament report pages (`AccountStatementPage`, `TrialBalancePage`, `DonorFinancialReportPage`, `ComprehensiveFinancialTransactionsPage`, `ProjectsGeneralFinancialPage`, `ProjectFinancialDetailsPage`) are now permission-gated, each with its own `reports.<page>.view`/`reports.<page>.export` pair from the pre-existing `PermissionRegistry` (unchanged — the registry already had these 12 permissions and correct role defaults from Task 1). One reusable trait, `App\Filament\Pages\Concerns\AuthorizesReportAccess`, is `use`d by all 6 pages; each page explicitly declares its own two permission names via `reportViewPermission()`/`reportExportPermission()` (no name-based mapping from the class name). The trait provides: `canAccess(): bool` (overrides Filament's `Concerns\CanAuthorizeAccess::canAccess()`, which every `Filament\Pages\Page` already calls from `mountCanAuthorizeAccess()`/`hydrateCanAuthorizeAccess()` — i.e. on the initial mount **and** on every subsequent Livewire request for that component, including action calls — and from the static `registerNavigationItems()` at sidebar-build time); `authorizeReportExport()` (call as the first statement of every export method — `abort_unless` both the view **and** export permission, independent of `canAccess()` having already run, independent of the header action's `visible()` state); and `canExportReport()` (used only by the header actions' `->visible()` for the UI layer). This gives the required two-layer export protection: UI (`->visible(fn () => $this->canExportReport())` added to every `exportExcel`/`exportWord`/`exportXlsx` header `Action`) and server (`$this->authorizeReportExport();` added as the literal first line inside every `exportExcel()`/`exportWord()`/`exportXlsx()` method body, before any `canExport()`/business-guard check) — proven by real `Livewire::test()->call('exportExcel')->assertForbidden()` calls that bypass the hidden action entirely, not just by hiding the button. `ProjectFinancialDetailsPage::$shouldRegisterNavigation = false` is untouched (`Page::registerNavigationItems()` checks that flag before `canAccess()`, so it stays permanently absent from the sidebar regardless of permission) — it is still fully permission-gated on its direct `{project}` route. Zero changes to any report calculation/query/filter/export-service/format/Arabic label, and zero changes to the Task 2A Resource Policies. New tests: `tests/Feature/Reports/ReportPageAccessTest.php` (30 tests — real HTTP `$this->get()`/`assertForbidden()`/`assertOk()` for every page, plus navigation-label `assertSee`/`assertDontSee` against the Dashboard sidebar for the 5 nav-registered pages, cross-report permission isolation, Super Admin, and `ProjectFinancialDetailsPage`'s permanently-disabled navigation flag) and `tests/Feature/Reports/ReportExportAuthorizationTest.php` (28 tests — `assertActionHidden`/`assertActionVisible` for the UI layer, direct `->call('exportMethod')->assertForbidden()` for the server layer bypass proof, `Notification::assertNotified()` proving the exact pre-existing `canExport()` warning text is unchanged for all 4 filter-driven reports, and a `hasSubmitted`-resets-on-filter-change regression for Comprehensive Financial Transactions). Both files register a test-only SQLite emulation of MySQL's `FIELD()` function (via `PDO::sqliteCreateFunction`) purely so `ProjectFinancialDetailsPage`'s pre-existing, unmodified alerts-ordering query is executable under the SQLite test database — no production query was touched. User/Role/Permission management pages (`UserResource` beyond Task 1's lockdown, `RoleResource`, `PermissionResource`) remain **pending** — not built in this task. Full suite 574/574 excluding the same 1 pre-existing unrelated `ExampleTest` 404-on-`/` failure seen in every prior entry above (571 passed, 2 skipped, 1 pre-existing failure).

- **2026-07-21 (OMS Permissions Task 5 — read-only permission management)**: Added a Filament `PermissionResource` (`النظام` → `الصلاحيات`) that lets authorized users list/search/filter/view every `Permission` row and — Super Admin only — safely re-run permission synchronization, while structurally forbidding creation, renaming, and deletion of permissions through the UI, including for a real Super Admin. One new registered permission, `permissions.sync` (`مزامنة الصلاحيات`, new `PermissionRegistry::SYSTEM_PERMISSIONS`/`'system_permissions'` group labelled `النظام والصلاحيات`), takes the registry from 154 to **155** total permissions; it is granted to Super Admin only by construction — `adminDefaults()` already filters out anything starting with `permissions.`, and `accountantDefaults()`/`projectManagerDefaults()`/`viewerDefaults()` build explicit allow-lists that never reference the new group, so no code change was needed in any of those four methods to keep it out of their sets. A new pure helper `PermissionRegistry::moduleForPermission(string $name): ?string` (scans `groups()`, returns the module key or null for an unregistered/custom permission) backs the table/infolist's module column without duplicating grouping logic. New `App\Policies\PermissionPolicy` (registered explicitly via `Gate::policy(Permission::class, PermissionPolicy::class)` in `AppServiceProvider`, since Spatie's `Permission` model sits outside `App\Models` — same reasoning as `RolePolicy`) maps `viewAny`/`view` to `permissions.view_any`/`permissions.view` and returns `false` unconditionally for every mutation ability (`create`/`update`/`delete`/`deleteAny`/`restore`/`restoreAny`/`forceDelete`/`forceDeleteAny`). Because `Gate::before` bypasses every Policy check for a real Super Admin (exactly the same structural gap `RoleResource` already works around), Policy denial alone is insufficient — `PermissionResource` additionally hard-overrides `canCreate()`/`canEdit()`/`canDelete()`/`canDeleteAny()`/`canForceDelete()`/`canForceDeleteAny()`/`canRestore()`/`canRestoreAny()` to return `false` directly, never through Gate/`can()`, and only registers `index`/`view` routes (`getPages()` — no `CreatePermission`/`EditPermission` page classes exist at all, so `/admin/permissions/create` and `/admin/permissions/{id}/edit` are genuine 404s, not 403s). `PermissionsTable` (`->modifyQueryUsing(...)->withCount('roles')->with('roles')`, preventing N+1 on both the roles-count and roles-name columns) shows technical name, Arabic label (`PermissionRegistry::all()` reverse-lookup, itself searchable), module (via `moduleForPermission()`), guard, roles count, associated role names, and a registered/custom status badge, with `module`/`guard_name`/`status` `SelectFilter`s; only `ViewAction` is registered (no Edit/Delete/bulk/force-delete/restore action anywhere). A DB `Permission` row outside the registry (legacy/custom) is never hidden — it falls back to its technical name as the Arabic label, is placed under `صلاحيات مخصصة`, and is flagged `مخصصة`; nothing is ever deleted or renamed by this resource. New `App\Services\Permissions\PermissionManagementService` is a thin authorization wrapper — never duplicates `PermissionSyncService`'s sync logic — whose `sync(?User $actor)` requires, in combination: a non-null authenticated actor, the actor's **exact** `Super Admin` role via `hasRole()` (a plain relation check, never short-circuited by `Gate::before`), and the `permissions.sync` ability; it throws `AuthorizationException` before calling `PermissionSyncService::sync()` if any condition fails, so a non-Super-Admin manually granted `permissions.sync` is still rejected (the `hasRole()` check alone stops them) and a rejected call never touches the database. The one new header action on `ListPermissions`, "مزامنة الصلاحيات" (`requiresConfirmation()`, calls `PermissionManagementService::sync()` through plain PHP — no Artisan shell-out from the UI), is hidden via `->visible()` for non-Super-Admins as UX only; the real guarantee proved in tests is that Filament's own `Action::isDisabled()` (which folds in `isHidden()`) already blocks a hidden action from ever mounting even via a raw crafted `mountAction()`/`callMountedAction()` Livewire call, and — independently, at the layer that actually matters — `PermissionManagementServiceTest` proves the service itself rejects the same actor with zero UI involved at all, so the rejection does not depend on `->visible()` staying wired correctly. On success the action shows a single Arabic notification with permissions-created/permissions-found/roles-created/roles-found counts (exactly `PermissionSyncService::sync()`'s existing return-array keys — no new key names invented) and no sensitive user data; the table naturally re-renders on the next Livewire round-trip, showing newly-created permissions with no extra refresh call needed. `RoleManagementService::isProtectedPermissionName()` already treated any `permissions.*` name as protected before this task, so `permissions.sync` is automatically excluded from `assignablePermissionNames()` for non-Super-Admins and automatically appears correctly grouped in `RoleForm`'s checkboxes — zero changes were needed to `RoleResource`/`RoleManagementService`/`RoleForm`. New tests: `PermissionPolicyTest` (8), `PermissionResourceHttpTest` (8), `PermissionResourceLivewireTest` (11, incl. an N+1 query-count bound), `PermissionSyncActionTest` (8, incl. the crafted-Livewire-call and rejected-sync-leaves-tables-unchanged proofs), `PermissionManagementServiceTest` (4) — 39 new tests across 5 new files — plus 1 new test each added to the existing `SystemRoleDefaultPermissionsTest`/`SyncPermissionsCommandTest`. No migration, no Filament Shield, no custom Spatie Permission model subclass (none needed), zero changes to `UserResource`/`RoleResource` behavior, financial logic, report/export authorization, or any existing Resource Policy. Full suite 760/763 excluding the same 3 pre-existing, unrelated issues seen in every prior entry above (1 `ExampleTest` failure hitting `/`, 2 skips in `ResourceHttpAuthorizationTest` for the pre-existing read-only `transactions`/`transaction_lines` rows, 1 pre-existing risky flag in `tests/Feature/Users` — none of the three touched by this task, confirmed via `git status --short` showing zero changes to any file in those areas).

- **2026-07-20 (OMS Permissions Task 4 — secure role management)**: Added a Filament `RoleResource` (`النظام` → `الأدوار`) letting authorized users view all roles, create/edit/delete custom roles, and assign permissions through grouped Arabic checkboxes — while the five system roles (Super Admin, Admin, Accountant, Project Manager, Viewer) stay visible/viewable but structurally uneditable/undeletable, including for a real Super Admin. New `App\Services\Roles\RoleManagementService` is the single authoritative mutation layer (mirrors `UserManagementService`'s architecture): `isSystemRole(Role)` (exact-name match against `PermissionRegistry::SYSTEM_ROLES`); `canManageRole(actor, role)` — returns false for any system role or any role the actor is currently assigned to (checked unconditionally, before any Super-Admin branch, so it also protects a real Super Admin — this method never calls `Gate`/`can()`, only `hasRole()` and plain permission-name arrays, so it is never short-circuited by `Gate::before`), otherwise true for Super Admin, otherwise true only when every permission the role currently holds is both free of protected names and effectively held by the actor; `assignablePermissionNames(actor)` — every existing Permission row under the project's guard for Super Admin, or the actor's own effective permissions minus protected ones otherwise. Protected permissions (for a non-Super-Admin) are exact `users.assign_super_admin` or any name starting with `roles.`/`permissions.` — exact/prefix matching only, mirroring `UserManagementService::containsProtectedPermission()`. `createRole()`/`updateRole()`/`deleteRole()` all require the matching `roles.create/update/delete` ability (checked via `$actor->can()`, thrown as `AuthorizationException` if missing — Gate::before still legitimately grants this to Super Admin, since the ability check itself has no target-specific component), run inside `DB::transaction()`, re-derive `canManageRole()`/system-role rejection from a freshly-`fresh()`-fetched role (never trusts the instance passed in), reject a protected/duplicate name, revalidate every submitted permission name against `assignablePermissionNames()` (never trusts submitted IDs), and call `PermissionRegistrar::forgetCachedPermissions()` after every mutation. `deleteRole()` additionally rejects a role currently assigned to any user (`$role->users()->exists()`) — no automatic detach. There is no SoftDelete for roles. **Critical architectural point**: `Gate::before` bypasses `RolePolicy` entirely for a real Super Admin, so `RolePolicy::update()`/`delete()` correctly returning `false` for a system role is *never actually consulted* for that actor via the normal `$user->can('update', $role)` path — the real, load-bearing protection is `RoleResource::canEdit()`/`canDelete()`, which call `RoleManagementService::canManageRole()` **directly** (bypassing Gate/Policy entirely) alongside a plain `$user->can('roles.update'|'roles.delete')` ability check, and every table/page action's `->visible()` closure calls those same overridden `RoleResource::canEdit()`/`canDelete()` rather than relying on Filament's default action-authorization resolution (which itself resolves through the Gate and would otherwise inherit the same bypass). `Spatie\Permission\Models\Role` lives outside `App\Models`, so Laravel's naming-convention policy auto-discovery (confirmed by tracing `Gate::guessPolicyName()`) never finds `App\Policies\RolePolicy` for it — registered explicitly via `Gate::policy(Role::class, RolePolicy::class)` in `AppServiceProvider::boot()`. Permission checkboxes are grouped by `PermissionRegistry::groups()` module (one collapsible `Section` + `CheckboxList` per module, bound to nested `permissions.{module}` form state, flattened to one list<string> by the Create/Edit pages before ever reaching the service — deliberately not `->relationship()`-backed, since that would sync permissions directly from the form and bypass the service); any existing DB `Permission` row outside the registry is shown under an additional `صلاحيات مخصصة` (custom permissions) section, labelled by its technical name, and is never silently dropped from an existing role's options (a role's own current permission names — registered or not — are always merged into the options, exactly like `UserForm`'s roles Select merges in a target's current, possibly-no-longer-assignable roles). Filament's own `CheckboxList` "in" validation (rejecting a submitted value outside current options) is a first line of defense before the request even reaches `RoleManagementService`'s independent revalidation — proven by a Livewire test asserting the exact `data.permissions.{module}.{index}` error key. No `PermissionResource` was created (remains pending — Task 5); no Filament Shield installed; no custom Spatie Role/Permission model subclass created (none needed). Full suite 711/714 excluding the same 1 pre-existing unrelated `ExampleTest` 404-on-`/` failure seen in every prior entry above (2 skipped) — confirmed to also fail identically on a clean `git stash`.

- **2026-07-21 (OMS authorization acceptance testing — Tasks 1–5)**: Ran the final OMS authorization acceptance test pass across Permissions Tasks 1–5 — testing only, no production authorization redesign. Audited the ~80 existing test methods across `tests/Feature/{Permissions,Roles,Users,Reports}` first; coverage was strong, so exactly one new file was added, `tests/Feature/Permissions/AuthorizationAcceptanceTest.php` (22 tests, 200 assertions, all passing), closing only the confirmed gaps: an absolute (not merely relative) 155-permission-count assertion; real rendered-sidebar navigation per actually-assigned system role (Super Admin/Admin/Accountant/Project Manager/Viewer), driven entirely by `PermissionRegistry::defaultPermissionsForRole()` rather than a second hardcoded matrix; real HTTP view/edit access for Projects, Project Costs, Project Cost Receipts, General Expenses, and General Exchanges (the existing `ResourceHttpAuthorizationTest` only exercised index/create for these); the 404 (not 403) for the two read-only modules' (`transactions`/`transaction_lines`) nonexistent create routes; an actually-assigned Viewer role failing to reach create routes and an actually-assigned Super Admin completing a real create mutation (previously proven only at the permission-set/policy level, not with a real assigned role); generic cross-module negative pairs (roles↔users, permissions↔roles, finance↔projects and vice versa, finance/projects↔reports); and a crafted Users Livewire create-form submission attempting to assign Super Admin (complementing the existing service-level-only proof). Test data was created entirely via the real `PermissionSyncService` (one active user per system role, exactly one role each) — never a hardcoded role/permission matrix — and the file reuses `ResourceHttpAuthorizationTest::resourceProvider()`/`ReportPageAccessTest::reportProvider()` rather than inventing a second map. **No authorization defect was found anywhere in the tested surface.** Five-role sidebar navigation, direct URL access, create/update/delete mutation authorization, report access, export authorization, and Users/Roles/Permissions system-page management were all verified end-to-end; cross-module permission leakage was checked and not found. Confirmed the acceptance spec's `users.assign_roles` permission does not exist in `PermissionRegistry` and is not a defect — normal role assignment remains controlled by `users.create`/`users.update` plus `UserManagementService`'s privilege-subset validation (already fully tested by `RoleAssignmentSafetyTest`/`PrivilegeSubsetProtectionTest`), and `users.assign_super_admin` remains the separate, independently-checked permission for assigning Super Admin — this is the confirmed, approved design, not a gap; no registry change was made. Full suite 782/785, matching the same pre-existing unrelated `ExampleTest` failure and the same pre-existing unrelated risky Task 3 test recorded in every prior entry above — neither touched. Zero production files changed; every test ran against the in-memory SQLite test database already configured in `phpunit.xml` — no real database write, no real user/role/permission changed, no migration, no seeding of the real database. A short manual browser verification remains before the authorization milestone is formally closed (see NEXT_STEPS.md).

- **2026-07-21 (OMS Task 6A — private attachment security foundation)**: Closed the attachment-security gap the same-day audit found: every one of the 5 financial workflows' uploaded files (receipts, disbursements, execution payments, general expenses, general exchanges) was served from the `public` disk via a raw `Storage::disk('public')->url()` HTML link on each View page — reachable by anyone with the guessable URL, with zero authentication or authorization, once known. Built the foundation only (the 5 Resources' Schemas/View pages were deliberately **not** cut over — that is Task 6B): a new private `attachments` disk (`storage/app/private/attachments`, never symlinked, `serve => false` specifically so Laravel's built-in local-disk signed-URL route never auto-registers for it); a new `disk` column on `attachments` (existing rows default `public`, so the transitional coexistence of old-public and new-private rows is explicit and queryable, not inferred); `App\Services\Attachments\AttachmentStorageService` (the only place that ever calls `Storage::disk($attachment->disk)`, gated by `Attachment::APPROVED_DISKS`; validates the stored `file_path` against traversal/absolute/UNC/drive-letter/null-byte shapes before touching the disk; builds a Content-Disposition-safe filename via a custom OS-independent path-segment splitter, not PHP's own `basename()` — which only treats `\` as a separator on Windows and would silently leak Windows-style directory segments into the header on a Linux server); and one authenticated route, `attachments.show` (`GET /attachments/{numeric-id}/{view|download}`, Filament's own `Authenticate` middleware, `Gate::authorize('view', $parent)` against the attachment's actual parent record via an explicit 5-model allowlist — reusing the 5 existing financial-model policies unchanged, no new permission). The controller returns 404 for a missing-or-soft-deleted parent **before** calling `Gate::authorize()` at all (not falling through to a 403 from the reused policy), so "trashed" and "never existed" are indistinguishable to any caller. The standalone `AttachmentResource` (zero real rows; its own `FileUpload` resolved to the pre-existing `local` disk, itself incidentally exposed by Laravel's `serve => true` signed-URL route) was hardened to structurally read-only — hidden from navigation, only `index`/`view` routes, all 8 mutation `canX()` hard-`false` — mirroring `PermissionResource`'s exact pattern, without touching its `attachments.*` permissions or `AttachmentPolicy`. 64 new tests across 4 files; full suite 844/848 passed (1 pre-existing unrelated `ExampleTest` failure, 3 skipped, 1 risky — all pre-existing/unrelated, unchanged). See DECISIONS_LOG.md (three 2026-07-21 "Task 6A" entries) for the disk-allowlist, `AttachmentResource`-hardening, and soft-deleted-parent-before-authorize reasoning.

- **2026-07-20 (OMS Permissions Task 3 — secure user management)**: Replaced the Task-1 hardcoded `UserPolicy` lockdown (Super-Admin-only via `Gate::before`) with granular `users.*` permissions plus a full safety layer, since `UserResource` now needs to be usable by ordinary operators, not just Super Admin. New migration adds `users.is_active` (`boolean`, `default(true)`, no index — small table, no query needs one); `User` gained `'is_active' => 'boolean'` cast, `is_active` in its mass-assignment `#[Fillable(...)]`, and a `protected $attributes = ['is_active' => true]` model default (**not optional** — without it, `User::factory()->create()`/any in-memory `new User()` reads `is_active` as `null` until a `fresh()`/re-fetch, since Eloquent never reflects a DB column default back onto the in-memory instance after insert; this silently broke `canAccessPanel()` for exactly this reason during Task 3's own test-writing, cascading into 403s on every route for a freshly-created acting user — fixed at the model level, not by patching every test). `canAccessPanel()` is now `! trashed() && is_active` (runs on every panel request already, so deactivation takes effect on the user's very next request, no session invalidation needed). New `App\Services\Users\UserManagementService` is the single authoritative mutation layer — `UserPolicy`/Filament UI visibility call its read-only helpers for convenience, but every actual create/update/delete/restore goes through it, wrapped in `DB::transaction()`: `canManageUser(actor, target)` is a **full effective-privilege-subset comparison** (not just "is target Super Admin") — a non-Super-Admin actor may touch another user only when the target doesn't hold the exact `Super Admin` role, holds no protected permission (`users.assign_super_admin` exact, or any `roles.*`/`permissions.*` prefix) directly or via a role, and every one of the target's effective permissions (`getAllPermissions()`, role-derived + direct) is also held by the actor — this gates name/email/password/is_active changes too, not just role edits, closing a gap where a `users.update`-only operator could otherwise take over a more-powerful Admin account by changing their credentials. `assignableRoleNames(actor)` is a separate, narrower check (which *new* role an actor may hand out: Super Admin gets every role; otherwise a role's own permissions must be protected-free and a subset of the actor's) — `updateUser()` applies it only to *added* roles (`resolvedRoles - existingRoles`), never to roles already on an already-manageable target, so removing an over-broad role a non-Super-Admin didn't grant is still allowed. Self-edit is a separate code path that never goes through `canManageUser` at all: `roles`/`is_active` absent from the submitted data (the normal case — the form `->disabled()`s both fields for the authenticated user's own record, and Filament does not dehydrate disabled fields) silently preserves the current value, but a *present-and-different* value for either field on a self-edit throws (this is what rejects a crafted-request self-elevation attempt while still allowing safe self name/email/password changes). Last-active-Super-Admin protection (`isActiveSuperAdmin` = not trashed, `is_active`, exact `Super Admin` role) uses a real locking **read** inside the same transaction as the mutation — `User::query()->whereNull('deleted_at')->where('is_active', true)->whereHas('roles', ...)->lockForUpdate()->get(['users.id'])`, counted in PHP — never a locked `count()` query, and only taken on the branch that could actually reduce the count (deactivate/demote/delete an active Super Admin), not on every mutation. `deleteUser()`/`restoreUser()` are the **only** ways `UserResource` ever mutates trashed state, and `restoreUser()` deliberately never touches `is_active` — a restored user keeps whatever active/inactive value they had at deletion time; only `DatabaseSeeder`'s bootstrap-recovery path (see below) is allowed to reactivate as part of a restore, and it bypasses this service entirely. Force-delete is now **structural, not just policy-denied**: no `ForceDeleteAction`/`ForceDeleteBulkAction` is registered anywhere in `UserResource` (table, list, edit, or bulk) — `UserPolicy::forceDelete()`/`forceDeleteAny()` returning `false` is explicitly documented as defense-in-depth only, since `Gate::before` bypasses the Policy entirely for a real Super Admin actor; the actual guarantee is that the action simply doesn't exist to be authorized in the first place. Bulk actions are removed entirely for `UserResource` in this task (no `DeleteBulkAction`/`RestoreBulkAction`) — single-record delete/restore only, each wired to call `UserManagementService::deleteUser()`/`restoreUser()` directly (never `$record->delete()`/`$record->restore()`), from both `UsersTable`'s row actions and `EditUser`'s header `DeleteAction`; `->visible()` closures on those same actions hide self-delete and last-active-Super-Admin cases for UX, but the service's transactional check is what's actually authoritative (a Super Admin actor bypasses the Policy via `Gate::before` but not the service). `UserForm`'s roles `Select` no longer uses `->relationship()` — the service owns role sync via `syncRoles()`, not Filament's automatic pivot save — options come from `assignableRoleNames(actor)` merged with the record's current role names; password field no longer pre-hashes via `dehydrateStateUsing(bcrypt(...))` (removed — the model's existing `'password' => 'hashed'` cast is now the single hashing point, avoiding the double-hash risk), gained a `->confirmed()` + non-dehydrated `password_confirmation` sibling field. `DatabaseSeeder::seedSuperAdmin()`'s guard is now `is_active`-aware (`User::role(SUPER_ADMIN)->where('is_active', true)->exists()`) and its recovery logic now has 4 branches instead of 2: no configured user → create active; soft-deleted configured user → restore + reactivate + apply the configured recovery password (bypassing `UserManagementService` — this is the one documented exception); **inactive-but-present** configured user (new) → just flip `is_active` true, leave name/password untouched (a known account being turned back on, not a fresh recovery credential); active configured user → untouched beyond the role assignment. `PermissionSyncService::superAdminUserCount()` (backing `oms:sync-permissions`'s warning) is likewise now `is_active`-aware and stays read-only (never creates/updates a user). `RoleResource`/`PermissionResource` remain **pending** — not built in this task; the roles `Select` is the only role-assignment surface, direct-permission assignment was deliberately not added to `UserResource`. New tests, `tests/Feature/Users/` (`UserResourceLockdownTest.php` deleted — its "non-Super-Admin always 403s" premise is exactly what this task replaces): `UserPanelAccessTest`, `UserResourceAuthorizationTest`, `PrivilegeSubsetProtectionTest` (the `canManageUser` privilege-subset gap specifically), `SuperAdminProtectionTest`, `SelfProtectionTest`, `RoleAssignmentSafetyTest`, `UserFormBehaviorTest` (real `Livewire::test()` create/edit/delete/restore + force-delete/bulk-action-absence proofs) — plus new cases added to the existing `DatabaseSeederSuperAdminTest`/`SyncPermissionsCommandTest`. Full suite 631/634 (2 skipped, 1 pre-existing unrelated `ExampleTest` 404-on-`/` failure, unchanged from every prior entry above) — the new migration was never run against the real local database, only the in-memory SQLite test database already configured in `phpunit.xml`.

- **2026-07-21 (OMS Task 6B — financial Resource attachment cutover)**: Cut all 5 financial Resources (Project Cost Receipts, Project Cost Budget Disbursements, Execution Payments, General Expenses, General Exchanges) over to the private `attachments` disk built in Task 6A, closing the gap Task 6A deliberately left open. New `App\Services\Attachments\AttachmentUploadService` centralizes the upload/rename/move logic that was previously duplicated across all 10 Create/Edit page classes: it reuses `AttachmentStorageService::isSafeRelativePath()` for temp-path validation (never duplicates it), validates the caller-supplied directory/prefix against explicit allowlists (never accepts either from request data), creates the `Attachment` row first — with a temporary file_name/file_path — purely so a real DB id is available for the deterministic `{prefix}_{id}_{Ymd}_{amount}.{ext}` filename (never `MAX+1`), then moves the file and updates the row with final metadata. Directory/prefix mapping preserved exactly from the original per-Resource code: receipts/receive, payments/pay, execution-payments/pay, general-expenses/gen, general-exchanges/ext. Failure handling matters here because filesystem moves aren't covered by `DB::transaction()`: the `attachments` disk is configured `throw => false`, so a real Flysystem move failure surfaces as `move()` returning `false`, not an exception — checked explicitly, not assumed; a failed move force-deletes the just-created row (never soft-delete, since the row never had a real file); a DB failure *after* a successful move deletes only the newly-moved file and force-deletes the row; the previous/old attachment is never touched by a failed `store()` call. Create pages now call the service once after the parent record saves. Edit pages no longer prefill the private file path into `FileUpload` (previously `$data['receipt_image'] = $attachment?->file_path`, now removed entirely) — a new `Checkbox::make('remove_current_attachment')` plus a reusable `View::make('filament.components.secure-attachment-preview')` schema component (visible only when editing an existing active attachment, via Filament's automatic `?Model $record` closure injection — `null` on Create) replace it: replacement is stored before the old row is soft-deleted (so a failed replacement leaves the old attachment untouched and active), removal alone soft-deletes without a replacement, replacement+removal-checked-together soft-deletes the old row exactly once (replacement wins), and doing neither leaves the current attachment untouched. View pages' hand-built `Storage::disk('public')->url()` + raw `<img>`/`<a>` HTML blocks were all replaced by the same shared Blade component — inline image preview, "عرض بالحجم الكامل"/"تنزيل" links built server-side via `route('attachments.show', [$id, mode])` (never a raw path), a safe file-card fallback for non-images, and a clear Arabic empty state — used identically across all 5 View and 5 Edit pages, disk-agnostic (`disk = public` legacy rows and `disk = attachments` new rows render through the exact same secure route). 77 new tests across 3 files (`AttachmentUploadServiceTest`, `FinancialAttachmentCutoverTest` — parameterized Create/Edit across all 5 resources via the codebase's established `ReflectionMethod`-on-page-handler pattern, `FinancialAttachmentViewFlowTest` — real `Livewire::test()` of all 5 View pages), all passing; confirmed via `grep` that no `Storage::disk('public')`, `->url(`, or raw `/storage/` reference remains anywhere in the 5 financial Resource directories. One coverage gap flagged rather than silently claimed: the post-move DB-update-failure cleanup branch is implemented and code-reviewed but not deterministically unit-tested (SQLite in the test environment doesn't enforce the varchar-length constraint that would have triggered it, and mocking `Attachment::update()` itself was judged too invasive for this task). After approval, ran the real `2026_07_21_000001_add_disk_to_attachments_table` migration against the real local MySQL database (previously verified 0 rows, no `disk` column) after a full `mysqldump` backup to `C:\laragon\backups\oms\` — `disk varchar(32) default 'public'`, row count confirmed still 0 post-migration, all six pre-existing orphan files under `storage/app/public/{execution-payments,payments}` confirmed byte-for-byte unchanged (SHA-256 before/after identical), no directory created under the real private attachments disk. See DECISIONS_LOG.md (2026-07-21 "Task 6B" entries) for the move-failure-checked-by-return-value, Attachment-row-created-before-final-filename, and shared-secure-preview-component reasoning.

- **2026-07-22 (OMS Task 6C — obsolete orphan public-file cleanup)**: The six obsolete orphaned public attachment files under `storage/app/public/{execution-payments,payments}` (no matching DB row, 706,133 bytes aggregate per the 2026-07-15 audit) are **gone** — the user confirmed the old financial records were intentionally deleted and the files no longer needed. **This task deleted nothing: the files were already absent from disk when Task 6C ran.** `storage/app/public` now contains only the Laravel control file `.gitignore` (14 bytes); neither orphan directory exists (verified via Bash `find` and PowerShell `Get-ChildItem -Recurse -Force`). The files were present and byte-identical as of commit `27969b5` (2026-07-21) but were never git-tracked (uploaded files, git-ignored), so their removal doesn't show in `git status`; per-file pre-deletion SHA-256/sizes could **not** be recorded (files already gone — not fabricated). Read-only DB verification (tinker, `withTrashed()`): **0** rows referencing `execution-payments`/`payments` (active or trashed), 0 public-disk rows, 0 attachments-disk rows, **0 total rows** — the `attachments` table is empty. Old public URLs return **HTTP 403 Forbidden** (live `curl`), serving no file; `/storage/.gitignore` returns 200, proving the `public/storage` symlink is intact and static serving is live — so the 403 is the server's uniform response for a nonexistent `/storage/` file, confirming the orphans are gone without any 200/file exposure (note: 403, not the 404 the task wording anticipated). **No quarantine or legacy-migration tooling is or will be necessary:** the empty table means there are no `disk = public` rows to migrate, and every current/future financial attachment already uses the private `attachments` disk + protected `attachments.show` route (Task 6B). Code posture re-confirmed unchanged: 5 Forms `->disk('attachments')`, 5 View pages render the shared `secure-attachment-preview` component (`route('attachments.show', ...)` only), zero `Storage::url()`/raw `/storage/` in any financial-display `app/` or Blade path. Targeted tests only: `tests/Feature/Attachments` 141/141 (400 assertions), including `FinancialAttachmentCutoverTest` + `FinancialAttachmentViewFlowTest`. Documentation-only change — no application code, migration, DB row, storage file, symlink, or `.gitignore` touched; not committed (awaiting review).

- **2026-07-22 (OMS Task 6D — secure financial attachment registry)**: Rebuilt and re-enabled the standalone `AttachmentResource` in the sidebar (`النظام` → `سجل المرفقات`, singular `مرفق`) as a **strictly read-only registry** over the five supported financial attachable types only. New `App\Services\Attachments\FinancialAttachmentRegistry` is the single source of truth: attachable FQCN → {Arabic operation label, parent-module `.view` permission, per-type eager-load relationships, display accessors}. **Read-only preserved from Task 6A:** all eight `canX()` still hard-false (verified even for Super Admin), only `index`/`view` routes, no Create/Edit page, no bulk actions, and `AttachmentForm` (the only `FileUpload`) deleted — the resource can host no upload schema (`form()` override removed; a test asserts it doesn't declare its own `form()`). **Two-layer authorization:** opening requires `attachments.view_any` (unchanged `AttachmentPolicy::viewAny`, gates nav + list); every listed/viewed row additionally requires `attachments.view` AND the parent-module `.view` — the list query is scoped via `whereHasMorph('attachable', <actor's allowed types>)` (one EXISTS subquery per allowed type, requiring an existing non-trashed parent; an actor with none of the five parent perms sees `1=0`/no rows), and `AttachmentResource::canView()` re-enforces the identical rule on the View page so a direct URL to a supported-but-unauthorized record returns **403** (not merely a hidden row) and the table `ViewAction` is gated the same way against crafted calls. Super Admin sees all five via `Gate::before`. Parent-permission map (note the Task 2A "misleading" model↔prefix pairs): ProjectCostReceipt→`project_cost_receipts.view`, ProjectCostBudget→`project_cost_budgets_payments.view`, ProjectCostBudgetsPayment→`execution_payments.view`, GeneralExpense→`general_expenses.view`, GeneralExchange→`general_exchanges.view`. **Only the five supported types** are ever listed/bound (`getEloquentQuery()` `whereIn` supported types); unsupported `Project`/`Transaction`/`Partner` rows are excluded and 404 on direct URL — a guard test asserts `FinancialAttachmentRegistry::supportedTypes()` stays byte-identical to `AttachmentController::SUPPORTED_ATTACHABLE_TYPES` (the controller, the Task 6A security choke point, was left untouched). **Preview/download stay behind `attachments.show`** + its parent Policy (the shared `secure-attachment-preview` component); missing files (Status `ملف مفقود`), soft-deleted rows (audit metadata via the `المحذوفة` filter, safe notice, no links), and invalid-disk rows never expose a file link or the stored path. Display accessors read only approved denormalized parent fields (never recompute from transaction lines): operation number = parent `transaction.transaction_number`; project via `projectCost.project`/`projectCostBudget.projectCost.project` (or `—`); date = `date` except `ProjectCostBudget` (no own date column → `transaction.transaction_time`); amount+currency = own `amount`+`currency` for the three single-currency ops, `final_amount`+`disbursementCurrency` for the two multi-currency ops (disbursement, exchange — never mixed; see DECISIONS_LOG). Polymorphic parent + project chain + currency + transaction + uploader eager-loaded via `MorphTo::morphWith` per type (no N+1, proven by a query-count test). The **file-availability filter was intentionally omitted** (unbounded per-row filesystem work — existence isn't a SQL predicate); it's surfaced per-row in Status + open/download visibility, bounded to the page. 42 new `AttachmentRegistryTest` tests (101 assertions); three existing tests updated only where nav/supported-type behavior genuinely changed (`AttachmentResourceHardeningTest` nav-now-registered + supported-type view; `ResourceHttpAuthorizationTest` opt-out list now only `ProjectCostResource`; `AuthorizationAcceptanceTest` treats attachments as a normal `attachments.view_any`-gated nav resource). Full targeted set 274 → 271 passed / 3 pre-existing skips / 856 assertions. All tests on SQLite `:memory:` + `Storage::fake` — no real DB row or file changed; the real `attachments` table (read-only) now holds 3 rows (0 at Task 6C — real app data added since), all supported `GeneralExpense` on the private `attachments` disk (2 trashed, 1 active), 0 public, 0 unsupported.

- **2026-07-22 (test-suite cleanup)**: Removed the obsolete stock Laravel `tests/Feature/ExampleTest.php` (asserted `GET /` = 200; OMS is Filament-only and intentionally defines no user-facing `/` route, so it could never pass — **no production route was added**, and it was not replaced with a filler assertion). Fixed the one pre-existing risky Users test, `RoleAssignmentSafetyTest::test_submitted_super_admin_role_is_rejected_server_side` (PHPUnit "did not perform any assertions" — the expected `ValidationException` was caught by an empty block): added `assertArrayHasKey('roles', $e->errors())`, asserting the crafted Super Admin assignment is rejected on the `roles` field. Targeted `tests/Feature/Users/`: 64 passed, 0 failed, 0 skipped, 0 risky, 134 assertions. **No production behavior changed** — test-only. (Full suite not run in this task.)

## Important Notes

- `transaction_lines.debit_base`/`credit_base` are **not** converted-to-company-base-currency figures despite the name — they always equal `amount_currency` placed on the debit or credit side, in the line's own currency (confirmed by tracing `CreateGeneralExpense.php`/`CreateGeneralExchange.php` write paths, including cases where `fx_rate != 1`). Any report that sums these fields across accounts must scope to a single currency at a time, or totals will be silently blended and misleading. This drove the Trial Balance's required `currency_id` filter.
- `accounts.current_balance` must never be used for report totals (per project rule and confirmed audit finding) — Trial Balance computes everything from `transaction_lines` directly.
- The system has **no account nature** (asset/liability/equity): `accounts_type` rows are cash/bank/wallet/partner categories only. Every account is uniformly debit-normal — a debit line always `increment`s `current_balance`, a credit line always `decrement`s it, in every financial page. The opening-balance feature follows this convention (target account always debited); clearing accounts "أرصدة افتتاحية" therefore carry negative balances by design.
- `transactions.description` is now system-generated on every create/edit of the 6 financial write flows (see 2026-07-14 entries above) — never expose it as a user-editable field on those 6 flows' forms. The raw `TransactionResource` admin CRUD (`admin/transactions`) is now **read-only/audit-only** end to end (see the 2026-07-14 "read-only audit resource" entry above) — its `description` field is `disabled()->dehydrated(false)` and its create/edit routes, actions, and authorization are all disabled, so this class of overwrite risk is closed.
- `transaction_lines.description` and `transaction_lines.line_role` are system-generated, additive metadata only — display/search/audit, never a source of truth for accounting amounts, never parsed by any report/calculation. `line_role` values must come from `App\Enums\TransactionLineRole` only; `transaction_lines.notes` and the per-model `LINE_*` constants remain the authoritative tags that **calculations** (e.g. Donor Financial Report's admin/transfer deduction sums) actually match on — do not migrate any calculation reader from `notes` to `line_role` without a separately approved report-impact audit. As of 2026-07-15, all deterministically-classifiable historical rows have been backfilled (see below) and the 4 reports named in that task display (never calculate from) both fields — display use is approved; calculation use is not.
- **2026-07-15**: historical `transactions.description`/`transaction_lines.line_role`/`transaction_lines.description` are no longer universally NULL — run `php artisan transactions:backfill-descriptions --dry-run` to see current classified/unclassified counts on this DB at any time. The command is idempotent and safe to re-run after future data imports (e.g. a restored backup). Any transaction it cannot classify is left untouched and reported by id — check its output before assuming every historical row has these fields populated.
- This repo's migrations cannot currently run cleanly via `migrate:fresh`/`RefreshDatabase` end-to-end — `2026_06_24_000005_backfill_denormalized_currency_and_amounts.php` uses MySQL-only `UPDATE ... JOIN` syntax, and `2026_06_24_000009_add_supporting_indexes_for_financial_report.php` guards each index with a MySQL-only `SHOW INDEX FROM` existence check. Any future test needing the full stack will hit both; either mock around them (as `TransactionDescriptionBuilderTest`/`CleanOperationalDataCommandTest` do, by migrating everything except these two files) or get explicit approval to rewrite them to be cross-database compatible.
- The 6 orphan files found by `oms:clean-operational-data --dry-run`'s attachment audit (2026-07-15, 706133 bytes total, under `receipts/`/`general-expenses/`/etc.) are almost certainly leftovers from the 2026-07-06 manual data-reset entry above, which deliberately purged `attachments` DB rows via TRUNCATE without touching the physical files on disk. This new tool's audit only reports orphans, never deletes them.
- **2026-07-16**: Approved OMS financial-form UI convention for credit/debit account sections (UI-only, no accounting behavior changes): **desktop/wide screens** — creditor account section on the right, debtor account section on the left, side by side in one responsive `Grid::make(['default' => 1, 'lg' => 2])` row with equal widths (native CSS Grid RTL auto-placement puts the first schema child on the right — no custom CSS needed, since the app already renders with `dir="rtl"`); **mobile/narrow/tablet (< lg)** — stacks to one column, creditor section above debtor section, following the same source order. **Multi-account flows** (disbursement, general exchange) keep the credit/source account first in reading order followed by debit/deduction/destination accounts in their existing accounting order — left unchanged where the account fields already share one unified `Section::columns(2)` block, to avoid a risky layout rewrite for a cosmetic-only change. Applied to `GeneralExpenseForm.php` (credit/debit reordered + wrapped, was debit-before-credit and vertically stacked), `ProjectCostReceiptForm.php` (same), and `ExecutionPaymentForm.php` (credit section was already first from the 2026-07-16 credit-account-editable task; just wrapped in the shared Grid for side-by-side desktop). `ProjectCostBudgetsPaymentForm.php` and `GeneralExchangeForm.php` deliberately left unchanged (see DECISIONS_LOG.md). No field names, validation, cascades, `afterStateUpdated` handlers, dehydration, Create/Edit handlers, models, or migrations were touched in any of the 3 changed files.
- **2026-07-18**: Every user-entered financial amount across the 5 write flows must be ≥ 0.01, every `fx_rate` must be ≥ 0.000001 (never silently defaulted from a submitted `0`), every percentage must be in [0, 100], the two percentages combined must be strictly < 100, and both derived amounts (`amount_after_deductions`, `final_amount`) must be > 0 — enforced independently server-side by `App\Services\Validation\FinancialAmountGuard`, not just by Filament's `minValue()`/`maxValue()` (which only guard the client-rendered form and can be bypassed by directly submitted Livewire state, per the same-day audit). Any new financial write path must call this guard before its `DB::transaction()`, the same way the 10 existing Create/Edit pages do.
- **2026-07-18**: Every submitted account_id across the 5 financial write flows is now independently re-verified server-side by `App\Services\Validation\FinancialAccountGuard` (existence/not-trashed, account_type_id match, bank_type_id match, currency match, and active-on-Create-or-on-change) — never trust `account_type_id`/`bank_type_id` submitted alongside an `account_id` without re-deriving them from the account itself first, since those two fields are pure Livewire UI state and are never persisted anywhere. The same account is explicitly allowed on multiple roles (debit=credit, source=destination, etc.) — do not add a distinct-account check. Any new financial write path must call this guard (via `assertAccounts()`) before its `DB::transaction()`, exactly like the 8 existing pages, and must use the guard's returned `Account` objects for balance `increment`/`decrement` rather than a fresh `Account::find($data[...])?->` call.
- **2026-07-16**: Execution Payment's credit account is now user-editable (see entry above) but `project_cost_budgets_payments` still has **no account column** — the credit `Account` actually used is only ever recoverable from `transaction_lines.account_id` on the active line where `notes = ProjectCostBudgetsPayment::LINE_CREDIT` (`line_role = execution_source`). `ExecutionPaymentForm::budgetDestinationLine($budgetId)` still returns the *budget's* current destination line (used only to compute the auto-default and for `budgetCurrencyId()`/`budgetCurrencyName()`) — it must never be used to read back a specific execution payment's actual saved credit account; use the payment's own `LINE_CREDIT` transaction line for that instead (see `EditExecutionPayment::mutateFormDataBeforeFill()`).
- **2026-07-18**: `SUM(debit_base) == SUM(credit_base)` across a transaction's lines is only a valid balance check for the 3 single-currency workflows (receipts, execution payments, general expenses). It is **mathematically wrong** for the 2 multi-currency workflows (project cost budget disbursements, general exchanges) whenever `fx_rate != 1`, because the destination line's `debit_base` is in the disbursement currency while the other three lines' `debit_base`/`credit_base` are in the source currency — see the `debit_base`/`credit_base` note above. Do not write a new report/check that sums these columns across a multi-currency transaction's lines without first scoping to one currency, exactly like `App\Services\Validation\FinancialTransactionBalanceGuard::assertBalancedMultiCurrencyLines()` does (source-currency conservation and destination FX conversion checked as two separate same-currency equations, never summed together). Every financial write path (all 10 Create/Edit handlers) now calls this guard, with exact integer-minor-unit comparisons (no float tolerance), before `DB::transaction()` opens — any new financial write path must do the same.
- **2026-07-19 (permissions foundation, Task 1)**: The OMS roles/permissions phase began with a read-only audit (see PROMPTS_LOG for the audit prompt) that found `spatie/laravel-permission` installed and `User::HasRoles` wired, but **zero actual enforcement** anywhere — no `Gate::before`, no Policies, no Filament Shield, and the old `DatabaseSeeder` seeded a coarse `"{action} {module}"` permission grid (e.g. `"view finance"`) that nothing in the app ever checked. `UserResource` (`app/Filament/Resources/Users/`) was fully open to any authenticated user, including unrestricted role assignment. Task 1 built the foundation: `App\Support\Permissions\PermissionRegistry` (single source of truth — 154 permission names, `module.action` convention, Arabic labels, grouped by module, plus `defaultPermissionsForRole()` for the 5 system roles), `App\Services\Permissions\PermissionSyncService` + `php artisan oms:sync-permissions` (idempotent: creates missing permissions, creates the 5 system roles if missing, reconciles each system role's permissions to the registry on every run — never deletes a permission, never touches a role outside the 5 system roles), a central `Gate::before` in `AppServiceProvider::boot()` (bypasses only for `hasRole('Super Admin')`, returns `null` — not `false` — for everyone else so normal permission checks still apply, never bypasses guests), and `App\Policies\UserPolicy` (every ability hardcoded `false` — a **temporary emergency lockdown**, not a granular permission check) which Laravel auto-discovers for `App\Models\User` with zero changes to `UserResource`/its Pages. `DatabaseSeeder` now calls `PermissionSyncService::sync()` instead of its old inline seeding; the old coarse permissions are **not deleted** from any existing database (harmless/unused) — only an explicit later cleanup task should remove them. `TransactionResource`/`TransactionLineResource`'s hardcoded `canCreate() = false` (etc.) are plain overrides that never call `Gate`, so they stay read-only for everyone including Super Admin — confirmed by a regression test, unaffected by the new bypass.
- **2026-07-19 (superseded — see the entry below)**: An initial pass found real HTTP panel requests 403ing under `APP_ENV=testing` and traced it to `Filament\Http\Middleware\Authenticate`'s rule that a user model without `Filament\Models\Contracts\FilamentUser` can only enter the panel when `config('app.env') === 'local'`, then worked around it with a test-only `config(['app.env' => 'local'])` rather than implementing the interface. **This workaround was removed the same day** — see the next entry.
- **2026-07-19 (FilamentUser implemented)**: `App\Models\User` now `implements Filament\Models\Contracts\FilamentUser` with `canAccessPanel(Panel $panel): bool { return ! $this->trashed(); }` (entry-only — no permission/role check, no hardcoded email/ID; see `docs/DECISIONS_LOG.md` for why authorization stays entirely in `Gate::before`/Policies/Resource `canX()`). This closes a real production risk: without the interface, Filament silently rejects *every* user, including Super Admin, in any non-`local` environment — not just a testing quirk. No `app.env` override is needed anywhere anymore; `tests/Feature/Users/UserResourceLockdownTest.php`'s real HTTP tests pass under the suite's normal `APP_ENV=testing`. The one remaining test-only fix (unrelated to authorization) is `URL::forceRootUrl('http://localhost')` in that test's `setUp()`, because `route()`/`url()` bake this environment's `APP_URL` (`http://172.16.0.100/oms/public`) into generated URLs that the test client then mis-resolves (plain `config(['app.url' => ...])` does **not** work here — the URL generator's root is already cached by the time a child TestCase's `setUp()` body runs). `tests/Feature/Users/UserFilamentAccessTest.php` verifies `canAccessPanel()` directly: true for a non-deleted user across `testing`/`production`/`staging`/`local`, false once soft-deleted.
- **2026-07-19 (correction)**: The real administrator account in this environment is `oms@oms.com` — confirmed (read-only) to exist, not soft-deleted, holding the exact Spatie role `Super Admin`, recognized by the `Gate::before` bypass, and able to pass `UserResource::canViewAny()`/`canCreate()`. **`superadmin@oms.com` is not a real account here** — an earlier check for it (before this correction) returned "no" and must not be read as "no Super Admin exists" for this environment.
- **2026-07-19 (DatabaseSeeder fixed same day, superseded by the next entry)**: `DatabaseSeeder::seedSuperAdmin()` no longer unconditionally targets `superadmin@oms.com` — it now opens with `if (User::role(PermissionRegistry::SUPER_ADMIN)->exists()) { return; }` (checked by role, not by email), so it is now safe to run against a database that already has a real administrator (e.g. `oms@oms.com`) under any email. At this point it still fell back to a hardcoded `superadmin@oms.com`/`password123` bootstrap when no Super Admin existed — **that hardcoded credential was removed the same day, see the next entry.**
- **2026-07-19 (no hardcoded bootstrap credential — config/oms.php)**: `DatabaseSeeder::seedSuperAdmin()`'s bootstrap email/name/password are no longer hardcoded anywhere — they come from `config('oms.bootstrap_admin.*')` (new `config/oms.php`), itself sourced only from `OMS_BOOTSTRAP_ADMIN_EMAIL`/`_NAME`/`_PASSWORD` environment variables (no default for email/password; `name` defaults to `'Super Admin'`, not sensitive). If no active Super Admin exists and this config is missing (blank email or password), the seeder throws a `RuntimeException` naming only the required env var *names*, never a value, before writing anything. When bootstrapping, it looks up the configured email with `User::withTrashed()->where('email', $email)->first()` — `users.email` is unique, so this handles three cases correctly: no row → create; **active** existing row under that email → `assignRole()` only, zero field writes (promote, don't overwrite); **soft-deleted** existing row → `restore()` + reset only the `password` field (freshly hashed — the old one is unrecoverable/unverifiable) + `assignRole()`, never a second `INSERT` (which would otherwise fail on the unique constraint). `.env.example` documents the three new optional vars as blank placeholders — never a real value. `DatabaseSeeder::run()`/`php artisan db:seed` is now safe to run against any database, including one with a real existing administrator under any email (skips entirely) or none (requires the env vars to be set first, fails loudly and safely otherwise) — no scenario creates a duplicate or silently succeeds with a missing/guessable credential.

- **2026-07-19 (OMS Permissions Task 2A — Filament Resource/RelationManager authorization)**: All 23 non-User Filament Resources and all 5 existing RelationManagers are now permission-gated. One reusable trait, `App\Policies\Concerns\AuthorizesCrud`, implements `viewAny/view/create/update/delete/deleteAny/restore/restoreAny` as a single `<module>.<operation>` permission check (`forceDelete`/`forceDeleteAny` hardcoded `false` unconditionally — stays Super-Admin-only via the existing `Gate::before` bypass, never a normal permission); 23 concrete Policy classes each just declare `permissionModule(): string` and are picked up by Laravel's standard model→policy auto-discovery convention (`App\Models\X` → `App\Policies\XPolicy`), the same mechanism already proven for `UserPolicy` in Task 1 — **zero explicit registration, zero Resource/RelationManager source file changes**. `TransactionPolicy`/`TransactionLinePolicy` set `mutable(): false`, keeping create/update/delete/restore hardcoded `false` regardless of permission — this mirrors, and does not replace, `TransactionResource`/`TransactionLineResource`'s own pre-existing hardcoded `canX()` overrides (both layers now independently enforce the same read-only-audit rule; neither was touched). Soft-delete rule (uniform, built into the shared trait): a trashed record can't be normally viewed/edited/deleted-again through any of the 22 mutable modules; `restore`/`restoreAny` require `<module>.restore` and only ever apply to an already-trashed record; the 2 read-only audit modules (`transactions`, `transaction_lines`) stay permission-gated for `view`/`viewAny` but are trashed-blind on `view` (viewing deleted audit history is the point). Confirmed empirically — not assumed — that Filament authorizes a RelationManager's create/edit/delete/view actions against its **related** model's own policy (read from `vendor/filament/filament/src/Resources/Concerns/InteractsWithRelationshipTable.php::getAuthorizationResponse()`, then proven via `Livewire::test()` against all 5 RelationManagers): viewing a Project (`projects.view`) does not grant `project_costs.create` via `Projects/CostsRelationManager`, because that component's `getTable()->getModel()` is `ProjectCost`, not `Project`. **Critical mapping (deliberately non-obvious, documented in both policy classes' docblocks):** `ExecutionPaymentResource`'s model is `App\Models\ProjectCostBudgetsPayment`, gated by the `execution_payments` permission prefix; `ProjectCostBudgetsPaymentResource`'s model is the *different* `App\Models\ProjectCostBudget`, gated by `project_cost_budgets_payments` — each policy's prefix is the opposite of what its class/model name would suggest. This was the pre-existing `PermissionRegistry` design from Task 1, now confirmed correct and locked in by `PolicyDiscoveryTest`. `ProjectCosts/BudgetsRelationManager` (planned-budget rows, `transaction_id IS NULL`) shares `ProjectCostBudgetPolicy`/`project_cost_budgets_payments` with the disbursement resource (`transaction_id IS NOT NULL`) since both use the same `ProjectCostBudget` model — expected today, but revisit if a standalone "planned budget" Resource is ever built on this model (flagged as a future risk, not a current gap). No migration, no Resource/RelationManager/Form/Table/financial-service file modified — confirmed via `git status --short`/`git diff --stat` showing only new untracked files. 285 new test assertions across 5 new files; full suite 513/516 (1 pre-existing unrelated `ExampleTest` failure, unchanged since 2026-07-15).

- **2026-07-20**: Every custom Filament report page's view/export permission names live only on the page class itself (`reportViewPermission()`/`reportExportPermission()` from `App\Filament\Pages\Concerns\AuthorizesReportAccess`) — never derive a report's permission name from its class name or slug. Any new export method on these 6 pages (or a future 7th report page reusing this trait) must call `$this->authorizeReportExport();` as its first statement, before any `canExport()`/business-guard check and before any file is generated — the header action's `->visible()` alone is not sufficient, since a direct Livewire call to the export method bypasses action-level visibility entirely.
- **2026-07-20 (Task 3)**: `App\Models\User` now declares `protected $attributes = ['is_active' => true]` — required because Eloquent never reflects a database column default back onto an in-memory instance after `create()`; any future boolean column on `User` (or any model) with an in-memory-sensitive read path (e.g. `canAccessPanel()`, evaluated on the *same* object `actingAs()` reuses across a whole test) needs the same treatment, not just a migration-level default. `UserManagementService::restoreUser()` never touches `is_active` — the only exception is `DatabaseSeeder::seedSuperAdmin()`'s bootstrap-recovery branch, which bypasses the service entirely and is the sole place in the codebase allowed to reactivate a user as part of a restore. `UserResource` has no `ForceDeleteAction`/`ForceDeleteBulkAction`/`DeleteBulkAction`/`RestoreBulkAction` anywhere — single-record delete/restore only; do not add bulk or force-delete actions to this resource without a separately approved task. `RoleResource`/`PermissionResource` are still pending (Task 4/5) — the roles `Select` on `UserForm` is the only role-assignment surface today, and direct-permission assignment does not exist on `UserResource`.

- **2026-07-21 (OMS Task 6A)**: Attachment files must never be served via `Storage::disk('public')->url()`/raw HTML again — always link to the `attachments.show` route (`route('attachments.show', [$attachment, 'view'|'download'])`). Any code that needs to read/stream an attachment's bytes must go through `App\Services\Attachments\AttachmentStorageService` — never call `Storage::disk($attachment->disk)` directly; `disk` is a plain DB string with no constraint, and the service's `Attachment::APPROVED_DISKS` check is the only thing preventing an arbitrary disk name from being resolved. A private-disk `Filament\Forms\Components\FileUpload` field (`visibility: 'private'`) cannot render an **existing** file's preview via its own default mechanism — `BaseFileUpload::getUploadedFile()` calls `Storage::disk()->temporaryUrl()`, which the new `attachments` disk deliberately does not support (no `serve => true`, on purpose, to avoid exposing an unauthenticated signed-URL route) — Task 6B's cutover of the 5 financial Resources must design around this (e.g. a separate secure preview/download link component for the current file, `FileUpload` itself used only for picking a replacement upload). The 5 financial Resources' Schemas/Create/Edit/View pages are still on the `public` disk as of this entry — do not assume they have been cut over without checking `git log`/the current file contents.

- **2026-07-22 (OMS-wide CRUD redirect standard)**: After a successful full-page **Create** or **Edit**, the redirect goes to the saved record's **View** page; after a successful **page-level Delete**, to the resource **Index**. This is centralized in one concern, `App\Filament\Concerns\RedirectsToResourceView`, which overrides only `getRedirectUrl()` and is used by every Create/Edit page of the 22 editable resources (44 page classes). It resolves URLs through the Resource (`$resource::getUrl('view'|'index', ...)`) — never a hard-coded `/admin/...` string — and redirects to View **only when `$resource::canView($record)`**; otherwise it falls back to the Index, so a user allowed to Create/Edit but not View is never sent to a predictable 403 (the View page's own policy still runs on arrival — authorization is never weakened or bypassed). Do NOT add this concern to read-only resources (Attachments, Permissions, TransactionLines, Transactions — no create/edit routes) or to RelationManager/modal CRUD (Currencies, ProjectCosts, Projects, Transactions — intentional exceptions that must stay on the parent page). Page-level Delete was already correct via Filament's `InteractsWithRecord::getDefaultActionSuccessRedirectUrl()` (→ Index) and was intentionally left unchanged — do not add a redundant delete override. No View page has (or needs) a DeleteAction. When adding a new full-page resource, add `use RedirectsToResourceView;` to its Create/Edit pages; `tests/Feature/Crud/CrudRedirectStandardStructureTest` enforces this for all current pages.

- **2026-07-23 (OMS Task 7B.1 — backup core foundation)**: Implemented the **foundation only** of the OMS backup system — creation, self-verification, retention, and a secure download endpoint. **No Restore and no Filament management page exist yet** (Restore is deliberately deferred to Task 7C as an independent CLI process, outside the web request/queue-worker lifecycle that stores its own operational metadata; the Filament page is Task 7B.2). Backup content is exactly two things: a complete `mysqldump` of the configured MySQL connection, and `storage/app/private/attachments` (the private financial-attachment disk) — `storage/app/public` is explicitly excluded from v1. New `backup_operations` table (SoftDeletes, UUID, `type` manual/daily/weekly/pre_restore, `scope` database/files/full, `status` machine, `deduplication_key` nullable-unique) + `App\Models\BackupOperation` + 3 backed enums (`BackupType`/`BackupScope`/`BackupStatus`). New private `backups` disk (`storage/app/private/backups`, `serve => false`, same never-symlinked shape as the existing `attachments` disk) with a `.work/{uuid}/` scratch area cleaned up on both success and failure. Archives are ZIP (`manifest.json` + `database/dump.sql` + `files/attachments/**`) encrypted with a dedicated versioned envelope, `App\Services\Backup\SecretstreamEnvelope` — libsodium `crypto_secretstream_xchacha20poly1305` (XChaCha20-Poly1305, authenticated, chunked — never Laravel's `Crypt` facade, which only handles one in-memory string and is unsuitable for a multi-GB archive), format version 1 (magic bytes + version + key_id + Secretstream header + length-prefixed authenticated chunk frames, terminated by exactly one `TAG_FINAL` frame with nothing after it). `App\Services\Backup\BackupKeyRing` reads `OMS_BACKUP_ENCRYPTION_KEY`/`OMS_BACKUP_ENCRYPTION_KEY_ID`/`OMS_BACKUP_PREVIOUS_KEYS` from `config('oms.backup.encryption')` and **fails closed** (throws, never falls back to an unencrypted path or a default key) on a missing key, invalid base64, wrong decoded length, or an unrecognized key ID — verified by dedicated unit tests, not assumed. **Full content verification happens before a backup is ever published or marked `completed`**: `App\Services\Backup\BackupCreationOrchestrator` encrypts to an unpublished `.enc.partial` candidate under `.work/`, decrypts it back out, and runs it through `App\Services\Backup\BackupArchiveContentVerifier` (behind the narrow `App\Services\Backup\Contracts\BackupArchiveContentVerifier` interface both the orchestrator and the standalone `BackupIntegrityVerifier` depend on — the concrete class is `final`) — manifest schema/version, UUID **and** type **and** scope match, the database dump's SHA-256 (when scope requires one), every attachment's SHA-256 (when scope requires files), and an exact archive-entry-set match (no missing, no unexpected extra entries) — only then is the candidate atomically renamed to its final published filename (`{type}_{Ymd_His}_{uuid8}.omsbak.enc`, never overwriting an existing one), `status` set to `completed`, **and `verified_at` stamped at that same moment** (creation itself is a full verification pass, not merely a write). A failed verification leaves no completed row, no published file, and no plaintext artifact anywhere — the whole working directory is removed and the operation is marked `failed` with a sanitized `error_summary` (absolute `storage_path()`/`base_path()` roots and any `MYSQL_PWD=...`-shaped substring stripped before the message is ever persisted). Scheduled (`daily`/`weekly`) backups get a deterministic `deduplication_key` (`daily:{Y-m-d Asia/Gaza}:{scope}` / `weekly:{ISO-year}-W{ISO-week}:{scope}`, always null for `manual`/`pre_restore`) enforced by the database's own UNIQUE constraint — `enqueue()` always attempts the INSERT and catches the constraint violation to return the already-existing row rather than doing a race-prone SELECT-then-INSERT check; this is the **final** concurrency guard, separate from and in addition to the single global `Cache::lock('oms-backup-operation', …)` held for the duration of any create/verify/retention run. A **second, per-backup** lock (`App\Services\Backup\BackupFileLock::name($uuid)` → `oms-backup-file:{uuid}`) protects an individual published archive from being deleted while it's being read: `App\Http\Controllers\Backups\BackupDownloadController` (route `GET /backups/{uuid}/download`, Super-Admin-`hasRole()`-**and**-`backups.download`-permission gated — the `hasRole()` check is what actually stops a manually-granted permission from being sufficient on its own) holds it for the *entire* streamed-response lifetime by releasing it inside the `StreamedResponse`'s own transmission callback (not merely while the controller method returns), returning HTTP **423** if the lock is already held; `BackupIntegrityVerifier` holds it while re-verifying a published backup; `BackupRetentionService` holds it before deleting a physical file and, if unavailable, **skips that row entirely this run** (neither file nor metadata touched, reported in `BackupRetentionReport::$inUseOperationIds`) rather than racing the reader — it will be reconsidered on a later run once the lock is free. `mysqldump`'s stdout is never buffered as a complete string anywhere: `App\Services\Backup\DatabaseDumper` streams every chunk straight to `dump.sql.partial` (renamed only after a non-empty, exit-code-0 result; deleted on any failure) via an injectable `App\Services\Backup\Contracts\ProcessRunner`, and the production `SymfonyProcessRunner` — verified directly against the installed `vendor/symfony/process` v7.4.13 source, not assumed — uses `Process::start()` with **no callback** plus `Process::getIterator()` (default flags, i.e. **not** `ITER_KEEP_OUTPUT`) to drain the process's internal output buffer progressively; `disableOutput()` is deliberately never called, since `getIterator()` throws `LogicException` when output is disabled (confirmed in `readPipesForOutput()`) — the two APIs are mutually exclusive. `MYSQL_PWD` is passed only via the child process's environment (never a CLI argument, never logged); captured stderr is bounded to 4KB and passed through `SymfonyProcessRunner::sanitizeStderr()` (public/static, unit-tested) before being stored; `App\Services\Backup\Contracts\ProcessRunResult` structurally has only `exitCode`/`stderr`/`timedOut` — it can never carry a SQL body, verified by reflection tests both against the fake and against the real runner exercised with actual multi-megabyte child-process output (`PHP_BINARY`, never `mysqldump`). `App\Services\Backup\AttachmentCollector`/`BackupArchiveBuilder` reject every symlink unconditionally via an injectable `App\Services\Backup\Contracts\SymlinkDetector` (production: `NativeSymlinkDetector`/`is_link()`; tests: a deterministic `FakeSymlinkDetector` that doesn't need real OS symlink-creation privileges — this Windows/CI environment can't reliably grant those, so the two genuine-symlink OS-level tests skip gracefully here; a **real** Linux symlink-escape rehearsal is still owed as part of Task 7D staging acceptance). `CreateBackupJob`/`VerifyBackupIntegrityJob`/`RetentionCleanupJob` all run on the dedicated `backups` queue (`config('oms.backup.queue')`), never the default one. New `PermissionRegistry` module `backups` (`view_any`/`view`/`create`/`download`/`verify`/`delete` — **no `backups.restore` yet**), own group label `النسخ الاحتياطي والاستعادة`, Super-Admin-only by construction (`adminDefaults()` explicitly excludes `backups.*`, and the other three roles' explicit allow-lists never reference it). Scheduler (`bootstrap/app.php`): daily 02:00, weekly Friday 02:30, retention 03:00, all `->timezone('Asia/Gaza')` (distinct from `config('app.timezone')=Asia/Jerusalem`), `->withoutOverlapping()->onOneServer()` (verified safe — this app's cache stores, `database` in prod/local and `array` in testing, both implement `LockProvider`). Every command/job/controller/service here only ever ran against `Storage::fake()`/SQLite `:memory:`/a `FakeProcessRunner` or real-but-harmless `PHP_BINARY` child processes in tests — **no real backup was ever created, no real `mysqldump` was ever executed, no real migration was ever applied to the real MySQL database**, and the two pre-existing manual `.sql` dumps under `storage/app/private/backups`/`storage/app/backups` were never touched. Final targeted run: **143 total, 141 passed, 0 failed, 2 skipped (the two OS-level symlink tests, unavailable without Windows elevation — the deterministic fake-detector symlink-rejection tests passed), 0 risky, 411 assertions**; directly-affected permission tests re-run clean: **16 passed, 0 failed, 429 assertions**.

- **2026-07-23 (OMS Task 7B.2 — Filament backup management page)**: Added the Super-Admin-only Filament management surface on top of the 7B.1 backup core: a new custom page, `App\Filament\Pages\BackupManagementPage` (nav group `النظام`, label `النسخ الاحتياطي والاستعادة`, slug `backup-management`), providing backup statistics (4 stat cards via `App\Filament\Widgets\BackupOverviewWidget`, backed by bounded-aggregate `App\Services\Backup\BackupOverviewStatsService` — last successful backup, next scheduled run via `BackupScheduleCalculator` (fixed daily 02:00 / Friday 02:30 Asia/Gaza, mirroring `bootstrap/app.php`'s schedule, DST-safe via `CarbonImmutable`), total active completed size, latest failed operation), manual-backup queueing (header action → `BackupCreationOrchestrator::enqueue()` + `CreateBackupJob::dispatch()`, never run synchronously in the request), integrity-verification queueing (row action → `VerifyBackupIntegrityJob::dispatch()`, duplicate-request-guarded via a short-lived Cache flag), encrypted download (row action linking only to the existing named `backups.download` route — never a raw `/storage/...` URL, never the stored path), safe manual deletion (new `App\Services\Backup\BackupDeletionService`, typed-`DELETE`-confirmation modal, single-record only, no bulk action), 10-second table polling for queued/running/verifying rows, and persistent database notifications (`App\Notifications\BackupOperationNotification` + `App\Services\Backup\BackupNotifier`, wired into both `CreateBackupJob` and `VerifyBackupIntegrityJob`'s success/failure paths). **Restore is intentionally absent**: no restore action, permission, job, or route exists anywhere in this phase — the page shows a static, non-actionable Arabic notice only ("سيتم تفعيل الاستعادة بعد استكمال محرك الاستعادة الآمن واختبارها."); Restore itself remains OMS Task 7C. Every page/action authorization check is the same explicit two-part rule `App\Support\Backup\BackupAuthorization::check()` already established by `BackupDownloadController` in 7B.1 — a real `Super Admin` role **and** the specific `backups.*` permission, re-verified inside each action's own closure (not only via `->visible()`/`canAccess()`), so a non-Super-Admin manually granted every `backups.*` permission is still denied everywhere, including a crafted direct Livewire call. **One corrected 7B.1 defect, approved before implementation**: `BackupCreationOrchestrator::enqueue()` previously set `is_protected = true` for every manual backup unconditionally, which would have made manual deletion permanently impossible — contradicting this task's own "manual backups may be manually deleted unless protected" design. Fixed to default `is_protected = false` for manual backups; automatic retention protection for manual backups is unaffected, since `BackupRetentionService::mustKeep()` already independently protects `type === Manual` regardless of `is_protected`. `BackupDeletionService` refuses to delete a backup that is `is_protected`, currently active (queued/running/verifying/deleting/restoring), the single most recent completed-and-verified ("last known-good") backup, or a `pre_restore` backup still referenced by another operation's `pre_restore_safety_backup_id` — mirroring `BackupRetentionService`'s own rules and reusing the same per-backup `BackupFileLock`; an unsafe/traversal `stored_path` or an already-missing physical file is handled safely (metadata still soft-deleted, no unrelated path ever touched); a mid-delete failure restores the row's prior status rather than leaving it stuck at `deleting`. Notification infrastructure was previously entirely absent — added the standard Laravel `notifications` table migration and enabled `->databaseNotifications()` on the admin panel (`AdminPanelProvider`); `App\Models\User`'s existing `Notifiable` trait needed no change. Notification recipients: a manually-created operation notifies only its initiating `created_by` user (if still active); a scheduler-created operation (`created_by` null) notifies every active, non-soft-deleted Super Admin; a normal user is never notified merely for holding a manually-granted `backups.*` permission. Every notification/table cell is scrubbed of secrets — no encryption key, DB password, `MYSQL_PWD`, absolute path, `stored_path`, raw command, or stack trace; a new `App\Support\Backup\BackupErrorSanitizer` (extracted from `BackupCreationOrchestrator`'s existing private sanitizer) is applied inside `CreateBackupJob::failed()` so even a raw, never-orchestrator-seen exception is sanitized before being persisted or notified. The pre-existing `AuthorizationAcceptanceTest::test_permission_registry_contains_exactly_155_permissions` (written before the 6 `backups.*` permissions existed) was replaced with a durable, registry-driven assertion — no fixed total; asserts `PermissionRegistry::names()` has no duplicates, every registry name is synced to the database exactly once, no unexpected extra permission exists, and all six `backups.*` permissions remain explicitly present. Targeted backup tests: **238 total, 236 passed, 0 failed, 2 skipped (pre-existing 7B.1 OS-level symlink tests), 0 risky, 658 assertions**. Corrected permission tests: **45 total, 45 passed, 0 failed, 0 skipped, 0 risky, 838 assertions**. No real backup, `mysqldump`, migration, permission sync, database row, or storage file was ever touched.

- **2026-07-23 (backup management page — deletion-eligibility UI clarification)**: The backup management table's "الحماية" column showed only the raw `is_protected` flag, which was misleading — a backup can be undeletable for reasons that have nothing to do with manual protection (it's the "last known-good" completed+verified backup, it's currently active, it's a referenced `pre_restore` backup, or its per-backup file lock is momentarily held). The column is renamed "إمكانية الحذف" and now shows one badge reflecting the real decision: "مسموح" (success), "ممنوع — آخر نسخة ناجحة" (danger), "ممنوع — محمية يدويًا" (danger), or "ممنوع — قيد الاستخدام" (warning), with a tooltip reusing the page's existing `deletionRejectionMessage()` text. This is powered by a new public `App\Services\Backup\BackupDeletionService::eligibility(BackupOperation $operation): BackupDeletionEligibility` method that mirrors `delete()`'s exact rule order (already_deleted → active_status → protected → last_known_good → referenced_pre_restore → locked — the last one a best-effort acquire-then-release peek of the same per-backup `BackupFileLock`, since that lock is otherwise only observable at actual delete time) — `delete()` itself was refactored to call `eligibility()` and throw via a new `BackupDeletionRejectedException::forReason(string $reasonCode)` dispatcher, so the badge and the real delete rejection can never drift apart; no rejection rule was duplicated or changed. `lastKnownGoodId()` is memoized per service instance via `once()`, and `BackupManagementPage::table()` now resolves one `BackupDeletionService` instance shared by every row's column closure (instead of one per row) so this doesn't reintroduce the N+1 the pre-existing `test_query_count_remains_bounded_as_row_count_increases` test guards against. The details modal's `is_protected` `TextEntry` keeps its "الحماية" label (it genuinely is the manual-protection field) but its values are renamed "محمية يدويًا"/"غير محمية يدويًا" to make that scope explicit. The `is_protected` `TernaryFilter` and its "محمية"/"غير محمية" labels were deliberately left unchanged (out of this task's requested scope). Targeted backup suite: **174 total, 173 passed, 0 failed, 1 skipped (pre-existing, unrelated), 499 assertions**. No deletion rule changed, no migration, no real backup/database/storage touched.

- **2026-07-23 (OMS Task 7C.1 — restore domain/schema/config/authorization foundation)**: First implementation slice of the independent Restore engine (Task 7C), scoped to schema/enum/config/authorization/deletion-eligibility foundation only — no locking, progress files, launch routes, restore commands, archive extraction, or database/attachment restore logic exist yet, and no restore UI action is visible anywhere. A restore operation's own audit row **reuses** `backup_operations` rather than a new table (the Task 7B.1 schema already reserved `source_backup_id`/`pre_restore_safety_backup_id` and the `restoring`/`restored`/`restore_failed` status vocabulary for exactly this): new migration `2026_07_23_150000_add_restore_columns_to_backup_operations_table` adds two additive columns — `restore_metadata` (nullable JSON; the single bounded audit container for requester identity snapshot, source/safety backup UUID snapshots, confirmation timestamp, phase history, and sanitized reconciliation/result — never a confirmation phrase, password, key, raw command line, or unbounded exception text; no writer exists yet, `BackupOperation::RESTORE_METADATA_MAX_PHASE_HISTORY_ENTRIES = 50` documents/locks in the phase-history bound a future 7C.2+ progress-writer must enforce) and `launch_nonce` (nullable string(64), a concurrency/idempotency key — not audit data — the future signed launch endpoint, Task 7C.4, will match on in an atomic conditional UPDATE so a replayed signed launch URL can never spawn a second process). `App\Enums\BackupType` gains `Restore = 'restore'`; `App\Enums\BackupStatus` gains `RestorePartial = 'restore_partial'`, deliberately excluded from `isActive()`'s list (terminal, not in-progress) and given a new `isSuccessfulOutcome()` method (`true` only for `Completed`/`Restored`) so no caller can mistake a degraded partial-restore outcome for a clean success by only checking `isTerminal()`. `App\Models\BackupOperation` gained `restore_metadata`/`launch_nonce` to `$fillable`, `restore_metadata` cast as `'array'`, and one small helper `isRestoreOperation(): bool` — no orchestration logic added to the model. New permission `backups.restore` (`استعادة نسخة احتياطية`) registered in `PermissionRegistry::BACKUP_PERMISSIONS` — automatically excluded from every non-Super-Admin role's defaults by the pre-existing `str_starts_with($name, 'backups.')` guard in `adminDefaults()` (accountant/project-manager/viewer defaults are explicit allow-lists that never reference `backups.*` at all); registering the permission never authorizes anything by itself — every real restore entry point in later phases must still independently enforce the real `Super Admin` role via `BackupAuthorization`, exactly as every other `backups.*` action already does. New private `restores` disk (`storage/app/private/restores`, same never-symlinked/`serve=>false` shape as `attachments`/`backups`, declared but unused by any code yet) and a new `config('oms.backup.restore')` section (disk name, `progress_schema_version`, `heartbeat_seconds`, `stale_after_minutes`, `min_free_space_reserve_bytes` default 1 GiB, `free_space_margin_percent` default 20, a fixed `attachments_denied_extensions` denylist, `mysql_import_timeout`) — foundation values only, nothing speculative beyond what the already-approved Task 7C architecture will consume starting in later phases. `App\Services\Backup\BackupDeletionService` gained one new rule, `isRestoreSourceInUse()` (mirrors the existing `isReferencedPreRestore()` query shape): a completed backup that is the `source_backup_id` of a non-terminal restore-type row is now undeletable — "non-terminal" is derived from `BackupStatus::isActive()`'s own case list (queued/running/verifying/deleting/restoring today), not hard-coded to `Restoring` alone, so the rule automatically covers whichever active status a restore row is actually in and automatically excludes every terminal one (`Restored`/`RestoreFailed`/`RestorePartial`) without a second, separately-maintained status list. Reason code `restore_source_in_use`, surfaced identically by `eligibility()` (badge) and `delete()` (rejection) since both already shared one rule set; proven for every active status via a data-provider test reusing the file's existing `activeStatusProvider()`, and proven to *not* block for every terminal restore outcome via a second data-provider test. **Found and fixed a real latent bug while testing this**: `BackupDeletionRejectedException::forReason()` had its own separate, exhaustive-with-default `match()` mapping reason codes to factory methods — an unrecognized code (any new one, including this task's `restore_source_in_use`) silently fell through to `unexpectedFailure()`, **overwriting** the real reason code rather than preserving it; added `restoreSourceInUse()` + its `match` arm so `delete()`'s thrown exception's `reasonCode` now always matches what `eligibility()` computed, closing a gap that would have silently broken this exact "badge and rejection must agree" guarantee for any future new reason code, not just this one. `BackupLabels::type()`/`status()` (unguarded `match()`, no default arm by design) gained arms for the two new enum cases (`استعادة` / `استعادة جزئية`) — without them, rendering a future restore-type or `restore_partial`-status row would have thrown `UnhandledMatchError`; `BackupManagementPage`'s status-color `match()` (also unguarded) gained the same `RestorePartial => 'warning'` arm. `BackupPermissionsAndModelTest`'s two tests that explicitly asserted `backups.restore` must **not** be registered were updated to assert the opposite (7 permissions now, not 6), since that was the correct behavior for Task 7B.1/7B.2 and is now superseded by this approved task. New tests: `tests/Unit/Enums/{BackupTypeTest,BackupStatusTest}.php`, `tests/Unit/Support/Backup/BackupLabelsTest.php` (exhaustiveness — every current and future case must have a label), plus new methods in `BackupPermissionsAndModelTest`/`BackupDeletionServiceTest`. Targeted suite (`tests/Feature/Backup` + `tests/Unit/Services/Backup` + `tests/Unit/Enums` + `tests/Unit/Support/Backup` + `SystemRoleDefaultPermissionsTest` + `SyncPermissionsCommandTest`): **291 total, 289 passed, 0 failed, 2 skipped (pre-existing 7B.1 OS-level symlink tests, unrelated), 1189 assertions**. No migration was run against the real database, no real backup/restore was created, no restore UI is reachable. Not committed — awaiting review.

- **2026-07-23 (OMS Task 7C.2 — independent lock, signed progress protocol, atomic progress storage, restore-activity dual gate)**: Second implementation slice of the Restore engine (Task 7C), scoped to locking + the progress-file protocol only — no restore launch route, launch nonce, detached process launcher, restore command, archive extraction, database/attachment restore, maintenance mode, watchdog, or recovery acknowledgment exist yet, and still no restore UI action anywhere. **Lock design**: new `App\Services\Backup\{LockMode,BackupSubsystemLockHandle,BackupSubsystemLock}` — a real OS `flock()` on `{restores disk}/.locks/subsystem.lock`, deliberately never a database-backed `Cache::lock()` (this app's `cache.default` is `database` in production and local, so a Cache lock's own backing row would live inside the very database a restore replaces). `BackupSubsystemLock::acquireExclusive()`/`acquireShared()` are both non-blocking (`LOCK_NB`); restore will hold exclusive for its whole process lifetime (restore itself is not built yet — only the primitive exists), and every ordinary backup-subsystem operation now holds shared for its whole duration. `BackupSubsystemLockHandle` is `final`, exposes no raw resource accessor, and its `isLive()` re-affirms the lock on its own already-open file descriptor (a harmless no-op re-assertion per POSIX flock semantics, never a release-then-reacquire that could lose the lock) rather than merely checking `is_resource()` — a genuine liveness check, not a type check. **Corrected lock-order applied to all five ordinary operations**, each now: shared filesystem lock → existing global Cache lock or existing per-backup file lock (never a peek-and-release, always held for the real duration) — `BackupCreationOrchestrator::run()` and `BackupRetentionService::run()` (shared → Cache lock), `BackupIntegrityVerifier::verify()` and `BackupDeletionService::delete()` (shared → per-backup `BackupFileLock`, held through the up-front validity checks too, not only the locked read/delete), `BackupDownloadController::show()` (shared → per-backup lock, both held through the *entire* `StreamedResponse` transmission, released together in the same callback `finally`). A restore's exclusive lock now makes every one of these fail/423 exactly like their pre-existing contention paths already did — no new exception taxonomy was invented; `BackupLockedException::alreadyRunning()`/`BackupIntegrityException`/`BackupDeletionRejectedException::locked()`/HTTP 423 are all reused as-is. **Nested-lock deadlock eliminated by construction**: `BackupCreationOrchestrator` gained `runWithLockAlreadyHeld(int $operationId, BackupSubsystemLockHandle $handle)` for the future pre-restore safety backup (Task 7C.6+) — it acquires neither the filesystem lock nor the Cache lock itself, first validating (`BackupSubsystemLock::validateHandle()`) that the supplied handle is live, `Exclusive`, and was issued for *this* lock's own canonical path (merely being an instance of the class is never sufficient); `run()` and `runWithLockAlreadyHeld()` both delegate to one shared private `execute()` so the creation pipeline itself is never duplicated. **Signed progress protocol**: new `App\Services\Restore\{RestoreProgressSnapshot,RestoreProgressSigner,RestoreProgressWriter,RestoreProgressReader,RestoreActivityState,RestoreActivityGuard}` + `Exceptions\RestoreProgressIntegrityException`. `RestoreProgressSnapshot` is the only way to obtain validated progress content — its private constructor is reachable only through `create()`, which enforces UUID format, a fixed `ALLOWED_SCOPES`/`ALLOWED_PHASES` (the full corrected Task 7C flow's 16 phases, fixed now even though orchestration isn't built until 7C.6+)/`ALLOWED_RESULTS` vocabulary, bounded string lengths (reason/name/email/error_summary), a single canonical `DATE_ATOM` timestamp format, and a phase-history cap reusing `BackupOperation::RESTORE_METADATA_MAX_PHASE_HISTORY_ENTRIES` (50, the same single source of truth from Task 7C.1) — a writer can never be handed an arbitrary unvalidated array. `RestoreProgressSigner` computes an HMAC-SHA256 over the canonical (signature-field-excluded) JSON, keyed by `hash_hmac('sha256', 'oms-restore-progress-v1', config('app.key'))` — deliberately `APP_KEY`-derived, not `BackupKeyRing`-derived, since archive-encryption keys rotate independently of any single restore and binding progress-file integrity to them would tie two unrelated rotation lifecycles together. `RestoreProgressWriter::write()` is crash-safe and durable (durability hardened in a same-day follow-up correction): writes to a uniquely-named temp file in the same directory, then applies a **durability gate before the rename** — `fwrite` must fully succeed, `fflush()` must succeed, and — on any runtime where `fsync()` exists (core PHP since 8.1; this project runs 8.3, so it genuinely runs) — the temp-file `fsync()` must succeed. A present-but-failed `fsync()` (or a failed `fflush`) is treated as a hard write failure: the temp file is **not** renamed over `progress.json`, the current valid file is preserved untouched, the temp file is cleaned up, and a sanitized `RestoreProgressWriteException` is thrown. Only a genuinely-absent `fsync` (a hypothetical sub-8.1 runtime) is treated as "durable enough" to proceed after `fflush`. The stream is closed in every path (failure and success both `fclose()` first). It then copies the current valid `progress.json` to `progress.previous.json` *before* replacing it (best-effort/suppressed, so a previous-snapshot copy failure can never abort the write or corrupt the authoritative current file; `progress.previous.json` is only ever a recovery copy of an already-valid file and is never read as authoritative — `RestoreProgressReader` only ever reads `progress.json`), renames the fully-flushed/`fsync`'d/closed temp file onto `progress.json`, and — **after** a successful rename — `fsync`es the containing directory so the rename's own directory entry survives an OS crash. The file sync and the post-rename directory sync both go through an injectable `RestoreProgressDurability` seam (production `NativeRestoreProgressDurability`; a `FakeRestoreProgressDurability` drives the failure paths in tests). **Rename + directory-sync behavior by platform**: on Linux (the production Hostinger VPS target) the full authoritative sequence is write temp → `fflush` → `fsync` temp → close → `rename` → `fsync` parent directory, with same-filesystem `rename(2)` genuinely atomic; on Windows/Laragon (local dev only) `rename()` maps to `MoveFileEx`+`MOVEFILE_REPLACE_EXISTING` (replaces an existing destination — proven by the two-write/previous-snapshot tests passing on this host — but not guaranteed POSIX-atomic) and parent-directory `fsync` is unsupported (`syncDirectory()` returns false there) — both accepted local-dev limitations, never a production concern. The post-rename directory sync is best-effort by contract: a failure or unavailability only weakens crash-durability of the rename metadata and never corrupts the already-swapped file (which is the correct new content by that point), so it is deliberately not escalated to an exception. The guarantee this class actually depends on holds on both platforms regardless: the current valid file is only ever touched by the rename itself (copied to `progress.previous.json` first, never unlinked ahead of a fully-written replacement), so even a torn/failed Windows replace can never leave zero valid progress files. `RestoreProgressReader` verifies the signature before trusting anything else (schema version, UUID match, field-level re-validation via `RestoreProgressSnapshot::create()`), bounds the raw file read to 256 KiB independent of field-level bounds, and never authorizes anything — only ever answers "what does this file honestly say." **Dual restore-activity gate**: `RestoreActivityGuard::isActive()` returns `RestoreActivityState::Active`/`TamperedOrInvalid`/`Inactive` — active when a non-terminal `type=restore` `BackupOperation` row exists in the database, **OR** a valid signed non-terminal progress file exists on the restores disk (OR logic, not DB-only — the database itself can be mid-replacement during a real restore); a progress file that fails signature/schema/UUID/content validation is never silently treated as safe, surfacing as `TamperedOrInvalid` instead, which `blocksNewRestore()` treats identically to a genuine active restore. The directory scan is bounded and deterministic (only the restores disk's immediate subdirectories, one `progress.json` read per directory, and — corrected during final review — only directories whose name is a valid UUID are ever inspected, so `.locks` and any other stray non-UUID directory is skipped without being read and can never create a false permanent "tampered" block; `RestoreProgressReader` additionally re-checks the file's own `restore_uuid` matches its containing directory, so a valid signature lifted into the wrong directory is rejected too — a malformed/tampered file inside a genuine restore-UUID directory still surfaces as `TamperedOrInvalid` for manual review). No acknowledgment/recovery method exists on this guard — clearing a stale or tampered state remains an explicit, later-phase, audited Super-Admin action. `BackupTestCase` now also fakes the `restores` disk and sets `oms.backup.restore.disk`/`progress_schema_version`. Targeted suite (`tests/Feature/Backup` + `tests/Unit/Services/Backup` + `tests/Unit/Services/Restore` + `tests/Feature/Restore`): **347 total, 345 passed, 0 failed, 2 skipped (pre-existing 7B.1 OS-level symlink tests, unrelated), 856 assertions** (the Backup subset — 287 tests, 2 skips — was unchanged by the final-review corrections, which touched only `RestoreProgressWriter` comments/one message string and `RestoreActivityGuard`'s UUID filter; the Restore subset — 60 tests, +3 new UUID-scan tests — was re-run after the corrections and passed 60/60). No lock, progress file, or database row from this phase's own tests ever touched a real backup/restore; the one Windows-specific atomic-write-failure test (forcing a rename onto a read-only destination) gracefully self-skips rather than failing on an environment where that exact OS behavior doesn't hold (documented as Windows-specific — Linux's `rename(2)` doesn't consult the destination's permission bits at all). Not committed — awaiting review.

- **2026-07-25 (OMS Task 7C.3 — restore preflight, decryption/verification, safe extraction, private staging)**: Third implementation slice of the Restore engine (Task 7C), scoped to non-destructive preflight, disk-space estimation, private workspace staging, decrypt+full-verify, and safe scope-limited extraction only — no restore launch, execution, maintenance mode, database import, attachment live-directory swap, UI, or orchestration exists yet. **Preflight**: new `App\Services\Restore\RestorePreflightChecker` (+ `RestorePreflightResult`) is the single non-destructive gate before maintenance mode. It re-fetches the source `BackupOperation` via `withTrashed()` and checks, in order: exists, not soft-deleted, not itself a restore-type row, `Completed`, `verified_at` present, encrypted archive path exists on its configured disk (via `SafeBackupPath`), then selected-scope compatibility (`database` requires a `database`/`full` source, `files` requires `files`/`full`, `full` requires exactly `full`). Encryption resolvability is checked WITHOUT decrypting the archive: a new `SecretstreamEnvelope::peekKeyId(string $path): string` method (reusing the existing private `readHeaderPrefix()` — no duplicated framing/crypto logic) reads only the envelope's cleartext prefix (magic/version/key_id) and already throws `BackupEnvelopeUnsupportedVersionException`/`BackupEnvelopeCorruptException` for a bad header; the resolved key_id is then checked via the existing `BackupKeyRing::resolve()` (active key first, then `previous_keys`) — any failure surfaces as a generic sanitized `RestorePreflightException` (`archive_header_invalid`/`encryption_key_unavailable`), never the real key_id or error text. Database-scope preflight (only when selected scope includes database) checks `config('oms.backup.mysql_client_path')` is a non-empty, existing regular file — `is_executable()` is additionally required on non-Windows only (documented Windows-local-testing limitation, since PHP's `is_executable()` doesn't reflect real Windows ACL executability) — and that the resolved `database.connections.{name}` config has a `mysql` driver plus non-empty `host`/`port`/`database`/`username`; mysql is never invoked. Files-scope preflight checks the restores-disk root and the live `attachments` disk's parent directory are on the same filesystem via a new injectable `App\Services\Restore\Contracts\FilesystemIdentity` seam (production `NativeFilesystemIdentity`: Linux uses `stat()['dev']`, a real device identifier; Windows compares only the drive letter/UNC share root — a documented, accepted local-dev-only limitation, since production never runs on Windows) — mirroring the existing `SymlinkDetector` seam's pattern exactly, bound in `AppServiceProvider` the same way. Restore-activity is checked via the existing `RestoreActivityGuard::isActive()`, extended with a new optional `$excludeRestoreUuid` parameter (additive, defaults to `null`, every pre-existing call site and test untouched) so a later orchestration phase can re-run preflight for the very restore it is continuing without that restore's own (not-yet-terminal) progress file or DB row blocking itself — while any OTHER active/tampered restore still blocks exactly as before, including when an exclusion is supplied (proven by dedicated tests). Finally, preflight computes and enforces the disk-space estimate (below) — always before any workspace directory is ever created.

  **Disk-space estimator**: new `App\Services\Restore\RestoreDiskSpaceEstimator` (+ `RestoreDiskSpaceEstimate`) — deliberately NOT "3 × encrypted archive size" (explicitly forbidden by the task). `computeRequiredBytes(BackupOperation $sourceBackup, BackupScope $selectedScope): int` is pure/deterministic given its inputs plus the current size of the live `attachments` disk and current config, and sums real, potentially-coexisting disk consumers: the decrypted-ZIP workspace file, the staged SQL dump and/or attachments (combined into one "staged content" figure — exact for a `full`-scope source, since dump + attachments literally sum to `original_size_bytes` at creation time; a safe upper bound for a database-only/files-only restore of a mixed-scope source), the current live `attachments` disk size (files scope only, real measured value via `Storage::disk('attachments')->allFiles()`+`size()`, not an estimate), two safety-backup proxies (working files + published archive, both approximated by the same `original_size_bytes` — documented conservative proxy for "current live data is of comparable scale to the backup being restored"), and a fixed `ATOMIC_WRITE_OVERHEAD_BYTES` class constant (100 MiB, not config — an implementation-overhead constant, not an operator policy knob). When `original_size_bytes` is null/zero (shouldn't happen for a `Completed` row, but handled rather than assumed), it falls back to `4 × size_bytes` (the encrypted archive's own on-disk size) — "unavailable ⇒ conservative estimate, never zero." The raw sum is multiplied by `(1 + free_space_margin_percent/100)` (default 20%) and floored at `min_free_space_reserve_bytes` (default 1 GiB) — both pre-existing `config('oms.backup.restore.*')` keys from Task 7C.1, no new config added. `freeBytesAt(string $path)` walks up to the nearest existing ancestor directory before calling `disk_free_space()` (handles a not-yet-created workspace root). `RestoreInsufficientDiskSpaceException::forBytes()` carries only the two sanitized integer totals — no path, no disk name, no config value.

  **Private workspace**: new `App\Services\Restore\RestoreWorkspace` — `storage/app/private/restores/{restore_uuid}/workspace/`, a SUBDIRECTORY of the restore's own UUID directory (a sibling of `progress.json`/`progress.previous.json` and any future attachment-quarantine directory, never a parent of them). The constructor validates `$restoreUuid` against the same canonical UUID regex `RestoreProgressWriter`/`RestoreActivityGuard` already use, before any path is ever built from it. `prepare()` only ever calls `Storage::disk('restores')->makeDirectory()` + a best-effort `chmod(0700)` — never touches the live `attachments` disk or the database. Every path-returning method (`decryptedArchivePath()`, `stagedDumpPath()`, `stagedAttachmentsRoot()`, `resolveStagedAttachmentPath(string $relativePath)`) validates the relative segment via the existing `SafeBackupPath` and re-confirms the resulting absolute path still starts with this workspace's own root — no caller-supplied relative path (including a manifest-declared attachment path) can ever resolve outside it. `cleanup()` only ever calls `deleteDirectory()` on the `"{uuid}/workspace"` subtree — structurally incapable of touching `progress.json`/`progress.previous.json`/`.locks` (which lives at the restores-disk ROOT, a sibling of every UUID directory, not inside one)/a quarantine directory/another restore's workspace, regardless of when or why it's called; there is no age-based stale-cleanup sweep in this class at all (see `docs/DECISIONS_LOG.md`), and safety instead comes from this structural scoping, proven by dedicated isolation tests (cleaning up restore A's workspace while restore B's own workspace/progress file is still active leaves B completely untouched).

  **Decrypt + verify + extract**: new `App\Services\Restore\RestoreArchivePreparer` enforces the exact required order — `RestorePreflightChecker::check()` (non-destructive) → `RestoreWorkspace::prepare()` (only after preflight passes) → `SecretstreamEnvelope::decryptFile()` using `BackupKeyRing::resolve()` as the key resolver (both reused completely unchanged) → `BackupArchiveContentVerifier::verify()` against the decrypted plaintext ZIP (reused unchanged, the exact same shared verifier `BackupCreationOrchestrator`/`BackupIntegrityVerifier` already use) → only on full verification success, `RestoreArchiveExtractor::extract()`. Any failure from workspace creation onward is caught, the workspace is cleaned up best-effort (`RestoreWorkspace::cleanup()`), and a sanitized `RestoreArchivePreparationException` (`workspace_unavailable`/`decryption_failed`/`verification_failed`/`unexpected_failure`) or the extractor's own `RestoreArchiveExtractionException` is thrown — the source backup's own encrypted archive on the `backups` disk is never modified by any of this (proven by a dedicated test comparing its checksum before/after a failed preparation). On success, the workspace is deliberately left in place — cleanup responsibility transfers to a later orchestrator, which does not exist yet.

  **Safe extraction**: new `App\Services\Restore\RestoreArchiveExtractor` never uses `ZipArchive::extractTo()`. It derives every entry it writes strictly from the already-verified `$manifest` array handed to it (never from the ZIP's own raw directory listing) — `database/dump.sql` when the selected scope includes database, each `attachments.files[]` entry when it includes files — so an entry present only in the ZIP (not the manifest) or referenced only by the manifest (not physically in the ZIP) is caught as a `locateName()` failure (`unexpected_entry`), never silently written. Every attachment path is validated by the existing `SafeBackupPath::isSafe()` (rejects absolute Unix/Windows/UNC/drive-letter paths, `..` traversal on either `/` or `\`, null bytes) before use; a duplicate-normalized-path check (collapsing `\`→`/` and empty/`.` segments) catches two differently-spelled manifest paths that resolve to the same logical file. Each candidate entry is checked for Unix-symlink/socket/block/char/FIFO external attributes (`ZipArchive::getExternalAttributesIndex()`, bit-masked against `S_IFMT`) before ever being opened for streaming — defense in depth, since `BackupArchiveBuilder` never archives anything but regular files in the first place. `statIndex()`'s own declared size is compared against the manifest's declared size before streaming (fast fail), the destination directory path is walked (bounded to the workspace root) checking `SymlinkDetector::isLink()` at every level to refuse writing through an existing symlink, then the entry is streamed in 1 MiB chunks — hashing incrementally (never `file_get_contents()`), aborting immediately if a chunk pushes the running byte count past either the entry's own declared size or the manifest-declared cumulative total-byte budget (tracked across the whole extraction, not just per file) — and finally the accumulated SHA-256 is compared against the manifest's declared hash, unlinking the partial file on any mismatch. All streams (`ZipArchive::getStream()` handle and the destination file handle) are closed in every success/failure path via `finally`. The bounded `PreparedRestore` result (restore UUID, selected scope, the `RestoreWorkspace` instance itself — never a raw path string — source backup UUID, staged dump/attachment references, a manifest summary excluding the per-file attachment list, and declared/extracted byte totals) deliberately excludes keys, the database password, raw commands, unbounded manifest data, and any open stream/resource.

  **Pre-commit correction pass (same day, final review before approval to commit)**: four real defects were found and fixed. (1) The disk-space estimator originally scaled the mandatory pre-restore safety backup (which is always FULL, regardless of the selected restore scope) off the source backup's own `original_size_bytes`, and only counted current live attachments when the selected scope included files — both wrong, since the safety backup always covers the whole current system regardless of what's being restored, and the current system can be far larger than the backup being restored from. Corrected: a new injectable `App\Services\Restore\Contracts\CurrentDatabaseSizeEstimator` (production `MysqlInformationSchemaDatabaseSizeEstimator`, a single `SUM(data_length + index_length) FROM information_schema.tables` query against the configured connection, never a mysqldump, falling back to `max(source_original_bytes, 50 MiB)` — never zero — when unavailable) now sizes the current database, and current live attachments size is now always included regardless of selected scope. Final formula: `sourceDecryptedArchive (=original_size_bytes) + selectedSourceStaging (=original_size_bytes) + currentDatabaseBytes + currentAttachmentsBytes + safetyPipelineWorkspace (=3×(currentDatabaseBytes+currentAttachmentsBytes), covering BackupCreationOrchestrator's own archive.zip + encrypted candidate + verification-decrypt copy, traced directly against its execute()/verifyCandidate()) + attachmentQuarantine (=currentAttachmentsBytes, only when selected scope includes files) + 100 MiB atomic-write overhead`, then `max(ceil(raw × (1+margin%/100)), minReserveBytes)`. (2) `RestoreActivityGuard`'s new `$excludeRestoreUuid` parameter (added earlier the same day) had a real bug: it skipped reading the excluded UUID's `progress.json` entirely rather than only suppressing a valid non-terminal result — meaning a tampered/malformed file for the excluded UUID would have silently passed as safe. Fixed so the file is always read and validated; only a genuinely valid, non-terminal snapshot for that exact UUID is ever ignored — a corrupt one for the same UUID still surfaces as `TamperedOrInvalid`. (3) `RestoreArchiveExtractor` gained `assertEntrySetMatchesManifest()` — an independent re-derivation of the FULL manifest-declared entry set (not just the selected scope's subset), compared against the currently open ZIP's actual listing immediately before any staged content is written, closing the gap between `BackupArchiveContentVerifier::verify()`'s earlier pass and extraction itself; every manifest-declared attachment path is also safety/duplicate-checked at this same point, before it can matter whether a matching raw ZIP entry happens to exist. (4) `RestoreWorkspace` gained the same injectable `SymlinkDetector` seam already used by the Backup subsystem — `prepare()` and every path-returning method now walk from the target up to the restores-disk root checking for an existing symlink/junction at each level, since `SafeBackupPath` only ever validates the path *string*, never live filesystem state. `RestoreArchivePreparer`'s workspace-cleanup-on-failure call is now wrapped in its own try/catch (`cleanupBestEffort()`) so a cleanup failure can never mask the original preparation/extraction exception — verified by code review; a dedicated forced-failure test was not achievable on this Windows/Laragon host (`unlink()` empirically succeeds even against an open file handle here, so `Storage::deleteDirectory()` cannot be deterministically forced to fail — the same class of Windows-testability gap already documented for the pre-existing real-symlink-creation tests).

  Targeted suite after all corrections (`tests/Unit/Services/Restore` + `tests/Feature/Restore` + `tests/Unit/Services/Backup` + `tests/Feature/Backup`): **433 total, 431 passed, 0 failed, 2 skipped (pre-existing 7B.1 OS-level symlink tests, unrelated), 984 assertions**. Fixture archives for the decrypt/verify/extract tests were built through the real `BackupCreationOrchestrator` (fake process runner, faked disks) rather than hand-mocked crypto, per the task's own "use actual fixture archives" instruction; a genuine tampered-ciphertext test and a genuine tampered-manifest-then-re-encrypted test both exercise the real `SecretstreamEnvelope`/`BackupArchiveContentVerifier` failure paths. No real backup/restore was created against a real database, no real `mysql`/`mysqldump` was ever executed (the new `CurrentDatabaseSizeEstimator` is always faked in tests), and no restore UI is reachable anywhere.

- **2026-07-25 (OMS Task 7C.4 — replay-safe restore launch, global serialization, detached process launcher, `oms:restore` command shell)**: Fourth implementation slice of the Restore engine (Task 7C), scoped to launch/claim/serialization/process-spawn only — no restore request/confirmation UI, maintenance mode, safety-backup orchestration, database import, attachment swap, `RestoreOrchestrator`, watchdog, or recovery acknowledgment exists yet. **`RestoreLaunchService`** (`app/Services/Restore/RestoreLaunchService.php`) is the only place a queued restore is ever claimed and spawned — it never creates one (tests construct a valid queued `BackupOperation` row directly, exactly as a future request/confirmation UI will). `launch(uuid, nonce)` first validates every Task 7C.1 precondition on the row (`type=Restore`, `status=Queued`, `started_at` null, `launch_nonce` matches via `hash_equals()`, `source_backup_id` resolves to a `Completed`+`verified_at`-stamped backup, non-empty `operation_reason`, `scope` is a valid `BackupScope`, `restore_metadata` is an array with a well-formed `requester` snapshot (`user_id: int|null`, non-empty `name`/`email` strings) and a `confirmed_at` DATE_ATOM timestamp, and — defense in depth — contains none of a fixed forbidden-key denylist (`confirmation_phrase`/`password`/`encryption_key`/`key`/`db_password`/`database_password`/`secret`)) — any failure here returns a conflict outcome before the lock is even touched. Only then does it call `BackupSubsystemLock::acquireExclusive()`; if that returns `null` (another operation holds shared, or another launch holds exclusive), the outcome is `Locked` (423) with nothing else attempted. Inside the lock, in order: (1) `RestoreActivityGuard::isActive($uuid)` **excluding this restore's own UUID** — any OTHER active DB restore or active/tampered progress file still blocks (`Active`→`restore_already_active`, `TamperedOrInvalid`→`restore_state_requires_review`, both surfaced as 409 conflict — see `docs/DECISIONS_LOG.md` for why no separate HTTP status was invented for the tampered case); (2) the atomic conditional claim — a single query-builder `UPDATE backup_operations SET started_at=?, status='restoring', launch_nonce=NULL, restore_metadata=? WHERE id=? AND type='restore' AND status='queued' AND started_at IS NULL AND launch_nonce=?` inside `DB::transaction()` (the transaction scopes only this claim+audit update, never process execution) — exactly one row affected means this request won; zero means a replay or a lost race and returns conflict without writing progress or spawning; note this bypasses Eloquent's `array` cast for `restore_metadata` (query-builder `update()` calls `toBase()->update()` directly, confirmed against the Laravel source — the metadata array must be `json_encode()`d manually before the call); (3) the initial `RestoreProgressSnapshot` is built from the freshly-claimed row (`phase='lock_acquired'`, the only phase this flow ever reaches before success/failure) and written via the existing `RestoreProgressWriter` — a write failure marks the row `RestoreFailed` (sanitized `error_summary`, `launch_nonce` never restored) and returns `Failed` **before the launcher is ever called**; (4) only after progress is durably written does it call the injected `RestoreProcessLauncher::launch($uuid)` — a `RestoreProcessLaunchException` here also marks the row `RestoreFailed` and best-effort writes a terminal signed `restore_failed` progress snapshot (never throwing back out of the failure path). The exclusive lock is released in a `finally` around the entire section — every success and every failure path releases it exactly once. **Signed launch route**: `GET /restores/{uuid}/launch` (name `restores.launch`, `Filament\Http\Middleware\Authenticate::class` + Laravel's `signed` middleware) → `App\Http\Controllers\Restores\RestoreLaunchController` — the route/query accept only the UUID (path), a random `nonce` (query, included in the signature since Laravel signs the full URL including all non-`signature` query params), and Laravel's own `signature`/`expires` fields; the controller independently re-checks `hasRole(PermissionRegistry::SUPER_ADMIN)` **and** `can('backups.restore')` (never relying on `Gate::before` alone — same two-layer pattern `BackupDownloadController`/`BackupAuthorization` already established), then maps `RestoreLaunchOutcomeStatus` → HTTP 202/409/423/500. **No Filament UI button/link/URL generator was added** — the route exists but `BackupManagementPage` has zero reference to it (proven by both a source-grep test and a routing-table guard test that the pre-existing `test_no_restore_route_exists` was tightened into, since this task's one route necessarily makes the old "no restore route at all" assertion false). **`RestoreProcessLauncher`** (`app/Services/Restore/Contracts/RestoreProcessLauncher.php`) is bound in `AppServiceProvider` via a pure `RestoreProcessLauncherFactory::forOsFamily(PHP_OS_FAMILY)` (testable independent of the host OS) to `LinuxDetachedRestoreProcessLauncher` or `WindowsDetachedRestoreProcessLauncher`. **Linux** (authoritative production target, single Hostinger VPS): verified directly against the installed `symfony/process` 7.4.13 source that an array command on this platform (no `--enable-sigchild` fallback) is handed to `proc_open()` completely unmodified — no shell, no string interpolation, no injection surface at all, so the earlier-anticipated "small validated shell wrapper" fallback turned out to be unnecessary (see `docs/DECISIONS_LOG.md`). The child argv is exactly `[setsid, --fork, php_binary, artisan_path, 'oms:restore', uuid]` — `setsid --fork` forks a grandchild into a brand-new session (immune to any signal delivered to this web request's own session/process group) and execs there while the immediate `setsid` child exits within microseconds, so `Process::start()` returns almost instantly; `disableOutput()` attaches stdout/stderr to real `/dev/null` file descriptors (confirmed in `UnixPipes::getDescriptors()`) rather than pipes, so no fd tying the child back to the web request survives. Every binary (`setsid`, the configured PHP CLI path) is existence+executable-checked immediately before building the command, failing closed with a specific `RestoreProcessLaunchException` reason code (`detach_binary_missing`/`php_binary_missing`/`artisan_missing`/`invalid_uuid`/`spawn_failed`) rather than silently falling back to an undetached child — argv construction is factored into a separately-callable, spawn-free `buildArgv()` so every one of these failure modes is unit-testable without ever starting a process. **Windows/Laragon** launcher is local-dev-only and explicitly non-authoritative — no `setsid` equivalent exists through Symfony Process; it uses the same array-command/`disableOutput()` mechanism but with **no** claim of surviving the parent process/job object being torn down (documented, not tested, since this Windows dev host cannot prove that guarantee). `config('oms.backup.restore.php_binary')` defaults to `PHP_BINARY` (fine for local CLI/testing) but **must** be overridden via `OMS_RESTORE_PHP_BINARY` for a real php-fpm production deployment, where `PHP_BINARY` resolves to a non-CLI-invocable binary (see `docs/DECISIONS_LOG.md`). **`php artisan oms:restore {uuid}`** (`App\Console\Commands\RestoreCommand`) is the independent restore process's own entry point — never dispatched via the database queue, never acquiring the Cache lock (same reasoning as `BackupSubsystemLock` itself: a restore may replace the very database/cache backing store those would depend on). It validates the UUID, loads the row and requires `type=Restore`/`status=Restoring`/`started_at` present/`launch_nonce` consumed, reads a valid non-terminal signed progress file matching the UUID (any failure here rejects without mutating anything — the row is not this invocation's to touch if it isn't genuinely claimed), then acquires the lifetime exclusive `BackupSubsystemLock` with a short bounded retry (`oms.backup.restore.launch_lock_retry_timeout_seconds`/`launch_lock_retry_interval_ms`, new config, since the parent launch request may still hold the same lock briefly while writing the initial progress file), re-verifies row+progress once more after acquiring the lock, and — because the real execution engine (Task 7C.7) does not exist yet — always writes a terminal sanitized `restore_failed` progress snapshot and moves the DB row to `RestoreFailed` before releasing the lock and exiting non-zero, so a restore invoked through this shell can never be left in a permanently active state. The fail-closed body (`failClosed()`) is deliberately isolated so Task 7C.7 can replace only that call with a real `RestoreOrchestrator` invocation without touching the claim-verification/lock-acquisition/re-check logic above it. Targeted suite (`tests/Feature/Restore` + `tests/Feature/Backup` + `tests/Unit/Services/Restore` + `tests/Feature/Console/RestoreCommandTest.php`): **401 total, 399 passed, 0 failed, 2 skipped, 915 assertions** — 1 pre-existing (7B.1 OS-level symlink test) + 1 new documented skip (`test_bounded_retry_succeeds_once_the_parent_releases`: true concurrent-lock-release timing needs a second real OS process/thread racing this one, and this Windows PHP build has neither `pcntl_fork()` nor a safe way to spawn a second real process against the test's in-memory SQLite database — the retry loop itself is still covered by the sibling timeout test on the identical code path). No real process was ever spawned by any test (every HTTP/service test binds `FakeRestoreProcessLauncher`; the launcher unit tests only ever call the spawn-free `buildArgv()`), no real backup/restore was created against a real database, and no restore UI is reachable anywhere. Not committed — awaiting review.

- **2026-07-25 (OMS Task 7C.4 correction pass — POST route, restore-execution activity gate, progress-phase semantics, real cross-process retry test, process cwd)**: A same-day, pre-commit correction pass fixed five points in the 7C.4 entry above (which now reads as originally implemented, before these fixes — see `docs/TASKS_LOG.md`'s updated entry and `docs/DECISIONS_LOG.md` for full reasoning on each): (1) the launch route is `POST /restores/{uuid}/launch`, not `GET` — a signed GET could be triggered by prefetching/link-scanning/accidental navigation; there is no GET route at that path at all, so an unauthorized GET 405s via Laravel's own routing before ever reaching the controller. (2) New `RestoreActivityGuard::blocksOrdinaryOperations(): bool` closes the parent-launch-to-child-lock-acquisition handoff race: it blocks on a genuinely claimed/running restore (`status=Restoring` specifically — a merely `Queued`, unlaunched request never blocks) OR a valid non-terminal signed progress file OR a malformed/tampered one (fail-closed). Wired into `BackupCreationOrchestrator::run()`, `BackupRetentionService::run()`, `BackupIntegrityVerifier::verify()`, and `BackupDownloadController::show()` immediately after each acquires the shared subsystem lock (released immediately if blocked, before the existing Cache/per-backup lock is ever touched) — and, after a mid-pass correction, folded into `BackupDeletionService::eligibility()` itself as a new `restore_activity_in_progress` reason (memoized per-instance via `once()`, mirroring `lastKnownGoodId()`) rather than a separate check in `delete()`, because a standalone `delete()`-only check made `eligibility()` (the management page's badge) and `delete()` disagree about *why* a restore-source backup is blocked — violating this class's own "badge and rejection can never disagree" guarantee. `BackupCreationOrchestrator::runWithLockAlreadyHeld()` deliberately never performs this check (the mandatory pre-restore safety backup runs *inside* the active restore itself, under its own already-validated exclusive handle). New `tests/Feature/Backup/RestoreActivityOrdinaryOperationGateTest.php` (13 tests) proves all five ordinary paths block on both scenarios, that a merely-queued restore never blocks, and that `runWithLockAlreadyHeld()` stays exempt. (3) `RestoreProgressSnapshot::ALLOWED_PHASES` gained `launching` (before `lock_acquired`) — `RestoreLaunchService` now writes `phase=launching` as the initial/failure phase (never `lock_acquired`, since the parent can never have acquired a lock the not-yet-spawned child hasn't been given a chance to hold); `RestoreCommand` gained `advanceToLockAcquired()`, which writes the ONLY `phase=lock_acquired` update that ever exists, and only after the command has genuinely acquired its own lifetime exclusive lock, immediately before its existing fail-closed terminal write (`restore_failed_phase` is therefore always `lock_acquired` for this phase's fail-closed path, not `launching`). (4) The previously-skipped `test_bounded_retry_succeeds_once_the_parent_releases` is now a real cross-process test: a second, genuine external `php` process (spawned via Symfony `Process`, running a small standalone lock-holder script with no Laravel bootstrap) takes a real OS `flock()` on the exact lock file `BackupSubsystemLock` uses, signals readiness via a sentinel file, holds for 1.5s, then releases — the test then calls `Artisan::call('oms:restore', ...)` in-process and asserts it both genuinely waited (>500ms elapsed, not an instant false-positive success) and then succeeded (row reached `RestoreFailed`, proving real lock acquisition, not the "never acquired" outcome the sibling timeout test covers). Works identically on Windows/Laragon and Linux since it depends on nothing but standard `flock()` semantics across two independent OS processes — no `pcntl_fork()` anywhere. (5) Both platform launchers gained an explicit `buildProcess()` step setting `cwd=base_path()` (previously unset, meaning Symfony `Process` would have inherited whatever directory the web server process happened to start in) — proven by two new unit tests asserting `getWorkingDirectory()` without ever starting the process; argv shape, output-descriptor handling, and absence of secrets in argv/env were already correct from the initial implementation and were re-confirmed, not changed. Targeted suite after all five corrections: **418 total, 417 passed, 0 failed, 1 skipped (pre-existing, unrelated), 957 assertions** — up from 401/399/2-skipped/915 before this pass; two pre-existing `BackupDeletionServiceTest` tests were updated (not weakened) to expect the new, more accurate `restore_activity_in_progress` reason for the `Restoring`-status case specifically, since the new gate is a genuine, intentional broadening of protection there. Not committed at the time this entry was written — see `docs/TASKS_LOG.md` for the commit hash once approved.

- **2026-07-25 (OMS Task 7C.5 — streamed database restoration and post-import reconciliation services, NOT wired to any command/orchestrator)**: Fifth implementation slice of the Restore engine (Task 7C), scoped strictly to the database-import and post-import-reconciliation *services* — nothing here is called by `RestoreCommand`, `RestoreLaunchService`, or any future orchestrator yet; maintenance mode, safety-backup orchestration, attachment swap, and the real `RestoreOrchestrator` all remain unbuilt. **Streamed process input**: new `App\Services\Restore\Contracts\ProcessStreamInputRunner` + production `SymfonyProcessStreamInputRunner` — the mirror-image seam of the existing `ProcessRunner`/`SymfonyProcessRunner` (which streams a large *stdout*, mysqldump's direction) for a process whose large input is *stdin* (the `mysql` import's direction). Verified directly against the installed `symfony/process` 7.4.13 source (`ProcessUtils::validateInput()`) that a `resource` passed to `Process::setInput()` is returned completely unchanged — never coerced to a string — so the underlying pipes implementation streams it to the child's stdin incrementally; the runner opens the staged dump as a binary read handle, calls `disableOutput()` (safe here, unlike `SymfonyProcessRunner`, since this direction never needs progressive stdout — confirmed `buildCallback()` still invokes a passed callback for both stdout/stderr even with output disabled), and closes the handle in a `finally` covering every exit path. A new integration test (`SymfonyProcessStreamInputRunnerIntegrationTest`, PHP-child-only, never mysql) proves complete transmission of a 12MB file via SHA-256 comparison, bounded/sanitized stderr, timeout enforcement, and — via a genuinely Windows-specific technique — that the input file's handle is closed on every path (an open handle would make the immediately-following `unlink()` fail on Windows, so a successful unlink is direct evidence of closure). **`DatabaseRestorer`** (`app/Services/Restore/DatabaseRestorer.php`) re-validates everything independently of whatever `RestorePreflightChecker`/`RestoreArchivePreparer` already checked earlier in the flow (time has passed since then): selected scope includes database, staged dump is a regular file (not a symlink/junction — reuses the existing `SymlinkDetector` seam) confined to the restore's own workspace root, mysql client path is immediately re-checked (same is_file/is_executable rule as preflight, Windows-local-testing limitation included), and every required connection field is present. Builds the mysql argv as a plain array — `[mysql_path, --host=, --port=, --user=, --default-character-set=utf8mb4, database]` — no `--password` flag ever, no shell string; the password goes into the process environment as `MYSQL_PWD` only when non-empty (mirroring `DatabaseDumper`'s established pattern exactly), and a new sanitized `RestoreDatabaseException` (fixed generic messages + reasonCode, `importFailed()` additionally passes its bounded stderr excerpt through `BackupErrorSanitizer::sanitize()` as defense in depth on top of the runner's own sanitization) wraps every failure mode (missing/unreadable/symlinked/outside-workspace dump, non-database scope, missing mysql client, incomplete connection, process-launch failure, non-zero exit, timeout) — never a raw path/credential/unbounded stderr. **Post-import connection reset**: new `RestoreDatabaseConnectionResetter` contract + `LaravelRestoreDatabaseConnectionResetter` — `DB::purge($connection)` (discards the possibly-stale pre-import PDO and Laravel's cached resolved connection) followed by a real `select 1` on the freshly-resolved connection to prove it's genuinely usable, never merely that `getPdo()` returned an object. Proven with a real (but throwaway, file-backed) secondary SQLite connection — never the test suite's own `:memory:` connection, which would lose its whole schema if genuinely purged — comparing `spl_object_id()` of the PDO before/after to prove a real reconnect happened, not just a no-op. **`RestoreReconciler`** (`app/Services/Restore/RestoreReconciler.php`) runs the exact required 7-step sequence — connection reset → `migrate --force` → `oms:sync-permissions` → `permission:cache-reset` → metadata reconstruction → selective ephemeral cleanup → `queue:restart` — each step wrapped in its own try/catch throwing a distinct sanitized `RestoreReconciliationException` reasonCode, nothing after a failed step ever runs; deliberately not one outer `DB::transaction()` (migrations commit implicitly per-migration, Artisan commands may use separate connections, schema itself changes mid-run). Every one of the four dependencies (`RestoreDatabaseConnectionResetter`, new `ArtisanCommandRunner` contract + `LaravelArtisanCommandRunner`, new `RestoreMetadataReconstructor` contract, new `RestoreEphemeralTableCleaner` contract) is interface-typed with no default, purely so a single shared `RestoreReconciliationOrderLog` test spy can prove the exact cross-collaborator call order and that each failure genuinely short-circuits every later step — no real database purge, Artisan command, or metadata write happens in the reconciler's own order/failure test file. **Metadata snapshots**: new bounded, `create()`-validated value objects `App\Services\Restore\Metadata\BackupOperationSnapshot` (source/safety backup rows — `status` is not a constructor parameter at all, since every snapshot this class can produce is by construction an authoritative Completed+verified row, never a stale Running/Verifying state carried over from the imported dump) and `RestoreOperationSnapshot` (the restore's own row — `status` likewise fixed to `Restoring`, since only the future orchestrator decides the terminal `Restored`/`RestorePartial`/`RestoreFailed` result), mirroring `RestoreProgressSnapshot`'s established private-constructor/static-`create()`/no-arbitrary-array pattern; field lengths are bounded to the real `backup_operations` column widths (`disk` 32, `encrypted_filename` 255, `encryption_key_id` 64), and neither object has a field for a password/key/nonce/confirmation phrase/raw exception text. **`RestoreMetadataUpserter`** (implements `RestoreMetadataReconstructor`) upserts source, then safety, then restore, in that exact order, each in its own small `DB::transaction()` — a row is looked up `withTrashed()` by its own UUID (never a trusted old numeric ID) and either updated in place or created; the restore row resolves `source_backup_id`/`pre_restore_safety_backup_id` from the just-upserted rows' real (possibly new) auto-increment IDs. `created_by` is resolved against the freshly-migrated `users` table by the snapshot's captured `user_id`: preserved if that user still exists, otherwise `null` with the name/email identity snapshot preserved inside `restore_metadata['creator_identity']` instead — never a dangling FK. Each of the three sub-steps throws its own distinct `RestoreMetadataReconciliationException` reasonCode (`source_upsert_failed`/`safety_upsert_failed`/`restore_upsert_failed`), proven via a real DB-level failure (dropping the table for the source case; an `Eloquent::saving()` event hook forcing exactly the 2nd `save()` to throw for the safety case) that a source failure prevents both later upserts and a safety failure prevents the restore upsert, with the source row already durably committed. **`RestoreEphemeralTablePolicy`** (implements `RestoreEphemeralTableCleaner`) reads every table name from configuration (queue table/queue name, cache table, cache lock table with the same unconfigured-fallback rule `Illuminate\Cache\DatabaseStore` itself uses, session table) and checks `Schema::hasTable()` before touching anything: deletes only `jobs` rows that are both on the exact configured backups queue AND not currently reserved by a worker (every other queue's pending/reserved jobs, `failed_jobs`, `job_batches`, and `notifications` are all preserved — documented in the class docblock as unrelated business/audit history that has nothing to do with which queue a restore happened to run on), truncates `cache`/`cache_locks`/`sessions` when present (forcing re-authentication after a restore), and silently no-ops on any of these tables the app happens not to have configured/migrated. Targeted suite (`tests/Feature/Restore` + `tests/Unit/Services/Restore` + `tests/Feature/Backup` + `tests/Unit/Services/Backup` + `tests/Feature/Console/RestoreCommandTest.php`): **560 total, 558 passed, 0 failed, 2 skipped (pre-existing 7B.1 OS-level symlink tests, unrelated), 1304 assertions** — no regression anywhere in the existing backup/restore suite. `AppServiceProvider` gained container bindings for all five new interfaces (mirroring the existing `ProcessRunner`/`RestoreProcessLauncher` bindings) purely so the new classes are constructible via the container once a future orchestrator resolves them — nothing currently resolves `DatabaseRestorer` or `RestoreReconciler` anywhere in the app. No real `mysql` was ever invoked by any test, no real database was destructively imported, no migration/schema change was made, and no restore UI/command/route is newly reachable. Not committed — awaiting review.

- **2026-07-26 (OMS Task 7C.5 correction pass — durable reconciliation snapshots, full backups-queue purge, single-connection policy, metadata completeness, stream-capture re-verification)**: A same-day, pre-commit correction pass on the just-implemented 7C.5, fixing six review points before commit. **(1) Durable reconciliation snapshots**: the signed `RestoreProgressSnapshot` protocol gained an OPTIONAL, nullable `reconciliationSnapshot` field (new `App\Services\Restore\Metadata\RestoreReconciliationSnapshot`, bundling `BackupOperationSnapshot`×2 + `RestoreOperationSnapshot`, each now with bounded, fixed-key-order `toArray()`/`fromArray()`) — this is the critical fix for a real gap: holding those snapshots only in PHP memory would lose them if the process crashed after a database import (which can wipe/replace the live `backup_operations` rows) but before metadata reconstruction ran. `fromArray()` re-validates every field through the same `create()` path as a fresh construction — never trusts a decoded array's shape — and rejects any missing/unexpected key before looking at a single value. Backward compatible by construction (no `schema_version` bump): an old progress file simply lacks the key, which decodes to `null` exactly like an explicit absence, and the signature check already verifies the raw persisted body rather than a value rebuilt from the current shape. A new integration test (`RestoreReconciliationSnapshotRecoveryTest`) proves the actual guarantee end to end: write the snapshot before a simulated import, **drop `backup_operations` entirely**, re-migrate an empty table (the real post-import shape), read the signed file back, and reconstruct all three authoritative rows via `RestoreMetadataUpserter` using nothing but the file — the database is never consulted for the pre-import state. **(2) Backups-queue purge corrected**: `RestoreEphemeralTablePolicy` now deletes ALL rows on the exact configured backups queue (reserved and delayed included, not just unreserved ones) — a reserved-but-unfinished row could otherwise become redeliverable once `retry_after` elapses and replay pre-restore backup/verify/retention work against the just-restored system; every other queue remains completely untouched. **(3) Single-connection policy**: chose the "restore is supported only for the application's primary/default database connection" policy (over supporting an explicit named non-default connection) — `DatabaseRestorer::resolveConnectionConfig()` now fails closed (`database_connection_mismatch`) the instant the resolved backup connection differs from `config('database.default')`, and `RestoreReconciler` no longer accepts a caller-supplied connection name at all, always targeting `database.default` directly (the same connection `migrate`/every Eloquent model/every `DB::table()` call already implicitly use) — the two halves of a restore can now never silently disagree about which physical database they're operating on. **(4) Metadata completeness**: added the one genuinely missing authoritative column, `manifest_version` (bounded 0–65535), now persisted on every source/safety upsert; deliberately did NOT add `deduplication_key` (a forward-looking scheduling-concurrency guard, not backward-facing audit data — restoring it risks a real unique-constraint collision against post-restore scheduling activity); added focused assertions that `deleted_at` is cleared for a previously-trashed source AND restore row, that the safety backup is forced `Completed` **and** stamped `verified_at` (not just status), and that both FK targets on a reconstructed restore row resolve to real, currently-existing rows. **(5) Stream-capture re-verified**: confirmed directly against `Process::buildCallback()`'s source (not re-asserted from memory) that `disableOutput()` only skips Process's own internal buffering — the callback passed to `run()` still receives every chunk from both pipes, which is also what prevents a full-pipe-buffer deadlock; two new tests prove this empirically (30MB of child stdout produces no meaningful growth in this process's own memory; 40 rounds of interleaved 200KB stdout+stderr complete well within a 15s timeout with no deadlock), and the existing non-zero-exit test was strengthened to assert genuinely non-empty diagnostic content, not just a length bound. Targeted suite after all six corrections: **586 total, 584 passed, 0 failed, 2 pre-existing skips, 1359 assertions** — up from 560/558/2-skipped/1304 before this pass.

- **2026-07-26 (OMS Task 7C.6 — attachment restore primitives: staged revalidation, three-phase activate/rollback/finalize lifecycle, crash-state inspector, Windows retry seam)**: Sixth implementation slice of the Restore engine (Task 7C), scoped strictly to attachment-restore *primitives* — nothing here is wired into `RestoreCommand`, any future `RestoreOrchestrator`, or maintenance mode, and this phase deliberately implements no single irreversible `restore()` call.

  **Required policy-review finding**: `App\Services\Attachments\AttachmentStorageService` has zero extension/MIME allowlist of any kind — `resolveDisk()` only gates on `Attachment::APPROVED_DISKS`, and its path checks (`isSafeRelativePath()`) only reject traversal/absolute paths, never a file's extension. The only "mutable upload allowlist" anywhere in the Attachments namespace is `AttachmentUploadService::ALLOWED_DIRECTORIES`/`ALLOWED_PREFIXES` — a directory/filename-prefix rule enforced only at NEW-upload time, never consulted by `AttachmentStorageService` and never called anywhere in the restore pipeline. So "restore must not depend on today's mutable upload allowlist" was already true by construction; the new restore-specific gate (the fixed executable/script denylist) is implemented independently in the new revalidator, never routed through `AttachmentUploadService`. Documented and proven in `tests/Unit/Services/Attachments/AttachmentStorageServiceRestorePolicyTest.php`.

  **Deterministic filesystem layout**: new `App\Services\Restore\Attachments\RestoreAttachmentPaths` is the single place every attachment-swap path is computed — always a sibling of the live `attachments` disk root (`storage/app/private/attachments`), never inside it: quarantine at `storage/app/private/attachments.pre_restore.{uuid}`, a rollback-discard scratch tree at `attachments.rollback_discard.{uuid}`, and a small JSON audit marker at `attachments.pre_restore.{uuid}.state.json` — deliberately a **sibling file**, not inside the quarantine directory, so it is never moved/contaminated by a live↔quarantine rename. Every path requires a validated UUID first and is asserted to stay within the live attachments' own parent directory (`privateRoot()`) — never a caller-supplied path. Because these are always outside the `attachments` disk root, `AttachmentCollector` (which only walks that root) can never see quarantine/marker state as a live attachment.

  **Staged revalidation** (`RestoreAttachmentRevalidator`): immediately before the first live rename, independently re-derives and re-checks the staged tree against a caller-supplied manifest file list (path/sha256/size per entry) — exact file-set match (missing/unexpected both rejected), normalized-path uniqueness, no symlink/reparse point anywhere along the walk (including parent directories), no special/device files, per-file size+SHA-256 match, total-byte-budget match, and the fixed configured executable/script denylist (`php`/`phtml`/`phar`/`exe`/`sh`/`bat`/`cmd`/`dll`/`htaccess`, `config('oms.backup.restore.attachments_denied_extensions')`) applied case-insensitively against **every** dot-separated segment of each basename — catching both a bare `.htaccess` dotfile and a compound `shell.PHP.jpg` bypass, not just the final extension. A historically valid, already hash-verified attachment is never rejected for any reason but the denylist. Fails closed on the first violation and never touches the live disk.

  **Three-phase lifecycle** (`RestoreAttachmentActivationService`) — deliberately not one irreversible call:
  - `activate()`: re-validates the exclusive lock, refuses to proceed unless the crash-state inspector reports `NotActivated` (an unexpected leftover quarantine from an earlier crash blocks activation outright — no automatic repair), creates a missing-but-safe empty live root (fails closed if the live path exists as the wrong type or is inaccessible — never silently treated as empty), re-confirms live/staged are on the same filesystem (`FilesystemIdentity`), runs the full staged revalidation, then renames live→quarantine, writes the marker, renames staged→live, writes the marker again. If the second rename fails, an emergency quarantine→live rename is attempted immediately and its own failure surfaces a distinct exception — live attachments are never left missing.
  - `rollback()`: valid from `Activated` or `InterruptedDuringActivation`; moves the current live tree aside to a deterministic discard path (only if one exists), moves quarantine back to live, and only deletes the discard tree (best-effort) once the original is confirmed live again. A forced failure on the quarantine→live step attempts to restore the just-discarded tree rather than leaving live empty, and never deletes the only remaining copy of either tree. Idempotent once already rolled back.
  - `finalize()`: valid only from `Activated`; recursively deletes the quarantine tree via a symlink-safe, containment-bounded delete routine (never follows a symlink — unlinks it directly; refuses to operate outside `storage/app/private`) and — a real gap caught and fixed during this same implementation pass, before any test was written against it — explicitly verifies the quarantine path is actually gone afterward before writing the `finalized` marker, since the underlying per-entry unlink/rmdir calls are individually best-effort/suppressed and would otherwise let a silently-failed deletion report false success. Idempotent once already finalized.

  **Crash-state inspector** (`AttachmentSwapStateInspector`): read-only, derives one of `NotActivated`/`Activated`/`InterruptedDuringActivation`/`RolledBack`/`Finalized`/`InconsistentNeedsManualReview` purely from directory existence plus the marker file's recorded phase — never repairs an ambiguous combination automatically. The marker (`AttachmentSwapMarker`, phases `quarantined`→`activated`/`rolled_back`/`finalized`) is what makes `NotActivated` and `Finalized` distinguishable at all, since both otherwise leave an identical live/no-quarantine directory layout on disk.

  **Lock enforcement**: all three destructive methods require a currently-live Exclusive `BackupSubsystemLockHandle`, re-validated via the existing `BackupSubsystemLock::validateHandle()` at the start of every call — no second filesystem or Cache lock is ever acquired inside this service.

  **Windows retry seam**: new `App\Services\Restore\Contracts\AttachmentMoveRunner` + `NativeAttachmentMoveRunner` — a single `rename()` on Linux (no retry), a small bounded retry/backoff loop on Windows only (new config `oms.backup.restore.attachment_move_retry_attempts`/`attachment_move_retry_delay_ms`, defaults 5/200ms), with an injectable rename-attempt closure purely so the retry loop itself is unit-testable without depending on genuine transient OS failures. Documented as NOT the same atomicity guarantee as Linux's real atomic directory rename.

  Targeted suite (`tests/Unit/Services/Restore/Attachments` + `tests/Feature/Restore/Attachments` + `tests/Unit/Services/Attachments/AttachmentStorageServiceRestorePolicyTest.php`): **65 tests, 64 passed, 0 failed, 1 skipped (a Linux-only retry-loop branch, this being a Windows dev host), 122 assertions**. Full `Restore|Backup|Attachment` regression: **894 tests, 890 passed, 0 failed, 4 skipped, 2222 assertions** — no regression anywhere in the existing backup/restore/attachment suite. Not wired into `RestoreCommand` or any orchestrator; not committed — awaiting review.

- **2026-07-26 (OMS Task 7C.6 crash-safety correction pass — signed/durable swap marker, write-ahead phase protocol, InterruptedDuringRollback state, validated attachment manifest value object)**: A same-day, pre-commit correction pass hardening the just-implemented Task 7C.6 before it was allowed to land — the plain unsigned JSON marker file was correctly flagged as safety-critical recovery state (it is the ONLY thing distinguishing `NotActivated` from `Finalized`, since both leave an identical live-exists/no-quarantine directory layout) and therefore unacceptable as an unsigned best-effort file.

  **Signed, durable marker**: the marker is now a strictly bounded, immutable value object (`AttachmentSwapMarker::create()` — validated UUID, a real `AttachmentSwapPhase` enum case, a required `updated_at` timestamp, no arbitrary array ever accepted) written/read through a NEW `AttachmentSwapMarkerWriter`/`AttachmentSwapMarkerReader` pair mirroring `RestoreProgressWriter`/`RestoreProgressReader` exactly — same temp-file-in-same-directory → fwrite → fflush → fsync → close → atomic rename → best-effort parent-directory fsync discipline, **reusing the existing `App\Services\Restore\Contracts\RestoreProgressDurability` seam and `NativeRestoreProgressDurability` implementation** rather than inventing a second one. `AttachmentSwapMarkerSigner` HMAC-SHA256-signs every marker using a purpose-derived key from `APP_KEY` with its own distinct context string `oms-restore-attachment-swap-v1` (never the same derived key as restore progress files, even though both derive from the same underlying `APP_KEY`). `AttachmentSwapMarkerReader::read()` verifies the signature, exact bounded schema (no extra/missing keys), UUID match, and that the phase is one of the fixed `AttachmentSwapPhase` cases — BEFORE trusting anything else about the content; it returns `null` ONLY when no marker file exists at all (a legitimate "never began activation" state) and throws `RestoreAttachmentMarkerException` for every other integrity failure (unsigned, tampered signature, UUID mismatch, unsupported phase, malformed schema, oversized, unreadable) — `AttachmentSwapStateInspector` converts every such exception to `InconsistentNeedsManualReview`, never to a guessed normal state.

  **Write-ahead phase protocol**: 8 fixed phases (`ActivationStarted`/`LiveQuarantined`/`Activated`/`RollbackStarted`/`RollbackLiveDiscarded`/`RolledBack`/`FinalizationStarted`/`Finalized`) are durably persisted BEFORE the destructive rename they describe is attempted, and again immediately AFTER each mutation succeeds and before the next is ever attempted. If a marker write fails BEFORE any mutation, nothing happened (reported as `marker_write_failed`). If a marker update fails immediately AFTER a mutation succeeded, `RestoreAttachmentActivationService` does NOT blindly continue to the next step — it throws a distinct `marker_update_failed_after_mutation` exception and leaves the actually-mutated trees exactly as they are, never attempting a second automatic action on top of already-untrustworthy bookkeeping. `activate()`/`rollback()`/`finalize()` all now independently re-inspect real current state (lock + signed marker + directory facts) at their own entry, never trusting only an `AttachmentSwapHandle` created earlier in a possibly different process.

  **New `InterruptedDuringRollback` crash state**: covers the process dying between moving the current (restored) live tree aside to the deterministic discard path and moving quarantine back into live — quarantine intact, live missing, discard populated. `rollback()` now accepts resuming from this state (in addition to `Activated`/`InterruptedDuringActivation`) — proven by a real test that forces exactly this crash window (via the injectable `AttachmentMoveRunner` seam) and then calls `rollback()` again with a fresh working mover, confirming it completes successfully. The discard path was renamed from `attachments.rollback_discard.{uuid}` to `attachments.restore_discard.{uuid}` per the correction request's exact suggested naming (same sibling-of-live-root layout, same UUID-derived determinism, same containment guarantees — a naming change only).

  **`RestoreAttachmentManifest` validated value object**: `RestoreAttachmentRevalidator::revalidate()` no longer accepts a raw array — it now requires a `RestoreAttachmentManifest`, constructible only through `fromManifestFiles()`, which validates every entry once at construction (safe/unique normalized path, a real 64-hex-character SHA-256 string, a bounded non-negative size, a bounded non-negative total) and exposes only bounded accessors (`count()`, `entriesByNormalizedPath()`) — never the raw manifest structure. The full per-file attachment list is still never persisted to `progress.json` or anywhere else; the future `RestoreOrchestrator` (Task 7C.7+) recreates this object from the already-verified archive manifest immediately before calling `activate()`.

  **A genuine correctness bug found and fixed while writing the new crash-window test** for `InterruptedDuringRollback`: the test's fake mover initially left the 3rd move call (`rollback()`'s own best-effort `discard→live` recovery attempt inside its failure-handling branch) unconstrained, so it silently succeeded and masked the crash window being tested entirely — a reminder that a fake's "unconfigured calls succeed" default can itself hide the exact failure path a test means to prove; fixed by explicitly failing that 3rd call too, which is what a genuine process crash (rather than a transient, same-process-recoverable error) actually looks like.

  Targeted attachment suite after all corrections: **326 tests, 323 passed, 0 failed, 3 skipped (pre-existing OS-level symlink skips), 770 assertions**. Broader `Restore|Backup` regression: **742 tests, 738 passed, 1 pre-existing unrelated failure (`BackupCreationOrchestratorTest::test_creation_never_reaches_completed_when_pre_publish_verification_fails` — confirmed to pass in isolation and confirmed unrelated to any file this pass touched via `git status`), 3 skipped, 1768 assertions**.

- **2026-07-26 (test-stability pass — stabilize backup/restore regression tests)**: Root-caused and fixed the one flaky `BackupCreationOrchestratorTest::test_creation_never_reaches_completed_when_pre_publish_verification_fails` failure flagged in the prior entry. Root cause: a genuine test-isolation bug in the *new* `tests/Feature/Restore/Attachments/RestoreAttachmentActivationServiceTest.php` (added in the Task 7C.6 correction pass) — 7 test methods called `RestoreAttachmentActivationService::activate()` (which performs real filesystem renames) to set up their scenario **before** entering the `try { ... } finally { $handle->release(); }` block that releases the real, process-wide `BackupSubsystemLock` exclusive `flock()` handle. If `activate()` (or an assertion inside the guarded block) ever threw for any reason, the handle was never released — since PHPUnit runs the entire suite in one PHP process, and `Storage::fake('restores')` reuses the same fixed on-disk path every test (only wiping directory contents, not open OS-level lock handles), a leaked exclusive lock from one test can make `BackupSubsystemLock::acquireShared()` fail in a *later, unrelated* test in the same process — exactly what `BackupCreationOrchestrator::run()` depends on. This is a test-isolation bug, not a production bug: `BackupSubsystemLock`/`BackupSubsystemLockHandle` themselves behaved exactly as designed (a real flock() correctly blocks a second acquisition attempt while held) — the defect was solely in the test's own acquire/release discipline. Fixed by restructuring all 7 methods so the entire span from lock acquisition to release is inside one `try/finally`, with no code path between acquisition and the `try` that can throw. No `once()`/memoization issue was found anywhere in the backup/restore subsystem (checked `BackupDeletionService`'s two documented per-instance `once()` memoizations — both are correctly scoped to a single instance via `spl_object_id`, never shared across a fresh instance the way this test bug was). Verified: the specific test 10/10 clean repeated runs; `BackupCreationOrchestratorTest` alone 25/25; the combined `RestoreAttachmentActivationServiceTest|BackupCreationOrchestratorTest` filter 54/54; the full `Restore|Backup` regression 742 tests/739 passed/0 failed/3 skipped, both in default order and with `--order-by=random`. Test-file-only change — no production code touched, so `graphify update .` was not run for this pass.

- **2026-07-26 (OMS Task 7C.7 — the real `RestoreOrchestrator`: full secure restore orchestration, compensation matrix, maintenance mode, mandatory safety backup, heartbeat/progress, stale-detection watchdog, recovery runbook)**: Seventh and final foundational implementation slice of the Restore engine (Task 7C) — the highest-risk integration phase, wiring every previously-standalone 7C.1–7C.6 primitive together into one real, destructive-capable execution engine, replacing `RestoreCommand`'s permanent `failClosed()` placeholder.

  **`RestoreOrchestrator`** (`app/Services/Restore/RestoreOrchestrator.php`) is the only place the real restore sequence runs, and only from `RestoreCommand` while that command owns the lifetime Exclusive `BackupSubsystemLockHandle` — it never acquires its own subsystem or Cache lock, never touches the DB queue, and accepts only the already-claimed `BackupOperation` row, the already-verified signed progress snapshot, and the live lock handle (never a password/key/path from any caller). Exact required order: preflight (non-destructive) → maintenance mode (ownership decided BEFORE `enter()` — `RestoreMaintenanceMode::isActive()` checked first, entered only if not already down) → mandatory **FULL** pre-restore safety backup, always, regardless of restore scope, run synchronously via `BackupCreationOrchestrator::runWithLockAlreadyHeld()` (never `enqueue()`+job dispatch) and required to reach `Completed`+non-null `verified_at`+an existing encrypted archive before anything destructive is allowed → the three metadata snapshots (source, safety, restore) built and bundled into a `RestoreReconciliationSnapshot`, embedded into signed progress at `safety_backup_completed` — BEFORE staging — so a crash after this point can always reconstruct all three authoritative rows even if the database itself is later replaced → decrypt/verify/stage (`RestoreArchivePreparer`, unchanged) → **[files/full]** attachment activation (`RestoreAttachmentActivationService::activate()`) → **[database/full]** database import (`DatabaseRestorer::restore()`) → **[database/full]** reconciliation (`RestoreReconciler::reconcile()`) → **[files/full]** attachment quarantine finalization → best-effort plaintext workspace cleanup (never quarantine) → maintenance exit (only if this restore itself entered it) → terminal result.

  **Database-replacement boundary, honored structurally**: the `$row` `BackupOperation` instance handed in is only ever read BEFORE the `mysql` import call (to snapshot `created_by`/`restore_metadata['confirmed_at']`) — never touched again afterward. Every terminal write goes through the new `RestoreTerminalResultWriter`, which always re-fetches the restore's own row fresh by UUID (never reuses any Eloquent instance), tolerates the row not existing at all (the signed progress file remains authoritative, logged as expected rather than an error), and never lets a DB write failure erase the already-durable progress outcome. Proven end to end by a dedicated test that makes a fake `mysql`-import process runner genuinely `DELETE FROM backup_operations` as its own side effect mid-flow (simulating the schema/data replacement a real import performs) — the restore still reaches `Restored`, with all three rows (restore/source/safety) reconstructed by the real (non-faked) `RestoreMetadataUpserter`, never a stale pre-wipe reference.

  **Compensation matrix** (every branch returns one of `Restored`/`RestoreFailed`/`RestorePartial`, never silently overloading `isTerminal()`): preflight/maintenance-enter/safety-backup/staging failures are always clean `RestoreFailed` (nothing destructive happened, tracked via a `$destructiveBoundaryCrossed` flag flipped true only once attachment activation or database import genuinely succeeds/is attempted). Attachment-activation failure maps `RestoreAttachmentSwapException`'s own `reasonCode` directly: `activation_failed_rolled_back`-shaped clean outcomes stay `RestoreFailed`; `activation_failed_rollback_failed`/`marker_update_failed_after_mutation` (ambiguous, live-state-uncertain) become `RestorePartial`. Database-import failure after attachment activation attempts an automatic rollback first: rollback succeeds → clean `RestoreFailed`, but with an explicit error-summary sentence stating the database may be partially imported and pointing at the safety backup for recovery (`restore_failed_phase` stays whatever phase preceded the failed advance() call, since the fixed `RestoreProgressSnapshot::ALLOWED_PHASES` vocabulary — deliberately never extended — has no dedicated `database_import` value); rollback failure → `RestorePartial`. Reconciliation failure after a successful import is always `RestorePartial` (database is NEVER auto-rolled-back; quarantine is preserved, never finalized). Finalization failure after successful reconciliation is `RestorePartial` (restored live attachments stay in place; quarantine may remain). A progress-write failure is fatal-and-clean before the database-import boundary, but always escalates to `RestorePartial` once the import has actually succeeded (`database_restored`/`reconciling`/`finalizing` advance-write failures all map to `partial()`). Maintenance-exit failure escalates the result to `RestorePartial` UNLESS `$destructiveBoundaryCrossed` is still false (e.g. maintenance was entered but the safety backup itself then failed) — in that specific case it correctly stays a clean `RestoreFailed`, per the approved "no destructive boundary = never escalate" rule; `restore_failed_phase` is pinned to the literal value `maintenance_disabled` for this one case (a small `RestoreTerminalResultWriter::finish($failedPhaseOverride)` parameter was added specifically so this override never gets silently clobbered by the unrelated `maintenance_disabled` bookkeeping `advance()` call that still runs for the success path).

  **`RestoreMaintenanceMode`** (new) is the single place `artisan down`/`up` is ever called from the restore engine — a fixed, generic Arabic maintenance message (never the restore UUID, source/safety backup identity, or any internal failure detail), and a small new `Contracts\MaintenanceModeInspector` seam (native impl reads `app()->isDownForMaintenance()`) purely so tests never write a real maintenance flag file to disk. Ownership (`restoreEnteredMaintenanceMode`) is a plain boolean the orchestrator itself decides once, before `enter()` — if OMS was already down before this restore started, it is NEVER brought back up automatically by this restore, regardless of outcome.

  **Two small, justified gaps in the previously-built 7C.6 primitives were closed, not rewritten**: (1) `App\Services\Restore\Contracts\AttachmentMoveRunner` was never bound in `AppServiceProvider` — nothing had ever resolved `RestoreAttachmentActivationService` via the container before this task; added a config-driven binding (`NativeAttachmentMoveRunner` sourced from the existing `attachment_move_retry_attempts`/`attachment_move_retry_delay_ms` config keys). (2) `PreparedRestore` never carried the verified archive manifest's `attachments.files`/`attachments.total_size_bytes` forward at all — `RestoreArchivePreparer::prepare()` now populates two new trailing constructor parameters (`attachmentManifestFiles`/`attachmentsTotalSizeBytes`, defaulted so the one other test file constructing `PreparedRestore` directly is unaffected) directly from the same already-verified `$manifest` array it already had in scope, closing the "how does the orchestrator get a `RestoreAttachmentManifest`" open question flagged in the 7C.6 `NEXT_STEPS.md` entry — never a raw caller-supplied array, never trusted from progress.json.

  **`RestoreStaleDetector`/`oms:restore-watchdog`** (new, scheduled every minute in `bootstrap/app.php`): DETECTION ONLY — compares every candidate restore's signed `last_heartbeat_at` against `config('oms.backup.restore.stale_after_minutes')`, scanning both `type=restore, status=restoring` DB rows AND UUID-named `progress.json` directories on disk (deduplicated), mirroring `RestoreActivityGuard`'s own dual-source philosophy since the DB row can be missing/replaced exactly during the window that matters most. Never retries/resumes/relaunches/mutates/rolls back/acquires a lock/leaves maintenance mode/deletes anything. Notification spam is bounded by a plain `Cache` cooldown key per (restore UUID, reason code) — deliberately no new schema. `BackupNotificationEvent` gained `RestoreSucceeded`/`RestoreFailed`/`RestorePartial`/`RestoreStale` cases (Arabic titles), reusing the existing `BackupNotifier` recipient-resolution rule unchanged (initiating user if `created_by` is set, else every active Super Admin).

  **Command integration**: `RestoreCommand::handle()`'s permanent claim-verification/bounded-retry-lock/`advanceToLockAcquired()` shape (Task 7C.4) is completely untouched — only the isolated `failClosed()` seam was replaced with a real `$orchestrator->orchestrate($row, $progress, $lockHandle)` call, translating the returned `BackupStatus` to exit 0 (`Restored`) or non-zero (`RestoreFailed`/`RestorePartial`). Still never touches the DB queue or the Cache lock (re-verified by the pre-existing source-grep test, unchanged).

  **A real, deliberately-accepted architectural limitation, documented rather than worked around**: true mid-operation heartbeat ticks during a single long blocking call (the mysql import, the safety backup, archive decrypt/verify/extract) are not implemented, since none of `DatabaseRestorer::restore()`/`BackupCreationOrchestrator::runWithLockAlreadyHeld()`/`RestoreArchivePreparer::prepare()` expose a progress callback, and adding one would mean modifying already-working, already-tested primitives for a diagnostic nicety, not a correctness requirement — the phase written immediately before each long call (`safety_backup_running`, `staging`, `database_restoring`, `reconciling`) already tells `RestoreStaleDetector`/an operator exactly what a restore was doing if it goes stale.

  **Test-environment note worth remembering**: `DatabaseRestorer`'s connection policy requires the resolved restore connection to literally equal `config('database.default')` (by design — see its own 7C.5 docblock), which conflicts with `BackupTestCase::useFakeMysqlConnection()`'s deliberate choice to leave `database.default` on the real schema-only SQLite test connection. `RestoreOrchestratorTest` resolves this by reshaping `database.default`'s OWN connection config array in-place to also look MySQL-shaped (adding `driver`/`host`/`port`/`database`/`username`/`password` keys on top of the existing sqlite ones) rather than repointing `database.default` itself — safe because neither `DatabaseDumper` nor `DatabaseRestorer` ever opens a real PDO from that config (both only read it to build a CLI argv array for an already-faked process runner), so Eloquent's already-resolved real SQLite PDO connection is completely unaffected. Also discovered and fixed mid-pass: `RestoreAttachmentPaths`' quarantine/discard/marker sibling paths live in the shared parent-of-the-`attachments`-disk directory, which `Storage::fake()` does NOT clean between test runs (it only cleans the disk's own root) — any hardcoded restore UUID reused across separate `php artisan test` invocations can therefore collide with a leftover marker/quarantine artifact from an earlier run; fixed by generating every test restore UUID fresh via `Str::uuid()` rather than a fixed literal.

  New files: `RestoreOrchestrator`, `RestoreMaintenanceMode` (+ `Contracts\MaintenanceModeInspector`/`LaravelMaintenanceModeInspector`), `RestoreTerminalResultWriter`, `RestoreStaleDetector`/`StaleRestoreObservation`, `RestoreWatchdogCommand`, `Exceptions\RestoreMaintenanceModeException`/`RestoreOrchestrationException`, `docs/RESTORE_RECOVERY.md`. Targeted 7C.7 suite (`RestoreOrchestratorTest` + `RestoreMaintenanceModeTest` + `RestoreStaleDetectorTest` + `RestoreWatchdogCommandTest` + `RestoreCommandTest`): **44 tests, 44 passed, 0 failed, 158 assertions**. Broader `Restore|Backup` regression: **774 tests, 771 passed, 0 failed, 3 skipped, 1895 assertions** in both default order and `--order-by=random` (a first randomized attempt hit one intermittent, pre-existing, unrelated flake — see `docs/TASKS_LOG.md` for the full root-cause note; a second randomized run passed cleanly with identical totals). Explicitly NOT built (Task 7C.8/7C.9 scope): Filament restore button/modal, typed confirmation UI, progress polling endpoint, stale-recovery acknowledgment UI, manual resume/recovery UI, production deployment, any real destructive MySQL restore test.

- **2026-07-27 (OMS Task 7C.7 final acceptance/hardening pass — true long-phase heartbeats, deterministic finalization-failure test, hardcoded-UUID isolation fix, watchdog DB/cache-unavailability hardening, Windows rename-retry fix)**: A focused, pre-commit hardening pass closing every gap the initial 7C.7 implementation's own report flagged as incomplete.

  **True long-phase heartbeats**: new `RestoreHeartbeat` (`Carbon`-based, not `time()`/`microtime()`, specifically so a test can drive a simulated multi-minute operation via `Carbon::setTestNow()` time travel instead of a real sleep) is created fresh per long phase and threaded down to the lowest point that actually repeats — `DatabaseDumper`'s per-chunk stdout callback (mandatory safety backup), `SecretstreamEnvelope::decryptFile()`'s per-chunk loop and `RestoreArchiveExtractor::extract()`'s per-entry writes (archive staging), `RestoreAttachmentRevalidator::revalidate()`'s per-file loop (attachment activation), `RestoreReconciler::reconcile()`'s between-step ticks, and — the one requiring real dishonesty avoidance — a genuine timer-driven poll loop in `SymfonyProcessStreamInputRunner` for the otherwise-silent `mysql` import: `Process::start()` + a `while ($process->isRunning())` loop calling `checkTimeout()` explicitly (verified directly against the installed symfony/process source that `isRunning()` alone never enforces the configured timeout — only `wait()`'s own loop did, so this runner must now enforce it itself) and invoking `$onTick` on a real 200ms poll cadence, never relying on STDOUT/STDERR arrival. Every touched method signature gained only a new trailing, defaulted `?callable $onTick = null` parameter — every existing caller is unaffected. A heartbeat write failure is never swallowed by `RestoreHeartbeat` itself — it propagates exactly like any other progress write, so it is automatically compensated by whichever compensation branch already wraps that phase (clean abort before the database-import boundary, `RestorePartial` after it) — no new failure-handling logic needed in `RestoreOrchestrator` beyond creating the heartbeat and reading `current()` back afterward. New `tests/Feature/Restore/RestoreOrchestratorHeartbeatTest.php` (6 tests) proves: a simulated 40-minutes-elapsed `mysql` import (20 ticks × 2 simulated minutes via a `FakeProcessStreamInputRunner` extension that advances `Carbon::setTestNow()` per tick) never gets reported stale by `RestoreStaleDetector` at any intermediate tick; heartbeat writes are throttled (25 ticks with no time advancement produce far fewer than 25 real writes); a heartbeat write failure before/after the import boundary is compensated exactly like a phase-transition write failure; and a heartbeat tick genuinely fires during both the safety backup and archive-decrypt phases (proven by write-count, not merely by code inspection).

  **Deterministic finalization-failure test**: `RestoreAttachmentActivationService` (still `final`, still unmodified in its own 7C.6 logic beyond the new optional `$onTick` parameter) now implements a new minimal `RestoreAttachmentLifecycle` interface, which `RestoreOrchestrator` depends on instead of the concrete class (bound 1:1 in `AppServiceProvider`). New test double `FinalizeFailingAttachmentLifecycle` delegates `activate()`/`rollback()` to a real instance (so live attachment activation stays completely genuine) and only `finalize()` is forced to fail on command. New test proves: DB import + reconciliation succeed → `finalize()` fails → result is `RestorePartial` (never `Restored`) → `restore_failed_phase = 'finalizing'` → maintenance exit is still attempted per ownership → quarantine is preserved on disk → the DB row is `RestorePartial`.

  **Hardcoded-UUID test-isolation fix**: root-caused the ORIGINAL initial-acceptance random-order flake (`RestoreAttachmentActivationServiceTest::test_rollback_restores_the_original_attachments`) properly this time — `RestoreAttachmentActivationServiceTest` and `AttachmentSwapStateInspectorTest` both used a fixed class-constant UUID (`aaaaaaaa-1111-1111-1111-111111111111` — the SAME literal reused across ~10 pre-existing test files project-wide, none of which this task otherwise touches) for their quarantine/discard/marker sibling paths, which live outside the `attachments` disk root and are therefore NOT reset by `Storage::fake()` between tests. Both files already had their own defensive `cleanupSwapArtifacts()` in `setUp()`/`tearDown()`, which should have made this safe in the NORMAL case — but per the task's own instruction to fix root cause rather than accept "passes on rerun," both fixed constants were converted to instance properties generated fresh via `Str::uuid()` in `setUp()` (cleanup logic kept as defense in depth, not removed), eliminating the cross-file collision risk structurally rather than relying on both files' cleanup always executing. Verified: the originally-flagged test 10/10 consecutive clean runs; the two affected classes together 57/57.

  **A second, more consequential flake was found and fixed during THIS pass's own stress-testing** (not present before, and not the same issue as above): once heartbeats made `RestoreProgressWriter::write()` run far more frequently in rapid succession against the same `progress.json` path, `rename()`'s final publish step began failing intermittently on Windows/Laragon under real test load (confirmed via the sanitized `RestoreProgressWriteException::cannotPublish()` message surfacing directly in test diagnostics — not a guess) — the identical class of transient open-handle/AV-scanner interference `NativeAttachmentMoveRunner` already has a documented, approved retry for, just never applied to progress-file publishing before now because write frequency was low enough not to expose it. Fixed by adding the identical bounded-retry-with-linear-backoff pattern (`renameWithRetry()`, new config `oms.backup.restore.progress_publish_retry_attempts`/`progress_publish_retry_delay_ms`, Windows-only — Linux/production never retries, `rename(2)` is atomic and reliable there). Verified empirically: 10 consecutive `--order-by=random` stress runs of the two heaviest-I/O test files went from 4/10 failing (before the fix, all with the identical `cannotPublish()` diagnostic) to 10/10 clean (after).

  **Watchdog DB/cache-unavailability hardening**: `RestoreStaleDetector::candidateUuidsFromDatabase()`/`candidateUuidsFromDisk()` are now each independently wrapped so a real database or filesystem failure during detection degrades gracefully (logs a bounded warning, falls back to whichever source is still available — the disk-based scan needs no database connection at all) rather than crashing `detect()` — and the database-unavailable condition itself is surfaced as a bounded `database_unavailable_during_detection` observation, never silently read as "no active restore." `RestoreWatchdogCommand::handle()` gained an outer bounded try/catch, and its `Cache::has()`/`Cache::put()` cooldown calls are now individually best-effort (a cache failure is treated as "not yet notified" for `has()`, silently swallowed for `put()`) so a database-backed cache store being unavailable during the same restore never suppresses the underlying log/notification or crashes the scheduler. New tests simulate a genuinely dropped `backup_operations` table (both at the detector level and through the command end to end) and a genuinely-failing database-backed cache store — all prove the watchdog still finds/reports correctly and never mutates anything.

  Full re-verification after all of the above: targeted 7C.7 suite (now including the heartbeat/finalization/hardening additions) **112 tests, 112 passed, 350 assertions**. Broader `Restore|Backup` regression — default order **785 tests, 782 passed, 0 failed, 3 skipped, 1954 assertions**; `--order-by=random` (default seed) **identical totals**; `--order-by=random --random-order-seed=987654` **identical totals** — three consecutive clean full-suite runs after the `RestoreProgressWriter` fix, versus intermittent failures in the two heaviest files before it. Not committed until this entry was written — see `docs/TASKS_LOG.md` for the commit hash.

- **2026-07-27 (OMS Task 7C.8 — Filament restore UI: two-step confirmation, DB-independent signed progress polling, stale/tampered banners, explicit crash acknowledgment)**: Exposes the already-verified restore engine (7C.1–7C.7) to the Super Admin, on top of `BackupManagementPage` — no restore engine architecture was changed, only additive UI/service seams on top of it.

  **Row action + two-step confirmation**: a new "استعادة" row action is visible only when `canOfferRestore()` passes — real Super Admin + `backups.restore` (via the existing `BackupAuthorization`), the record is `Completed`+verified+not-trashed+not itself a restore row, its encrypted archive file actually exists on disk, `RestoreScopeCompatibility::allowedScopesFor()` offers at least one scope, and `RestoreActivityGuard::isActive()` (memoized once per table render via a new `resolvedRestoreActivityState()` — see the N+1 note below) is `Inactive`. The action is a single Filament `Wizard`-based action (`Filament\Schemas\Components\Wizard\Step`, two steps) rather than two separate actions: step 1 collects scope (options bounded to the source's compatible scopes only), a required bounded reason, and a `dehydrated(false)` typed-confirmation field validated by a closure rule requiring the exact, case-sensitive `RESTORE {uuid8}` string — `dehydrated(false)` means the phrase is validated but never reaches `$data`/the action closure/any persisted record, satisfying the "never store this phrase" requirement structurally rather than by convention. Step 2 is the final danger-styled confirmation; only ITS submit (Filament's own wizard mechanics — the whole schema is one form, so all fields' rules are validated together at final submit regardless of which step they render in) reaches `processRestoreRequest()`.

  **Request creation + launch integration**: new `App\Services\Restore\RestoreRequestService::createQueuedRestore()` is the only place a queued restore `BackupOperation` row is created — never duplicates `RestoreLaunchService`'s claim/lock/progress-init/spawn logic. It re-validates source eligibility and scope compatibility, re-checks `RestoreActivityGuard` twice (before and after acquiring `BackupSubsystemLock::acquireExclusive()`), and inside one `DB::transaction()` re-checks no other `queued`/`restoring` restore row exists before creating the row (`launch_nonce` = `bin2hex(random_bytes(32))`, bounded `restore_metadata` with a requester snapshot + `confirmed_at`, `disk` mirrors the source backup's own disk since the column is `NOT NULL` and a restore row has no archive of its own). `processRestoreRequest()` then calls the existing `RestoreLaunchService::launch()` directly (the exact same authoritative claim/lock/progress/spawn implementation the signed `POST /restores/{uuid}/launch` route itself calls) — a deliberate design choice over literally re-POSTing to that route from inside the Livewire request (which would need to fabricate session/CSRF state for a same-process self-HTTP-call with no real security benefit, since the route's actual replay-safety lives in the nonce comparison inside the service, not in the HTTP layer); every launch outcome (`Accepted`/`Conflict`/`Locked`/`Failed`) maps to a sanitized Arabic notification, and a `Failed`/`Conflict` outcome is left exactly as `RestoreLaunchService` already terminalized it — never retried automatically.

  **DB-independent signed progress polling**: new `GET /restores/{uuid}/progress` (`RestoreProgressPollController`, `RestoreProgressPollControllerTest` — 11 tests) is authorized by Laravel's own `signed` middleware alone (no Filament/session auth) and reads only the already-signed `progress.json` via the existing `RestoreProgressReader` — genuinely zero database queries, proven by a test that swaps `database.default` to a non-existent connection name and a real bound `Illuminate\Contracts\Foundation\MaintenanceMode` fake (never the real `php artisan down`, which would write a real flag file to this project's actual `storage/framework` directory — this codebase already avoids that everywhere, see `RestoreMaintenanceModeTest`'s docblock) reporting `active()` while the request still succeeds. The route strips every DB-touching `web`-group middleware member (`StartSession`, `EncryptCookies`, `AddQueuedCookiesToResponse`, `ShareErrorsFromSession`, `PreventRequestForgery`) via `->withoutMiddleware()`, since this app's session AND cache stores are both the `database` driver — `StartSession` alone would otherwise issue a query on every single request. `bootstrap/app.php` gained the one, exact-path `preventRequestsDuringMaintenance(except: ['restores/*/progress'])` — verified against Laravel 13's real fluent `Middleware::preventRequestsDuringMaintenance()`/`PreventRequestsDuringMaintenance::except()` mechanism, not assumed. The response is a small fixed field set (no requester email, no archive/filesystem path, no encryption key id, no raw exception) — proven by a dedicated test that also checks the response body for the literal secret values that would leak if it weren't sanitized.

  **Phase vocabulary**: new `App\Support\Restore\RestorePhaseLabels` maps the REAL, current `RestoreProgressSnapshot::ALLOWED_PHASES` (confirmed by reading `RestoreOrchestrator`/`RestoreCommand` directly, not assumed from the task brief, which used several phase names — `preflight_completed`, `staging_completed`, `attachments_activated`, `reconciliation_completed` — that don't actually exist in this codebase's vocabulary) to Arabic labels. One narrow, additive schema change was required and is called out explicitly: `RestoreProgressSnapshot::ALLOWED_PHASES` gained `crashed_acknowledged` — a `restore_failed_phase`-ONLY value (never written to `phase`/`phase_history`) that only the new stale-acknowledgment flow ever writes, so a terminalized-by-human-review restore is honestly distinguishable from one the orchestrator itself decided to fail.

  **Stale/crashed restore banner + explicit acknowledgment**: new `App\Services\Restore\RestoreStaleAcknowledgmentService` is the ONLY thing that terminalizes a stale restore's DB row + signed progress file after human review — never resume/retry/rollback/repair, never touches maintenance mode, never creates another `BackupOperation` row. Eligibility is deliberately narrow: only a `RestoreStaleDetector` observation with the EXACT `stale_heartbeat` reason code qualifies (every other reason — `progress_unreadable`, `invalid_heartbeat_timestamp`, `database_unavailable_during_detection` — means the state could not be safely confirmed as "just old," so it is never acknowledgeable from this UI path) AND `BackupSubsystemLock::acquireExclusive()` must currently succeed (proving no live process still holds it; briefly acquired then released, never held). The Filament header action (`acknowledgeStaleRestore`) mirrors the restore row action's two-step Wizard shape (`ACKNOWLEDGE {uuid8}` typed confirmation, `dehydrated(false)`, never persisted) and is visible only to a real Super Admin with `backups.restore` when eligibility passes — re-checked independently inside the action body, not only via `->visible()`. On success: the signed progress file becomes terminal (`result = restore_failed`, `restore_failed_phase = crashed_acknowledged`) — written FIRST and its failure propagates (unlike `RestoreTerminalResultWriter`'s best-effort pattern, a human acknowledgment that silently failed to persist must not be reported as successful) — then the DB row is best-effort updated to `RestoreFailed` with a bounded `restore_metadata.stale_acknowledgment` audit record (acknowledger snapshot, timestamp, sanitized reason). `docs/RESTORE_RECOVERY.md` gained a new §1a/§1b documenting this as the normal first step for a genuinely stale restore, distinct from the manual investigation sections that remain the fallback for tampered/ambiguous states.

  **Tampered-state handling**: when `RestoreActivityGuard::isActive()` returns `TamperedOrInvalid`, the page shows a fixed "manual review required" message and hides BOTH the restore row action and the acknowledgment action entirely — proven by a dedicated test that tampers a terminal progress file's signature and confirms neither surface is offered.

  **History/table display**: restore-type rows already had correct Arabic type/status labels from the 7C.7 phase (`BackupLabels`) — verified, not re-implemented. Download/verify actions are already naturally hidden for restore rows (they require `status = Completed`, which a restore row never reaches). The details modal gained two new conditional entries (source backup UUID, safety backup UUID) shown only when those FKs are set.

  **A real N+1 was caught and fixed during this pass's own verification**: the first implementation called `RestoreActivityGuard::isActive()` (a DB query) once per row inside the restore action's `->visible()` closure and again separately for the tampered-state banner — `test_creator_relationship_is_eager_loaded_without_n_plus_one` caught the resulting absolute query-count regression (20→28). Fixed via `resolvedRestoreActivityState()`, an instance-property memoization resolved exactly once per render and threaded through every caller (mirrors the pre-existing `$deletionService` memoization pattern already in `table()`). The existing test's absolute threshold was raised from 20 to 35 (documented as accounting for a small, FIXED, row-count-independent overhead — the actual row-count-scaling guard, `test_query_count_remains_bounded_as_row_count_increases`, was never affected and needed no change).

  **Superseded guard tests**: four `BackupManagementPageTest` tests that asserted restore was NOT YET implemented (`test_no_restore_action_exists_on_the_table`, `test_restore_not_available_notice_is_displayed`, `test_only_the_signed_restore_launch_route_exists`, the `BackupAuthorization::authorize(` call-count assertion) were updated to assert the new, real behavior instead — the same pattern this codebase already used when 7C.4 legitimately superseded an earlier "no restore route at all" guard.

  Verification: targeted 7C.8 suite (`RestoreProgressPollControllerTest` 11 + `RestoreRequestServiceTest` 12 + `RestoreStaleAcknowledgmentServiceTest` 12 + `RestoreManagementUiTest` 25 + `BackupManagementPageTest` 64) **124 tests, 124 passed, 387 assertions**. Broader `Restore|Backup` regression — default order **845 tests, 841 passed, 1 error, 3 skipped, 2158 assertions** (the 1 error, `RestoreAttachmentActivationServiceTest::test_rollback_rejects_a_shared_handle`, is a real filesystem `rename()`-during-quarantine call this task's own code never touches; it passed 29/29 in isolation immediately afterward and did not reproduce in either of the two required randomized runs below — consistent with this codebase's already-documented class of transient Windows/Laragon rename contention, not a regression); `--order-by=random` **845 tests, 842 passed, 0 failed, 3 skipped** (clean); a second `--order-by=random --random-order-seed=424242` **845 tests, 842 passed, 0 failed, 3 skipped** (clean). `php -l` clean on every changed/new PHP file; `git diff --check` clean. Not committed — see `docs/TASKS_LOG.md`.

- **2026-07-27 (OMS Task 7C.8 acceptance pass — attachment-test retry-margin fix, real Laravel maintenance-mode test, stale-acknowledgment dual-gate proof, request-serialization proof, eligibility fresh-read fix)**: A focused, pre-commit acceptance pass closing every gap the initial 7C.8 report left open, run to a genuine zero-failure bar before ever committing.

  **Root cause of the one default-order failure, found and fixed (not hand-waved)**: `RestoreAttachmentActivationServiceTest`'s `service()` helper constructed its default (no explicit `$mover`) `NativeAttachmentMoveRunner` with `maxAttempts: 1, retryDelayMs: 0` — a single, zero-retry rename attempt — versus the REAL production binding's config-driven default of 5 attempts (`AppServiceProvider`, `oms.backup.restore.attachment_move_retry_attempts`). Every "happy path" test in that file (including `test_rollback_rejects_a_shared_handle`, which isn't even testing move behavior — it's testing lock rejection) inherited this artificially fragile zero-margin default, making it susceptible to exactly the class of one-off Windows/Laragon transient rename contention `NativeAttachmentMoveRunner`'s retry logic exists to absorb — confirmed by tracing the exact exception (`RestoreAttachmentSwapException::liveToQuarantineFailed()` fires from ANY `Throwable` out of `$this->mover->move()`) back to its single call site. Fixed by raising the test default to `maxAttempts: 5` (matching production's retry COUNT) while keeping `retryDelayMs: 0` (zero cost — every `usleep()` in the loop is `usleep(0)`); every test needing an exact, deterministic failure count already injects its own `FakeAttachmentMoveRunner` and was completely unaffected. Verified: the specific test 10/10 consecutive clean; the whole class 29/29; all 9 directly-interacting attachment-state test classes together 114 tests/112 passed/2 pre-existing skips/0 failed.

  **A genuine testing-process mistake was found and corrected mid-pass**: while investigating a SECOND wave of seemingly-flaky failures (spanning completely unrelated files — `BackupManagementPageTest`, `RestoreWatchdogCommandTest`, `RestoreArchivePreparerTest`, `RestoreOrchestratorHeartbeatTest`, `RestoreStaleDetectorTest`, `BackupNotificationTest`), the actual root cause turned out to be self-inflicted: a background `php artisan test --filter="Restore|Backup"` run was still executing while several foreground `php artisan test` commands were ALSO run — and this codebase's `Storage::fake()` disks (`backups`/`restores`/`attachments`) resolve to FIXED, non-per-process-randomized physical paths (`storage/framework/testing/disks/{disk}`), so two concurrent PHPUnit processes genuinely stomp on each other's fake filesystem state (one process's `cleanDirectory()`/file writes racing the other's reads, and real `flock()` contention on the shared restore-subsystem lock file). Every one of that second wave's failures (`Storage::disk()->exists()` returning false for a file just written in the SAME test, `Could not acquire the restore subsystem lock in time`, `FilesystemIterator` "system cannot find the file specified") is fully explained by this — confirmed by re-running every affected file/class individually, strictly sequentially with zero concurrent test processes, coming back 100% clean every time (`RestoreRequestServiceTest` 4/4 clean repeats, `RestoreStaleAcknowledgmentServiceTest`, `RestoreManagementUiTest`, `RestoreProgressPollControllerTest`, and the watchdog/heartbeat/archive-preparer/notification files all individually clean). **No production code defect exists here** — the lesson is purely procedural: never run a background and a foreground `php artisan test` (or two backgrounds) concurrently in this codebase.

  **Real Laravel maintenance-mode polling test** (section 2 of the acceptance ask): `RestoreProgressPollControllerTest` gained `test_real_laravel_maintenance_mode_blocks_normal_routes_but_not_the_signed_poll_route`, which uses the REAL `Artisan::call('down')`/`('up')` commands and the REAL `Illuminate\Foundation\FileBasedMaintenanceMode` flag file at `storage_path('framework/down')` (confirmed by reading that class's source directly, not assumed) — the exact mechanism this project's real dev instance uses (`APP_MAINTENANCE_DRIVER=file` per `phpunit.xml`). Proves, in one test: a normal route (`GET /`) genuinely 503s while down; the signed poll route still returns valid progress; `database.default` is simultaneously pointed at a nonexistent connection with zero queries required; an expired AND a tampered signature are both still denied while all of the above remains in effect. Safety (this genuinely touches the real project directory): a hard precondition refuses to run at all if the flag file already exists (never clobbers unknown real state); cleanup (`Artisan::call('up')` + direct `@unlink()`) runs in a `finally` block AND is repeated unconditionally in a class-level `tearDown()` override as an independent second safety net. The pre-existing fake-based maintenance test was kept alongside it (fast, zero filesystem risk, still exercises the real middleware's own contract lookup) — both now coexist with distinct purposes.

  **Stale-acknowledgment dual-gate proof** (section 3): five new `RestoreStaleAcknowledgmentServiceTest` tests make explicit what was previously only implied: `test_dual_gate_both_terminalize_after_acknowledgment` independently reads both the DB row's status AND a freshly-read, signature-verified `RestoreProgressReader::read()` result before and after acknowledgment, plus `RestoreActivityGuard::isActive()->blocksNewRestore()` transitioning true→false; `test_missing_db_row_still_safely_terminalizes_the_progress_gate` proves the disk-only candidate path (no `BackupOperation` row at all) still works via `RestoreStaleDetector`'s independent disk scan; `test_db_already_terminal_but_progress_still_active_blocks_until_acknowledged` proves the guard's OR-logic inconsistency case (DB says done, progress file still open) still blocks until acknowledged; `test_a_new_restore_request_can_be_created_through_the_real_service_after_acknowledgment` proves the END of the chain — a real `RestoreRequestService::createQueuedRestore()` call succeeds afterward, not merely an isolated guard-state assertion; `test_a_progress_write_failure_propagates_and_never_updates_the_db_row` (via an injected `FakeRestoreProgressDurability(syncFileResult: false)`) proves a failed terminal-progress write throws out of `acknowledge()` uncaught and the DB row is left completely untouched (never falsely marked `RestoreFailed` off the back of a write that didn't actually happen) — the ordering (`$this->writer->write($terminal)` before any DB update) already guaranteed this; the test makes it explicit.

  **Request-serialization proof, not a code change** (section 4): re-reading `RestoreRequestService::createQueuedRestore()` line by line confirmed the check-then-create critical section (eligibility re-check, activity re-check, existence-check, row creation) was already ENTIRELY contained between `BackupSubsystemLock::acquireExclusive()` succeeding and its `finally` release — genuinely serialized by a real OS `flock()`, not merely by sequential non-overlapping calls. New `test_two_truly_concurrent_requests_never_create_two_rows` proves this with real lock contention (a second, independent `BackupSubsystemLock` instance manually holds the exclusive lock at the exact moment the service tries to acquire it, forcing the real `locked` rejection path — not just two calls that happen not to overlap) and confirms zero rows are created while contended, then exactly one succeeds once the lock frees.

  **Eligibility fresh-read fix** (section 5): `assertSourceEligible()` was already always called against a freshly-`fresh()`-fetched `BackupOperation` inside the locked section (confirmed: archive-existence, type, status, and `verified_at` were already re-derived from the database at execution time, not trusted from the caller's possibly-stale in-memory instance) — but `RestoreScopeCompatibility::isCompatible()` was NOT: it only ever checked the caller's original, potentially-stale `$sourceBackup->scope`. Fixed by adding a second scope-compatibility check against `$freshSource->scope` inside the locked section. Two new tests (`test_eligibility_is_rechecked_against_a_fresh_read_not_the_callers_stale_instance`, `test_scope_compatibility_is_rechecked_against_a_fresh_read`) prove this by mutating the underlying DB row directly (bypassing the caller's in-memory `$source` object entirely, which still shows the old, eligible values) and confirming the service still rejects.

  **Live browser walkthrough** (section 6): performed against the REAL local Laragon instance (not `Storage::fake()`), using a throwaway Super Admin user and a throwaway eligible backup fixture (both created via `tinker`, both fully deleted afterward — confirmed via a post-cleanup user/backup-row/file count check restoring the exact pre-walkthrough numbers) and Playwright driving the system-installed Chrome. Confirmed visually via screenshots: the "استعادة" action renders correctly (RTL, Arabic) on eligible rows; Step 1's scope `<select>` offered exactly the three compatible options for a `Full`-scope source; an incorrect typed confirmation was rejected in place with the exact Arabic validation message ("نص التأكيد غير مطابق."); the correct `RESTORE {uuid8}` phrase advanced to Step 2, which rendered the exact required heading/body/red danger button; canceling Step 2 closed the modal and left the database with zero restore-type rows (confirmed both visually and via a direct `tinker` count). The real destructive final-launch button was deliberately never clicked (this was the real environment, not the test suite's `FakeRestoreProcessLauncher`). Stale/tampered banners were not exercised against real fixtures (would require writing a real stale/tampered signed progress file into the live `restores` disk — left to the existing, thorough automated coverage instead) — noted as the one walkthrough item not performed live.

  **7C.9 scope correction** (section 7): `docs/NEXT_STEPS.md` previously described OMS Task 7C.9 as "production Hostinger deployment." Corrected: **7C.9 is real LOCAL end-to-end restore testing on Windows/Laragon** (database-only, files-only, full restore, and intentional failure/recovery drills, followed by one full application test-suite run) — Hostinger VPS deployment is a separate, later task that only follows once Task 7C (including 7C.9) is fully accepted locally.

  **Graphify separation** (section 8): confirmed via `git status`/`git diff --stat -- graphify-out/` that zero `graphify-out` changes exist anywhere in this working tree before the commit below — nothing to revert or clean.

  Final acceptance verification, all run strictly sequentially with zero concurrent test processes: targeted 7C.8+attachment-fix suite **162 tests, 162 passed, 535 assertions**. Broader `Restore|Backup` regression — default order **854 tests, 851 passed, 0 failed, 0 errors, 3 skipped, 2207 assertions**; `--order-by=random --random-order-seed=111222` **854/851/0 failed/3 skipped** (clean); `--order-by=random --random-order-seed=998877` **854/851/0 failed/3 skipped** (clean). `php -l` clean on every changed/new PHP file; `git diff --check` clean.

- **2026-07-27 (OMS Task 7C.9 — real local Windows/Laragon end-to-end backup/restore acceptance)**: The final acceptance phase for Task 7C — every prior 7C.1–7C.8 phase was verified only against `Storage::fake()`/SQLite `:memory:`/mocked collaborators; this phase ran the REAL backup and restore pipeline against the real local MySQL database and real private filesystem disks, through the real Filament UI, as a real (temporary, disposable) Super Admin.

  **Independent emergency baseline created first**, entirely outside the OMS backup/restore system and outside the project directory (`C:\Users\laptop\oms_7c9_emergency_baseline_20260727\`): a real `mysqldump --single-transaction` of the live `oms` database, a recursive copy of `storage/app/private/attachments`, and a SHA-256 manifest of both — this exists solely to recover the developer machine if 7C.9 itself had exposed a critical bug, independent of anything the restore engine touches.

  **A real, proven, E2E-only defect was found and fixed**: `RestoreMaintenanceMode::enter()` called the real `Artisan::run('down', ['--message' => ...])` — Laravel's actual `down` command in this installed version has no `--message` option at all (confirmed directly via `php artisan down --help`: only `--redirect`/`--render`/`--retry`/`--refresh`/`--secret`/`--with-secret`/`--status`). Every real restore therefore failed 100% of the time at the preflight/maintenance-entry step. This was completely invisible to all 854 prior mocked tests because `RestoreMaintenanceModeTest`'s `FakeMaintenanceModeController` never validates a call's parameters against the real Artisan command's actual defined options — it was exactly the class of gap real E2E testing exists to catch. Fixed narrowly: `enter()` now calls `down` with no options (Laravel's default 503 maintenance response is sufficient; a custom message would require introducing a new Blade view, which is out of scope for a bug fix). Updated the one test (`RestoreMaintenanceModeTest`) that had asserted the old, wrong behavior. Verified: 5/5 focused tests pass after the fix, and the very next real restore attempt against the same corrected code succeeded end-to-end.

  **A real, documented Windows/Laragon operational limitation was confirmed** (not a code defect, and not newly introduced by this task): `WindowsDetachedRestoreProcessLauncher` spawns `php artisan oms:restore {uuid}` via Symfony `Process::start()` with `disableOutput()`; in this actual local Laragon environment the detached child does not reliably keep running/progressing after the launching web request completes — the claimed restore row sits at `phase=launching` indefinitely with no visible `php.exe` process and no error surfaced anywhere (output is discarded by design). This exactly matches the launcher class's own pre-existing docblock caveat ("nothing here has been verified to guarantee survival past the parent process/job object being torn down" on Windows). Confirmed safe recovery: manually re-running the exact same command, `php artisan oms:restore {uuid}`, in the foreground for the SAME already-claimed row picks up cleanly (`RestoreCommand::isClaimedForExecution()` only requires `status=Restoring`+`started_at` set+`launch_nonce` null, all already true) and runs the real, unmodified `RestoreOrchestrator` to completion — same code, same DB row, same real orchestrator, just not backgrounded. `docs/RESTORE_RECOVERY.md` §14 gained this as a new, explicit local-dev-only operational note. Production (Linux/Hostinger) uses the separate, `setsid`-based `LinuxDetachedRestoreProcessLauncher` and is unaffected — out of scope for this local-only phase.

  **Three full restore-scope cycles, all through the real UI + real orchestrator, each with independent before/after markers and SHA-256 hash verification**:
  - **Database-only**: a dedicated `Setting` marker row (`BEFORE_DB_RESTORE` → `AFTER_DB_RESTORE`) proved the value correctly reverted after restore; the safety backup, source backup, and restore rows' FK relationships (`source_backup_id`/`pre_restore_safety_backup_id`) all correctly cross-referenced; maintenance mode correctly exited; queue restart signal (`Cache::get('illuminate:queue:restart')`) confirmed set.
  - **Files-only**: an attachment's content was replaced twice (`BEFORE_FILES_RESTORE` → backup → `AFTER_FILES_RESTORE`), each replacement going through the real `AttachmentUploadService`/Filament Edit flow (which always creates a new Attachment row + file, never overwrites in place — soft-deleting the previous one). The restore correctly reverted the physical `attachments` directory to exactly the backup's file set (removing the post-backup file entirely) while leaving the database completely untouched (a second, independent DB control marker proved this). **Observed, not a defect**: since the DB was intentionally untouched but the post-backup attachment file was correctly removed, the live `GeneralExpense`'s Attachment row (created after the backup) was left pointing at a now-missing file — confirmed to fail SAFELY everywhere (`AttachmentStorageService::exists()` returns false, the secure controller would 404 not 500, and the Filament Edit page itself already shows a graceful "attachment file could not be found" message) — this is an inherent, expected consequence of restoring files without also restoring the database when the two have diverged since the backup, not a bug in the restore mechanism. Also observed: `pre_restore_safety_backup_id` is never backfilled onto a files-only restore's own row (only database/full-scope restores run `RestoreMetadataUpserter`'s post-import reconciliation, since only a database import can ever stale/wipe the `backup_operations` table itself) — the real safety-backup relationship is still fully present and verifiable via the signed `reconciliation_snapshot` inside `progress.json`; left as-is, a minor FK/UI-surface gap, not a functional or safety defect.
  - **Full**: independent DB marker (`FULL_BEFORE_DB`/`FULL_AFTER_DB`) and attachment content (`FULL_BEFORE_FILE`/`FULL_AFTER_FILE`) both correctly reverted together, consistently — no dangling reference this time, since DB and files revert as one unit. Confirmed the real post-import ID-shift behavior directly: because a full/database-scope restore replaces the ENTIRE `backup_operations` table via the imported dump, that table's own auto-increment IDs are not stable across the restore — `RestoreMetadataUpserter` correctly re-resolves every relationship by UUID lookup afterward, never trusting a pre-import numeric ID, exactly as its docblock claims.

  **Two controlled failure/recovery drills, both without ever touching a real/legitimate backup or killing a real process**:
  - **Pre-irreversible-boundary failure**: a deliberately corrupted COPY of a real archive (first 64 bytes zeroed, registered as its own synthetic `backup_operations` row, never the original) produced a clean `RestoreFailed` (never `RestorePartial`) at the exact `RestorePreflightChecker::assertArchiveEncryptionResolvable()` check, before maintenance mode was ever entered — DB/attachments completely unchanged, no automatic retry, signed terminal progress present, `RestoreActivityGuard` confirmed `Inactive` immediately after (a new restore is allowed right away).
  - **Stale/crash drill**: constructed a deterministic "crashed mid-restore" scenario using the REAL `RestoreProgressWriter`/`RestoreProgressSnapshot` classes (genuinely valid signature, not faked) with a heartbeat 25 minutes old — `php artisan oms:restore-watchdog` correctly detected and logged it (`reason_code=stale_heartbeat`) without mutating anything. Proved, with real (not simulated) contention: a live `BackupSubsystemLock` held by a background process correctly blocks acknowledgment (`lock_held`); a progress file tampered in place (content edited without recomputing its signature) correctly blocks acknowledgment (`not_eligible`) and is never offered in the UI. Terminalized the valid stale restore through the real Filament acknowledgment UI as Super Admin — `restore_metadata.stale_acknowledgment` recorded the acknowledging Super Admin's identity + timestamp + reason (the "operator recovery information" requirement), signed terminal progress written (`restore_failed_phase=crashed_acknowledged`), and `RestoreActivityGuard` confirmed `Inactive` only once BOTH the DB row and the progress file were terminal.
  - **Post-DB-import/partial failure**: deliberately NOT injected against the real environment (would require either an uncontrolled crash of a real in-flight `mysql` import, or a new production-only failure hook — both explicitly out of scope). Verified instead that `tests/Feature/Restore/RestoreOrchestratorTest.php`'s existing deterministic coverage (from 7C.7) already proves `RestorePartial` after a fully successful DB import + reconciliation when only `finalize()` fails: maintenance exit still attempted, signed terminal progress carries `restore_failed_phase=finalizing`, quarantine preserved for manual recovery, already-live attachments untouched.

  **Post-restore application smoke check**, all through the real UI logged in as the real (temporary) Super Admin: dashboard, Projects, Accounts (balances reflecting the real test transaction exactly), the Trial Balance report (`متوازن`, difference `0.00`), and Backup Management page all loaded correctly; the secure attachment controller (`/attachments/{id}/view`) served the real private file inline; the direct filesystem path returned a real `403 Forbidden`; restore-action eligibility correctly re-enabled once all prior restores were terminal.

  **Financial integrity check** (existing guards only, no new project started): all 5 financial resources' `BalanceGuardIntegrationTest` suites + `FinancialTransactionBalanceGuardTest` — **91/91 passed, 218 assertions** — against isolated in-memory SQLite (`phpunit.xml`'s `DB_CONNECTION=sqlite`/`DB_DATABASE=:memory:`, entirely separate from the real dev database). Live-data checks against the actual restored MySQL database: zero dangling `source_backup_id`/`pre_restore_safety_backup_id` FKs across all 16 `backup_operations` rows; zero broken Account→Currency/AccountType/BankType relationships; zero orphaned `TransactionLine` rows.

  **Cleanup**: the E2E fixture `GeneralExpense` record was removed via the REAL Filament delete action (`GeneralExpensesTable::deleteExpense()`), which properly reversed both account balances back to their exact pre-task baseline and soft-deleted the transaction/lines/attachment/expense row (`SoftDeletes` respected throughout — physical attachment files for soft-deleted rows were deliberately left on disk, consistent with this codebase's existing precedent for previously soft-deleted attachments). The synthetic corrupted-archive and stale-crash-drill `backup_operations` rows (manually constructed for controlled failure testing, never produced by the real pipeline) were hard-deleted along with their files, since they are not genuine application history. Every genuine backup/restore row produced by an actual UI action + real orchestrator run across all three restore scopes was deliberately KEPT as acceptance evidence, including the first Section-D restore attempt that failed on the real `--message` bug — direct evidence of the defect found and fixed. The temporary Super Admin was deleted via the real `UserManagementService::deleteUser()`, acted by the original Super Admin. The independent emergency baseline was intentionally left in place pending formal acceptance of this task.

  **Final full application suite** (the one required run per instructions): **1875 tests, 1869 passed, 0 failed, 0 errors, 6 skipped, 5659 assertions** — all 6 skips are pre-existing, environment-conditional, and individually documented in their own test files (Windows/Linux-specific code paths, local symlink-permission limits, per-model/per-resource loop conditions), none newly introduced by this task.

  **Hostinger VPS production deployment is explicitly out of scope for this task** — it is a separate, later phase that only follows once Task 7C (including this 7C.9 local acceptance) is formally accepted.

- **2026-07-27 — OMS Task 8 (Financial & Database Integrity + Trial Balance Correctness Review) — implemented, verified, NOT committed.**

  **Critical audit finding (drives every design choice below): `transaction_lines.debit_base`/`credit_base` are NOT a company-base-currency conversion, despite the column names.** Every real write path (`CreateAccount`, `CreateGeneralExpense`, `CreateGeneralExchange`, `CreateProjectCostBudgetsPayment`, `CreateExecutionPayment`, `CreateProjectCostReceipt`) and three independent pre-existing docblocks (`TrialBalanceReportService`, `TrialBalancePage`, `ComprehensiveFinancialTransactionsPage`) confirm `debit_base`/`credit_base` always equal that line's own `amount_currency`, in that line's own currency — never multiplied by `fx_rate`, never normalized into one shared base currency. Consequently: (1) 2-line single-currency transactions balance via `debit_base(line) == credit_base(line)` in the same currency, trivially `SUM=SUM`; (2) 4-line multi-currency transactions (general exchanges, budget disbursements) balance via the FX **equation** already in `FinancialTransactionBalanceGuard::assertBalancedMultiCurrencyLines()` — `credit(source) - debit(admin) - debit(transfer) = afterDeductions`, `destination.debit_base = afterDeductions × fx_rate` — **never** a raw `SUM(debit_base)=SUM(credit_base)` across the whole transaction, since that would silently sum two different currencies. The new integrity checker replays this exact guard logic against persisted data rather than inventing a parallel comparison.

  **Trial Balance current design confirmed as intentional "Phase 1"** (per `docs/DECISIONS_LOG.md`, 2026-07-05): single required currency, period-only totals, **no opening/closing balance concept exists** — not a defect, a documented scope decision. Excel/Word exports (`TrialBalanceExcelExportService`/`TrialBalanceWordExportService`) render only the exact `$rows`/totals already computed and displayed by `TrialBalancePage` — structurally incapable of drifting from the page, since neither export method accepts a currency/date parameter to requery with. Soft-deleted lines/transactions excluded; soft-deleted accounts excluded from the population entirely; account-type filter restricts population only, never arithmetic; zero-balance toggle uses a documented 0.01 tolerance (safe — `DECIMAL(15,2)` columns are exact at the DB layer). **Gap found and closed**: zero dedicated tests existed for `TrialBalanceReportService`/`TrialBalancePage` before this task (only export-authorization coverage existed) — 14 new focused tests now cover same-currency balance, the canonical 1000-source/2999.94-destination multi-currency exchange (explicitly proving neither currency's report ever includes the other's raw amount, and that no blended-currency grand total is ever produced), date boundaries (before/on-start/on-end/after), account-type filter, zero-balance toggle, soft-delete exclusion, page/Excel/Word exact-value agreement (verified by reading back real generated `.xlsx` cell values via PhpSpreadsheet and real `.docx` `document.xml` content, not merely asserting a `StreamedResponse` instance).

  **Real local DB audit (read-only, before any change)**: all FK relationships the task named already carry real MySQL constraints (confirmed via `SHOW CREATE TABLE`) — `transaction_lines`→transaction/account/currency/project_cost, `transactions`→fiscal_year/type/partner/bank_account, `accounts`→type/currency/bank_type/parent(self-ref) — plus two real relations not in the task's own list (`accounts.parent_id`, `transactions.bank_account_id`) discovered during the audit and confirmed already-constrained. `transactions.transaction_number` already carries a DB UNIQUE constraint. Real preflight queries: **0 duplicate transaction numbers, 0 orphaned transaction_lines, 0 account currency/type mismatches, 0 soft-deleted-account-referenced-by-live-line cases.** `accounts.current_balance` **is a real persisted column** (not purely ledger-derived) — reconciliation query against `SUM(debit_base)-SUM(credit_base)` per account showed **0 discrepancies**. Given this clean baseline, no new FK/UNIQUE constraints were added — only two proven-missing supporting indexes and numbering-race hardening (both approved by the user before implementation).

  **Real, unplanned finding, explicitly out of this task's scope**: the `migrations` DB table has 66 recorded rows but only 63 files exist on disk. Two of the three missing files (`create_budget_lines_table`, `create_project_activities_table`) are harmless — both tables were later dropped by a migration that still exists. The third, `create_bank_accounts_table`, is **not harmless**: `bank_accounts` still exists live and is FK-referenced by `transactions.bank_account_id`, but no migration file anywhere would recreate it (or `accounts.parent_id`, or `accounts.account_code NOT NULL`) on a fresh install — `migrate:fresh` today would not reproduce the live schema. Per explicit user decision: **documented only, no code change this task.** (A minimal test-only SQLite shim for these three items lives in `tests/Support/Integrity/IntegrityTestFixtures.php::shimUndocumentedSchemaDrift()`, mirroring this codebase's own existing precedent — `ReportPageAccessTest`'s `sqliteCreateFunction('FIELD', ...)` — for a different pre-existing MySQL-only test gap; it touches no real migration or the real database.)

  **Implemented — read-only integrity checker** (`app/Services/Integrity/`): `FinancialIntegrityChecker` orchestrates four focused, injectable collaborators — `DatabaseRelationshipIntegrityChecker` (12 named relationships, bounded LEFT JOIN/COUNT queries, soft-deleted references correctly never orphans), `TransactionNumberIntegrityChecker` (duplicate `transaction_number` defense-in-depth), `JournalBalanceIntegrityChecker` (chunked `chunkById`, replays `FinancialTransactionBalanceGuard` against every live transaction's non-deleted lines, classifies by `line_role` set into 2-line-single-currency / 4-line-multi-currency / **unclassified — reported as a bounded WARNING, never a false-positive violation** — the last bucket exists specifically for pre-2026-07-14 historical rows where `line_role` is `NULL`, of which the real local DB currently has zero), `AccountCurrencyIntegrityChecker` (currency-mismatch join + persisted-balance reconciliation; inactive historical accounts explicitly never flagged, matching `FinancialAccountGuard::requireActiveOnChange()`'s own write-time-only design). `IntegrityCheckReport`/`IntegrityViolation` are small bounded value objects — every violation carries a category, description, total count, and a sample of **at most 10** identifiers, never full financial records or amounts.

  **Implemented — `php artisan oms:check-financial-integrity`** (`app/Console/Commands/CheckFinancialIntegrity.php`): fully read-only (every collaborator is SELECT/COUNT-only by construction), `--json` mode, exit 0 = OK, 1 = violations found, 2 = runtime/checker failure (an unexpected exception is caught, logged, and never confused with "no violations"). Never schedules itself (explicitly out of scope, deferred to a future Monitoring task).

  **Write-path hardening (approved scope)**: (1) two new supporting indexes — `transaction_lines(account_id, currency_id)` and `transactions(transaction_time)` — matching the exact WHERE/JOIN shape Trial Balance/Account Statement/the new checker all use; the sibling pre-existing migration's `SHOW INDEX FROM` (MySQL-only, breaks under this project's SQLite test connection) was avoided by using Laravel's driver-agnostic `Schema::hasIndex()` instead. (2) The six near-identical `generateTransactionNumber()` MAX+1 implementations (one per financial Create page) are unchanged in logic/format, but every page's `handleRecordCreation()` now wraps its existing `DB::transaction()` call in a new shared `App\Filament\Concerns\GeneratesSequentialTransactionNumbers::retryOnTransactionNumberCollision()` — a bounded (3-attempt) retry that only catches a genuine `transaction_number` `UniqueConstraintViolationException` (the one race window `lockForUpdate()` cannot close: two concurrent creates racing to produce the very first number under a brand-new prefix). Any other exception propagates immediately, unretried.

  **Files changed**: 6 Create pages (`CreateProjectCostReceipt`/`CreateProjectCostBudgetsPayment`/`CreateGeneralExpense`/`CreateGeneralExchange`/`CreateExecutionPayment`/`CreateAccount`) — retry wrapper only, `generateTransactionNumber()` deduplicated into the new trait; `app/Filament/Concerns/GeneratesSequentialTransactionNumbers.php` (new); `app/Services/Integrity/{FinancialIntegrityChecker,DatabaseRelationshipIntegrityChecker,TransactionNumberIntegrityChecker,JournalBalanceIntegrityChecker,AccountCurrencyIntegrityChecker,IntegrityCheckReport,IntegrityViolation}.php` (all new); `app/Console/Commands/CheckFinancialIntegrity.php` (new); `database/migrations/2026_07_27_100000_add_supporting_indexes_for_financial_integrity.php` (new, additive-only, applied to the real local DB). Tests (all new): `tests/Support/Integrity/IntegrityTestFixtures.php`, `tests/Feature/Integrity/{DatabaseRelationshipIntegrityCheckerTest,TransactionNumberIntegrityCheckerTest,JournalBalanceIntegrityCheckerTest,AccountCurrencyIntegrityCheckerTest}.php`, `tests/Feature/Commands/CheckFinancialIntegrityCommandTest.php`, `tests/Feature/Reports/TrialBalanceReportServiceTest.php`, `tests/Unit/Filament/Concerns/GeneratesSequentialTransactionNumbersTest.php` — 44 new tests total.

  **Attachment single-active-attachment rule**: confirmed via `docs/DECISIONS_LOG.md` grep — never formally approved. Per the task's own instruction: **deferred / not applicable, no schema change.**

  **Verification**: targeted Task 8 suite (Integrity + Commands + Reports + the trait) **44/44 passed**; broader financial-workflow regression (the 6 modified Create pages + Reports + Commands) **234/234 passed**; real local DB scan via `php artisan oms:check-financial-integrity` — **0 relationship/numbering/balance/FX/currency-mismatch/persisted-balance violations** (12 relationships, 4 transactions, 3 live journal transactions, 6 lines, 3 accounts checked), exit 0; real Trial Balance run against every local currency — USD: 3 accounts, debit=credit=125000, balanced; ILS/EUR: no accounts yet, trivially balanced. **Final full application suite (the one required run): 1919 tests, 1913 passed, 0 failed, 0 errors, 6 skipped, 5767 assertions** — exactly the 44 new tests added cleanly on top of the prior 1875/1869 baseline; all 6 skips are the same pre-existing, environment-conditional ones, none newly introduced.

  **Not committed — awaiting review**, per instructions (STOP before commit; no Graphify run; no push).

- **2026-07-28 — OMS Task 8.1 (Schema Drift + Dead Schema Reconciliation) — implemented, verified, NOT committed.**

  **Resolved the schema/migration-history drift Task 8 (2026-07-27) explicitly deferred** (`docs/DECISIONS_LOG.md`, 2026-07-27): `bank_accounts`, `accounts.parent_id`, `transactions.bank_account_id` existed live with real FK constraints but no migration file anywhere on disk. Read-only audit first (no Graphify, per instructions): zero application code usage found anywhere — no Eloquent model, no relationship method, not in either model's `$fillable`, no Filament resource/form field, no factory/seeder — only referenced by `DatabaseRelationshipIntegrityChecker` (added by Task 8 itself, defensively) and its test shim (`IntegrityTestFixtures::shimUndocumentedSchemaDrift()`, whose own docblock already flagged this as "a pre-existing, documented schema/migration-history gap"). Live data: 0 rows in `bank_accounts` (0 active, 0 soft-deleted), 0 non-null `accounts.parent_id` (of 41 accounts), 0 non-null `transactions.bank_account_id` (of 47 transactions). Git history: `create_bank_accounts_table` (`2026_06_09_130011`) was part of the very first schema batch (sits between `create_partners_table`/`create_accounts_table` by timestamp) but was **never** committed to this git repo at all — confirmed via `git log --diff-filter=A` showing all 64 migration files ever added across history still match all 64 files currently on disk (nothing was ever deleted from git); its two harmless siblings (`create_budget_lines_table`/`create_project_activities_table`) were legitimately dropped later by a migration still on disk. Historical backups: the earliest captured snapshot (`storage/app/private/backups/oms_backup_2026-07-06.sql`) shows exactly one `bank_accounts` row ever existed (created 2026-06-09, placeholder-looking data — a phone number in the IBAN field), already purged by the separate, user-approved 2026-07-06 "clean start" operational data reset; `accounts.parent_id`/`transactions.bank_account_id` are NULL in every row of that same backup. Classified **DEAD_UNUSED** for all three (unanimous evidence, no conflicting signals).

  **Action taken**: new migration `database/migrations/2026_07_28_100000_drop_dead_bank_accounts_schema.php` (guarded `hasColumn`/`hasTable` checks, faithful `down()` for documentation/reversibility) drops `transactions.bank_account_id` (FK then column), `accounts.parent_id` (FK then column), and `bank_accounts` (table) — applied to the real local `oms` database via `php artisan migrate`. The orphaned `migrations` table row `2026_06_09_130011_create_bank_accounts_table` (file absent, schema now intentionally removed) was then deleted directly; the two harmless orphaned rows for `budget_lines`/`project_activities` were left untouched (out of this task's named scope). Pre-task `mysqldump` backup created outside the project directory at `C:\Users\laptop\oms_db_backups\oms_pre_task8.1_20260728_100905.sql` before any change.

  **Repository cleanup**: removed the two now-dead relationship entries from `DatabaseRelationshipIntegrityChecker::RELATIONSHIPS` (12 → 10 checked relationships); removed `IntegrityTestFixtures::shimUndocumentedSchemaDrift()` and its now-unused `Blueprint`/`Schema` imports (no longer needed — the SQLite test schema now matches the real schema without a shim); updated `DatabaseRelationshipIntegrityCheckerTest` (`relationships_checked` assertion 12 → 10; dropped the now-meaningless `'bank_account_id' => null` from its nullable-relation test). Corrected one stale documentation claim in `OMS_Master_Reference.md`'s "Core" schema cheat-sheet (`accounts` column list no longer lists `parent_id`).

  **Fresh-install verification**: created an isolated scratch database (`oms_task81_freshcheck`, same local MySQL server, dropped again immediately after) via a `DB_DATABASE` environment override — never touched the real `oms` database. `php artisan migrate:fresh --force` against it ran all 64 migrations cleanly with 0 errors; confirmed `bank_accounts`/`accounts.parent_id`/`transactions.bank_account_id` are absent (they never existed in any migration file, so this was already true even before today — today's fix only reconciled the *live local DB* to match). `SHOW CREATE TABLE` comparison of `accounts`, `accounts_type`, `transactions`, `transaction_lines` between the fresh DB and the reconciled local `oms` DB: **accounts and transactions now match exactly** on the three named objects. **Two pre-existing, unrelated drifts were discovered as a side effect of this comparison and are explicitly out of this task's scope** (not touched, not authorized by the task's named objects): (1) `accounts.account_code` is `NOT NULL` live but nullable on a fresh install (already flagged in Task 8's own 2026-07-27 finding — "or `accounts.account_code NOT NULL`"); (2) `accounts_type` live has a `nature` enum column plus `created_by`/`updated_by` audit columns that no migration file reproduces — a materially larger, previously-undiscovered drift on a table not named in this task. Both are recommended as their own future dedicated reconciliation tasks (see `docs/NEXT_STEPS.md`). A separate, harmless index difference was also noted (local `oms`'s `transaction_lines` carries a redundant standalone `account_id` index that a fresh install does not produce, superseded by the composite `(account_id, currency_id)` index) — pre-existing, non-functional, not touched.

  **Verification**: focused regression suite (`tests/Feature/Integrity`, `CheckFinancialIntegrityCommandTest`, `tests/Feature/Reports`, `tests/Feature/GeneralExchanges`, `tests/Feature/ProjectCostBudgetsPayments`, `GeneratesSequentialTransactionNumbersTest`, `tests/Unit/Services`) — **511/513 passed, 2 skipped** (pre-existing, environment-conditional); `ReportPageAccessTest` + `ReportExportAuthorizationTest` (covers Trial Balance/Account Statement page & export authorization) — **58/58 passed**. Real local DB: `migrate:status` shows all 64 migrations `Ran`; `php artisan oms:check-financial-integrity` — **Result: OK, exit code 0** (10 relationships checked, 0 orphans; 0 duplicate numbers; 0 unbalanced transactions; 0 FX/currency-mismatch/persisted-balance violations). Real smoke checks against reconciled local data (not rolled back — all read-only): `TrialBalanceReportService::generate()` and `AccountStatementReportService::generate()` both executed cleanly with real data (3 trial-balance rows for USD); `Account`/`Transaction` Eloquent queries with all relationships loaded cleanly; `/admin/login` returned HTTP 200; `/admin/accounts` returned HTTP 302 (correct — auth redirect, not an error). Working tree confirmed to contain only the intended changes (`git status`/`git diff --stat`).

  **Not committed — awaiting review**, per instructions (STOP before commit; no Graphify run; no push; no Audit Log started).

- **2026-07-28 — OMS Task 8.2 (Final Schema Drift Reconciliation) — implemented, verified, NOT committed.**

  **Resolved the two out-of-scope drifts Task 8.1 discovered and deferred**: `accounts.account_code` `NOT NULL` live vs. nullable fresh, and `accounts_type` carrying a live `nature` enum + `created_by`/`updated_by` audit columns with no reproducing migration. Read-only audit found: (1) the **original** `create_accounts_table` migration (unchanged since the initial commit) has always declared `account_code` `->nullable()->unique()`, `AccountForm` explicitly sets `->nullable()`, and **every** read site across the codebase (~20 files — reports, exports, dropdowns) defensively guards `$account->account_code ? ... : ''` — the live `NOT NULL` was the drift, not the migration/code. (2) `accounts_type.nature` has zero code usage anywhere and `docs/AI_PROJECT_MEMORY.md` already documented (2026-07-06, predating this audit) that "the system has no account nature... every account is uniformly debit-normal" — live values were mostly a MySQL implicit-ENUM-backfill artifact (7 of 10 rows: `asset` + `created_by` NULL) from whenever the column was manually added, not deliberate business data; classified DEAD_UNUSED. (3) `accounts_type.created_by`/`updated_by` have zero current usage on `AccountType` specifically, but match the exact FK-to-users/nullable/`ON DELETE SET NULL` convention actively used via `App\Traits\HasUserTracking` on 20 other current models, and carry real historical data (3 rows, `created_by=1`); classified ACTIVE_REQUIRED — reproduced as schema only, `AccountType` was **not** wired to `HasUserTracking` (that would be starting the Audit Log task, explicitly out of scope).

  **A real bug was found and fixed mid-task**: the first draft of the `account_code` migration's idempotency guard used raw MySQL-only `information_schema.columns` SQL, which broke every focused test under the project's SQLite test connection (173/178 errored). Fixed by switching to Laravel's database-agnostic `Schema::getColumns()`, which exposes a `nullable` key identically across drivers — no behavior change on the real DB, since the guard only decides whether to skip an already-idempotent `->nullable()->change()`.

  **Action taken**: two new guarded migrations applied to the real local `oms` database — `2026_07_28_110000_make_account_code_nullable_on_accounts_table.php` (relaxes `accounts.account_code` to nullable; intentionally irreversible/no-op `down()`, since fresh installs have always had it nullable and forcing `NOT NULL` on rollback would impose a constraint no fresh install ever had) and `2026_07_28_110001_reconcile_accounts_type_schema_drift.php` (drops `nature`; adds `created_by`/`updated_by` with FKs to `users`; `down()` cleanly drops the two added columns but does **not** recreate `nature` — same no-resurrecting-dead-schema principle as Task 8.1). Zero rows/values modified — 5 accounts (0 null/empty/duplicate `account_code` before or after), 10 `accounts_type` rows (3 `created_by` values preserved byte-identical), 8 transactions / 22 transaction_lines counts unchanged.

  **Fresh-install verification**: isolated scratch database (`oms_task82_freshcheck`, same MySQL server, dropped after use; real `oms` never targeted). `migrate:fresh` — all 66 migrations clean. Rollback-safety check: rolled back both new migrations on the fresh DB and confirmed neither `nature` was resurrected nor `account_code` was forced back to `NOT NULL` — both correctly no-op on the "never existed as a constraint on fresh" side, while `created_by`/`updated_by` correctly reverted (proper reversible addition). `SHOW CREATE TABLE` comparison: `accounts_type` now matches **exactly** between fresh and live; `accounts.account_code` now matches. One remaining, still out-of-scope difference newly caught during this comparison (correcting an omission in the previous Task 8.1 report, which only flagged `account_code`): `accounts.created_by`/`updated_by` are **also** present live but absent from a fresh install — the identical drift class as `accounts_type`'s, on the sibling table, never named in this task's scope — left untouched, recommended as its own follow-up in `docs/NEXT_STEPS.md`.

  **Verification**: focused regression (`tests/Feature/Integrity`, `CheckFinancialIntegrityCommandTest`, `tests/Feature/Reports`, `FinancialAccountGuardTest`, all 5 `BalanceGuardIntegrationTest` suites, 4 account-validation tests, `ExecutionPaymentCreditAccountTest`, `GeneratesSequentialTransactionNumbersTest`) — **178/178 passed**; plus `tests/Feature/Crud`, both description-builder unit suites, and both data-command tests — **158/158 passed**. Real local DB: `migrate:status` shows both new migrations `Ran`; `php artisan oms:check-financial-integrity` — **Result: OK, exit code 0** (unchanged: 10 relationships, 0 violations). Real smoke checks (not rolled back, all read-only or already-idempotent): `Account`/`AccountType` Eloquent loads, `TrialBalanceReportService`/`AccountStatementReportService` both executed cleanly against real data; `/admin/login` → HTTP 200, `/admin/accounts` and `/admin/account-types` → HTTP 302 (correct auth redirects). Transaction/line counts and balances confirmed byte-identical before/after.

  **Not committed — awaiting review**, per instructions (STOP before commit; no Graphify run; no push; no Audit Log started).

- **2026-07-28 — OMS Task 8.3 (Accounts User-Tracking Schema Reconciliation) — implemented, verified, NOT committed.**

  **Resolved the last remaining named drift Task 8.2 discovered and deferred**: `accounts.created_by`/`updated_by` exist live (nullable, FK→`users`, `ON DELETE SET NULL`) with no migration file anywhere in this repo's history — the sibling-table twin of `accounts_type.created_by`/`updated_by`, reconciled one task ago. Read-only audit found zero application code usage on `Account` (not `$fillable`, no relationship, no form field — the `created_by`/`updated_by` writes inside `CreateAccount.php` are all for the opening-balance `Transaction`/`TransactionLine` side-effect rows, never the `Account` row itself) and zero non-null values, both currently (0 of 5 accounts) **and** in the earliest captured historical backup, 2026-07-06 (0 of 16 accounts) — an unbroken, always-NULL history, unlike `accounts_type`'s 3 legacy-tagged rows.

  **Classified ACTIVE_REQUIRED, not DEAD_UNUSED**, despite zero data: the column shape is column-for-column identical to the active `HasUserTracking` convention used by 20+ other current models and to `accounts_type`'s already-reconciled copies. Removing now and likely re-adding once the (explicitly upcoming) Task 9 Audit Log needs creator/updater tracking on the system's most central financial entity would be pure churn — and this task's own title/framing ("final schema-drift task *before* Audit Log") anticipates keeping it. **Schema reconciled only — `Account` was deliberately NOT wired to `App\Traits\HasUserTracking`.** Activating real tracking behavior is explicitly deferred to Task 9, per this task's repeated "do not start Audit Log" restriction and for consistency with the identical choice already made for `accounts_type` in Task 8.2.

  **Migration**: `2026_07_28_120000_reconcile_accounts_user_tracking_schema_drift.php` — a per-column guard (`ensureUserTrackingColumn()`) that adds the column+FK only if the column is missing, or repairs only a missing FK if the column already exists without one (checked via Laravel's driver-agnostic `Schema::getForeignKeys()`), so it never duplicates a constraint/index name. On the real local DB both columns and FKs already existed correctly, so `up()` executed as a **complete no-op** — applying it changed nothing but the `migrations` table bookkeeping. `down()` is intentionally irreversible (no-op), same principle established for `accounts_type` in Task 8.2: the migration cannot tell "I added these columns" from "these already existed as live drift," so a rollback must never risk deleting real pre-existing (or, once Task 9 populates them, real historical) tracking data.

  **Isolated scenario verification** (scratch databases only, real `oms` never targeted by `migrate:fresh`/rollback): Scenario A (fresh install) — `migrate:fresh` produces nullable `created_by`/`updated_by` with correct FK (`ON DELETE SET NULL`) and indexes; rolling back the final migration causes no error and leaves the columns intact (no-op, as designed). Scenario B (simulated live drift) — used the no-op `down()` itself to naturally recreate genuine drift (columns/FK physically present, migration row absent from the `migrations` table), inserted a harmless account row with real `created_by`/`updated_by` values, re-ran the migration and confirmed **zero duplicate FK/index** and the row's values byte-identical, then rolled back again and confirmed the values still intact. A third scratch check directly proved the FK's `ON DELETE SET NULL` behavior end-to-end: hard-deleting the referenced user set both `created_by`/`updated_by` to `NULL` on the account row, exactly as required.

  **Fresh-vs-live parity**: isolated scratch database — `accounts`' full `SHOW CREATE TABLE` (columns, indexes, FKs, nullability, `ON DELETE` behavior) now matches the real local DB exactly (only cosmetic charset-display/key-declaration-order artifacts, not real drift, consistent with every prior comparison in this project). **No unexplained Account schema drift remains.**

  **Verification**: focused regression (`tests/Feature/Integrity`, `CheckFinancialIntegrityCommandTest`, `tests/Feature/Reports`, `FinancialAccountGuardTest`, all 5 `BalanceGuardIntegrationTest` suites, 4 account-validation tests, `ExecutionPaymentCreditAccountTest`, `GeneratesSequentialTransactionNumbersTest`) — **178/178 passed** (unchanged from Task 8.2, since no application code was touched). Real local DB: `migrate:status` shows the new migration `Ran`; `php artisan oms:check-financial-integrity` — **Result: OK, exit code 0**; 0 non-null `created_by`/`updated_by` before and after (unchanged); `current_balance` sum unchanged (0.00); account/transaction/line counts unchanged. Real smoke checks: `/admin/login` → HTTP 200; `/admin/accounts`, `/admin/accounts/create` → HTTP 302 (correct auth redirect); `Account` Eloquent loads, `AccountForm` schema build, `TrialBalanceReportService`, `AccountStatementReportService` all executed cleanly.

  **Not committed — awaiting review**, per instructions (STOP before commit; no Graphify run; no push; no Audit Log started).

- **2026-07-28 — OMS Task 9A (Audit Log design audit, read-only) — complete, no code/schema changes.**

  A read-only design audit (no implementation) established: no audit/activity-log package is installed (verified directly against `composer.json` — only `spatie/laravel-permission`, `astrotomic/laravel-translatable`, `filament/filament`, `phpoffice/phpspreadsheet`/`phpword`); `App\Traits\HasUserTracking` (`created_by`/`updated_by`, actor pointer only, no history/old-new values) is wired to 21 models but not `Account`/`AccountType` (their columns exist, deliberately unwired per Task 8.3); zero old/new-value capture, zero security/attachment/backup-restore/report-export event logging exists anywhere. Recommended architecture: a first-party hybrid (shared CRUD diffing + explicit domain audit calls) rather than adopting a Composer package — mirrors the same in-house-over-package reasoning already used for the Task 7B backup engine. Full 22-point report covering taxonomy (MUST_AUDIT/OPTIONAL/DO_NOT_AUDIT per event category), proposed `audit_events` schema, immutability design (no DB triggers — application-level, mirroring `PermissionResource`/`TransactionResource`'s existing structurally-read-only pattern), central redaction policy, financial-event payload policy (bounded snapshot + transaction identifier, never a full `TransactionLine` dump), background/system actor typing, Filament UI design (Super-Admin-only initially), and an 8-phase implementation plan (9B.1–9B.8) delivered as the final output. No code, schema, or database was touched; no Graphify run.

- **2026-07-28 — OMS Task 9B.1 (Audit Log foundation) — implemented, verified, NOT committed.**

  **First phase of the Task 9A-approved architecture, foundation only — nothing wired to any existing model/controller/command yet.** Built: the `audit_events` migration (append-only — no `updated_at`/`deleted_at`/SoftDeletes; `subject_type` stores a short hand-maintained alias, never a raw FQCN; `actor_user_id` FK→`users` `ON DELETE SET NULL`); `App\Models\AuditEvent` (application-level immutable — `update()`/`delete()`/`forceDelete()`/`replicate()` all throw `AuditImmutableRecordException`; uuid always freshly generated in `creating()`, never caller-supplied even via mass assignment); `App\Services\Audit\AuditRedactor` (central, recursive, whole-underscore-segment-matched secret denylist — e.g. `encryption_key_id` survives via an explicit, narrowly-scoped safe-exception list, while `account_key`/`session_token`/etc. all fail closed); `App\Services\Audit\AuditPayloadBounder` (1000-Unicode-char string cap via `mb_*`, independent 8192-encoded-byte cap per `old_values`/`new_values` via deterministic key-dropping — never `substr()` on the encoded JSON string — with an explicit `_truncated: true` marker; DateTime→`DATE_ATOM`, BackedEnum→`->value`, resources/closures/plain objects→a safe marker string); `App\Services\Audit\AuditActorContext` (typed factories `forUser()`/`guest()`/`system()`/`scheduler()`/`queue()`/`command()` — the API structurally enforces the Task 9A §K login-failure rule: there is no constructor path that accepts a raw email string, only a real hydrated `User` model or nothing at all); `App\Services\Audit\AuditLogger::record()` (validates category/action/subject-type format before anything else, redacts then bounds every payload, inserts one row, and — critically — never opens its own DB transaction, so a future `AuditFailureMode::Required` call from inside an existing financial `DB::transaction()` commits or rolls back exactly with that transaction, proven directly by `AuditLoggerTransactionTest`).

  **Verification**: 53 new focused tests, 53 passed, 197 assertions (migration/schema, immutability, redaction, bounding, actor context, logger REQUIRED/BEST_EFFORT/transaction-participation behavior) — no existing test file touched, full application suite not run per instructions. Real local DB: pre-task `mysqldump` backup created outside the repo (`C:\laragon\backups\oms\oms_before_audit_9b1_20260728_115843.sql`); migration applied and shows `Ran`; `audit_events` confirmed empty (0 rows) post-migration; `accounts`/`users`/`transactions` row counts unchanged; `php artisan oms:check-financial-integrity` — **Result: OK, exit code 0**; `/admin/login` → HTTP 200.

  **Not committed — awaiting review**, per instructions (STOP before commit; no Graphify run; no push; no wiring into any existing model/controller/command; no UI/permissions added).

- **2026-07-28 — Graphify hygiene: project-local `.graphifyignore` added — implemented, verified, NOT committed.**

  Discovered the generated graph was indexing `storage/app/public/**` (uploaded financial-attachment filenames — receipts/payment proofs) and `storage/framework/views/**` (compiled Blade cache) — neither excluded by `.gitignore` (which only covers `/storage/*.key` and `/storage/pail`), so Graphify had nothing telling it to skip the rest of `storage/`. Confirmed via the installed package's own source (`graphify/detect.py`) that a project-root `.graphifyignore` (gitignore syntax, merged with `.gitignore`, last-match-wins, honored identically by `graphify update` and the post-commit hook's rebuild) is the correct project-local exclusion mechanism — not a Composer/`--exclude`-flag/global-config approach. Added `.graphifyignore` excluding `storage/**`, `bootstrap/cache/**`, `public/storage/**`, `vendor/**`, `node_modules/**`, `.git/**`, `graphify-out/**`, `.env`, `.env.*`, `*.sql` — while explicitly preserving `resources/views/vendor/filament-panels/**` (real tracked UI source, not excluded merely for containing "vendor" in its path). A real `graphify update` quirk was found and worked around: the incremental merge (and even `--force`) does not prune nodes for newly-excluded files when a prior `graph.json` still exists — a genuinely clean rebuild required removing the three output files first (backed up, then regenerated) so no stale merge target existed. Verified deterministic via two independent from-scratch rebuilds (`PYTHONHASHSEED=0`), byte-identical SHA-256 across `graph.json`/`manifest.json`/`GRAPH_REPORT.md`. Confirmed zero references anywhere to `storage/app/public`, `storage/app/private`, `storage/framework`, `storage/logs`, `public/storage`, `.env`, `.sql`, composer `vendor/`, or `node_modules`, while every Task 9B.1 node and all legitimate source (`app/`, `database/migrations/`, `tests/`, `resources/views/vendor/filament-panels/`, `config/`, `routes/`) remained fully indexed. `php artisan oms:check-financial-integrity` — Result: OK, exit code 0 (no application code touched). Not committed — awaiting review.

- **2026-07-28 — OMS Task 9B.2 (Audit Log — general CRUD integration) — implemented, verified, NOT committed.**

  **Second phase of the Task 9A-approved architecture: the 9B.1 foundation is now wired to the real write paths of eleven general/master-data models only** — `Project`, `ProjectCost`, `Partner`, `PartnerType`, `ProjectSuper`, `ProjectStatus`, `BankType`, `FiscalYear`, `TransactionType`, `TransactionSuperType`, `Setting`. Events: `created`/`updated`/`deleted`/`restored`, all under `event_category = crud`, all in `AuditFailureMode::Required`. **No financial workflow model** (`Transaction`, `TransactionLine`, `ProjectCostReceipt`, `ProjectCostBudget`, `ProjectCostBudgetsPayment`, `GeneralExpense`, `GeneralExchange`, `Account`, `AccountType`, `Currency`, `ExchangeRateHistory`), **no security model** (`User`/`Role`/`Permission`), no auth listeners, no `Attachment`, no `BackupOperation`, no report exports, no audit UI, no audit permissions, and no schema change — those remain 9B.3+. `Account`/`AccountType` were again **not** wired to `HasUserTracking` (still deferred).

  **Architecture — a service, deliberately not model observers.** A read-only audit of the real write paths found that **no Filament write path in this application runs inside a database transaction**: the panel never calls `->databaseTransactions()` (so `Filament\Pages\Concerns\CanUseDatabaseTransactions` resolves false for every Create/Edit page) and `Filament\Actions\Concerns\CanUseDatabaseTransactions` defaults to false for every Action. An `created`/`updated`/`deleted` observer would therefore fire with the business row already committed, so a failing REQUIRED audit insert would leave a mutation with no audit row. Instead, `App\Services\Audit\Crud\AuditedCrudService` owns both the mutation and the `AuditLogger::record()` call inside one `DB::transaction()` — they commit or roll back together, and nesting inside a caller's existing transaction is savepoint-safe (an outer rollback discards the event too). Supporting classes: `AuditSubjectRegistry` (closed model→alias allowlist; an unregistered class throws `AuditSubjectNotRegisteredException` rather than inventing an alias, which is what structurally guarantees no FQCN ever reaches `subject_type`), `AuditSubjectDefinition`, `AuditModelSnapshotter` + `AuditFieldDiff`, `SettingValuePolicy`, and `App\Services\Audit\AuditActorResolver` (the one place `auth()`/`request()` become an `AuditActorContext`; request metadata is attached only when a real routed HTTP request exists, never fabricated under `artisan`).

  **Filament wiring** (34 files): `App\Filament\Concerns\AuditsRecordCreation`/`AuditsRecordUpdate` replace `handleRecordCreation()`/`handleRecordUpdate()` on the 11 Create/Edit pages; `App\Filament\Concerns\AuditedActions` returns stock `DeleteAction`/`DeleteBulkAction`/`CreateAction`/`EditAction` with only their process closure swapped via `->using()`, so labels, confirmation modals, notifications, authorization, trashed-hiding and the create→View / edit→View / delete→List redirect standard are all untouched. `AuditedActions::deleteBulk()` pins `fetchSelectedRecords()` on so Filament can never take its mass `$query->delete()` branch and bypass the models. On resource pages a table's Create/Edit/View actions are plain links to the Create/Edit/View pages and perform no write, so they are deliberately left alone; the `CostsRelationManager` (Projects → تكاليف المشروع) is the one in-place modal write path and uses `relationCreate()`/`edit()`/`delete()`/`deleteBulk()`.

  **Payload policy.** Each subject declares a closed allowlist of real business columns, so `id`/`created_at`/`updated_at`/`deleted_at`/`created_by`/`updated_by`/`remember_token`/observer-maintained flags are excluded by construction, not by a denylist. `created` = full snapshot; `updated` = only changed audited fields on both sides (no event at all when nothing audited changed); `deleted` = pre-delete snapshot with the primary key preserved; `restored` = post-restore snapshot. Date casts are stored as plain `Y-m-d`. `astrotomic/laravel-translatable` is installed but **no target model uses it** (verified) — the Arabic display values are plain columns, so nothing serializes translation rows. `ProjectCost` stores `project_id` **plus** one bounded `project_label` snapshot, **on both sides of a reassignment** — each side resolved from its own foreign key value via `Project::withTrashed()->select(['id','code','name'])->find()`, never from the model's `project` relationship (which on the pre-change side already points at the new project). Lookups are memoized per logical action; an update that does not touch `project_id` performs none. `changed_fields` still lists `project_id` only — a label is a readability snapshot, not a changed field. No related model or collection is ever serialized. `settings.value` passes a fail-closed policy: redacted when the setting key is credential-shaped (judged by `AuditRedactor`'s own rules after separator normalization, so `mail.password` is caught) or the value is a PEM block or a long opaque token — the free-form `settings` table has no enumerable safe-key list (nothing in `app/` reads a fixed key). `settings.key` is emitted under the safe semantic name **`setting_name`** (`AuditSubjectDefinition::$fieldAliases`, the only alias in the codebase), so a key rename records both the old and the new name; the raw `key` field name never reaches the payload, and `AuditRedactor` was not weakened.

  **Duplicate prevention** is structural: auditing lives only in the service, so `ProjectObserver`/`ProjectCostObserver` (which flip `ProjectFinancialSnapshot.is_dirty` on a different, unaudited table), `HasUserTracking`'s `updated_by` write, nested saves and Filament lifecycle callbacks cannot manufacture a second event. For the same reason **no audit-suppression switch was built or needed**: seeders, migrations, factories, permission sync and test fixtures write these tables directly and simply produce no history.

  **Two accountability corrections applied after the first review (same day)**: (1) `settings.key` had been `[REDACTED]` on both sides of a rename, losing the previous key — now emitted as the safe semantic field `setting_name` via the new `AuditSubjectDefinition::$fieldAliases`, with `AuditRedactor` untouched and no general `key` exception added; secret-shaped settings still record `[REDACTED]` **values** while their **names** are preserved. (2) A `ProjectCost.project_id` change had labelled only the new side — relation-label resolvers now take the foreign key **value** rather than the owning model, so both sides carry their own `project_label` (old side via `withTrashed()`, so a retired project still resolves), memoized per action, with `changed_fields` still listing `project_id` alone. A `AuditedFilamentLifecycleTest` was added at the same time proving Filament's before/after create/save hooks still fire around the replaced handlers, validation still blocks both the write and the event, a failing bulk delete reports failure and deletes nothing, post-bulk deselection still dispatches, and BelongsTo `Select` relationships are captured by the audited write itself (nothing left for `saveRelationships()`).

  **Verification**: 45 new focused tests (98 in `tests/Feature/Audit/` total with 9B.1's 53), 557 assertions, all passing — alias stability, unregistered-model rejection, technical-field exclusion, 255-char label bounding, no-op updates, one-event-per-action, event immutability, per-model create/update/delete/restore content, actor snapshot, the Project dirty-flag observer, FK+label relationship handling, Setting redaction/bounding, and four forced-REQUIRED-failure rollback proofs plus the surrounding-transaction rollback. Regression: `tests/Feature/Crud` + `tests/Unit/Filament` + `tests/Unit/Policies` 117/117, `tests/Feature/Permissions` 341 (338 passed, 3 pre-existing by-design skips). Real local DB: `audit_events` still **0 rows** (no artificial events created), all business row counts unchanged, `php artisan oms:check-financial-integrity` — **Result: OK, exit code 0**, `/admin/login` → 200, target resource pages → 302 to login.

  **Not committed — awaiting review**, per instructions (STOP before commit; no Graphify run; no push; no 9B.3).

- **2026-07-28 — Graphify cache-tracking hygiene: `graphify-out/cache/**` untracked — implemented, verified, NOT committed.**

  **`graphify-out/cache/**` is local generated cache and must never be tracked.** Triggered by three stale absolute paths to real local database-backup `.sql` files inside the committed `graphify-out/cache/stat-index.json`. A read-only audit of the installed Graphify source (`cache.py`, `extract.py`) established that the whole directory is a pure speed optimisation — its module docstring is literally "per-file extraction cache - skip unchanged files on re-run" — and that the leak was far broader than three paths: the stat index is keyed on `str(p.resolve())` by design, so the committed copy held **1511 keys, 100% absolute machine paths**, 695 of them under `storage\` (506 `framework\testing`, 145 `framework\views` compiled Blade cache, 31 `app\private` including the 3 backup `.sql` names and `livewire-tmp` upload scratch files, 12 `app\public` uploaded financial-attachment filenames), plus `.claude/settings.local.json`. Graphify's cache *hash* is deliberately relative "so shared caches and CI work correctly"; the index *key* is not — the file can never be portable.

  Nothing depended on it being tracked: a cache miss is non-fatal (both `load_cached()` call sites in `extract.py` fall through to fresh extraction), `_ensure_stat_index()` starts from `{}` when absent, `cache_dir()` creates its own directories, `_flush_stat_index()` swallows `OSError`, the repo has no CI, and the post-commit hook's only "cache" references are its own `~/.cache/graphify-rebuild.log`. The only tracked mentions of `graphify-out/cache` anywhere are historical `docs/PROMPTS_LOG.md` entries that already said never to commit it.

  **Fix (durable repository policy, not redaction):** added `/graphify-out/cache/` to `.gitignore` and ran `git rm --cached -r graphify-out/cache` — 2157 files removed from the index, all 2157 kept on disk. `.graphifyignore` unchanged. Hand-redacting the three paths was rejected because the file regenerates every run; a post-generation scrubber was rejected as a permanent moving part guarding a file that should not be in version control at all. **The tracked Graphify outputs remain `graph.json`, `manifest.json`, `GRAPH_REPORT.md`, `cost.json`, the `.graphify_*` markers and the dated curated snapshots. Absolute runtime/backup paths must never enter a commit. No real backup file was read, moved, modified or deleted.**

  **Verified**: `git check-ignore -v` resolves to `.gitignore:16`; `git ls-files graphify-out/cache` → 0; zero `??` cache entries in `git status`; `PYTHONHASHSEED=0 graphify update .` succeeded (822/822 files, 12464 nodes, exit 0), rewrote all 2157 local cache files with zero entering git, and kept every Task 9B.1 foundation node and Task 9B.2 CRUD-audit class indexed across 823 files with 0 hits for `storage/app`, `storage/framework`, `public/storage`, `bootstrap/cache`, `node_modules`, `.env`, `.sql`, `livewire-tmp`. `oms:check-financial-integrity` — **Result: OK, exit code 0**. PHPUnit deliberately not run. Two further tracked sensitive-path sources found in this pass were reported rather than acted on, then **resolved by the extension below**.

- **2026-07-28 — Graphify hygiene extension: dated snapshots untracked + `.claude/**` excluded from indexing — implemented, verified, NOT committed.**

  **Only the canonical root-level Graphify outputs are tracked; dated generated snapshots are local artifacts.** The 15 `graphify-out/20*/` directories are created by `graphify.export.backup_if_protected()` — a pre-overwrite safety snapshot that is **write-only**: all four call sites (`watch.py:847`, `__main__.py:3533/4704/4819`) call `_backup(out)` and discard the return value, and nothing in Graphify enumerates, reads or restores a dated folder. Nothing in the repo depends on them (no CI, no hook reference, only prose in `docs/TASKS_LOG.md`). They were also the largest remaining leak — as frozen copies taken *before* the Task 9B.1 `.graphifyignore` work they still carried 2681 `storage/app` references, 85 `livewire-tmp`, 30 `.claude` and 27 `.sql` across 42 of their 75 files. **Git history preserves the same information far better: 75 commits touch `graphify-out/graph.json`, each tied to the exact source revision that produced it**, versus 15 daily snapshots. Applied `/graphify-out/20*/` in `.gitignore` (verified to match only dated directories) plus `git rm --cached -r` — 75 files unindexed, all 75 retained on disk. This explicitly supersedes the earlier entry's retention of "the dated curated snapshots" as tracked output.

  **`.claude/**` added to `.graphifyignore`** so local Claude Code state is never indexed — Graphify had been walking the git-ignored per-machine `.claude/settings.local.json` and recording its filename in the tracked `manifest.json`. The whole directory is excluded so future local Claude runtime/config files are covered by default; **the repository-root `CLAUDE.md` is deliberately not excluded and is confirmed still indexed**. No Claude configuration contents were read and no `.claude` file was added to git.

  **Clean deterministic rebuild** (root outputs backed up outside the repo, the three generated root files removed, `PYTHONHASHSEED=0 graphify update .` run twice as independent from-scratch rebuilds): both runs produced 820 files / 12480 nodes / 31657 edges / 512 communities and **byte-identical SHA-256** for `graph.json`, `manifest.json` and `GRAPH_REPORT.md`. The seven remaining tracked `graphify-out` files show **0 hits** for `storage/app`, `storage/framework`, `storage/logs`, `public/storage`, `bootstrap/cache`, `livewire-tmp`, uploaded attachment directories, `.sql`, `.claude/`, `settings.local.json`, `.env*`, `node_modules` and composer `vendor/` source, while legitimate source identifiers survive (`BackupCreationOrchestrator` 649, `RestoreCommand` 384, `AttachmentStorageService` 264, `AuditedCrudService` 387) and all `app`/`tests`/`database`/`resources`/`config`/`routes` source stays indexed. `oms:check-financial-integrity` — **Result: OK, exit code 0**. **No real backup, uploaded attachment, or Claude local-config file was read, modified, moved or deleted.** One residual finding left for a separate decision: `graphify-out/.graphify_python` is tracked and contains a single absolute machine-local interpreter path (the hook's second of four probes, unusable on any other machine).

## Sensitive Data Rules


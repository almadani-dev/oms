# Next Steps

## Recommended Next Step (2026-09-20, تقرير الحركات المالية الشامل — multi-account, نوع البنك, all notes, expandable classifications, dual scrollbars; focused + regression green, NOT committed)

**Open the report in a browser and confirm the two interactive pieces by eye, then review and approve the commit.** Everything is covered by automated tests — including real Livewire tests that drive the component and exports that are opened back and inspected — but **the scroll-sync and expand/collapse behaviour has not been exercised in a live browser**, and it is the one part of this task that automated tests cannot fully prove.

**What to check on `تقرير الحركات المالية الشامل`:**

- **Dual scrollbars.** Drag the **top** bar → the table and its native bottom bar follow. Drag the **bottom** bar → the top follows. No stutter, jitter or runaway feedback. Then resize the window and confirm both stay aligned and the top bar's thumb resizes with the table.
- **RTL specifically.** The table starts flush **right**. Scrolling reveals content to the **left**, and both bars agree on position the whole way. This is the highest-risk item: browsers use a **negative** `scrollLeft` for RTL, and the implementation deliberately mirrors the raw value rather than assuming a sign — worth confirming in whichever browsers the team actually uses.
- **Keyboard.** Tab to the table (it is now a focusable `role="region"`) and confirm the arrow keys scroll it horizontally. This did not work before.
- **Classification expansion.** Click the chevron beside a classification name: the 10-column detail table opens **below that row**, showing only that classification's movements. Open a second classification and confirm the first **closes automatically**. Click the open one again and confirm it collapses. Confirm the chevron rotates and reads naturally in RTL.
- **Notes.** A row with notes shows a `N ملاحظة` button; a single short note also shows inline. Expanding shows **every** note with its Arabic source label, **untruncated**, with the author's line breaks preserved. Confirm a note containing something like `<b>x</b>` renders as literal text, not as markup.
- **`نوع البنك`.** Sits directly beside `الحساب`. Expect **many `—` values** — `accounts.bank_type_id` is nullable and only bank-like accounts carry one. Confirm a bank-to-bank `التحويلات العامة` entry shows **two different** bank types on its two lines.
- **Multi-account filter.** Select several accounts; confirm `الفلاتر المطبقة` lists them all joined with `، `, and that clearing the selection restores all-accounts behaviour.
- **Exports.** Open the Excel and Word files and confirm RTL layout, that `نوع البنك` and the full `الملاحظات` are present, and that the figures match the screen exactly.

**One thing the reviewer must consciously accept:** the audit payload key for this report changed from `account_id` (scalar) to `account_ids` (array). **Historical `audit_events` rows were not rewritten** and still carry `account_id`. Anything that reads that payload for this report specifically must handle both shapes. No existing test asserted the old key for this page, so nothing broke — this is a forward-compatibility note, not a defect.

**Two findings recorded, no action taken:**

- **Pint is not enforced on this codebase.** All four modified production files failed `pint --test` with the **exact same fixer sets before any edit** (verified by running Pint against the pristine `HEAD` copies). Formatting was therefore deliberately left matching the surrounding code rather than reformatting, which would have produced a large unrelated diff. If the team wants Pint enforced, that is its own task.
- **`php artisan optimize:clear` cannot complete locally.** Its `cache:clear` step needs MySQL (the cache driver is `database`) and the local MySQL is not running. `view:clear`, `route:clear`, `config:clear` and `clear-compiled` were run individually and all succeeded. `config:cache` and `route:cache` were **not** run, per the brief.

**Added in the final verification pass:** `accounts.notes` is now surfaced as `ملاحظات الحساب` (line scope — each line carries its own account's notes), reaching the panel, Excel and Word with its source label. It is free: `accounts` is inner-joined on `tl.account_id` and already filtered by `whereNull('a.deleted_at')`, so a line's account is always present and unambiguous. `bank_types.notes` is deliberately **excluded** — it describes the lookup row, not the movement — and a test asserts it never leaks into the notes collection.

**Still open, deliberately out of scope:** `transactions.reference` exists and is still not surfaced by this report. It is free (already joined). It was not in the brief, so it was not added.

---


## Recommended Next Step (2026-08-19, BUD/EXT optional deduction lines — 0% percentages; focused + regression green, NOT committed)

**Open both Create and Edit screens in the browser and confirm the four account cards and the disabled-card behaviour by eye, then review and approve the commit.** Everything below is verified by automated tests — including real Livewire tests that drive the actual component, not just the page methods — but **not yet in a live browser session**.

**What to check on `صرف مبلغ المشروع` and `التحويلات العامة` alike:**

- **Desktop:** four separate cards — `حساب المصدر (دائن)` · `حساب النسبة الإدارية (مدين)` on the first row, `حساب التحويل / الصرافة (مدين)` · `حساب الوجهة (مدين)` on the second. RTL ordering natural, no layout break, correct in **both dark and light** mode.
- **Narrow/mobile:** the four cards stack to one column with no horizontal overflow.
- **`النسبة الإدارية = 0`** → its card stays **visible but disabled**, showing `النسبة الإدارية 0% - لا يوجد خصم إداري ولن يُنشأ سطر قيد لهذا الحساب.` Set it to `1` → the card enables and is **empty**. Same for `نسبة التحويل`.
- **`احسب` still fills `مبلغ النسبة الإدارية` with `0.00`** at 0% — a zero deduction amount is legitimate and must show as `0.00`, not blank and not `—`.
- **Edit an existing 0% record:** the card opens disabled and empty. Raise it to a positive value and confirm **no stale account reappears**. Then take a positive record down to `0` and confirm the card clears immediately, disables, saves successfully, and the balances stay correct.

**Two things the reviewer must consciously accept:**

- **A new rejection was added that the brief did not ask for.** `FinancialAmountGuard::assertDeductionsAreRecordable()` rejects a percentage `> 0` whose derived amount rounds to `0.00` (e.g. 0.4% of 1.00), on the percentage field. Nothing that previously succeeded now fails — that input was already a silent no-op — but it is a deliberate addition. Its structural purpose is to make `percentage > 0` and `amount > 0` equivalent, which is what keeps the line payload, account validation, balances and audit roles from disagreeing. See `docs/DECISIONS_LOG.md`.
- **BUD/EXT transactions now carry 2–4 lines.** Any future report, export or consumer must treat a missing `LINE_ADMIN`/`LINE_TRANSFER` as a valid 0% deduction, never as missing data. Pre-existing 4-line rows with zero-valued deduction lines were deliberately **not** rewritten (no migration), and `JournalBalanceIntegrityChecker` still reports those as `invalid_fx_base_conversion` exactly as before — none exist on the local database.

**Still open, deliberately deferred:** the repo-wide guard-key refactor. All three financial guards throw `ValidationException` keyed by domain field (`admin_account_id`, and `lines` for every line-level check) while Filament resolves errors by state path (`data.admin_account_id`), so ~30 rejection points across all six financial workflows still do not bind to their fields. The four BUD/EXT pages now surface those messages as a danger notification (Layer 1), which closes the silent-no-op symptom but not its cause. That refactor deserves its own task and verification pass.

**Unrelated observation, no action taken:** the local development database grew from 3 to 4 transactions (and 5 to 11 accounts) between 09:53 and 10:27 on 2026-08-19, from ordinary local browser use — before any edit in this task, and not from the test suite (`phpunit.xml` pins tests to `sqlite` / `:memory:`). `oms:check-financial-integrity` reports **OK** on all of it.

---

## Recommended Next Step (2026-08-18, Muwakha Family Account Statement — `كشف حساب الأسرة`; recovered after a power outage, verified clean, NOT committed)

**Open `كشف حساب الأسرة` in the browser from a Family View page and confirm it visually, then review and approve the commit.** Everything below was verified through focused tests and the rendered output, **not yet in a live browser session**. Worth checking by eye: RTL and dark theme; a family whose Accounts span two currencies rendering as two separate sections with no blended figure; several Accounts in one currency merging into a single chronological section with each row naming its own Account; a soft-deleted mapped Account appearing in the Account filter marked `— محذوف`; the `عرض` gate (changing any filter clears the result and both export buttons refuse until `عرض` is pressed again); and the Excel and Word files opening with correct RTL layout and matching the on-screen figures exactly.

**Also worth confirming by eye:** the Project filter is **hidden entirely** when the family's movements resolve to no Project at all, which is the common case for a family that has only ever received execution payments outside a project-cost budget. That is intended, not a rendering fault.

The feature survived the outage complete. The recovery audit found no truncated or partial file, no surviving process, no migration for this report, and no production defect; the sole recovery edit was one test expectation (`MuwakhaFamilyResourceTest`'s exact page list, which legitimately gained `account-statement`). Verified sequentially and clean: statement tests **33 passed / 194**; `tests/Feature/Muwakha` **177 passed / 920**; `tests/Feature/Audit` **375 passed / 2 658**; `tests/Feature/Permissions` **338 passed, 3 pre-existing skips / 1 819**; `tests/Feature/Crud` **91 passed / 275**; `php artisan oms:check-financial-integrity` → **Result: OK, exit 0**. **The full test suite was intentionally NOT run**, and Graphify was deliberately not run.

**One thing the reviewer must consciously accept:** this report reads **soft-deleted** Accounts, which no other Muwakha screen does. `MuwakhaFamilyAccountStatementService` resolves Accounts with `withTrashed()` and never filters `accounts.deleted_at`, so a deleted Account's history appears here and nowhere else. That is the approved behaviour — omitting it would silently delete financial history — but it is a deliberate, narrowly scoped widening and should be accepted knowingly rather than discovered later.

**Unrelated observation, no action taken:** the local development database currently holds **5 accounts**, where the 2026-08-17 entry below recorded 1 (id 45) after the approved test-residue cleanup. The drift predates this recovery and nothing in it touched real business data — tests run on the `testing` connection. Worth a glance before any future local cleanup; it does not affect the feature or any test result above.

**Next: review this feature together with the still-uncommitted Muwakha work below, then approve the commit.** Everything in the entries below remains outstanding and unchanged.

---

## Previously Recommended Next Step (2026-08-17, Muwakha Family View layout — compact full-width linked-accounts table, repositioned; focused tests green, NOT committed)

**Open the Family View page in the browser at desktop width and confirm the section visually.** This was verified through the rendered HTML and focused tests, **not yet in a live browser session**. Check that `الحسابات المرتبطة بالأسرة` now renders as one compact table row per Account — headings once in the header row, each cell a single line, no label repeated inside any cell — that it spans the full content width, and that the lower page reads `ملاحظات` → `الحسابات المرتبطة بالأسرة` → `مشاريع المؤاخاة`. Confirm RTL and the dark theme look right, and that a family with several Accounts stays readable without horizontal overflow (the seven column widths total 100%, and `نوع البنك / وسيلة الدفع` and `اسم صاحب الحساب` wrap their headings).

The change is presentation-only: the single production file touched is `app/Filament/Resources/MuwakhaFamilies/Schemas/MuwakhaFamilyInfolist.php` (section moved to last, `columnSpanFull()`, `hiddenLabel()` + `TextSize::Small` on the cells, `TableColumn::width()`, `wrapHeader()`). No schema, model, service, matching, reuse, creation, export, permission, audit or financial change. Behaviour is unchanged and still asserted: current Account first and badged `الحساب الحالي`, previous badged `سابق`, per-mapping historical `account_holder_name`, soft-deleted Accounts hidden with mappings intact, the Account name linked only when authorized, no actions in the section, and the `الحساب الحالي محذوف` warning untouched.

Verified: `tests/Feature/Muwakha/MuwakhaFamilyLinkedAccountsTest.php` **12 passed / 121 assertions**; `tests/Feature/Muwakha` **144 passed / 726 assertions**. **The full test suite was intentionally NOT run, by explicit user instruction**, and Graphify was deliberately not run.

**Next: review this layout change together with the still-uncommitted Muwakha feature below, then approve the commit.** Everything in the entry below remains outstanding and unchanged.

---

## Previously Recommended Next Step (2026-08-17 FINAL, Muwakha historical family Accounts — immutable Accounts, exact reuse, ownership mapping; focused + regression green, NOT committed)

**The final pre-commit refinement of أسر المؤاخاة is implemented and awaiting review.** A Muwakha Account is now immutable: `MuwakhaFamilyService` issues **no Account UPDATE anywhere**. Account names are canonical — `أسرة الشهيد {martyr_name} - {currency display name} - ({account_code})` — so the general Accounts screen is self-describing. An edit compares the submitted **material identity** (canonical name + `أفراد` + currency + account number + bank type + normalized IBAN + account-holder name) with the current Account's and then either writes only the family row, **reuses an exact Account the family already owns**, or creates exactly one new Account plus one ownership mapping — always atomically. The new `muwakha_family_accounts` table is durable ownership only: **no dates, no versions, no snapshots, no account-history event system.**

Verified sequentially: Muwakha **118 passed / 527**; Audit Crud + Reports **86 passed / 640**; Crud + Integrity **111 passed / 322**; ExecutionPayments + Audit/Financial **67 passed / 549**; Permissions **341 tests, 338 passed, 3 pre-existing skips / 1 819** — **720 passed, 3 857 assertions, 0 failures**. Both migrations applied to the real local database; `php artisan oms:check-financial-integrity` → **Result: OK, exit 0**.

**The full test suite was intentionally NOT run, by explicit user instruction.**

**Plus one UI refinement (same day, after the above):** the Family **View page** gained a read-only section `الحسابات المرتبطة بالأسرة` listing every Account mapped through `muwakha_family_accounts` — current one badged `الحساب الحالي` and first, then previous Accounts newest-mapping-first, each row carrying its **own** historical `account_holder_name`. Soft-deleted Accounts are excluded as a **display filter only** (mapping kept, nothing restored or reactivated). No actions of any kind in the section; the account name links to the ordinary Accounts view page only when authorized. `tests/Feature/Muwakha` → **125 passed / 579 assertions**. No migration, no schema change, no service change.

**Plus two final UI refinements (same day):** **(A)** a soft-deleted current Account is now **reported, not described** — `بيانات الحساب` shows only `الحساب الحالي محذوف` and hides its name, currency, number, bank, IBAN and holder; a display rule only, with the mapping, `account_id` and the Account itself untouched. This **supersedes** my earlier decision to leave that section reading the `withTrashed()` relation. **(B)** the Family **Create** form gained the create-only section `ربط بمشاريع المؤاخاة` (Repeater, `إضافة مشروع`, project + optional card code), whose rows are replayed through `MuwakhaFamilyProjectService::link()` **inside the create transaction** — so one save writes Account + family + ownership mapping + every link, duplicates within a submission are caught by the existing rules, and any rejected row rolls back the whole creation. No schema change; `ProjectsRelationManager` is unchanged and still owns post-create link management.

**Local test residue cleaned (approved by the user):** Accounts **43 and 44** were hard-deleted from the local database after re-verifying, for each, zero references from `muwakha_families.account_id`, `muwakha_family_accounts`, and `transaction_lines`, plus a zero balance — and a schema-wide scan confirming those are the only three foreign keys onto `accounts.id`. The local database now holds 1 family, 1 account (id 45), 1 mapping, 0 transactions, 0 lines. No cleanup code or migration was added.

**Next: review the implementation and the documentation diff together, then approve the commit.** Graphify was deliberately not run.

### Three things the reviewer must consciously accept

- **Account 45 was renamed once** to `أسرة الشهيد احمد محمود ننننن - شيكل اسرائيلي - (230)`. Permitted because ownership is unambiguous through the foreign key **and** it has zero `transaction_lines`. An Account with ledger history is never renamed by a migration.
- **A family's Account count grows with each distinct identity**, including several Accounts in one currency. That is the intended financial record. Returning to an exact previous identity reuses that Account rather than adding another.
- **`oms:check-financial-integrity` still runs against a ledger-free local database** (0 transactions, 0 transaction lines), so its numbering/balance/FX/currency checks were trivially clean. The relationship check (10 relationships, 0 orphans) and the account balance check (3 accounts, 0 mismatches) did run.

---

## Previously Recommended Next Step (2026-08-17, Muwakha family-account currency — selectable, currency change forks a new Account; SUPERSEDED the same day)

**The one approved follow-up change to أسر المؤاخاة is implemented and awaiting review.** Family accounts are no longer fixed to ILS: `بيانات الحساب → العملة *` is a required searchable Select over the live OMS currencies, the Account name is `أسرة الشهيد {martyr_name} - {currency display name}` (from `currencies.name`), and **changing a family's currency creates a new Account and repoints `muwakha_families.account_id`, leaving the previous Account and its entire ledger untouched**. Martyr-name, account-number, bank and IBAN changes still update the same Account. `نوع الحساب = أفراد` is still a read-only invariant. **No account-history table, no new migration, no column on `muwakha_families`, and no change to global Currency behaviour, Project financial workflows, Transaction posting, balances or FX.**

Verified sequentially: Muwakha **84 passed / 363 assertions**; Crud + AccountCurrencyIntegrityChecker **97 passed / 286**; Audit Crud + Reports **86 passed / 629**; ExecutionPayments + ExecutionPaymentAudit + FinancialMasterDataAudit **31 passed / 202** — **298 passed, 1 480 assertions, 0 failures**. `php artisan oms:check-financial-integrity` → **Result: OK, exit 0**.

**The full test suite was intentionally NOT run, by explicit user instruction.**

**Next: review the implementation and the documentation diff together, then approve the commit.** Graphify was deliberately not run.

### Three things the reviewer must consciously accept

- **A family that changes currency accumulates one Account per currency era.** That is the intended financial record, not drift — each superseded Account keeps its own balance and ledger in the currency the money was actually moved in. The list table's `العملة` column and the view page show the currently linked Account's currency.
- **`oms:check-financial-integrity` currently reports 0 transactions, 0 lines and 0 accounts on the local database**, so its balance/FX/currency checks were trivially clean rather than exercised against real data. The relationship check (10 relationships, 0 orphans) did run.
- **The old fixed-ILS API is gone, not deprecated.** `MuwakhaReference::CURRENCY_CODE`, `MuwakhaReference::currencyId()` and `MuwakhaReferenceException::currencyMissing()` were removed; an invalid currency is now a field-level `ValidationException` on `currency_id`, not a reference-data exception.

---

## Previously Recommended Next Step (2026-08-16, OMS Muwakha Families — implemented, focused + regression green, NOT committed)

**`المشاريع` → `أسر المؤاخاة` is implemented and awaiting review.** A family/beneficiary register for `مشروع المؤاخاة`, built on top of the existing financial architecture: each family owns one ordinary OMS `Account` (`أفراد` / `ILS`, zero balance, **no opening-balance transaction**) that the existing صرف مبالغ التنفيذ workflow can select as its beneficiary. **No Muwakha payment engine, monthly-cycle table or scheduler was created, and Transaction posting, balances, FX, project reports and the Execution Payment workflow are untouched.**

Verified: Muwakha **63 passed / 244 assertions**; Permissions+Crud+ExecutionPayments **447 tests, 444 passed, 3 pre-existing skips**; Audit+Reports **447 passed / 2801 assertions**; Unit+Integrity+GeneralExpenses+GeneralExchanges+ProjectCostReceipts+ProjectCostBudgetsPayments **550 tests, 548 passed, 2 pre-existing skips** — **1502 passed, 6388 assertions, 0 failures**. `php artisan migrate --force` applied 4 migrations to the real local database; `php artisan oms:check-financial-integrity` → **Result: OK, exit 0**.

**The full test suite was intentionally NOT run, by explicit user instruction.** Verification was scoped by the user to the Muwakha suite, the directly affected regression suites, and the integrity check.

**Next: review the implementation and the documentation diff together, then approve the commit.** Graphify was deliberately not run; the post-commit hook produces its own refresh commit.

### Five things the reviewer must consciously accept

- **The `accounts.account_code` UNIQUE index cannot be restored once duplicates exist.** `down()` on `2026_08_16_100000` will fail with a duplicate-key error, by design — de-duplicating real beneficiary payment accounts, or deleting rows to force the constraint back on, would destroy financial data. Reinstating it is a reviewed data decision, not a schema rollback.
- **`martyr_national_id` is UNIQUE across soft-deleted rows, and there is no Restore UI.** Deleting a family permanently reserves that national id. A composite unique on `(martyr_national_id, deleted_at)` was rejected: MySQL treats NULLs as distinct, so it would have allowed unlimited duplicate **live** rows.
- **Personal identifiers are redacted in audit payloads, so the audit trail records THAT a national id or phone changed but not the values.** The live `muwakha_families` row is the authoritative source. This was an explicit user decision.
- **Creating one family writes TWO audit events** — one `muwakha_family` and one `account`. Two different subjects from one submission; this is not the financial "one logical action = one event" rule, which governs a single ledger operation.
- **`muwakha_families` is the first `ReportExportSubject` alias that is a resource table rather than a report page.** It reuses the existing recorder, REQUIRED mode and `export_requested` action rather than introducing a second export-audit mechanism, and its permission stem is `muwakha_families.export` rather than `reports.*.export`.

### Consciously deferred (not defects)

- **No Excel/CSV import for families** — explicitly out of scope; families are entered through the resource.
- **Accountant holds no `muwakha_families` permissions.** Deliberate for now; one line in `PermissionRegistry::accountantDefaults()` if that changes.
- **No family attachments and no `card_code` history.** Both explicitly excluded by the approved design.
- **`transaction_lines` role tagging still matches on the `notes` string**, not `line_role`, in every read-back path. Pre-existing and untouched by this feature, but it is the fragile seam any future Muwakha-aware reporting would sit on.

---

## Previously Recommended Next Step (2026-08-01, root route redirect — implemented, focused test green, NOT committed)

**`GET /` now redirects to the Filament Admin login through the verified named route `filament.admin.auth.login`, instead of rendering the Laravel welcome page.** Because the redirect is built from the named route, Laravel composes it on top of the incoming request's own scheme, host, port and base path — so the one line serves both **root-domain production hosting** (`https://<host>/admin/login`) and **local `/oms/public` subdirectory hosting** (`http://<host>/oms/public/admin/login`). No host, IP, subdirectory or `APP_URL` value is hard-coded, `/admin/login` itself is unchanged, and no other route, Filament setting, middleware or deployment file was touched.

Verified: focused `tests/Feature/Routing/RootRedirectTest.php` **3 passed / 7 assertions** (root redirect, `/oms/public` base path preserved, `/admin/login` still 200 for a guest); `route:list` unchanged at 125 routes with `filament.admin.auth.login` intact; `oms:check-financial-integrity` — **Result: OK**; `git diff --check` clean. The full suite was deliberately not run.

**Next: review the diff and the documentation entry, then approve the commit.** Nothing else about this change is outstanding. The items below remain as they were.

---

## Previously Recommended Next Step (2026-07-30, OMS Task 9B.8 — FINAL Audit acceptance COMPLETE, full suite green, NOT committed)

**THE COMPLETE AUDIT INITIATIVE (Tasks 9B.1 – 9B.8) IS MARKED `COMPLETE`.**

Final acceptance passed every gate with **zero production or test code changes** — no fix was required. Full application suite: **2337 tests, 8528 assertions, 0 failures, 0 errors, 6 pre-existing skips, 1002.9 s (16.7 min), run sequentially, once.** Final contract: **6 categories, 27 actions, 34 subject aliases**, no PHP FQCN reachable as a `subject_type`. Sensitive-data scan **PASS** (every secret-shaped hit in audit code is a docblock or a display label; all 8 real local audit rows scanned read-only and **CLEAN**). Immutability and real-Super-Admin-only access **PASS**, with **no `audit` permission in `PermissionRegistry`**. Index review: all six required lookups index-served, **no migration needed and none made**. Isolated migration acceptance on a disposable database: 68 DONE / 0 FAIL, seeders exit 0, permission sync 162 = 162 with zero drift. `migrate:status` all **Ran**, 0 pending. `oms:check-financial-integrity` → **Result: OK, exit 0**.

**Next: review this acceptance report and the documentation diff together, then approve the commit.** Nothing else in the Audit initiative remains.

### Two things the reviewer must consciously accept before approving

- **The real local `audit_events` count moved from 4 to 8 during acceptance — reviewed and ACCEPTED as legitimate live application traffic.** Rows 5–8 are genuine interactive browser traffic (`default-livewire.update`, `attachments.show`; IPs `172.16.0.40` and `172.16.0.100`; Windows-Chrome and Android user agents; 13:55–14:18) — the OMS app was in use while acceptance ran. This phase issued only `SELECT`/`SHOW`/`EXPLAIN` against MySQL and ran every test on in-memory SQLite; neither can produce a Livewire route name or a browser user agent. The other six counts were unchanged at snapshot time (users 5, roles 7, permissions 186, transactions 9, transaction_lines 26, backup_operations 13). The same activity added attachment 18 and edited `general_exchange` 8, and integrity still reports OK — so double-entry held. **No AuditEvent was deleted, edited, reset or backfilled, and none ever should be** — the table is append-only by design. This is positive evidence: real usage produced correct, correctly-shaped, leak-free events across four categories.

- **Every local database count in these documents is a timestamped acceptance snapshot, not a permanent invariant.** `audit_events` in particular is expected to keep growing whenever the application is used, so a later reading higher than **8** is normal traffic and not a regression. Historical entries below record the counts that were true when each task ran; none of them constitutes a figure the database must still match. A future acceptance run should capture a fresh snapshot immediately before it starts rather than reusing a figure from an earlier session.
- **`vendor/bin/pint --test` fails repo-wide and was deliberately not fixed.** 351 of 712 PHP files are flagged on a clean working tree — i.e. all already-committed code — including 24 Audit files, under Pint's default `laravel` preset. There is **no `pint.json`** and the preset has clearly never been enforced. Fixing only the 24 Audit files would be a broad formatting change outside the files this phase touched (which the brief forbids) and would leave the repo half-formatted. **This needs its own decision: either adopt a `pint.json` matching the code's actual style, or schedule one repo-wide formatting commit that touches nothing else.**

### The one deployment-only acceptance item
**A real production restore drill is still outstanding — as an operations item, not a code defect.** No disposable MySQL restore harness exists in this project (the whole suite runs on SQLite) and none was built, per scope. Restore audit behaviour is proven by the **simulated** database replacement in `RestoreAuditTest`, which documents its own simulation honestly at the top of the file: every `audit_events` row is removed by raw delete while the signed journal on the private `restores` disk is left untouched, so what the replay reconstructs can only have come from that journal. 30 passed / 320 assertions. The remaining gap is exercising a real `mysql` import on real infrastructure, which belongs to deployment.

### Reports and exports — no pending task
**The reports are working and approved, and no report or export task is currently pending.** Exports are delivered in **Excel (`xlsx`) and Word (`docx`) where implemented**; no export service in this codebase produces PDF and no PDF package is installed.

**The previously listed "Excel + PDF (Arabic) export / mPDF" roadmap item is CANCELLED as of 2026-07-30** and is no longer the next system phase. **No PDF or mPDF work is planned.** No next system phase is currently set — see `OMS_Master_Reference.md` §6 for the remaining open candidates, none of them started or scheduled.

---

## Previously Recommended Next Step (2026-07-30, OMS Task 9B.7 read-only Audit Log UI — implemented, focused tests green, real-local verification DONE, NOT committed)

**Implemented and verified; awaiting review before commit.** `النظام` → `سجل التدقيق` (`/admin/audit-events`) is now a read-only viewer over `audit_events`, restricted to the real `Super Admin` role. Two pages only — `ListAuditEvents` and `ViewAuditEvent`. No create/edit route exists (a direct URL is **404**), every mutation ability is hard-`false`, there are no relation managers, no bulk actions, no export and no import.

**No schema change was required and none was made.** The mandatory read-only inspection confirmed all five indexes the approved filters need already exist from the 9B.1 migration, so the §1 STOP condition did not trigger. Two low-cardinality filters (`actor_type`, and `event_action` used without a category) deliberately run without a dedicated index — see the DECISIONS_LOG entry for why adding one would be cost with no benefit on an append-only table.

**Next: review the implementation and the diff together, then approve the commit.** After that, **OMS Task 9B.8 — final Audit acceptance and the full test suite** is the remaining phase. It must not be started without a fresh explicit request. Graphify was deliberately **not** run before review, as instructed; the post-commit hook produces its own refresh commit.

Scope check for the reviewer — the brief's exclusions were all honoured: no Create/Edit page, no delete/bulk-delete/restore/force-delete/replicate/import/export/inline editing, no mutating relation manager, no `audit.view`/`audit.manage` permission, no `PermissionRegistry` change, no change to `UserPolicy`/`Gate::before`/`canAccessPanel`/any role rule, no Graphify run, no push, no full-suite run, no concurrent PHPUnit, no 9B.8, and **no audit event inserted into the real local database to test the UI**.

### Applied correction (2026-07-30) — unknown categorical values can no longer break the page
The reviewer's one required correction is done. The two enum-cast displayed fields were `actor_type` and `status`; both are plain varchars in the schema, so a row written by a later build could hold a value this build's enums do not declare, and touching the attribute would throw. The fix is UI-only: new `App\Support\Audit\AuditRawValue` reads a categorical column through `getRawOriginal()` (falling back to the raw attribute array), and `AuditLabels::actorType()`/`status()` now take that string and fall back to it verbatim, exactly like the three open string columns already did. **`AuditEvent`'s casts, `AuditRedactor` and the immutability hooks are untouched**, and no stored row was modified. Focused suites after the fix: **93 passed / 614 assertions** (`tests/Feature/Audit/Ui` + `tests/Unit/Support/Audit`), including 19 new regression tests that insert unknown values straight through the query builder.

### Correction to what 9B.6 predicted
The previous entry (below) said 9B.7 would come "plus the `audit.view` permission it will need". **That is superseded**: the 9B.7 brief explicitly forbade creating any `audit.*` permission, and access is instead the real `Super Admin` **role alone**, checked in plain PHP that never resolves through `Gate`. That is strictly safer — with no permission in existence, there is nothing a future role edit or permission sync could grant by accident. Proven with a fixture holding all 186 registered permissions and no Super Admin role: navigation hidden, list and view both 403.

### The item 9B.6 left open for this phase — answered
9B.6 asked that the UI **surface** rather than flatten (a) the shape difference between an original restore row and one replayed after a database replacement, and (b) failure information that is deliberately only ever a code/category pair. Both `replayed_after_database_replacement` and `failure_code`/`failure_category` now carry explicit Arabic labels in `AuditLabels`, rendered as their own rows in the detail view's القيم الجديدة / القيم القديمة tables, with `AuditLabelCoverageTest` failing if any of the three ever loses its label.

### Five things a reviewer should consciously accept
- **Access is a role check with no permission second factor, unlike `BackupAuthorization`.** That asymmetry is deliberate and is the safer direction here — see the DECISIONS_LOG entry. The moment anyone wants to delegate audit reading to a non-Super-Admin, one method (`AuditViewAuthorization::check()`) is the single seam to change.
- **Mutation abilities are hard-`false` in plain PHP and there is no `AuditEventPolicy`.** A Policy would be bypassed by `Gate::before` for 100% of the actors who can reach this page, making its denials decorative. Adding one later would be actively misleading.
- **The payload presenter returns unescaped plain text on purpose.** Escaping happens exactly once, in Filament's `KeyValueEntry` (`e()` on key and value). Escaping in both places would display an audited `<script>` as `&lt;script&gt;` — wrong in a forensic tool. The rule that keeps this safe is that nothing in the resource namespace may ever call `->html()`/`->markdown()`; the rendered-HTML test asserts the outcome directly.
- **Full-text search over `subject_label`/`subject_key`/`actor_name`/`actor_email` compiles to `LIKE %term%` and cannot use an index.** §4 asked for it explicitly; it only runs when an administrator types. If search ever becomes the bottleneck, a full-text/trigram index is the separate later decision — not a guess made now.
- **The UI reads every categorical column as a raw string, never through a cast.** `status` and `actor_type` are cast to `AuditStatus`/`AuditActorType` on the model but stored as plain varchars, so an out-of-vocabulary historical value would have thrown a `ValueError` on attribute access and broken the page. The list and detail view now read all five categorical columns — `event_category`, `event_action`, `actor_type`, `subject_type`, `status` — through `App\Support\Audit\AuditRawValue` and label them via `AuditLabels`, which falls back to the stored value verbatim. **The domain enum casts were not removed or weakened**; the write side keeps its strict typing, and a regression suite proves both facts at once (the model still throws on direct access; the UI still renders). An unknown `status` gets a neutral grey badge rather than a green "نجاح", because claiming success for a value this build cannot interpret would be a false statement about the record.

### Two deliberate non-features
- **No export of any kind.** §2 forbids it, and an audit trail whose contents can be exported from the same screen that reads it is a different, larger decision (retention, redaction on export, and who may hold the file) that belongs to a phase of its own.
- **No dashboard counters, no polling, no caching.** §9 forbids caching that could leak rows between users; counters and polling would each add per-request queries over a table designed only to grow.

---

## Previously Recommended Next Step (2026-07-30, OMS Task 9B.6 backup & restore audit — implemented, focused tests green, real-local verification DONE, NOT committed)

**Implemented and verified; awaiting review before commit.** `event_category = backup_restore` now covers the real backup lifecycle (`backup_requested`, `backup_completed`, `backup_failed`, `backup_downloaded`, `backup_download_denied`, `backup_delete_requested`, `backup_deleted`) and the real restore lifecycle (`restore_requested`, `restore_started`, `restore_reconciled`, `restore_completed`, `restore_failed`, `restore_partial`, `restore_interrupted`), under two subject aliases — `backup` and `restore` — with `correlation_id` always the operation's own UUID.

**The read-only audit's STOP condition did not trigger.** The existing signed restore progress journal (`restores/{uuid}/progress.json` — private 0700/0600, HMAC-signed, bounded by `RestoreProgressSnapshot`, already carrying Task 7C.5's `reconciliation_snapshot` with the requester identity and `confirmed_at` written *before* the import) genuinely preserves restore audit facts across a database replacement. So there is **no new migration, no schema change, no second log and no custom encryption**: `RestoreReconciler` gained a new eighth, deliberately non-fatal step that replays the authoritative lifecycle into the restored `audit_events` table from that journal, and `RestoreTerminalResultWriter` re-attempts the same idempotent replay before its terminal event (covering the case where the import succeeded but reconciliation failed before the replay step).

Verification already completed in this session: new suites **30 passed / 320 assertions** (`BackupAuditTest` 20, `RestoreAuditTest` 10); `tests/Feature/Backup` **214 passed, 1 pre-existing skip**; `tests/Feature/Restore` **287 passed**; `tests/Feature/Audit` **305 passed / 2081 assertions**; `Commands`+`Console` **60 passed**; `tests/Unit` **450 passed, 2 pre-existing skips** — 0 failures, 0 errors. Forced audit-failure drills (the `audit_events` table dropped) proved no queued backup is created, a fully published/verified archive is **never** falsified to `failed`, a private archive is not served and stays in place, a manual delete refuses and the archive stays in place, a restore request creates no row, a restore claim rolls back relaunchable with `status = queued` and its nonce intact, and a 403 download denial still returns 403. A simulated database replacement (every `audit_events` row removed by raw delete, signed journal untouched) reconstructed `restore_requested`/`restore_started`/`restore_reconciled`/`restore_completed` exactly once each with the original requester snapshot and correlation id preserved. Real local data read-only and **unchanged** (`audit_events` 0 / `backup_operations` 13, 12 live / 11 backup files with unchanged byte sizes / 9 journal files / users 5, roles 7, permissions 186 / transactions 8, lines 22, `debit_base` = `credit_base` = 235,000.00 / accounts 5, per-currency balances 0.00); `oms:check-financial-integrity` → **Result: OK, exit 0**; `/admin/login` → 200, `/admin/backup-management` and `/backups/{uuid}/download` → 302 → `/admin/login`, `/restores/{uuid}/progress` → 403 unsigned, `audit_events` still 0 afterwards. **No real backup was created, downloaded, deleted, decrypted or restored, and no real restore was performed.**

**Next: review the implementation and the diff together, then approve the commit.** After that, **OMS Task 9B.7 — the read-only Audit Log UI** (`النظام` → `سجل التدقيق`, Super-Admin-only, plus the `audit.view` permission it will need) is the remaining phase before 9B.8 final acceptance. It must not be started without a fresh explicit request. Graphify was deliberately **not** run before review, as instructed; the post-commit hook produces its own refresh commit.

Scope check for the reviewer — the brief's exclusions were all honoured: no invented UI actions or lifecycle states, no second log, no custom encryption or restore mechanism, no new route, no broadened permission, no Audit UI, no `audit.view` permission, no push, no full-suite run, no concurrent PHPUnit, and no real backup/restore performed for testing.

### Five items a reviewer should consciously accept
- **`backup_completed`, `backup_failed` and `backup_deleted` are BestEffort, not Required — deliberately.** Each is written after the filesystem has already changed irreversibly. Making completion Required would let an audit outage mark a fully generated, encrypted, verified and published archive as `failed` (the creation pipeline's own catch block does exactly that with a thrown exception) while leaving that good archive on disk. The accountable half of each irreversible operation is the Required event written *before* it: `backup_requested` before the row exists, `backup_delete_requested` before the unlink. This is the §6 rule, not a shortcut. **The tests found a real defect here** — the ledger's existence probe was outside the best-effort guard, so a dropped audit table threw a `QueryException` into that catch block; it now sits inside.
- **Idempotency is an application-level pre-insert check, not a database constraint.** `BackupRestoreAuditLedger` refuses an insert when `(correlation_id, event_action)` already exists in this category. No partial unique index was added, because `audit_events` is shared by every category and such an index would also forbid legitimately repeatable events elsewhere. It is safe because every guarded state is written from inside a path already serialized by the subsystem's own mutual exclusion (`BackupSubsystemLock`, the per-backup `Cache` lock, the atomic launch claim, or the single detached restore process).
- **`enqueue()` now opens a transaction, and a Required audit failure there prevents a backup from being queued at all.** That is the intended fail-closed behavior, and it applies to the scheduled entries too: a scheduler run whose audit insert fails queues nothing. The deduplication path stays outside that transaction, so a scheduled duplicate that loses the unique race rolls back completely and the winner keeps exactly one event.
- **`restore_started` is Required inside the atomic claim transaction**, so an audit failure rolls the claim back rather than spawning an unaudited restore process. The row stays `Queued` with `started_at = null` and its nonce intact, which means the launch is retryable — but a persistent audit-table outage would leave a queued restore that cannot be started until the audit table is writable again.
- **Everything from `restore_reconciled` onwards cannot throw, by construction.** The replay step is deliberately not wrapped in `RestoreReconciler`'s `step()` helper: an audit-write problem must never downgrade a genuinely successful restore to `RestorePartial`, and must never suppress the terminal record. The signed journal remains the authoritative terminal record in every case.

### Three paths that were reported as non-existent rather than implemented
- **No restore-file upload or file-selection event.** A restore always reads an existing, completed+verified `BackupOperation` archive on the approved disk; nothing is uploaded and no path is ever accepted from a request.
- **No cancellation event.** There is no cancel path. `RestoreStaleAcknowledgmentService` is explicitly not resume/retry/rollback/repair — it only terminalizes a crashed restore after explicit human review, which is recorded as `restore_interrupted` (never `restore_failed`, which would claim the engine decided the outcome).
- **No separate confirmation event.** The two-step wizard validates both steps and the typed `RESTORE {uuid8}` phrase before `RestoreRequestService` is reached, so the confirming actor and `confirmed_at` ride on `restore_requested`. The phrase is `dehydrated(false)` and never reaches `$data`, the row, or any payload.

### Answer to the item 9B.5 left open for this phase
9B.5 asked how non-transactional, long-running backup/restore work should treat the "Required audit inside the caller's transaction" rule that 9B.3/9B.4/9B.5 all rely on. The answer this phase settled on: **Required applies only where a database-only mutation genuinely allows it** (row creation, the atomic launch claim, and reading a private archive before any byte is served), and everywhere past an irreversible filesystem or database-replacement boundary the honest mode is BestEffort with the durable signed journal as the authoritative record. The audit layer never converts a real outcome into a different one.

### Item left open for OMS Task 9B.7
This phase created **no** `audit.view` permission and **no** UI, so nothing yet reads these events back. 9B.7 will need to present a category whose payloads deliberately vary in shape between an original and a replayed row (`replayed_after_database_replacement: true`) and whose failure information is only ever a `failure_code`/`failure_category` pair — never a message. It should surface that distinction rather than flatten it.

---

## Previously Recommended Next Step (2026-07-29, OMS Task 9B.5 attachment & report-export audit — implemented, focused tests green, real-local verification DONE, NOT committed)

**Implemented and verified; awaiting review before commit.** `event_category = attachment` now covers upload, replacement, deletion and authorized private access; `event_category = report_export` covers all ten export paths across the six report pages; and a genuine 403 on a private attachment is recorded as `security.attachment_access_denied`. Attachment writes are `Required` inside each financial workflow's existing `DB::transaction()`; private-file access is `Required` **before** the response, so a failed audit means the file is not served; a denial is `BestEffort`, so a 403 stays a 403 under an audit outage.

Verification already completed in this session: new suites **45 passed / 287 assertions** (`AttachmentWriteAuditTest` 10, `AttachmentAccessAuditTest` 13, `ReportExportAuditTest` 22); `Audit`+`Attachments`+`Reports` regression **529 passed / 2418 assertions, 0 failed**; the five financial workflow suites **70 passed / 285 assertions**; forced audit failure (the `audit_events` table dropped) proved a create-with-attachment rolls back both the `Attachment` and the `GeneralExpense` row, a private attachment is not served, and an export throws instead of delivering, while a 403 still stands; real local data read-only and **unchanged** (`audit_events` 0 / attachments 9, 7 trashed / transactions 8 / lines 22 / accounts 5 / balance sum 0.00 / 8 files on the private disk); `oms:check-financial-integrity` → **Result: OK, exit 0**; `/admin/login` → 200 and `/attachments/9/{view,download}`, `/admin/account-statement`, `/admin/trial-balance`, `/admin/donor-financial-report` → 302 → `/admin/login` while unauthenticated, with `audit_events` still 0 afterwards.

**Next: review the implementation and the diff together, then approve the commit.** After that, **OMS Task 9B.6 — backup & restore audit integration** is the remaining audit phase before the Audit Log UI. Graphify was deliberately **not** run before review, as instructed; the post-commit hook produces its own refresh commit.

Scope check for the reviewer — the brief's exclusions were all honoured: no ordinary report-view auditing, no pagination/filter auditing, no 404 auditing, no per-exported-row auditing, no backup/restore, no Audit UI, no `audit.view` permission, no broadened authorization, no push, no full-suite run, no concurrent PHPUnit, and no real local upload/delete/download/export performed for testing.

### Four items a reviewer should consciously accept
- **Attachment atomicity is a DATABASE guarantee only, and the docs say so.** The Attachment row and its audit event commit or roll back together. The filesystem is not covered: `AttachmentUploadService` moves the uploaded file to its final path before the surrounding transaction commits, so any rollback (audit failure included) leaves that one **new** file orphaned on the private disk. This was already true of every pre-9B.5 rollback in these workflows and was deliberately left unchanged rather than rewritten inside an audit task. Nothing can lose a **previous** file — no path in this application deletes a prior attachment file from disk at all.
- **`viewed` and `downloaded` are recorded as two actions, which is a claim about this application's route.** It holds only because `/attachments/{attachment}/{mode}` constrains `{mode}` to `view|download` and the controller maps it to `inline` versus `attachment`. If that segment is ever collapsed to a single path, these must collapse to one accurate `accessed` action rather than start inferring intent from browser headers. The dependency is documented at the top of `AttachmentAccessAuditRecorder`.
- **Exports record `export_requested`, never `export_completed`.** Every export serializes inside the `streamDownload` callback — after the response is returned and the headers are committed — and targets `php://output`, so no temporary file exists to prove success. A completion claim is not supportable by the current synchronous code. If exports later move behind a queue or a materialized file, a genuine completion event should be **added**, not substituted.
- **The `ProjectsGeneralFinancialPage` export deliberately records no table filters.** That export re-queries `ProjectFinancialSnapshot` unfiltered and always covers every project snapshot, so recording the screen's Filament filters would describe the screen rather than the file that was requested. Its payload carries `scope: all_project_snapshots` instead.

### Item left open for OMS Task 9B.6
The three `event_category = attachment` write actions are `Required` and assert an open caller transaction, matching 9B.3/9B.4. Backup and restore include genuinely non-transactional, long-running and partly out-of-process work (`NativeAttachmentMoveRunner`, the restore swap markers, the watchdog), so 9B.6 will have to decide per event whether a caller transaction can exist at all — and, where it cannot, whether `BestEffort` plus a durable marker is the honest contract rather than a `Required` event that has nothing to roll back.

---

## Previously Recommended Next Step (2026-07-29, OMS Task 9B.4 users/roles/permissions/authentication audit — implemented, focused tests green, real-DB verification DONE, NOT committed)

**Implemented and verified; awaiting review before commit.** `event_category = security` now covers every real identity/privilege write path (`UserManagementService`, `RoleManagementService`, `PermissionSyncService`) in `AuditFailureMode::Required` inside each service's existing `DB::transaction()`, plus the three authentication events (`login_success`/`login_failed`/`logout`) in `BestEffort` via one event subscriber. One logical action = exactly one event throughout: a user edit that renames, deactivates, resets the password and replaces both roles is a single event, a role permission replacement is a single event, and a full permission-sync run is a single summary event.

Verification already completed in this session: new security-audit suites **81 passed / 388 assertions**; `Users`+`Roles`+`Permissions` regression **493 tests, 490 passed, 3 pre-existing skips, 0 failed**; `Audit`+`Crud` regression **318 passed / 1741 assertions**; forced audit failure (the `audit_events` table dropped) proved full rollback for all eight mutation paths including Spatie pivots; real local DB read-only and **unchanged** (users 5 / roles 7 / permissions 186 / `model_has_roles` 4 / `model_has_permissions` 0 / `role_has_permissions` 599 / `audit_events` 0); `oms:check-financial-integrity` → **Result: OK, exit 0**; `/admin/login` → 200 and `/admin/users`, `/admin/roles`, `/admin/permissions`, `/admin/users/create` → 302 → `/admin/login` while unauthenticated, with `audit_events` still 0 afterwards.

**Next: review the implementation and the diff together, then approve the commit.** The post-commit hook will produce a separate Graphify refresh commit; Graphify was deliberately **not** run before review, as instructed.

Scope check for the reviewer — the brief's exclusions were all honoured: no attachments, no exports, no backup/restore, no Audit UI, no `audit.view` permission, no broadened authorization, no push, no full-suite run, no concurrent PHPUnit, and no test users/roles/permissions created in the real local database.

### Three items a reviewer should consciously accept
- **Seeding and post-restore reconciliation now write one audit event each.** `PermissionSyncService::sync()` is called by `DatabaseSeeder::run()` and by `RestoreReconciler` (as `oms:sync-permissions`, always after its `migrate --force` step), so both now produce one `permission_sync` event with `actor_type = command`. This deliberately narrows 9B.2's "seeders leave no audit trail" rule for this one service, because a seed and a restore reconciliation really do rewrite the five system roles' permission sets. No suppression switch was added. Verified green across `tests/Feature/Restore` + `tests/Feature/Console` + `tests/Feature/Commands` (**347 passed**).
- **No direct-user-permission write path exists in this application**, so none was audited end-to-end. `UserForm` exposes roles only and nothing in `app/` calls `givePermissionTo()`/`revokePermissionTo()`/`syncPermissions()` on a `User`. The snapshot, diff and single-event payload are implemented and tested at the recorder level (`SecurityAuditPayloadPolicyTest`) so it is audited correctly the moment such a path is added — but inventing one would have meant creating a privilege-granting surface the application deliberately does not have.
- **No password-reset flow exists** (`AdminPanelProvider` calls `->login()` but never `->passwordReset()`), so no `PasswordReset` listener was wired. An administrator resetting a user's password goes through `UserManagementService` and is covered by the single `user`/`updated` event's `password_changed` flag.

### Answer to the item 9B.3 left open for this phase
9B.3 asked whether `FinancialAuditRecorder`'s fail-closed `LogicException` outside an open transaction should stay a hard failure once non-HTTP callers appear. 9B.4 introduced exactly such a caller — `PermissionSyncService::sync()`, reachable from `oms:sync-permissions` — and the assertion was **kept as a hard failure**, with `SecurityAuditRecorder` following the same pattern. The command path already opens its own transaction, so the precondition cost nothing, and a `LogicException` at a wiring mistake is strictly better than a silently unaudited privilege change. Revisit only if a caller appears that genuinely cannot open a transaction.

### After that: OMS Task 9B.5 — attachments and exports
Attachment upload/replace/delete and report-export events. Not to be started without a fresh explicit request. Backup/restore auditing and the Audit UI (plus any `audit.view` permission) remain after that.

## Previously Recommended Next Step (2026-07-29, OMS Task 9B.3 financial audit integration — DONE, committed as 7449eb5 / da8c1f4)

**Do two things before commit, in this order.**

**1. Complete the real-database verification that this session could not run.** The `MySQL80` Windows service is **Stopped**, and `Start-Service MySQL80` failed with an access-denied error (elevation required). Nothing was written to the real database and no attempt was made to work around it. From an **elevated** terminal, start the service, then run these read-only checks:

```
php artisan oms:check-financial-integrity          # must print "Result: OK", exit code 0
php artisan tinker --execute="foreach (['audit_events','accounts','accounts_type','currencies','exchange_rate_histories','transactions','transaction_lines','project_cost_receipts','project_cost_budgets','project_cost_budgets_payments','general_expenses','general_exchanges'] as \$t) { echo str_pad(\$t,34).DB::table(\$t)->count().PHP_EOL; } echo 'balance sum: '.DB::table('accounts')->sum('current_balance').PHP_EOL;"
```

`audit_events` must still hold its pre-task row count (this task wrote **only** to in-memory SQLite test databases), every financial row count and the account-balance sum must be unchanged, and `/admin/login` must return 200 with the financial pages redirecting correctly when unauthenticated. **If any of these does not hold, stop and report rather than committing.**

**2. Then review the implementation and the diff together and approve the commit.** The post-commit hook will produce a separate Graphify refresh commit; Graphify was deliberately **not** run before review, as instructed.

Scope check for the reviewer — the brief's stated exclusions were all honoured: no users/roles/security work, no attachments, no backup/restore, no exports, no Audit UI, no push, no full-suite run, no concurrent PHPUnit, and no test financial data in the real local database.

**After that: OMS Task 9B.4 — users / roles / security auditing** (authentication events, user and role/permission changes, `AuditActorContext::guest()`'s login-failure contract). It should not be started without a fresh explicit request.

### One item worth a decision in 9B.4 or later
`FinancialAuditRecorder` and `AuditedCrudService::recordCreatedWithin()` both fail closed with `LogicException` when called outside an open transaction. That is deliberate, but it means a future non-HTTP caller (a queued job, an Artisan command) must remember to open one. If 9B.4 introduces such callers, consider whether the assertion should stay a hard failure or become a documented precondition on a narrower interface.

## Recommended Next Step (2026-07-28, Graphify tracking hygiene — cache + dated snapshots + `.claude` — implemented, verified, NOT yet committed)
**Implemented and fully verified; awaiting review before commit.** Three tracked sensitive-path sources removed in two passes. The durable repository policy is now: **tracked = the canonical root-level Graphify outputs only** (`graph.json`, `manifest.json`, `GRAPH_REPORT.md`, plus `cost.json` and the non-regenerable curated `.graphify_labels.json` and the two `.graphify_*` markers); **ignored = `graphify-out/cache/**`, `graphify-out/20*/`, runtime/upload/backup paths, and `.claude/**` local state**. 2157 cache files and 75 dated-snapshot files were removed from Git tracking with `git rm --cached` and every one remains on disk. A clean deterministic rebuild (`PYTHONHASHSEED=0`, two independent from-scratch runs) produced byte-identical SHA-256 for all three canonical outputs. `oms:check-financial-integrity` — **Result: OK, exit code 0**. No application code, test, migration, real backup, uploaded attachment or Claude local-config file was touched; PHPUnit deliberately not run.

Next: **review this report and the diff together, then approve the commit.** The post-commit hook will then produce a separate Graphify refresh commit if the graph changes, per the established two-commit pattern. **OMS Task 9B.3** should not be started without a fresh explicit request.

## Detail — first pass (2026-07-28, Graphify cache-tracking hygiene)
**`graphify-out/cache/**` is local generated cache and is now untracked: `/graphify-out/cache/` added to `.gitignore`, 2157 files removed from the index with `git rm --cached -r` (all 2157 kept on disk), `.graphifyignore` unchanged. The trigger was three stale absolute paths to real local database-backup `.sql` files in `graphify-out/cache/stat-index.json`; the audit found 1511 keys, 100% absolute machine paths, 695 of them under `storage\` (compiled Blade cache, `livewire-tmp` upload scratch, uploaded financial-attachment filenames). Hand-redaction and a scrubber were both rejected — the file regenerates on every run, so only repository policy is durable. `PYTHONHASHSEED=0 graphify update .` succeeds with the cache ignored (822/822 files, 12464 nodes, exit 0) and puts nothing back into git. `oms:check-financial-integrity` — **Result: OK, exit code 0**. No application code, test, migration or real backup file was touched; PHPUnit deliberately not run.

(For what happens next, see the single "Recommended Next Step" at the top of this file — the first pass and its extension are reviewed and committed together. **OMS Task 9B.3** is the phase after: financial/domain audit events, also where `Account`/`AccountType` would gain `HasUserTracking` per Task 8.3's deferral.)

### Extension applied the same day — both earlier findings resolved
- **Dated snapshots untracked.** `/graphify-out/20*/` added to `.gitignore`; all 75 files across 15 directories removed from the index with `git rm --cached -r`, all 75 retained on disk. They are created by `graphify.export.backup_if_protected()`, which is **write-only** (every call site discards the return value; nothing ever reads a dated folder back), and nothing in the repo depends on them. **Git history is the better retention mechanism — 75 commits touch `graphify-out/graph.json`, each tied to the source revision that produced it.** This supersedes the earlier decision that kept them tracked.
- **`.claude/**` excluded from indexing** via `.graphifyignore`, so local Claude state never reaches the tracked `manifest.json`. The repository-root `CLAUDE.md` is deliberately not excluded and remains indexed.
- **Clean deterministic rebuild** confirmed byte-identical SHA-256 across two independent from-scratch runs with `PYTHONHASHSEED=0` (820 files, 12480 nodes, 512 communities).

### Residual finding — your decision, not applied
- **`graphify-out/.graphify_python` is tracked and contains one absolute machine-local interpreter path.** It is only the post-commit hook's second of four interpreter probes, and the path cannot resolve on any other machine, so the probe simply falls through there. Untracking it is a zero-risk one-liner (`/graphify-out/.graphify_python` in `.gitignore` + `git rm --cached`), but it fell outside the two sources this task scoped, so it was reported rather than applied. Decide whether to untrack it, or accept a developer-local interpreter path in the repository.

## Previously Recommended Next Step (2026-07-28, OMS Task 9B.2 — Audit Log general CRUD integration — implemented, verified, NOT yet committed)
**Implemented and fully verified; awaiting review before commit.** The 9B.1 audit foundation is now wired to the real write paths of eleven general/master-data models only — `Project`, `ProjectCost`, `Partner`, `PartnerType`, `ProjectSuper`, `ProjectStatus`, `BankType`, `FiscalYear`, `TransactionType`, `TransactionSuperType`, `Setting` — emitting `crud.created`/`updated`/`deleted`/`restored` events in strict `AuditFailureMode::Required`. The phase's central finding is that **no Filament write path in this application runs inside a database transaction** (the panel never enables `->databaseTransactions()` and every Action defaults to false), so an after-save observer could not have been atomic; auditing is therefore owned by `App\Services\Audit\Crud\AuditedCrudService`, which wraps the mutation and the audit insert in one `DB::transaction()`. 34 Filament files were re-pointed at `AuditsRecordCreation`/`AuditsRecordUpdate`/`AuditedActions`, which are stock Filament behavior with only the process closure swapped — every existing notification, confirmation modal, policy and redirect (create → View, edit → View, delete → List) is unchanged. No financial, security, attachment, backup/restore or export auditing, no audit UI, no audit permissions, no schema change, and `Account`/`AccountType` still not wired to `HasUserTracking`. 33 new focused tests (86 in `tests/Feature/Audit`, 486 assertions, all passing) plus 117/117 CRUD/Filament/Policy regression and 341 Permissions tests (338 passed, 3 pre-existing by-design skips). Real local DB: `audit_events` still **0 rows**, all business row counts unchanged, `oms:check-financial-integrity` — **Result: OK, exit code 0**, `/admin/login` → 200.

Next: **review this report and the diff together, then approve the commit.** After that, run `graphify update .` and commit the graph separately, per the project's established pattern. **OMS Task 9B.3 (financial/domain events — also the point where `Account`/`AccountType` would gain `HasUserTracking`, per Task 8.3's own deferral)** should not be started without a fresh explicit request.

### Open items carried out of 9B.2 (not defects, decisions to revisit)
- **No restore write path exists in the UI for any target model.** OMS removed every Restore/ForceDelete action by design, so `AuditedCrudService::restore()` is implemented and tested but has no caller today. If a restore UI is ever added, it must go through the service — nothing else will audit it.
- **`SettingValuePolicy` can over-redact.** A 40+ character whitespace-free setting value is masked even under an innocent key. Fail-closed by design; loosen only with a concrete real-world example that it wrongly hides.
- **Bulk delete is one transaction per record, not one for the whole selection.** That matches Filament's own per-record failure reporting (a failing audit rolls back only its own record's deletion), but it means a partially-completed bulk delete is possible — exactly as it already was before this phase.
- **`ProjectCost` audit events cost up to three bounded primary-key lookups** (`subject_label`, plus the old and new `project_label` on a reassignment). Memoized per logical action; an update that does not touch `project_id` performs none of the relation lookups.
- **`AuditSubjectDefinition::$fieldAliases` is a narrowly-scoped tool, not a general escape hatch.** `settings.key` → `setting_name` is its only use. A future subject should only get an alias when a legitimate business column's literal name collides with `AuditRedactor`'s denylist — never to smuggle a genuinely sensitive column past redaction.

### Corrections applied after the first 9B.2 review (2026-07-28)
1. **`settings.key` is now recorded as `setting_name`.** Previously both sides of a key rename were `[REDACTED]`, losing the previous key. `AuditSubjectDefinition::$fieldAliases` renames the column to a safe semantic audit field before the payload reaches `AuditLogger`; the raw `key` name is never supplied. `AuditRedactor` was **not** weakened and no general `key` safe-exception was added. Values still pass `SettingValuePolicy` and the central redactor, so secret-shaped settings still record `[REDACTED]` values.
2. **`ProjectCost.project_id` changes now label both sides.** Relation-label resolvers take the **foreign key value** instead of the owning model, so the pre-change side is resolved from the original id (`Project::withTrashed()`, three columns only) rather than from a relationship that already points at the new project. `changed_fields` still lists `project_id` only.

## Previously Recommended Next Step (2026-07-28, Graphify hygiene — project-local `.graphifyignore` — implemented, verified, NOT yet committed)
**Implemented and fully verified; awaiting review before commit.** Added `.graphifyignore` (project root) excluding `storage/**`, `bootstrap/cache/**`, `public/storage/**`, `vendor/**`, `node_modules/**`, `.git/**`, `graphify-out/**`, `.env`, `.env.*`, `*.sql` — stopping Graphify from indexing uploaded financial-attachment filenames (`storage/app/public/**`) and compiled Blade cache (`storage/framework/views/**`), while explicitly preserving `resources/views/vendor/filament-panels/**` (real tracked UI source). Required deleting-then-regenerating the three top-level output files (backed up first) since `graphify update`/`--force` alone does not prune stale nodes for newly-excluded files against an existing graph — see `docs/DECISIONS_LOG.md` for the exact mechanism and the correct procedure for any future exclusion change. Verified deterministic (byte-identical SHA-256 across two independent from-scratch rebuilds) and confirmed sensitive-path-free while retaining every Task 9B.1 node and all legitimate source directories. No application code changed; `oms:check-financial-integrity` — Result: OK, exit code 0.

Next: **review this report and the diff together, then approve the commit** (as a Graphify-only commit, separate from any application-code commit, per the project's established pattern). After that, **OMS Task 9B.2 (CRUD/model integration)** should not be started without a fresh explicit request.

## Previously Recommended Next Step (2026-07-28, OMS Task 9B.1 — Audit Log foundation — implemented, verified, NOT yet committed)
**Implemented and fully verified; awaiting review before commit.** First phase of the Task 9A-approved Audit Log architecture: the `audit_events` schema, the application-level-immutable `App\Models\AuditEvent`, central redaction (`AuditRedactor`), deterministic payload bounding (`AuditPayloadBounder`), typed actor/request context (`AuditActorContext`), and the single write-gateway service (`AuditLogger`, with `AuditFailureMode::Required`/`BestEffort`). **Nothing in the application calls any of this yet** — no existing model, controller, command, financial workflow, user/role/permission action, attachment view/download, backup/restore event, report export, or Filament page was touched or wired in this phase, exactly as instructed. `Account`/`AccountType` were **not** wired to `HasUserTracking` (still deferred, unchanged from Task 8.3). 53 new focused tests, 53 passed, 197 assertions — two test-file bugs (not production bugs) found and fixed mid-task. Real local DB: pre-task backup taken outside the repo, migration applied (`audit_events` confirmed empty, 0 rows), `accounts`/`users`/`transactions` row counts unchanged, `oms:check-financial-integrity` — **Result: OK, exit code 0**, `/admin/login` → HTTP 200. See `docs/AI_PROJECT_MEMORY.md`/`docs/TASKS_LOG.md`/`docs/DECISIONS_LOG.md` (2026-07-28, "OMS Task 9B.1" entries) for full technical detail.

Next: **review this report and the diff together, then approve the commit.** After that, **OMS Task 9B.2 (CRUD/model integration — wiring the shared diffing layer into the existing financial/reference models)** should not be started without a fresh explicit request, per the approved 8-phase rollout (9B.1 → 9B.2 CRUD → 9B.3 financial/domain events (also the point where `Account`/`AccountType` would gain `HasUserTracking`, per Task 8.3's own deferral) → 9B.4 users/roles/permissions/auth → 9B.5 attachments → 9B.6 backup/restore/system events → 9B.7 read-only Filament UI (`النظام` → `سجل التدقيق`, Super-Admin-only) → 9B.8 final acceptance). No audit permission, no UI, and no wiring into any existing operation exists yet — `AuditLogger`/`AuditRedactor`/`AuditPayloadBounder`/`AuditActorContext` are the exact permanent seam every later phase must consume, not re-derive.

## Previously Recommended Next Step (2026-07-28, OMS Task 8.3 — Accounts User-Tracking Schema Reconciliation — implemented, verified, NOT yet committed)
**Implemented and fully verified; awaiting review before commit.** Resolved the last remaining named drift Task 8.2 discovered and deferred: `accounts.created_by`/`updated_by` exist live with no reproducing migration. Unlike `accounts_type`'s copies (3 real historical rows), these have zero non-null values anywhere — not currently (0 of 5 accounts), not in the earliest captured historical backup (0 of 16). Classified ACTIVE_REQUIRED anyway, on architectural-consistency grounds: the column shape exactly matches the active `HasUserTracking` convention used by 20+ models and `accounts_type`'s already-reconciled twin, and this task's own "final schema-drift task before Audit Log" framing anticipates keeping them rather than removing-then-re-adding once Task 9 needs them. Reconciled schema only via a guarded migration that independently repairs a missing column or a missing FK (never duplicating either) — a complete no-op on the real local DB, since both already existed correctly. `down()` was written irreversible from the start, directly applying the lesson from the same-day Task 8.2 rollback-safety correction. `Account` was deliberately **not** wired to `HasUserTracking` — activating real tracking behavior stays deferred to Task 9. Two isolated scratch-DB scenarios (fresh install; simulated live drift with a harmless tracked row) plus a direct proof that deleting a referenced user nulls both tracking columns all passed. Fresh-vs-live parity for `accounts` is now exact. Zero data modified; focused regression 178/178 passed; real local `oms:check-financial-integrity` — **Result: OK, exit code 0**. See `docs/AI_PROJECT_MEMORY.md`/`docs/TASKS_LOG.md`/`docs/DECISIONS_LOG.md` (2026-07-28, "OMS Task 8.3" entries) for full technical detail.

Next: **review this report and the diff together, then approve the commit.** No further named Account/AccountType schema drift remains. The next natural step is the Task 9 Audit Log itself (which would be the first place `HasUserTracking`-style behavior gets activated on `Account`/`AccountType`, and where `accounts_type`'s 3 legacy `created_by` values and `accounts`'s always-NULL ones both become meaningful) — do not start it without a fresh explicit request. Task 8.3 itself is otherwise functionally complete at the local-development level; no Audit Log, Monitoring, Performance, or Production Security work was started, per instructions.

## Previously Recommended Next Step (2026-07-28, OMS Task 8.2 — Final Schema Drift Reconciliation — implemented, verified, COMMITTED)
**Implemented, fully verified, and committed** (`5aea136` "reconcile final account schema drift", `5063b86` "refresh graphify after final account schema reconciliation"). Resolved the two schema differences Task 8.1 discovered and deferred as out of scope: `accounts.account_code` (`NOT NULL` live vs. nullable fresh) and `accounts_type` (live `nature`/`created_by`/`updated_by` with no reproducing migration). Evidence-based audit found: the original `create_accounts_table` migration (unchanged since the initial commit) and the current `AccountForm` both already declare `account_code` nullable, and every one of ~20 code sites that read it defensively guard for a missing value — the live `NOT NULL` was the drift, relaxed to match. `nature` has zero code usage anywhere and an existing, dated (2026-07-06, predating this audit) documentation entry already states the system has no account-nature concept — classified DEAD_UNUSED and dropped. `created_by`/`updated_by` match the active, systemwide `HasUserTracking` audit-column convention used by 20 other models and carry real historical data — classified ACTIVE_REQUIRED and reproduced as schema only (no model wiring — that would start the Audit Log task, out of scope). A real bug was caught and fixed mid-task: a MySQL-only raw-SQL idempotency guard broke 173/178 focused tests under the SQLite test connection, fixed with Laravel's driver-agnostic `Schema::getColumns()`. **Post-acceptance correction (before commit)**: `accounts_type`'s migration `down()` originally dropped `created_by`/`updated_by` unconditionally, which would have destroyed real pre-existing live data on rollback — corrected to a full no-op, verified via fresh-install and simulated-drift scratch-DB scenarios. Fresh-vs-local parity confirmed on an isolated scratch database (real `oms` DB never targeted by `migrate:fresh`). Zero financial/historical data modified. Focused regression 178/178 + 158/158 passed; real local `oms:check-financial-integrity` — **Result: OK, exit code 0**. One previously-unflagged instance of the same drift class was caught honestly during this task's fresh-vs-local comparison, reported rather than silently fixed, and left untouched (out of scope then) — resolved above, in the 2026-07-28 Task 8.3 entry. See `docs/AI_PROJECT_MEMORY.md`/`docs/TASKS_LOG.md`/`docs/DECISIONS_LOG.md` (2026-07-28, "OMS Task 8.2" entries) for full technical detail.

## Previously Recommended Next Step (2026-07-28, OMS Task 8.1 — Schema Drift + Dead Schema Reconciliation — implemented, verified, COMMITTED)
**Implemented, fully verified, and committed** (`08df090` "remove dead bank account schema drift", `6ed42f9` "refresh graphify after schema drift cleanup"). Resolved the exact schema/migration-history drift Task 8 (2026-07-27) deferred: `bank_accounts`, `accounts.parent_id`, `transactions.bank_account_id` existed live with real FK constraints but no migration file anywhere on disk. A read-only, evidence-based audit (code, git history, live DB, historical backups — no assumption either way) found unanimous evidence for **DEAD_UNUSED**: zero application code usage anywhere (no model, relationship, `$fillable` entry, Filament resource/form field, factory, or seeder), zero non-null/live data both currently and in the earliest captured historical backup, and `create_bank_accounts_table` was never committed to this git repository at all. A pre-task `mysqldump` backup was created outside the project directory; the exact planned SQL was compiled and shown before execution; a new guarded migration removed all three objects from the real local `oms` database; the orphaned `migrations` table row for the never-committed file was cleaned up; the two now-dead relationship checks and an unnecessary SQLite test shim were removed from the integrity-checker codebase; one stale documentation claim was corrected. Fresh-install parity was proven via an isolated scratch MySQL database (real `oms` DB never targeted by `migrate:fresh`). Focused regression (511/513 passed) + report-authorization suite (58/58) + real local `oms:check-financial-integrity` (**Result: OK, exit code 0**) + real read-only smoke checks all passed. Two unrelated, pre-existing schema drifts were discovered as a side effect of this task's required fresh-vs-local schema comparison and deliberately left untouched (out of this task's named scope) — resolved above, in the 2026-07-28 Task 8.2 entry. See `docs/AI_PROJECT_MEMORY.md`/`docs/TASKS_LOG.md`/`docs/DECISIONS_LOG.md` (2026-07-28, "OMS Task 8.1" entries) for full technical detail.

## Previously Recommended Next Step (2026-07-27, OMS Task 8 — Financial & Database Integrity + Trial Balance Correctness Review — implemented, verified, NOT yet committed)
**Implemented and fully verified; awaiting review before commit.** A read-only audit first established the ground truth this whole task depends on: `transaction_lines.debit_base`/`credit_base` are each line's own-currency amount, never a company-base-currency conversion (confirmed against every real write path and three pre-existing docblocks) — so the new integrity checker validates multi-currency (4-line) transactions via `FinancialTransactionBalanceGuard`'s real FX equation, never a raw cross-currency `SUM(debit_base)=SUM(credit_base)`. Real local DB preflight was clean (0 orphans/duplicates/mismatches; `accounts.current_balance` reconciled with 0 discrepancies). Trial Balance's existing "Phase 1" design (period-only totals, no opening/closing balance) was confirmed as an already-documented 2026-07-05 decision, not a defect — its previously-zero calculation test coverage is now filled with 14 focused tests, including the canonical 1000→2999.94 multi-currency exchange case. Implemented `app/Services/Integrity/*` (four focused, read-only collaborators) and `php artisan oms:check-financial-integrity` (`--json`, exit 0/1/2, bounded output, never mutates anything). Per explicit user approval: added two proven-missing supporting indexes (`transaction_lines(account_id, currency_id)`, `transactions(transaction_time)`) and bounded 3-attempt retry-on-collision hardening for the six duplicated transaction-number generators (deduplicated into one shared trait, format/logic unchanged). A real, unplanned schema/migration-history drift was found (`bank_accounts` + two real FK columns exist live with no migration file) — per explicit user decision, documented only, no schema change (since resolved — see the 2026-07-28 Task 8.1 entry above). The formerly-open "attachment single-active" rule remains unapproved — deferred, no schema change, per the task's own instruction. Final full application suite: **1919 tests, 1913 passed, 0 failed, 0 errors, 6 skipped, 5767 assertions** — exactly the 44 new tests added cleanly on top of the prior 1875/1869 baseline, same pre-existing skips. See `docs/AI_PROJECT_MEMORY.md`/`docs/TASKS_LOG.md`/`docs/DECISIONS_LOG.md` (2026-07-27, "OMS Task 8" entries) for full technical detail.

Next: review this report and the diff together, then approve the commit. The schema-drift follow-up named below has since been completed (2026-07-28, Task 8.1, above); the attachment single-active-attachment rule remains unapproved. Task 8 itself is otherwise functionally complete at the local-development level; no Audit Log, Monitoring, Performance, or Production Security work was started, per instructions.

## Previously Recommended Next Step (2026-07-27, OMS Task 7C.9 — real local Windows/Laragon end-to-end backup/restore acceptance — implemented, verified, NOT yet committed)
**Implemented and fully verified against the REAL local environment; awaiting review before commit.** This is the final acceptance phase for Task 7C: real (not `Storage::fake()`/mocked) backup and restore cycles were run against the actual local MySQL database and private filesystem disks, through the real Filament UI, as a real (temporary, disposable) Super Admin. An independent emergency baseline (mysqldump + attachments copy + SHA-256 manifest) was created outside the project entirely before anything destructive began. **One real, proven, E2E-only defect was found and fixed**: `RestoreMaintenanceMode::enter()` called Laravel's real `down` command with a `--message` option that does not exist in this installed Laravel version — every real restore failed 100% of the time at preflight, completely invisible to all 854 prior mocked tests because the test fake never validated real Artisan option signatures. Fixed by removing the unsupported option (narrowly — no new maintenance view/feature added); the one affected test was updated to match. **One real, documented Windows-only operational limitation was confirmed** (not a defect): the detached restore-process launcher does not reliably keep running independently of the launching web request on this local Laragon environment; the safe, verified recovery is to re-run `php artisan oms:restore {uuid}` in the foreground for the same already-claimed row, which completes the exact same real orchestrator — documented in `docs/RESTORE_RECOVERY.md` §14. All three restore scopes (database-only, files-only, full) were then run successfully end-to-end through the real UI, each independently verified via before/after DB markers and SHA-256 attachment hashes. Both required failure/recovery drills (a safe corrupted-archive pre-irreversible-boundary failure, and a safe stale/crash acknowledgment drill) were completed without ever touching a real backup or killing a real process; post-DB-import/partial coverage was accepted as already proven by 7C.7's existing deterministic tests, per the task's own explicit fallback instruction — no new failure-injection hook was added. Post-restore application smoke check and an existing-guards-only financial integrity check both passed cleanly. Every Task 7C.9 temporary fixture was cleaned up via real application services/UI (test expense + balances reverted, Setting markers removed, synthetic drill rows/files removed, temporary Super Admin deleted); every genuine backup/restore row produced by a real UI action was deliberately kept as acceptance evidence. Final full application suite: **1875 tests, 1869 passed, 0 failed, 0 errors, 6 skipped, 5659 assertions** — the one required run, all 6 skips pre-existing/environment-conditional/individually documented. See `docs/AI_PROJECT_MEMORY.md`/`docs/TASKS_LOG.md`/`docs/DECISIONS_LOG.md` (2026-07-27, "OMS Task 7C.9" entries) for full technical detail.

Next: **review this report and the small code diff (`RestoreMaintenanceMode.php` + its one test) together, then approve the commit.** Once Task 7C.9 is accepted, Task 7C (the entire Restore engine, 7C.1 through 7C.9) is fully closed at the local-development level. **Hostinger VPS production deployment is a separate, later task** that should not be started without a fresh explicit request — it was explicitly out of scope for 7C.9 and depends on this local acceptance being approved first. The independent emergency baseline (outside the project directory) should not be deleted until this acceptance is formally approved.

## Previously Recommended Next Step (2026-07-27, OMS Task 7C.8 + acceptance pass — Filament restore UI: two-step confirmation, DB-independent signed progress polling, stale/tampered banners, explicit crash acknowledgment — implemented, verified, ACCEPTED, COMMITTED)
**Implemented, verified, accepted, and committed.** `BackupManagementPage` now exposes the already-verified restore engine (7C.1–7C.7): a "استعادة" row action (visible only for a real Super Admin with `backups.restore` against an eligible, verified, non-restore backup with no active/tampered restore blocking it) opens a single two-step Filament Wizard action — step 1 collects a compatible scope, a required bounded reason, and an exact case-sensitive typed `RESTORE {uuid8}` confirmation (`dehydrated(false)`, so the phrase never reaches persisted state); step 2 is the final danger-styled confirmation, whose submit alone creates the queued restore row (`RestoreRequestService`, genuinely lock-serialized against a true concurrent race — proven, not assumed) and launches it through the existing, unmodified `RestoreLaunchService`. A new DB-independent `GET /restores/{uuid}/progress` endpoint (signed-URL-only authorization, zero database queries — proven against the REAL Laravel maintenance mechanism, not only a fake) drives a small Alpine widget for live phase/heartbeat display. A stale/crashed-restore banner plus a Super-Admin-only acknowledgment action (`RestoreStaleAcknowledgmentService`) terminalizes a genuinely stale restore — proven to close BOTH the DB and signed-progress gates independently, including the DB-row-missing and DB-already-terminal-but-progress-still-open edge cases — writing `restore_failed_phase = crashed_acknowledged` (the one narrow, additive `RestoreProgressSnapshot::ALLOWED_PHASES` addition this task required). A tampered/unreadable progress state shows a fixed manual-review message and hides both the restore and acknowledgment actions entirely. A same-day acceptance pass then: root-caused and fixed the one default-order regression failure (a test-only zero-retry-margin fragility, not a production defect); found and corrected a genuine testing-process mistake (concurrent `php artisan test` processes corrupting each other's shared `Storage::fake()` state — not a code defect either); added a real (non-mocked) Laravel maintenance-mode acceptance test; hardened `RestoreRequestService` to also re-check scope compatibility against a freshly-read source record; and performed a live local browser walkthrough (throwaway fixtures, fully cleaned up) confirming the full Step 1 → Step 2 → cancel flow renders and behaves correctly in Arabic/RTL. See `docs/AI_PROJECT_MEMORY.md`/`docs/TASKS_LOG.md`/`docs/DECISIONS_LOG.md` (2026-07-27 "Task 7C.8" and "acceptance pass" entries) for full technical detail. Final acceptance: targeted suite **162 tests, 162 passed**; broader `Restore|Backup` regression **854 tests, 851 passed, 0 failed, 0 errors, 3 skipped** in default order and two separate `--order-by=random` runs (distinct seeds) — all three genuinely clean.

Next: **OMS Task 7C.9 is real LOCAL end-to-end restore testing on Windows/Laragon** (database-only, files-only, full restore, and intentional failure/recovery drills against a disposable local copy, followed by one full application test-suite run) — **not** Hostinger deployment, which is a separate, later task that only follows once Task 7C is fully accepted locally. 7C.9 should not be started without a fresh, separately-confirmed explicit request given the destructive nature of a real restore drill, even locally. Graphify was refreshed and committed separately after the main commit, per instructions.

## Previously Recommended Next Step (2026-07-27, OMS Task 7C.7 + final hardening pass — full secure restore orchestration with true long-phase heartbeats — implemented, verified, COMMITTED)
**Implemented, verified, and committed.** `RestoreOrchestrator` is the real, destructive-capable execution engine for the Restore feature (OMS Task 7C) — the full required sequence (preflight → maintenance mode → mandatory FULL pre-restore safety backup → reconciliation snapshot embedded in signed progress → decrypt/verify/stage → attachment activation → database import → reconciliation → attachment finalization → maintenance exit → terminal result), with an explicit phase-aware failure/compensation matrix mapping every branch to `Restored`/`RestoreFailed`/`RestorePartial`, now with TRUE mid-operation heartbeats (`RestoreHeartbeat`, Carbon-based/testable) keeping a healthy long-running restore from ever being misclassified stale — including a genuine timer-driven poll mechanism for the otherwise-silent `mysql` import. A deterministic finalize()-failure test (`RestoreAttachmentLifecycle` seam), a properly-fixed hardcoded-UUID test-isolation flake, DB/cache-unavailability hardening for the stale-detection watchdog, and a real Windows `RestoreProgressWriter` rename-retry fix (discovered via this pass's own stress-testing, since heartbeats increased write frequency enough to expose it) round out the acceptance pass. See `docs/AI_PROJECT_MEMORY.md`/`docs/TASKS_LOG.md`/`docs/DECISIONS_LOG.md` (2026-07-26 and 2026-07-27 entries) for full technical detail. Final acceptance: targeted suite **112 tests, 112 passed**; broader `Restore|Backup` regression **785 tests, 782 passed, 0 failed, 3 skipped** in default order and two separate `--order-by=random` runs (different seeds), all identical, all clean.

Next: **OMS Task 7C.8 (the Filament restore button/modal, typed `RESTORE {uuid8}` confirmation UI, progress-polling endpoint/UI, stale-recovery acknowledgment UI) then 7C.9 (real LOCAL end-to-end restore testing on Windows/Laragon — database-only, files-only, full restore, and intentional failure/recovery drills, followed by one full application test-suite run)** should not be started without a fresh explicit request — `RestoreOrchestrator` is now the verified, tested, and hardened execution engine those phases will drive. Production Hostinger VPS deployment is a separate, later task that only follows once Task 7C (including 7C.9's local drills) is fully accepted — it is explicitly not part of 7C.9. Graphify was refreshed and committed separately after the main commit, per instructions.

## Previously Recommended Next Step (2026-07-26, test-stability pass — stabilize backup restore regression tests — implemented, verified, COMMITTED)
**Implemented, verified, and committed.** The one flaky failure flagged by the Task 7C.6 correction pass's broader regression run (`BackupCreationOrchestratorTest::...pre_publish_verification_fails`) was root-caused as a genuine test-isolation bug, not a production bug: 7 methods in `RestoreAttachmentActivationServiceTest.php` acquired a real `BackupSubsystemLock` exclusive `flock()` and called `activate()` (a real-filesystem-mutating operation) **before** entering the `try/finally` that releases it — since PHPUnit runs the whole suite in one process and `Storage::fake()` reuses the same fixed on-disk lock path every test, a handle leaked there could block the orchestrator's own lock acquisition in a later, unrelated test. Fixed by restructuring all 7 methods so the entire acquire-to-release span is inside one `try/finally`; `once()` memoization in the backup subsystem was reviewed and found unrelated (correctly per-instance-scoped). Test-file-only change (76 insertions, 58 deletions) — no production code touched, so `graphify update .` was not run. Verified: the specific test 10/10 clean; `BackupCreationOrchestratorTest` 25/25; combined filter 54/54; broader `Restore|Backup` regression **742 tests, 739 passed, 0 failed, 3 skipped, 1769 assertions** in both default and `--order-by=random` order. See `docs/TASKS_LOG.md`/`docs/DECISIONS_LOG.md` (2026-07-26 "test-stability pass" entries) for full detail.

## Previously Recommended Next Step (2026-07-26, OMS Task 7C.6 crash-safety correction pass — signed/durable swap marker, write-ahead phase protocol, InterruptedDuringRollback state, validated attachment manifest value object — implemented, verified, COMMITTED)
**Implemented, verified, and committed.** A same-day pre-commit crash-safety pass hardened Task 7C.6 before it was allowed to land: (1) the attachment-swap marker is now signed (HMAC-SHA256, `APP_KEY`-derived with its own distinct context `oms-restore-attachment-swap-v1`) and durably written/read (new `AttachmentSwapMarkerWriter`/`AttachmentSwapMarkerReader`, reusing the existing `RestoreProgressDurability` seam rather than a second implementation) — a tampered/unsigned/UUID-mismatched/unsupported-phase/malformed marker now always yields `InconsistentNeedsManualReview`, never a guessed state; (2) an 8-phase write-ahead protocol (`AttachmentSwapPhase`) is persisted before and after every destructive rename in `activate()`/`rollback()`/`finalize()`, and a marker-durability failure immediately after a successful mutation now fails closed to a distinct manual-review exception rather than continuing; (3) a new `InterruptedDuringRollback` crash state covers the process dying mid-rollback (quarantine intact, live missing, discard populated) — `rollback()` can now resume from it, proven by a real forced-crash-then-resume test; (4) the rollback-discard path is renamed `attachments.rollback_discard.{uuid}` → `attachments.restore_discard.{uuid}`; (5) `RestoreAttachmentRevalidator` now requires a validated `RestoreAttachmentManifest` value object instead of a raw array; (6) all three lifecycle methods independently re-inspect the lock and real current state at entry, never trusting only an earlier `AttachmentSwapHandle`. 36 files changed (29 added, 7 modified, 0 deleted). Targeted attachment suite: 326 tests, 323 passed, 3 pre-existing skips, 770 assertions — up from 65/64/1-skipped before this pass. Broader `Restore|Backup` regression: 742 tests, 738 passed, 1 pre-existing unrelated flaky failure (confirmed passing in isolation, touches no file this pass changed), 3 skipped, 1768 assertions. See `docs/TASKS_LOG.md`/`docs/DECISIONS_LOG.md` (2026-07-26 correction-pass entries) for full detail and `git log -1 --format=%H` (and its parent) for the exact commit hashes.

Next: **OMS Task 7C.7 (the real `RestoreOrchestrator` that wires `RestoreArchivePreparer` → `DatabaseRestorer`/`RestoreReconciler` → `RestoreAttachmentActivationService` → maintenance mode → `RestoreCommand`, including deciding how the orchestrator sources the attachment manifest file list `RestoreAttachmentRevalidator`/`RestoreAttachmentManifest` needs — in-process retention from `BackupArchiveContentVerifier::verify()`, or a persisted subset for crash recovery — see `docs/DECISIONS_LOG.md`'s 2026-07-26 Task 7C.6 entry on this open question)** should not be started without a fresh explicit request — everything built in 7C.1 through 7C.6 (including this correction pass) is the exact permanent shape that orchestrator must consume, not re-derive.

## Previously Recommended Next Step (2026-07-26, OMS Task 7C.6 — attachment restore primitives: staged revalidation, three-phase activate/rollback/finalize lifecycle, crash-state inspector, Windows retry seam — implemented, verified, superseded by the same-day correction pass above before commit)
**Implemented and verified, then hardened by the same-day correction pass above before ever being committed — this entry is kept for history only.** Task 7C.6 was done, as primitives only: `RestoreAttachmentPaths` computes a deterministic, UUID-validated quarantine layout as a direct sibling of the live `attachments` disk root, never nested under the `restores` disk. `RestoreAttachmentRevalidator` fully re-checked a staged attachment tree against a caller-supplied manifest file list immediately before the first live rename and never depended on `AttachmentUploadService`'s mutable upload-time allowlist. `RestoreAttachmentActivationService::activate()`/`rollback()`/`finalize()` implemented the three explicit lifecycle phases, each requiring a currently-live Exclusive `BackupSubsystemLockHandle`. `AttachmentSwapStateInspector` derived one of 6 states from directory existence plus a small **unsigned** JSON marker file — the correction pass above replaced that marker with a signed/durable one and added a 7th state (`InterruptedDuringRollback`) before this phase was ever committed. Targeted suite at the time: 65 tests, 64 passed, 1 skipped, 122 assertions; broader regression: 894 tests, 890 passed, 4 skipped, 2222 assertions.

## Previously Recommended Next Step (2026-07-26, OMS Task 7C.5 correction pass — durable reconciliation snapshots, full backups-queue purge, single-connection policy, metadata completeness, stream-capture re-verification — implemented, verified, COMMITTED)
**Implemented, verified, and committed.** A same-day pre-commit correction pass fixed six review points in 7C.5 before it was allowed to land: (1) the signed `RestoreProgressSnapshot` protocol now carries an OPTIONAL, nullable, bounded reconciliation snapshot (new `RestoreReconciliationSnapshot`) so the three authoritative `backup_operations` rows survive being reconstructed even if the process crashes after a database import and the live rows are gone — proven by a real drop-the-table-then-reconstruct-from-the-file integration test; (2) `RestoreEphemeralTablePolicy` now purges the ENTIRE backups queue (reserved/delayed included), not just unreserved rows, closing a real stale-transport-replay risk; (3) restore is now policy-locked to the application's primary/default database connection — `DatabaseRestorer` fails closed (`database_connection_mismatch`) if the configured backup connection ever differs from `database.default`, and `RestoreReconciler` no longer accepts any connection name at all; (4) `BackupOperationSnapshot` gained the one genuinely missing schema field (`manifest_version`), plus new deleted_at/verified_at/no-dangling-FK assertions; (5) `SymfonyProcessStreamInputRunner`'s stderr-despite-`disableOutput()` capture was re-confirmed directly against the Symfony Process source (not re-asserted from memory) and proven bounded-memory/deadlock-free under large simultaneous stdout+stderr. Targeted suite: 586 total, 584 passed, 0 failed, 2 pre-existing skips, 1359 assertions — up from 560/558/1304 before this pass. See `docs/TASKS_LOG.md`/`docs/DECISIONS_LOG.md` (2026-07-26 entries) for full detail and the exact commit hashes.

Next: **OMS Task 7C.6+ (maintenance-mode/safety-backup orchestration, then the real `RestoreOrchestrator` in 7C.7 that actually calls `DatabaseRestorer`/`RestoreReconciler` from inside `RestoreCommand`, including the future orchestrator writing the reconciliation snapshot into progress.json immediately before calling `DatabaseRestorer`)** should not be started without a fresh explicit request — everything built in 7C.5 (including this correction pass) is the exact permanent shape that orchestrator must consume, not re-derive.

## Previously Recommended Next Step (2026-07-25, OMS Task 7C.5 — streamed database restoration and post-import reconciliation services — implemented, verified, NOT wired to any command/orchestrator, NOT committed)
**Implemented and verified, awaiting approval to commit.** Phase 7C.5 is done, as services only: `SymfonyProcessStreamInputRunner` streams a staged SQL dump into `mysql`'s stdin without ever loading it into a PHP string (verified against the installed symfony/process source); `DatabaseRestorer` re-validates scope/staged-dump-safety/mysql-client/connection-completeness immediately before spawning `mysql` with a plain argv array (`--host=`/`--port=`/`--user=`/`--default-character-set=utf8mb4`/database, `MYSQL_PWD` in the process environment only, never argv), wrapping every failure in a sanitized `RestoreDatabaseException`; `LaravelRestoreDatabaseConnectionResetter` purges the possibly-stale pre-import PDO and proves a fresh connection usable via a real query; `RestoreReconciler` runs the exact required order (connection reset → `migrate --force` → `oms:sync-permissions` → `permission:cache-reset` → metadata reconstruction → selective ephemeral-table cleanup → `queue:restart`) via four interface-typed collaborators, each step throwing a distinct sanitized exception on failure with nothing after it ever running; new bounded `BackupOperationSnapshot`/`RestoreOperationSnapshot` value objects (no arbitrary-array constructor, no field for any secret) feed `RestoreMetadataUpserter`, which reconstructs source → safety → restore backup_operations rows in that order, resolving FKs by UUID (never a trusted old numeric ID) and `created_by` against the freshly-migrated `users` table (present → preserved, absent → null + identity snapshot); `RestoreEphemeralTablePolicy` removes only non-reserved backups-queue `jobs` rows, preserves `failed_jobs`/`job_batches`/`notifications`/every other queue, and truncates `cache`/`cache_locks`/`sessions` only when present. **Explicitly not connected to anything**: no route, command, or orchestrator calls `DatabaseRestorer` or `RestoreReconciler` — `AppServiceProvider` only gained container bindings so they're constructible once a future phase wires them in. Targeted suite: 560 total, 558 passed, 0 failed, 2 pre-existing skips, 1304 assertions.

Next: **review and approve this docs diff and the code diff together, then commit.** After that, **OMS Task 7C.6+ (maintenance-mode/safety-backup orchestration, then the real `RestoreOrchestrator` in 7C.7 that actually calls `DatabaseRestorer`/`RestoreReconciler` from inside `RestoreCommand`)** should not be started without a fresh explicit request — `DatabaseRestorer`, `RestoreReconciler`, and the metadata snapshot/upsert seam built here are the exact permanent shape that orchestrator must consume, not re-derive.

## Previously Recommended Next Step (2026-07-25, OMS Task 7C.4 — replay-safe restore launch, global serialization, detached process launcher, `oms:restore` command shell — implemented, corrected, verified, COMMITTED)
**Implemented, corrected in a pre-commit review pass, verified, and committed.** Phase 7C.4 is done: `RestoreLaunchService` validates every 7C.1 precondition on a queued restore row, then — holding one short-lived exclusive `BackupSubsystemLock` around the whole critical section — re-runs the dual `RestoreActivityGuard` check (excluding only the row's own UUID; any other active restore or a tampered file for the current UUID still blocks), performs the atomic conditional `UPDATE ... WHERE id AND type=restore AND status=queued AND started_at IS NULL AND launch_nonce=?` (exactly one row wins the claim; zero means replay/lost race → safe conflict, no progress write, no spawn), writes the initial signed progress snapshot (`phase=launching` — **corrected**, see below) via the existing `RestoreProgressWriter`, and only then calls the injected `RestoreProcessLauncher`; the lock is released in every success/failure path via `finally`. The signed `POST /restores/{uuid}/launch` route (`restores.launch` — **corrected from GET**, see below) requires Laravel's `signed` middleware plus the same real-Super-Admin-role-**and**-`backups.restore`-permission two-layer check every other backup endpoint uses — no Filament UI button/link/URL generator points at it yet. `RestoreProcessLauncher` has real Linux (`setsid --fork` + a pure argv array, no shell, verified against the installed symfony/process source, explicit `cwd=base_path()`) and Windows/Laragon (documented non-authoritative, local-dev-only) implementations, selected via a pure, OS-independently-testable `RestoreProcessLauncherFactory`. `php artisan oms:restore {uuid}` verifies the claim, acquires the lifetime exclusive lock with a short bounded retry (never the Cache lock, never the DB queue), writes `phase=lock_acquired` only now that it genuinely holds that lock, and — since the real execution engine doesn't exist yet — always fails closed to a terminal signed `restore_failed` progress snapshot + DB `RestoreFailed`, isolated so Task 7C.7 can replace only that one call. **Corrected** (pre-commit pass): the route is POST not GET (a signed GET is still triggerable by prefetching/link-scanning); a new `RestoreActivityGuard::blocksOrdinaryOperations()` gate closes the parent-launch-to-child-lock-acquisition handoff race for all five ordinary backup operations (create/retention/verify/delete/download), folded into `BackupDeletionService::eligibility()` specifically (not a separate `delete()` check) so the badge and the real rejection can never disagree; the previously-skipped bounded-retry test is now a real cross-process test (a second genuine external PHP process holds/releases the real OS lock). One pre-existing guard test (`BackupManagementPageTest::test_no_restore_route_exists`) had to be tightened rather than left as-is, since this phase's one required route makes its old "no restore route at all" assertion literally false; two pre-existing `BackupDeletionServiceTest` tests were updated to expect the new, broader `restore_activity_in_progress` reason for the `Restoring`-status case. Targeted suite after corrections: 418 total, 417 passed, 0 failed, 1 pre-existing skip, 957 assertions.

Next: **OMS Task 7C.5+ (restore request/confirmation UI, then eventually 7C.6 maintenance-mode/safety-backup orchestration and 7C.7 the real `RestoreOrchestrator`)** should not be started without a fresh explicit request — the launch/claim/lock/progress/command-shell seam built here is the exact permanent shape those phases must build on top of, not re-derive.

## Previously Recommended Next Step (2026-07-25, OMS Task 7C.3 — restore preflight, decryption/verification, safe extraction, private staging — implemented, corrected, verified, COMMITTED)
**Implemented, corrected in a pre-commit review pass, verified, and committed.** Phase 7C.3 is done: `RestorePreflightChecker` validates source-backup state/scope compatibility, encryption-header/key resolvability (via new `SecretstreamEnvelope::peekKeyId()`, no full decryption), mysql-client/database-connection presence (DB/full scope only), same-filesystem compatibility (new `FilesystemIdentity` seam), and restore activity (`RestoreActivityGuard::isActive()` now takes an optional exclude-UUID); `RestoreDiskSpaceEstimator` computes a conservative itemized byte requirement (never a flat multiplier), enforced before any workspace exists — **corrected** so the mandatory pre-restore safety backup (always full, regardless of selected scope) is sized from CURRENT live data (new `CurrentDatabaseSizeEstimator` seam + real current attachments size), never from the source backup's own size alone; `RestoreWorkspace` provides a UUID-validated, escape-proof private staging area whose `cleanup()` is structurally scoped to one subtree only, now also hardened against an existing symlink/junction anywhere along its path; `RestoreArchivePreparer` orchestrates preflight → workspace → decrypt → full verify → safe manifest-driven extraction, returning a bounded `PreparedRestore`, with cleanup-on-failure now guaranteed to never mask the original error; `RestoreArchiveExtractor` gained an independent full-manifest entry-set re-check immediately before extraction (closing a verify-vs-extract gap) on top of its existing zip-slip/duplicate/symlink/size/hash/byte-budget protections. A same-day pre-commit review caught and fixed all of the above; `RestoreActivityGuard`'s new exclude-UUID parameter also had a real bug (it skipped validating the excluded UUID's own progress file entirely, rather than only suppressing a valid non-terminal result) — fixed so exclusion can never turn a tampered state into "safe." No restore launch, execution, maintenance mode, database import, attachment swap, UI, or orchestration exists yet — nothing here is reachable from any route or command. Targeted suite after corrections: 433 total, 431 passed, 0 failed, 2 pre-existing skips, 984 assertions.

Next: **OMS Task 7C.4+ (restore launch endpoint, launch-nonce claim, detached process launcher, `oms:restore` command)** should not be started without a fresh explicit request — none of that exists yet, and `PreparedRestore`/`RestoreWorkspace` are the exact seam a future orchestrator must consume rather than re-deriving its own decrypt/verify/extract logic.

## Previously Recommended Next Step (2026-07-23, OMS Task 7C.2 — independent lock, signed progress protocol, atomic progress storage, restore-activity dual gate — implemented, verified, NOT committed)
**Implemented and verified, awaiting approval to commit.** Phase 7C.2 is done: `BackupSubsystemLock`/`BackupSubsystemLockHandle`/`LockMode` provide a real, crash-safe `flock()`-based mutex (never a database-backed Cache lock, since this app's cache store is database-backed and would be replaced mid-restore); all five ordinary backup-subsystem operations (creation, retention, verification, deletion, download) now hold the shared lock for their full duration before their existing Cache/per-backup locks, in the exact required order, with no peek-and-release anywhere; `BackupCreationOrchestrator::runWithLockAlreadyHeld()` exists for the future pre-restore safety backup, with handle validation (live, Exclusive, matching path) that rejects a dead/wrong-mode/foreign handle before any dump/file operation begins; the signed, bounded, atomically-written restore-progress protocol (`App\Services\Restore\*`) and the DB-row-OR-signed-progress-file dual activity gate (`RestoreActivityGuard`) are both fully built and tested, though nothing yet writes progress files in production use (no launch/orchestration exists). No restore UI is reachable anywhere. Targeted suite: 347 total, 345 passed, 0 failed, 2 pre-existing skips, 856 assertions (includes final-review corrections: accurate fsync-is-called-on-PHP-8.3 wording, Linux-atomic/Windows-replace-in-place documentation, and a `RestoreActivityGuard` UUID-only directory filter so a stray non-UUID directory can never create a false permanent block). A follow-up durability-hardening pass then made `RestoreProgressWriter` refuse to replace the current valid `progress.json` unless the temp file's `fflush`+`fsync` succeeded (sanitized `RestoreProgressWriteException` + temp cleanup + current-file preserved on failure), added best-effort parent-directory `fsync` after a successful rename (Linux; unsupported on Windows), and routed both through an injectable `RestoreProgressDurability` seam — restore subset now 66/66.

Next: **review and approve this docs diff and the code diff together, then commit.** After that, **OMS Task 7C.3 — archive decrypt/extraction/preflight validation** should not be started without a fresh explicit request. Per the approved architecture, 7C.3 reuses `SecretstreamEnvelope`/`BackupKeyRing`/`BackupArchiveContentVerifier` unchanged for decrypt+verify, then adds the new staging-extraction and preflight-check layer (mysql client present, key resolvable, itemized free-disk-space estimate with a 20% margin and 1 GiB floor, filesystem rename-compatibility) — all still before any restore launch mechanism exists.

## Previously Recommended Next Step (2026-07-23, OMS Task 7C.1 — restore domain/schema/config/authorization foundation — implemented, verified, NOT committed)
**Implemented and verified, awaiting approval to commit.** Phase 7C.1 of the approved OMS Task 7C restore architecture is done: `backup_operations` gained `restore_metadata` (JSON) and `launch_nonce` columns; `BackupType::Restore`/`BackupStatus::RestorePartial` enum cases added (with `isSuccessfulOutcome()` so a partial restore can never be mistaken for a clean one); `BackupOperation` gained `isRestoreOperation()` and the `RESTORE_METADATA_MAX_PHASE_HISTORY_ENTRIES` bound; `backups.restore` permission registered (still Super-Admin-only by construction, still not independently authorizing anything); a new private `restores` disk and `config('oms.backup.restore')` foundation section were added; `BackupDeletionService` now refuses to delete a backup that is the source of a non-terminal restore (`restore_source_in_use`). A real pre-existing bug (`BackupDeletionRejectedException::forReason()` silently coercing any unrecognized reason code to `unexpected_failure`) was found and fixed while wiring this through. No locking, progress files, launch routes, restore commands, archive extraction, or database/attachment restore logic exist yet, and no restore UI is reachable anywhere. Targeted suite: 291 total, 289 passed, 0 failed, 2 pre-existing skips, 1189 assertions.

Next: **review and approve this docs diff and the code diff together, then commit.** After that, **OMS Task 7C.2 — independent lock, signed progress protocol, atomic progress storage** — should not be started without a fresh explicit request. Per the approved lock design: ordinary backup-subsystem operations must acquire locks in the order shared filesystem lock → existing Cache lock (where applicable) → existing per-backup file lock (where applicable), and no service may use a different order; the future 7C.4 launch endpoint must serialize concurrent launch attempts under a short-lived exclusive `BackupSubsystemLock` around the dual activity-gate check, atomic restore-row claim, initial signed progress-file write, and detached process spawn, with the child restore process allowed a short bounded retry when acquiring its own lifetime exclusive lock.

## Previously Recommended Next Step (2026-07-23, backup management page — deletion-eligibility UI clarification — implemented, verified, NOT committed)
**Implemented and verified, awaiting approval to commit.** The backup management table's "الحماية" column (previously just the raw `is_protected` flag) is renamed "إمكانية الحذف" and now shows the real deletion-eligibility decision — "مسموح", or one of three "ممنوع" reasons (last known-good, manually protected, or currently locked/in-use) — computed by a new, reusable `BackupDeletionService::eligibility()` method that `delete()` itself now also calls, so the badge and the actual delete rejection can never disagree. The details modal's `is_protected` field values are renamed "محمية يدويًا"/"غير محمية يدويًا" to make clear it only describes manual protection, not overall deletability. No deletion rule changed, no migration. Targeted backup suite: 174 total, 173 passed, 0 failed, 1 pre-existing skip, 499 assertions.

Next: **review and approve this docs diff and the code diff together, then commit.** No further backup-page work is planned beyond this clarification; OMS Task 7C (the independent Restore CLI process) remains the next substantial backup-system task and should not be started without a fresh explicit request.

## Previously Recommended Next Step (2026-07-23, OMS Task 7B.2 — Filament backup management page — implemented, verified, documented, committed)

**Task 7B.2 (the Super-Admin-only Filament backup management page) is complete and committed.** `النظام` → `النسخ الاحتياطي والاستعادة` now provides overview statistics (last successful backup, next scheduled run, total active completed size, latest failed operation), manual-backup queueing (validates the encryption configuration without exposing it, dispatches `CreateBackupJob`, never runs synchronously), integrity-verification queueing (`VerifyBackupIntegrityJob`, duplicate-request-guarded), encrypted download (via the existing named `backups.download` route only), safe manual deletion (new `BackupDeletionService` — typed `DELETE` confirmation, rejects protected/active/last-known-good/referenced-pre_restore backups, single-record only), 10-second status polling, and persistent database notifications for creation/verification success and failure. **Restore remains entirely absent** — no action, permission, job, or route; a static Arabic notice states it will arrive later. Every page/action requires a real `Super Admin` role plus the matching `backups.*` permission, re-checked explicitly inside each action (not only via visibility), via the new shared `App\Support\Backup\BackupAuthorization` helper — a non-Super-Admin manually granted every `backups.*` permission is still denied everywhere. One genuine 7B.1 defect was found and corrected along the way (with explicit approval before proceeding): `BackupCreationOrchestrator::enqueue()` no longer forces `is_protected = true` on every manual backup (which would have made manual deletion permanently impossible) — retention's automatic protection for manual backups was always independently guaranteed by its own `type === Manual` check, so this only stops `is_protected` from over-blocking manual deletion. A separately pre-existing, unrelated permission-suite test failure (a hardcoded `155`-permission-count literal that predated Task 7B.1's 6 `backups.*` permissions, confirmed stale via `git stash`) was also corrected to a durable, `PermissionRegistry`-derived assertion. Targeted backup suite: 238 total, 236 passed, 0 failed, 2 skipped (pre-existing 7B.1 OS-level symlink tests), 0 risky, 658 assertions. Corrected permission tests: 45 total, 45 passed, 0 failed, 0 skipped, 0 risky, 838 assertions.

Next: **OMS Task 7C — the independent Restore CLI process** (deliberately not a web/queue-worker flow, since a restore can replace the very database the queue and `backup_operations` table live in — see the Task 7A audit's "restoring the database that stores the restore job itself" analysis). Do not start without a fresh explicit request. Also still pending, unrelated to backups: the manual five-role sidebar/authorization browser walkthrough noted under the earlier authorization-milestone entries below; and a short live-browser pass over the new backup management page itself (create/verify/download/delete a real queued backup end-to-end with a real queue worker) has not yet been done — this task was verified only via automated Livewire/HTTP tests against `Storage::fake()`/`Queue::fake()`/SQLite `:memory:`.

## Previously Recommended Next Step (2026-07-23, OMS Task 7B.1 — backup core foundation — implemented, verified, corrected, documented, committed)

**Task 7B.1 (backup creation, verification, retention, secure download) is complete and committed.** Every design point from the Task 7A read-only audit plus three rounds of focused correction is now implemented: libsodium Secretstream-encrypted archives (DB dump + private attachments only), full content verification of the candidate archive *before* it is ever published/marked `completed` (`verified_at` stamped at that same moment), a database-enforced unique `deduplication_key` for scheduled backups, a global operation lock plus a second per-backup file lock (download/verify/retention can never race each other on the same archive — a locked row is skipped by retention and reported, and a locked download returns HTTP 423), `mysqldump` streamed incrementally via `Process::getIterator()` (never buffered, verified against the real installed `symfony/process` source and a real child process), and deterministic (no-OS-privilege) symlink-rejection tests. Targeted suite: 143 total, 141 passed, 0 failed, 2 skipped (OS-level symlink tests only, unavailable without Windows elevation), 0 risky, 411 assertions.

Next (superseded by the entry above — kept for history): ~~OMS Task 7B.2 — the Filament management page~~ (done, see above), then OMS Task 7C.

## Previously Recommended Next Step (2026-07-22, OMS-wide CRUD redirect standard — implemented, verified, documented)

- The OMS-wide CRUD redirect standard is complete: full-page **Create → saved record's View page**, **Edit → updated record's View page**, **page-level Delete → resource Index**. It is centralized in one concern, `App\Filament\Concerns\RedirectsToResourceView` (overrides only `getRedirectUrl()`), adopted by every Create/Edit page of the 22 editable resources (44 page classes). URLs resolve through the Resource (`$resource::getUrl('view'|'index', ...)`) — no hard-coded `/admin/...` — and the View redirect is gated by `canView($record)`, with the resource Index as the safe fallback. Page-level Delete already used Filament's correct stock Index redirect and was intentionally left unchanged; RelationManager/modal CRUD and the four read-only resources (Attachments, Permissions, TransactionLines, Transactions) are intentional exceptions.
- No further work is required for this standard. When adding a **new** full-page resource, add `use RedirectsToResourceView;` to its Create/Edit pages — `tests/Feature/Crud/CrudRedirectStandardStructureTest` will fail if a current full-page Create/Edit page omits the concern, guarding against silent reversion to Filament's defaults.

## Recommended Next Step (2026-07-22, test-suite cleanup — committed)
Removed the obsolete stock Laravel `ExampleTest` (asserted `GET /` = 200; OMS is Filament-only with no user-facing `/` route — no production route was added) and fixed the one risky Users test (`RoleAssignmentSafetyTest::test_submitted_super_admin_role_is_rejected_server_side` now asserts the `ValidationException` carries a `roles` error). Targeted `tests/Feature/Users/`: 64 passed, 0 failed, 0 skipped, 0 risky, 134 assertions. Test-only; no production behavior changed.

Next: **run the full suite once** to record the actual post-cleanup totals (do not assume or pre-write the numbers — the earlier full run was 967/963/1-failed/3-skipped/1-risky, and the removed failure + fixed risky test should both be resolved, but only a real run establishes the final counts). Once green, Attachment Security Task 6 (6A → 6B → 6C → 6D + this cleanup) is fully closed. The earlier optional follow-ups still stand (live-browser pass over the `سجل المرفقات` registry; the manual five-role sidebar/authorization walkthrough) — none blocking, none to start without a fresh explicit request.

## Recommended Next Step (2026-07-22, OMS Task 6D — secure financial attachment registry — implemented, verified, documented, committed)
**Registry complete, approved, and committed.** `AttachmentResource` is rebuilt and re-enabled in the sidebar (`النظام` → `سجل المرفقات`) as a strictly read-only browse/search/view surface over the five supported financial attachable types. Opening requires `attachments.view_any`; every listed/viewed row additionally requires `attachments.view` plus the matching parent-module `.view` — enforced both as a `whereHasMorph` list scope and (independently) as `AttachmentResource::canView()` → 403 on a direct View URL. Only the five approved types are listed/bound; unsupported types are excluded (and a guard test keeps the registry's type list in sync with `AttachmentController::SUPPORTED_ATTACHABLE_TYPES`). Preview/download remain behind the protected `attachments.show` route; missing/soft-deleted/invalid-disk rows expose no file link or stored path. All Task 6A structural read-only guarantees held (every `canX` false even for Super Admin, index/view only, no `FileUpload`). The file-availability filter was intentionally omitted (unbounded filesystem work). Multi-currency ops show `final_amount`+`disbursementCurrency` (approved). New `App\Services\Attachments\FinancialAttachmentRegistry` centralizes the type→permission mapping and accessors. 42 new registry tests; full targeted set 274 → 271 passed / 0 failed / 3 pre-existing skips / 0 risky / 856 assertions — all on SQLite `:memory:` + `Storage::fake`, no real DB row or attachment file changed. See `docs/TASKS_LOG.md` (2026-07-22 "Task 6D" entry) for the full file list and `docs/DECISIONS_LOG.md` for the multi-currency-amount, two-layer-authorization, and omitted-filter reasoning.

Next: **the attachment-security phase (6A → 6B → 6C → 6D) is functionally complete** — foundation, live cutover, orphan cleanup, and the read-only registry are all done and committed. Recommended follow-ups, none blocking: (1) a short live-browser pass logged in as an Admin/Accountant to visually confirm the `سجل المرفقات` registry lists the real `GeneralExpense` rows correctly, the private-disk badge/preview/download behave, and the trashed rows appear only under the `المحذوفة` filter with no links; (2) the still-outstanding manual five-role sidebar/authorization browser walkthrough noted under the earlier authorization-milestone entries below (independent of attachment work). Do not start either without a fresh explicit request, per every prior task's pattern in this log.

## Recommended Next Step (2026-07-22, OMS Task 6C — obsolete orphan public-file cleanup — verified, documented, NOT committed)
**Task 6C is effectively complete: there was nothing to delete.** The six obsolete orphaned public files under `storage/app/public/{execution-payments,payments}` were **already absent** from disk when the task ran (only `.gitignore` remains under `storage/app/public`; neither orphan directory exists) — the user confirmed the old financial records were intentionally deleted and the files no longer needed. This task therefore deleted nothing and, since the files were already gone, could not record per-file pre-deletion SHA-256/sizes (not fabricated). Read-only DB verification confirmed the `attachments` table is empty — 0 rows referencing `execution-payments`/`payments` (active or trashed), 0 public-disk rows, 0 attachments-disk rows, 0 total. Old public URLs return HTTP 403 (no file served); the `public/storage` symlink is intact (`/storage/.gitignore` → 200). All 5 financial Forms still upload to the private `attachments` disk and all 5 View pages still serve via the protected `attachments.show` route (no `Storage::url()`/raw `/storage/` anywhere in financial display). Targeted tests `tests/Feature/Attachments` 141/141 (400 assertions). **No quarantine or legacy-migration tooling was built or is needed** — the empty table means no `disk = public` rows exist to migrate, and the public upload path is gone. Documentation updated across all five CLAUDE.md-required docs; **not staged, not committed — awaiting review.**

Next: **review the Task 6C documentation diff and approve the commit** (docs-only — no code/DB/storage change). After approval, run `graphify update .` (the graph is unaffected by this docs-only change, but the project rule refreshes it after approval) and commit the doc updates. With 6A (foundation), 6B (cutover), and 6C (cleanup) all done, the attachment-security phase is closed: every financial attachment now uses private storage behind the authenticated `attachments.show` route, and no legacy public exposure or migration debt remains. A remaining optional item from earlier phases still stands: the short manual browser verification across the five system roles noted below (authorization milestone), independent of attachment work.

## Recommended Next Step (2026-07-21, OMS Task 6B — financial Resource attachment cutover — implemented, verified, rolled out, and committed)
**Cutover complete, approved, rolled out to the real local database, and committed.** All 5 financial Resources (Project Cost Receipts, Project Cost Budget Disbursements, Execution Payments, General Expenses, General Exchanges) now upload to and display from the private `attachments` disk built in Task 6A — no financial page generates a raw `/storage/...` URL or calls `Storage::url()` anymore (confirmed via `grep` across all 5 Resource directories). New `App\Services\Attachments\AttachmentUploadService` centralizes what was previously 10 near-identical hand-written upload/rename/move blocks; a single reusable `secure-attachment-preview` Blade component replaces 10 hand-built display blocks across the View and Edit pages. Edit pages support keep/replace/remove, never prefill a private path into `FileUpload`, and never leave zero or two active attachments after a save. `disk = public` legacy rows keep working through the same protected `attachments.show` route as new `disk = attachments` rows — the transitional coexistence Task 6A's migration set up is now actually exercised. 77 new tests, all passing; full targeted pre-commit run (Attachments + the 5 financial Resource suites + `ResourceHttpAuthorizationTest` + `AuthorizationAcceptanceTest`) 300/303 (3 pre-existing unrelated skips). After approval: a full `mysqldump` backup was taken (`C:\laragon\backups\oms\oms_before_attachment_disk_20260721_143806.sql`), the real `2026_07_21_000001_add_disk_to_attachments_table` migration was run against the real local MySQL database, and post-migration verification confirmed the `disk` column matches the migration exactly, the Attachment row count is still 0, and all six pre-existing orphan files are byte-for-byte unchanged. See `docs/TASKS_LOG.md` (2026-07-21 "Task 6B" entry) for exact commands/counts/hashes and `docs/DECISIONS_LOG.md` for the row-created-before-final-filename, replace-before-soft-delete, and shared-preview-component reasoning.

Next: **OMS Task 6C — orphan-file quarantine, legacy migration tooling, and final public-link cleanup.** The real `attachments` table still has 0 rows (every new upload from here forward will use the private disk correctly, but nothing historical has been migrated) and the six pre-existing orphan files under `storage/app/public/{execution-payments,payments}` (no matching DB row) remain exactly as found by Task 6A's original audit. Task 6C's own explicit scope (per the Task 6A audit's plan, not yet re-confirmed): build a dedicated quarantine/manifest step for the six orphans (record path/size/MIME/SHA-256, manual review, never auto-attach to a financial record) plus a distinct, explicit legacy-migration Artisan command for any *real* future `disk = public` rows (private timestamped backup → copy to `attachments` disk → verify size+checksum → verify secure-route access → remove the public original only via that same explicit command → prove the old `/storage/...` URL 404s afterward) — get this scope re-confirmed with the user before starting, since Task 6A's plan was written before Task 6B's actual shape was known. Do not begin without a fresh explicit approval, per every prior task's pattern in this log.

## Previously Recommended Next Step (2026-07-21, OMS Task 6A — private attachment security foundation — implemented, corrected, and committed)
**Foundation complete, approved, and committed.** Attachment files are no longer served by a raw, guessable public URL with zero authorization: a new private `attachments` disk (never symlinked, `serve => false`), a transitional `disk` column on `attachments` (existing rows default `public`, preserving their true location), a new `AttachmentStorageService` (approved-disk allowlist, stored-path traversal/absolute-path/null-byte validation, OS-independent filename sanitization, safe MIME fallback), and one authenticated route (`GET /attachments/{attachment}/{view|download}`, Filament's own `Authenticate` middleware, `Gate::authorize('view', $parent)` against the attachment's real parent record — reusing the 5 existing financial-model policies unchanged, no new permission added) now exist and are fully tested. The standalone `AttachmentResource` (previously an unused latent upload path onto the pre-existing `local` disk, itself incidentally exposed via Laravel's built-in signed-URL serving route) is hardened to structurally read-only, mirroring `PermissionResource`'s pattern, and hidden from navigation. **The five financial Resources (Project Cost Receipts, Project Cost Budget Disbursements, Execution Payments, General Expenses, General Exchanges) were deliberately not touched in this phase** — their forms still upload to the `public` disk and their View pages still build raw `Storage::url()` HTML; this is intentional, not an oversight. The six pre-existing orphan files under `storage/app/public/{execution-payments,payments}` (no matching DB row) remain exactly as found. See `docs/TASKS_LOG.md` (2026-07-21 "Task 6A" entry) for exact file list/commands/counts and `docs/DECISIONS_LOG.md` for the disk-allowlist, `AttachmentResource`-hardening, and soft-deleted-parent-before-authorize reasoning.

Next: **OMS Task 6B — the five-financial-Resource cutover.** Switch each of the 5 Schemas' `FileUpload` from `disk('public')` to `disk('attachments')`, update the 10 Create/Edit page handlers to write through the new disk (keeping the same deterministic filename convention), and replace each View page's raw `Storage::disk('public')->url()` HTML with a link to the new `attachments.show` route. Per the Task 6A audit's own finding, a private-disk `FileUpload` cannot show an *existing* file's preview via the same mechanism it uses today (Filament's `BaseFileUpload::getUploadedFile()` calls `Storage::disk()->temporaryUrl()` for a `visibility: private` field, which the new `attachments` disk deliberately does not support) — Task 6B's design must account for this (e.g. a separate secure preview/download link component for the *current* file, keeping `FileUpload` itself only for picking a *replacement*). Only after 6B is live and verified should the separate legacy-file migration (Task 6C: private timestamped backup → copy to `attachments` disk → verify size+checksum → verify secure route access → remove public originals only via a distinct, explicit cleanup command → prove old `/storage/...` URLs 404) be planned — including a dedicated quarantine/manifest step for the six existing orphan files (path, size, MIME, SHA-256; manual review before any deletion; never auto-attached to a financial record).

## Previously Recommended Next Step (2026-07-21, OMS authorization acceptance testing — Tasks 1–5 — complete and approved)
**Acceptance testing complete and approved; new test file committed.** Ran the final authorization acceptance pass across all five OMS Permissions tasks: five-role (Super Admin/Admin/Accountant/Project Manager/Viewer) sidebar navigation, direct URL access, create/update/delete operations, report access, export authorization, and Users/Roles/Permissions system pages were all verified with real HTTP/Livewire tests using the real `PermissionSyncService` and actually-assigned system roles — not just permission-set-level checks. Cross-module permission leakage was checked and not found (roles↔users, permissions↔roles, finance↔projects, finance/projects↔reports). **No authorization defect was found; no production code or real database data was changed.** One new file, `tests/Feature/Permissions/AuthorizationAcceptanceTest.php` (22 tests, 200 assertions), closes the specific coverage gaps an audit of the existing ~80-method suite found. Normal role assignment remains controlled by `users.create`/`users.update` plus `UserManagementService`'s privilege-subset validation; `users.assign_super_admin` remains the separate permission for assigning Super Admin — confirmed as the approved design, not a gap (see `docs/DECISIONS_LOG.md`, 2026-07-21). The known unrelated `ExampleTest` failure and the known unrelated risky Task 3 test remain untouched, exactly as before this task. See `docs/TASKS_LOG.md` (2026-07-21 acceptance-testing entry) for exact counts.

Next: **a short manual browser verification remains before formally closing the authorization milestone.** Log in as each of the five system roles (or as close as practical) and visually confirm: the sidebar matches the automated navigation proof for that role; a direct URL to a resource/report/system page the role lacks permission for shows Filament's real 403 page; Users/Roles/Permissions management behaves as tested (no Create/Edit route for Permissions even for Super Admin, system roles view-only, Super Admin assignment blocked for non-Super-Admins, last active Super Admin protections hold). This was verified via 200 new automated assertions plus the full existing suite (782/785), but not yet visually confirmed in a live browser session — this is the one remaining item before the entire OMS permissions phase (Tasks 1→2A→2B→3→4→5→acceptance) can be considered fully closed. Attachment-security work is queued to start after this milestone closes.

## Previously Recommended Next Step (2026-07-21, OMS Permissions Task 5 — read-only permission management — implemented and verified, NOT committed)
**Implemented and fully verified, awaiting approval to commit.** `PermissionResource` (nav group `النظام`, label `الصلاحيات`) now lets authorized users list/search/filter (by module/guard/registered-or-custom status)/view every `Permission` row — Arabic label, module, guard, associated-role names and count, registered-vs-custom status — while structurally forbidding manual create/edit/delete, including for a real Super Admin: only `index`/`view` routes exist (no Create/Edit page classes at all), and all 8 mutation `canX()` methods are hard-`false`, the same structural pattern already used by `RoleResource` to survive `Gate::before`'s Super-Admin Policy bypass. One new registered permission, `permissions.sync` (`مزامنة الصلاحيات`, registry now 155 total), is granted to Super Admin only by construction (no changes needed to any of the other four roles' default-permission methods). The one UI mutation this resource allows — re-running permission synchronization via a confirmation-gated header action — requires, independently of the button being visible at all: an authenticated actor, the actor's **exact** `Super Admin` role, and the `permissions.sync` ability, all enforced inside the new `PermissionManagementService` (a thin wrapper that authorizes then delegates to the untouched, already-idempotent `PermissionSyncService`). This closes the loop the task explicitly worried about: a non-Super-Admin manually granted `permissions.sync` is still rejected, proven by a genuinely crafted Livewire `mountAction()`/`callMountedAction()` call (not just a hidden-button assumption) plus an independent, UI-free service-level test. Zero changes to `UserResource`, `RoleResource`, `RoleManagementService`, `RoleForm`, financial logic, report/export authorization, or any existing Resource Policy — `RoleManagementService`'s pre-existing `permissions.`-prefix protection already covered the new permission automatically. 39 new tests across 5 new files, plus 1 new test each added to `SystemRoleDefaultPermissionsTest`/`SyncPermissionsCommandTest`; full suite 760/763 excluding the same 3 pre-existing, unrelated gaps recorded in every entry below (`ExampleTest`, 2 `ResourceHttpAuthorizationTest` skips, 1 `tests/Feature/Users` risky flag — confirmed via `git status --short` to be outside every file this task touched). See `docs/TASKS_LOG.md` (2026-07-21 "Task 5" entry) for exact commands/counts and `docs/DECISIONS_LOG.md` for the exact-Super-Admin-role-check, structural-mutation-override, and new-permission-group reasoning.

Next: get approval and commit this permission-management layer — this was the last remaining item from the original OMS permissions-phase plan (Tasks 1 → 2A → 2B → 3 → 4 → 5, all now implemented and verified, none yet committed together). Also open the browser (logged in as Super Admin, and separately as a non-Super-Admin user with `permissions.view_any`/`permissions.view`) to visually confirm: the permissions list shows Arabic labels/modules/role counts correctly for registered permissions and a `صلاحيات مخصصة` section for any custom/legacy row; no Create/Edit/Delete button appears anywhere, even for Super Admin; the "مزامنة الصلاحيات" button is visible only to Super Admin and, after confirming, shows the expected Arabic summary notification and the table reflects any newly-created permission immediately; a direct `/admin/permissions/create` or `/admin/permissions/{id}/edit` URL 404s for everyone including Super Admin — verified here via 43 new automated test assertions including real HTTP and Livewire integration tests plus a crafted-request proof, but not yet visually confirmed in a live browser session. Once this and Task 4 are both committed, consider a full end-to-end permissions-phase browser walkthrough (create a custom role, assign it registered + custom permissions, confirm it reflects correctly across `RoleResource`/`UserResource`/`PermissionResource`) before onboarding real non-Super-Admin operators.

## Previously Recommended Next Step (2026-07-20, OMS Permissions Task 4 — secure role management — implemented and verified, NOT committed)
**Implemented and fully verified, awaiting approval to commit.** `RoleResource` (nav group `النظام`, label `الأدوار`) now lets authorized users view all roles, create/edit/delete safe custom roles, and assign permissions through grouped Arabic checkboxes (one collapsible section per `PermissionRegistry` module, plus a `صلاحيات مخصصة` group for any unregistered `Permission` row). The five system roles remain visible everywhere (list, view) but are structurally uneditable/undeletable — including for a real Super Admin, via `RoleResource::canEdit()`/`canDelete()` calling `RoleManagementService::canManageRole()` directly rather than through the Gate/Policy path `Gate::before` would otherwise bypass. `RoleManagementService` mirrors `UserManagementService`'s architecture: `canManageRole()` blocks any system role and any role the actor is currently assigned to, unconditionally, before considering Super-Admin status; `assignablePermissionNames()` gives Super Admin every existing permission and a non-Super-Admin only their own effective permissions minus protected ones (`users.assign_super_admin`, `roles.*`, `permissions.*`); `createRole`/`updateRole`/`deleteRole` all run inside `DB::transaction()`, re-derive every check from a freshly-fetched role, revalidate every submitted permission name server-side, and reject deleting a role currently assigned to any user. `RolePolicy` is explicitly registered via `Gate::policy()` since Spatie's `Role` model sits outside Laravel's auto-discovery convention. 80 new tests across 6 files; full suite 711/714 excluding the same 1 pre-existing unrelated `ExampleTest` failure recorded since 2026-07-15 (confirmed identical on a clean `git stash`). See `docs/TASKS_LOG.md` (2026-07-20 "Task 4" entry) for exact commands/counts and `docs/DECISIONS_LOG.md` for the structural-protection, explicit-policy-registration, and grouped-checkbox design reasoning.

Next: get approval and commit this role-management layer, then start **Task 5** (read-only `PermissionResource`) — the last remaining item from the original permissions-phase plan. Also open the browser (logged in as Super Admin, and separately as a non-Super-Admin user with `roles.*` permissions) to visually confirm: the five system roles show no Edit/Delete buttons anywhere, even for Super Admin; a direct `/admin/roles/{system-role-id}/edit` URL 403s for Super Admin; creating a custom role and assigning it a mix of registered permissions works end-to-end and the role becomes selectable on `UserResource`'s roles field; and a non-Super-Admin actor's permission checkboxes are visibly limited to what they themselves hold — verified here via 80 new automated test assertions including real HTTP and Livewire integration tests, but not yet visually confirmed in a live browser session.

## Previously Recommended Next Step (2026-07-20, OMS Permissions Task 3 — secure user management — implemented and verified, NOT committed)
**Implemented and fully verified, awaiting approval to commit.** `UserResource` no longer relies on the Task-1 Super-Admin-only lockdown — it now enforces granular `users.view_any/view/create/update/delete/restore` permissions, plus a full safety layer in the new `App\Services\Users\UserManagementService`: a non-Super-Admin actor may only manage another user whose entire effective permission set (role-derived + direct) is a subset of their own, who holds no protected permission (`users.assign_super_admin` exact, `roles.*`/`permissions.*`), and who isn't an exact `Super Admin`; self-edits can never change their own `roles`/`is_active` (crafted-request elevation is rejected, safe name/email/password self-edits still work); the last active Super Admin can never be deleted/deactivated/demoted, enforced via a real locking read inside the mutating transaction, not a locked count; role assignment is capped to what the acting user could already grant; restoring a soft-deleted user never reactivates them or bypasses Super Admin protections; and there is no force-delete or bulk delete/restore anywhere on this resource (single-record only), so those risks are structural, not just permission-denied. `DatabaseSeeder`/`oms:sync-permissions` were updated to the same `is_active`-aware "active Super Admin" definition, with 4-branch bootstrap recovery (create / restore-and-reactivate / reactivate-in-place / already-active-untouched). 111 new/extended tests across `tests/Feature/Users/*` + 2 extended Permissions test files; full suite 631/634 excluding the same 1 pre-existing unrelated `ExampleTest` failure recorded since 2026-07-15 (confirmed to also fail on a clean `git stash` of this task, i.e. genuinely pre-existing, not a regression). See `docs/TASKS_LOG.md` (2026-07-20 "Task 3" entry) for exact commands/counts and `docs/AI_PROJECT_MEMORY.md` for the full design rationale.

Next: get approval and commit this user-management safety layer, then start **Task 4** (`RoleResource` — full CRUD over the 5 system roles + custom roles, respecting the same protected-permission rules already enforced by `UserManagementService::assignableRoleNames()`) and **Task 5** (read-only `PermissionResource`) — both remain pending, not built in Task 3. Also open the browser (logged in as a real non-Super-Admin user with `users.*` permissions, and separately as Super Admin) to visually confirm: the roles multi-select only shows grantable roles for a non-Super-Admin actor and every role for Super Admin; the "is_active" toggle and roles select are genuinely disabled (not just hidden) when editing your own account; attempting to edit/delete a Super Admin as a non-Super-Admin shows Filament's real 403; the last active Super Admin's delete action is hidden/hard-rejected; and a normal create/edit/delete/restore cycle for an ordinary user works end-to-end — verified here via 111 new automated test assertions including real HTTP and Livewire integration tests, but not yet visually confirmed in a live browser session. The real local database's MySQL server was not reachable during this task, so a read-only confirmation that `oms@oms.com` is unaffected and the new `is_active` column's default state on that row should be done manually before or as part of running the migration there.

## Previously Recommended Next Step (2026-07-20, OMS Permissions Task 2B — custom report page and export authorization — implemented and verified, NOT committed)
**Implemented and fully verified, awaiting approval to commit.** All 6 custom Filament report pages (Account Statement, Trial Balance, Donor Financial Report, Comprehensive Financial Transactions, Projects General Financial, Project Financial Details) now enforce the pre-existing `reports.<page>.view`/`reports.<page>.export` permissions via one reusable `App\Filament\Pages\Concerns\AuthorizesReportAccess` trait: `canAccess()` gates navigation visibility, direct URL access, and every subsequent Livewire request (not just the first); `authorizeReportExport()` runs as the first statement of every export method, independent of the header action's `visible()`, so a crafted Livewire request calling `exportExcel()`/`exportWord()`/`exportXlsx()` directly is rejected before any file is generated — proven via real `Livewire::test()->call('exportMethod')->assertForbidden()` calls, not assumed. `ProjectFinancialDetailsPage`'s `shouldRegisterNavigation = false` is unchanged (still direct-URL-only, now permission-gated on that route). Zero report calculation/query/filter/export-service/format changes; zero Task 2A Resource Policy changes; no migration. 58 new tests (30 page-access + 28 export-authorization); full suite 574/574 excluding the same 1 pre-existing unrelated `ExampleTest` failure recorded since 2026-07-15 (571 passed, 2 skipped). See `docs/TASKS_LOG.md` (2026-07-20 "Task 2B" entry) for exact commands/counts and `docs/DECISIONS_LOG.md` (2026-07-20 "Task 2B" entries) for every judgment call made along the way.

Next: get approval and commit this report-page/export authorization layer, then start **Task 3** (safe granular `UserResource` — `is_active` migration, self-elevation guard, last-Super-Admin protections, replacing `UserPolicy`'s hardcoded lockdown with real `users.*` checks), **Task 4** (`RoleResource`), and **Task 5** (read-only `PermissionResource`) — all three remain pending, not built in Task 2A or 2B. Also open the browser (logged in as a real Accountant/Project-Manager/Viewer-role user, once such users exist) to visually confirm: the sidebar hides report pages the logged-in role lacks `view_any`/`.view` for; a direct-URL attempt without permission shows Filament's real 403 page; export buttons are actually absent (not just disabled) for a user with view-only access; and a Super Admin can still open and export every report subject to the existing "عرض" submission guards — verified here via 58 new automated test assertions (including genuine HTTP round trips and Livewire action tests), but not yet visually confirmed in a live browser session.

## Previously Recommended Next Step (2026-07-19, OMS Permissions Task 2A — Filament Resource/RelationManager authorization — implemented and verified, NOT committed)
**Implemented and fully verified, awaiting approval to commit.** All 23 non-User Filament Resources (Financial/Projects/Partners/Settings-reference-data/System groups) plus all 5 existing RelationManagers now enforce the Task 1 `PermissionRegistry` permissions via 23 new Policy classes sharing one reusable `App\Policies\Concerns\AuthorizesCrud` trait — `<module>.view_any/view/create/update/delete/restore`, auto-discovered by Laravel's standard model→policy convention, zero Resource/RelationManager source files touched. Force delete stays Super-Admin-only (never a normal permission); `TransactionResource`/`TransactionLineResource`'s pre-existing hardcoded read-only `canX()` overrides are untouched and now doubly enforced at the Policy layer too. A trashed record can no longer be normally viewed/edited/re-deleted through any of these 23 resources; restore requires its own permission and only ever applies to an already-trashed record — enforced uniformly by the shared trait, with no change to any Resource's existing `SoftDeletingScope` route-binding bypass. Confirmed empirically (via `Livewire::test()` against all 5 RelationManagers, not assumed) that Filament authorizes a RelationManager against its *related* model's policy — viewing a Project does not grant `project_costs.create`. The two intentionally-swapped permission-prefix pairs from the Task 2 discovery audit (`ProjectCostBudget`↔`project_cost_budgets_payments`, `ProjectCostBudgetsPayment`↔`execution_payments`) are documented in both policy classes' docblocks and covered by a dedicated `PolicyDiscoveryTest`. 285 new test assertions (5 new files: `PolicyDiscoveryTest`, `CrudPolicyBehaviorTest`, `ResourceHttpAuthorizationTest`, `ExecutionPaymentBudgetDisbursementScopeTest`, `RelationManagerAuthorizationTest`) all pass; entire `Permissions` namespace 273/275 (2 expected skips); full suite 513/516 (1 pre-existing unrelated `ExampleTest` failure, same one recorded since 2026-07-15); `git status`/`git diff --stat` confirm every change is a new untracked file — zero modification to any existing Resource, RelationManager, Form, Table, or financial-service file. See `docs/TASKS_LOG.md` (2026-07-19 "Task 2A" entry) for exact commands/counts and `docs/DECISIONS_LOG.md` (four 2026-07-19 "Task 2A" entries) for every judgment call made along the way.

Next: get approval and commit this Resource/RelationManager authorization layer, then start **Task 2B** — custom report-page and export authorization (the 6 report pages + their Excel/Word export buttons were explicitly out of scope for Task 2A), followed by **Task 3** (safe granular `UserResource` — `is_active` migration, self-elevation guard, last-Super-Admin protections, replacing `UserPolicy`'s hardcoded lockdown with real `users.*` checks), **Task 4** (`RoleResource`), and **Task 5** (read-only `PermissionResource`). Also open the browser (logged in as a real Accountant/Project-Manager/Viewer-role user, once such users exist) to visually confirm the sidebar now hides resources the logged-in role lacks `view_any` for, and that a direct-URL attempt to a resource without permission shows Filament's real 403 page — verified here via 285 new automated test assertions (including genuine HTTP round trips), but not yet visually confirmed in a live browser session. Note the one pre-existing, permission-independent RelationManager risk flagged again during this task (not addressed, since it's a hypothetical future addition, not a current gap): if a standalone "planned budget" Resource is ever built directly on `App\Models\ProjectCostBudget` (the `transaction_id IS NULL` rows, distinct from `ProjectCostBudgetsPaymentResource`'s `transaction_id IS NOT NULL` disbursement rows), it would automatically share `ProjectCostBudgetPolicy`/`project_cost_budgets_payments` permissions with the existing disbursement resource and the `ProjectCosts/BudgetsRelationManager` — revisit permission-module granularity first if that Resource is ever added.

## Previously Recommended Next Step (2026-07-19, OMS permissions foundation — Task 1, incl. three corrections — DONE, committed prior to Task 2A)
**Implemented and fully verified across four passes.** Permission foundation is live: `App\Support\Permissions\PermissionRegistry` (154 permissions, `module.action` naming), `App\Services\Permissions\PermissionSyncService` + `php artisan oms:sync-permissions` (idempotent, create-only, never deletes/modifies a custom role, warns — but never creates a user — when zero users hold `Super Admin`), a central `Gate::before` Super Admin bypass in `AppServiceProvider`, and `App\Policies\UserPolicy` — which locks the previously wide-open `UserResource` down to Super Admin only. Three corrections landed the same day: (1) `App\Models\User` now implements `Filament\Models\Contracts\FilamentUser` (`canAccessPanel()` = `! $this->trashed()`, entry-only, no permission logic) — closing a real production risk where Filament silently rejected every user in any non-`local` environment, and making the real HTTP authorization tests (`UserResourceLockdownTest`: 403 for normal users, 200 for Super Admin, on the actual `/admin/users` list/create/view/edit URLs) pass under normal `APP_ENV=testing` with no environment workaround; (2) `DatabaseSeeder::seedSuperAdmin()` skips bootstrap-account handling entirely whenever any active user already holds `Super Admin` (checked by role, not email); (3) the seeder's bootstrap email/password are no longer hardcoded at all — new `config/oms.php` (`bootstrap_admin.{email,name,password}`) sources them purely from `OMS_BOOTSTRAP_ADMIN_*` environment variables (no default password), fails with a clear `RuntimeException` if missing, and correctly restores-rather-than-duplicates a soft-deleted user under the configured email (`users.email` is unique). All permissions-related tests together: 38/38 passed, 466 assertions; full suite 247/248 (1 pre-existing unrelated `ExampleTest` failure, same one recorded since 2026-07-15). `grep` confirms zero hardcoded credentials remain in source. This foundation was committed and Task 2A (above) was built directly on top of it. See `docs/TASKS_LOG.md` (four 2026-07-19 entries) for exact commands/counts and `docs/DECISIONS_LOG.md` (twelve 2026-07-19 entries) for every judgment call made along the way.

## Previously Recommended Next Step (2026-07-18, financial transaction balance guard + automated verification — DONE, committed)
**Implemented, fully verified, and committed** as `validate transaction balance with multi-currency support`. All 10 Create/Edit page handlers (5 financial workflows) now build the exact `TransactionLine` payload in memory and validate it with `App\Services\Validation\FinancialTransactionBalanceGuard` — before `DB::transaction()` opens, after `FinancialAmountGuard`/`FinancialAccountGuard` have already run — then insert/update that identical, already-validated array with no recalculation. The guard checks two genuinely-separate same-currency equations for the 2 multi-currency workflows (source-currency conservation, destination FX conversion) rather than a naive cross-currency `SUM(debit_base) == SUM(credit_base)`, uses exact integer-minor-unit comparison everywhere (no `<= 0.01` float tolerance — a one-cent discrepancy is rejected), and independently validates line structure (single side, positive `amount_currency` matching the active side, `fx_rate` as a genuinely required key with no default/fallback anywhere, present/unique `line_role` matching its expected debit/credit direction, optional authoritative notes-tag matching). A same-day correction (before commit) fixed the one gap found during test-writing: `EditProjectCostReceipt`'s in-place update payload now explicitly writes `fx_rate = 1` (it previously omitted the key and let the guard assume `1.0`) — a valid receipt Edit now normalizes any historically-corrupted stored `fx_rate` to `1`, while a rejected Edit still leaves the old line untouched. 36 guard unit tests + 23 integration tests (one class per workflow, plus dedicated `EditProjectCostReceipt` update-in-place and fx_rate-normalization regressions) all pass; every pre-existing test for the 5 workflows and both earlier guards still passes unmodified (151/151); full suite 209/210 (1 pre-existing unrelated `ExampleTest` failure, unchanged from every prior entry in this log). See `docs/AI_PROJECT_MEMORY.md`, `docs/TASKS_LOG.md`, and `docs/DECISIONS_LOG.md` (2026-07-18 balance-guard + correction entries) for full detail.

Open the browser and visually re-confirm a normal valid submission on all 5 create/edit pages still completes and posts a correctly-balanced transaction with correct account-balance updates — this was verified via 236 new/targeted automated tests plus the full suite, not yet in a live browser session. Also decide whether to wire the guard's optional `$expectedNotesByRole` exact-tag-matching parameter into the 3 workflows that use authoritative notes tags (execution payments, project cost budget disbursements, general exchanges) — currently only unit-tested, not used in production, per the decision in `docs/DECISIONS_LOG.md`.

## Previously Recommended Next Step (2026-07-18, server-side financial account validation — DONE)
**Implemented and verified at the code/test level.** The 4 financial workflows that previously relied only on Filament Select filtering (project cost receipts, project cost budget disbursements, general expenses, general exchanges) now independently re-verify every submitted account server-side — existence/not-trashed, account type, bank type, currency, and active-on-Create-or-on-change — via the new `App\Services\Validation\FinancialAccountGuard`, called before every Create/Edit mutation. Execution payments' existing `ExecutionPaymentForm::validateCreditAccount()` gained the same active-account behavior for consistency. The same account is confirmed allowed on multiple roles (debit=credit, source=destination, etc.) — no distinct-account check was added. 60 new/targeted tests pass; full suite 150/151 (1 pre-existing unrelated failure). See `docs/AI_PROJECT_MEMORY.md`, `docs/TASKS_LOG.md`, and `docs/DECISIONS_LOG.md` (2026-07-18 entries) for full detail.

Open the browser (logged in as an admin) and visually confirm on each of the 5 create/edit pages: (1) an account deactivated from `/admin/accounts` no longer appears selectable in a **new** create form (or, if it's still shown by a stale page load, submitting it is rejected with a clear Arabic error); (2) editing an existing record without touching its account fields still saves successfully even if one of its accounts has since been deactivated; (3) switching that same record's account to a different, currently-inactive account is rejected; (4) a normal valid submission on each of the 5 forms still completes and posts a balanced transaction, with correct account balance updates, exactly as before. This was verified via automated tests only (60 new/targeted + full 150/151 suite), not yet visually in a live browser session. Also revisit the item explicitly left out of scope by the earlier positive-amount-validation task: whether the execution-payment over-budget warning should ever become blocking.

## Previously Recommended Next Step (2026-07-18, positive-amount validation — DONE)
**Implemented and verified at the code/test level.** All 5 financial workflows (project cost receipts, project cost budget disbursements, execution payments, general expenses, general exchanges) now reject zero/negative amounts, zero/negative FX rates, out-of-range or combined-to-100 percentages, and zero/negative derived amounts — both via Filament `minValue()`/`maxValue()` on the form and, independently, via the new `App\Services\Validation\FinancialAmountGuard` called server-side before every Create/Edit mutation. 33 new/targeted tests pass; full suite 104/105 (1 pre-existing unrelated failure). See `docs/AI_PROJECT_MEMORY.md`, `docs/TASKS_LOG.md`, and `docs/DECISIONS_LOG.md` (2026-07-18 entries) for full detail.

Open the browser (logged in as an admin) and visually confirm on each of the 5 create/edit pages: (1) submitting an amount of 0 or a negative number shows a clear Arabic validation error and creates nothing; (2) on the two deduction/FX forms (`/admin/general-exchanges/create`, `/admin/project-cost-budgets-disbursements/create`), submitting administrative % + transfer % ≥ 100 is rejected, and submitting `fx_rate` = 0 is rejected instead of silently becoming 1; (3) a normal valid submission on each of the 5 forms still completes and posts a balanced transaction exactly as before. This was verified via automated tests only (33 new/targeted + full 104/105 suite), not yet visually in a live browser session. Also revisit the two audit-flagged items explicitly left out of scope: server-side account/currency/type revalidation for the 4 workflows other than execution payments, and whether the execution-payment over-budget warning should ever become blocking.

## Previously Recommended Next Step (2026-07-16, General Exchange form rearrangement — DONE)
**Implemented and verified at the code/test level.** `GeneralExchangeForm.php` now orders its sections as: top row with "تفاصيل المعاملة" (right) and "النسب والمبالغ" (left) side by side on desktop (stacked, تفاصيل المعاملة first, on narrow screens), then the full-width "الحسابات" section (4 accounts, internally unchanged) underneath, then "المرفقات" last. Previously "الحسابات" sat between/beside the two transaction sections in a visually unbalanced order. No field names, calculations, validation, currency logic, or Create/Edit handlers were touched. 70 relevant existing tests pass (no dedicated form test exists for this page).

Open the browser (logged in as an admin) at a desktop width (≥1024px) on `/admin/general-exchanges/create` and confirm: "تفاصيل المعاملة" renders on the **right** and "النسب والمبالغ" on the **left**, equal width, at the top of the page; the "الحسابات" section (4 accounts: مصدر، نسبة إدارية، تحويل، وجهة) spans full width immediately below; "المرفقات" is last. Narrow the browser below ~1024px and confirm the two top sections stack with "تفاصيل المعاملة" first/above "النسب والمبالغ", followed by "الحسابات" then "المرفقات". Also confirm the "احسب" button, percentage calculations, and all four account cascades (source/admin/transfer/destination) still work exactly as before — this was verified via automated tests only, not yet visually in a live browser session. Repeat the same check on `/admin/general-exchanges/{record}/edit`.

## Previously Recommended Next Step (2026-07-16, responsive credit/debit account layout — DONE)
**Implemented and verified at the code/test level.** `GeneralExpenseForm.php`, `ProjectCostReceiptForm.php`, and `ExecutionPaymentForm.php` now wrap their credit and debit account sections in a shared `Grid::make(['default' => 1, 'lg' => 2])`, credit section first: desktop/wide screens show credit on the right, debit on the left, equal width; narrow/mobile screens stack credit above debit. `ProjectCostBudgetsPaymentForm.php` and `GeneralExchangeForm.php` (multi-account forms) were intentionally left unchanged — see `docs/DECISIONS_LOG.md` (2026-07-16). No field/validation/handler/model/migration changes. 40 targeted tests + full suite (78/79, 1 pre-existing unrelated failure) confirm nothing else was affected.

Open the browser (logged in as an admin) at a desktop width (≥1024px) and confirm on `/admin/general-expenses/create`, `/admin/project-cost-receipts/create`, and `/admin/execution-payments/create`: the credit account card renders on the **right** and the debit/beneficiary account card on the **left**, equal width, side by side, under the main details section and above the attachments section. Then narrow the browser (or use device emulation) below ~1024px and confirm both cards stack to full width with the credit card appearing **above** the debit card. On Execution Payments specifically, also re-confirm the editable credit-account cascade still works exactly as before (auto-default from the selected budget, manual override to a same-currency account, correct hydration on Edit) — the layout change should be purely cosmetic. This was verified via automated tests only; not yet visually in a live browser session.

## Previously Recommended Next Step (2026-07-16, execution payment credit account editable — DONE)
**Implemented and verified at the code/test level.** The Execution Payment form (`/admin/execution-payments/create`/`/edit`) now shows "الحساب الدائن" above "الحساب المدين (المستفيد)", with a real account-type → bank-type → currency → account cascade for the credit account (defaulting from the selected budget's destination account, editable to any other same-currency account before saving). Cross-currency replacement is rejected server-side. The pre-existing Edit drift bug (historical credit account silently overwritten by the budget's current destination account) is fixed. No migration was needed. 7 new tests pass; the full suite is 78/79 (1 pre-existing unrelated failure). See `docs/AI_PROJECT_MEMORY.md` and `docs/TASKS_LOG.md` (2026-07-16 entries) for full detail.

Open the browser (logged in as an admin) and visually verify: (1) on `/admin/execution-payments/create`, selecting a project → cost → budget auto-fills the new credit-account cascade (type/bank type/currency/account) with the budget's destination account, appearing above the beneficiary section; (2) changing the credit account's type/bank type/account to a different same-currency account works and the account list only shows accounts in that currency; (3) submitting creates a payment whose transaction shows the manually-selected account as the credit line in "سطور القيد" and in the parent transaction description; (4) editing that payment shows the previously-selected credit account (not the budget's default) on load; (5) editing an unrelated field (date/notes) and saving does not change which account is credited; (6) attempting to force a cross-currency account (e.g. via browser devtools) is rejected with a clear Arabic validation error and creates nothing. This was verified via 7 new automated tests + the full regression suite, but not yet in a live browser session.

## Previously Recommended Next Step (2026-07-15, operational data cleanup — DONE)
**Executed and verified.** `oms:clean-operational-data --apply` ran successfully at 12:21:16 on 2026-07-15 (backup: `storage/app/backups/oms_before_operational_cleanup.sql`, 120,266 bytes), and again at 12:22:27 to confirm idempotence (zero additional changes). All 12 operational tables are now empty (active+trashed), all 15 accounts' balances are 0.00, and every preserved dataset (2 donors, users/roles/permissions, 3 currencies, exchange-rate history, all reference tables, the `bank_accounts` legacy table, 61 migrations) is confirmed unchanged. Exactly 6 attachment files were deleted; the 6 pre-existing orphan files (706,133 bytes) were left in place, as were the now-empty `receipts/`/`general-expenses/` directories and the `public/storage` symlink. The database is now ready for clean real-data entry.

Open the browser (logged in as an admin) and visually confirm the app behaves correctly on an empty operational dataset: dashboards/list pages for Projects, Transactions, Receipts, Disbursements, Execution Payments, General Expenses/Exchanges all show empty states with no errors; Accounts list shows all 15 accounts with 0.00 balances and unchanged names/codes/currencies; the two donor Partners are still present and editable; creating a brand-new project/transaction/receipt end-to-end produces correct numbering starting from 001 for its prefix (numbering was not explicitly reset — it derives from `MAX()`/`count()` over now-empty tables, so this is expected but worth a visual confirmation). This was verified at the database/filesystem level only (tinker queries + `stat`/`find`), not yet in a live browser session.

Optional follow-ups (not yet decided): (a) the 6 orphan attachment files and the empty `receipts`/`general-expenses` directories under `storage/app/public/` — review and decide whether to remove them manually, they are unrelated to any live DB row; (b) the unexpected legacy `bank_accounts` table (0 rows, no Eloquent model) — decide whether to drop it via a new migration or leave it; (c) consider moving the new backup and the older `storage/app/private/backups/oms_backup_2026-07-06.sql` off-server to durable/offsite storage.

## Previously Recommended Next Step (2026-07-15, transaction descriptions)
Open the browser (logged in as an admin) and visually verify the 2026-07-15 backfill + report-display feature: (1) open `admin/transactions` and `admin/transaction-lines` and confirm every historical row now shows a real "وصف العملية المالية"/"دور سطر القيد"/"وصف سطر القيد" instead of blank; (2) open "تقرير الحركات المالية الشامل", press عرض over a wide date range, confirm the 3 new detail-table columns render with correct values and the Excel/Word exports download and open cleanly; (3) open "تقرير كشف الحساب" for an account with real movements and confirm the same; (4) open a project's "التقرير المالي للمشروع" details page and confirm the "بنود القيد" expandable widget under each receipt/budget/payment row shows the right account/currency/role/description, and the Word export renders the nested subtables; (5) open "تقرير الجهات المانحة" for a donor with real movements and confirm the same expandable widget plus its Excel/Word exports. This was verified via the backfill's own dry-run/apply/idempotence/integrity-snapshot sequence, `php artisan test` (52/52 relevant), `php artisan view:cache` (all Blade compiles cleanly), and a tinker script that generated and re-opened one real sample export per report/format against live dev data — but not yet visually in a live browser session.

## Previously Recommended Next Steps
Open the browser and visually verify the 2026-07-14 line-description feature: create one record in a couple of the 6 financial flows and open the transaction's view page ("سطور القيد" tab) plus `admin/transaction-lines` — confirm each line shows its "دور السطر" Arabic badge and its one-line "وصف السطر", that description search works, that the zero-amount deduction line of a 0%-percentage disbursement/exchange shows "—" (NULL, by design), and that `admin/transaction-lines` now has no create/edit/delete anywhere (list + view only). This was verified via 33 unit tests and a rolled-back tinker run of all 11 create/edit paths against real dev data, but not yet visually in a browser.

Open the browser and create/edit one record in each of the 6 financial flows (receipt, disbursement, execution payment, general expense, general exchange, and an account with an opening balance) to visually confirm the new auto-generated `description` looks correct on the transaction view/list — the 2026-07-14 transaction-description feature was verified via unit tests and a rolled-back tinker script against real dev-DB data, but not yet visually in a live browser session. Also open `/admin/transactions` in the browser (logged in as an admin) to visually confirm: no "إنشاء"/Create button on the list page, no "تعديل"/Edit button on any transaction's view page or table row, no bulk delete action, the description field renders read-only on... actually there is no form to view anymore (view-only page) — confirm the description text is visible on the view page, and that the "سطور القيد" (lines) tab shows lines with no create/edit/delete actions, sorted newest-first. This read-only conversion was verified headlessly (reflection-based `Table`/authorization checks) and via tinker, but not yet in a live browser session.

Open `/admin/project-cost-receipts/1` (and a couple of other receipts) in the browser to visually confirm "صورة الإشعار" now renders — the 2026-07-06 symlink fix was verified at the filesystem/tinker level only, not yet visually in a browser session. Since the missing `public/storage` symlink was a site-wide issue (not specific to this resource), also spot-check one execution-payment proof and one general-expense attachment to make sure those render too. Also open the sidebar (logged in as an admin) to visually confirm "تقارير المشاريع" and "المبالغ المرصودة" no longer appear, and click into "صرف مبلغ المشروع" and "صرف مبالغ التنفيذ" to confirm both still work end-to-end — all three removals so far were verified via `route:list`/file deletion/tinker only, not yet in a live browser session.

Also, create at least one real disbursement (`ProjectCostBudgetsPayments` → "صرف مبلغ") and one execution payment (`ExecutionPayments`) for the seeded donor's project, then re-run the Donor Financial Report and compare its disbursement-source, deductions, final-disbursement, execution-paid, and remaining figures against a manual calculation. This is the one part of the new Donor Financial Report the 2026-07-06 audit could not numerically verify, since the dev DB currently has 0 `project_cost_budgets`/`project_cost_budgets_payments` rows. Also create the first real accounts in the browser (/admin/accounts/create) using the "الرصيد الافتتاحي" section: one account without an opening balance, one with, and one in a second currency; confirm Trial Balance shows متوازن for each currency. Then continue entering real partners, projects, and transactions.

## Pending Items
- Responsive credit/debit account layout (2026-07-16) has not yet been visually verified in a live browser session — see the recommended next step above.
- `ProjectCostBudgetsPaymentForm.php` (صرف مبلغ المشروع) and `GeneralExchangeForm.php` (التحويلات العامة) still render their 4 accounts (1 credit/source + 3 debit) as one unified `Section` rather than the new right/left grid pattern — deliberately deferred (see `docs/DECISIONS_LOG.md`, 2026-07-16) since splitting them apart risks the shared internal `columns(2)` layout. Revisit only as its own explicitly-scoped, tested task if the accounting team wants the visual split.
- Execution Payment credit-account editability (2026-07-16) has not yet been visually verified in a live browser session — see the previously recommended next step below.
- If the business genuinely needs a cross-currency temporary credit account for execution payments (per the "later transfer back" scenario in the original request), that requires a separate, explicitly-approved fx-handling redesign of `CreateExecutionPayment`/`EditExecutionPayment`/`ExecutionPaymentForm` — currently out of scope and hard-rejected server-side (see `docs/DECISIONS_LOG.md`, 2026-07-16).
- Comprehensive Financial Transactions: consider pagination or a row cap if very wide date ranges become slow — the page and exports render all rows.
- Word detail table uses 7pt font with short headers to fit 11 columns on A4 portrait; if accountants find it cramped, consider a landscape section for the detail table only (no existing project pattern for landscape yet).
- Trial Balance Phase 3: opening/closing balance columns, reusing `AccountStatementReportService::calculateOpeningBalance`'s per-account logic (both exports and the on-screen table would need the extra columns).
- ~~Decide what to do with files under storage referenced by the now-purged `attachments` rows~~ — resolved 2026-07-15: those files are exactly the 6 orphans (706,133 bytes, under `payments/`/`execution-payments/`) identified by `oms:clean-operational-data`'s dry-run/apply attachment audit. Still on disk by design (this tool only deletes files deterministically linked to a deleted DB row, never orphans) — review and decide manually whether to remove them.
- Consider moving `storage/app/private/backups/oms_backup_2026-07-06.sql` off the app server to durable/offsite storage, since it's the only copy of the pre-reset data.
- Opening balances: negative opening balances are not supported (form enforces min 0) — if the accounting team ever needs credit-side openings (e.g. liabilities), the account-nature question must be revisited first.
- Deleting an account that has an opening transaction leaves the OPB transaction/lines in place (existing delete behavior untouched, per task constraint) — decide later whether account deletion should be blocked when transaction lines exist.
- Donor Financial Report: numeric validation of disbursement-source, deductions, final-disbursement, execution-paid, and remaining-execution sections is pending real `project_cost_budgets`/`project_cost_budgets_payments` data (see Recommended Next Step above) — cost-side (planned/received) was already validated against real data.
- Donor Financial Report (optional hardening, non-blocking): add a code comment near `executionPaidByProjectCurrency()` noting it relies on a payment's `currency_id` always matching its budget's `disbursement_currency_id`; consider a defensive check or DB-level uniqueness rule later if a future batch-disbursement feature could let multiple `project_cost_budgets` rows share one `transaction_id`.
- ~~Historical transactions have `description = NULL` — no backfill was done (explicitly out of scope for the 2026-07-14 task).~~ — done 2026-07-15: `php artisan transactions:backfill-descriptions --apply` backfilled all 10 deterministically-classifiable dev-DB transactions; safe to re-run after future data imports.
- ~~Historical transaction **lines** likewise have `description = NULL` and `line_role = NULL`.~~ — done 2026-07-15: the same command backfills both fields on all 24 dev-DB lines in the same idempotent run.
- Reports' **calculation logic** (e.g. Donor Financial Report's admin/transfer deduction sums) still matches lines on `transaction_lines.notes` `LINE_*` tags, unchanged — migrating a *calculation* reader from `notes` to `line_role` is still a separate future task requiring a full report-impact audit first (per the 2026-07-14 decision). Do not remove or change the `LINE_*` constants until then. (As of 2026-07-15, `line_role`/`description` are already used for *display* in Comprehensive Financial Transactions, Account Statement, Project Financial Details, and Donor Financial Report — display use is approved and done; this bullet is about calculation use only.)
- If a genuinely unclassifiable historical transaction is ever found (should not be possible given `TransactionResource`'s read-only hardening, but e.g. after a raw SQL import or restored backup), `php artisan transactions:backfill-descriptions --dry-run` will list it by id/reason and leave it untouched rather than guess — review any such row manually before deciding whether to leave it undocumented or classify it by hand. None exist in the dev DB as of 2026-07-15 (0/10 unclassified).
- ~~Consider locking down `description` on the raw `admin/transactions` (`TransactionResource`) CRUD form so it can't silently overwrite the generated value~~ — done 2026-07-14: the whole resource is now read-only/audit-only (no create/edit/delete/restore/force-delete on transactions or lines, authorization-hardened).
- The repo cannot run a full fresh `migrate`/`RefreshDatabase` end-to-end: `2026_06_24_000005_backfill_denormalized_currency_and_amounts.php` uses MySQL-only `UPDATE ... JOIN` raw SQL that SQLite can't execute. Only relevant if/when broader `RefreshDatabase`-based test coverage is wanted — would need explicit approval to rewrite that migration's SQL to be cross-database compatible (real financial backfill logic).

## Risks To Review
- Comprehensive report: per-currency status can legitimately read "غير متوازن" for periods containing cross-currency transactions (debit leg in one currency, credit leg in another). Expected, but may need a UI/export footnote if accountants find it alarming.
- Comprehensive report excludes lines whose account is soft-deleted (inner join on `accounts` with `deleted_at IS NULL`, per task spec) — same caveat as Trial Balance: raw ledger sums could differ if soft-deleted accounts hold in-range lines.
- Soft-deleted `accounts` that still have in-range `transaction_lines` are silently excluded from the Trial Balance report (since the account list is driven by `Account::query()`, which applies the SoftDeletes scope). This matches the account-driven design but means a raw ledger sum across all lines regardless of account soft-delete status could differ from the report's grand totals — worth a footnote if it ever causes confusion.
- No currency is guaranteed to be flagged `is_base` (the toggle has no uniqueness constraint in `CurrencyForm`) — the Trial Balance page falls back to the first currency by id, which is a reasonable default but worth confirming with the accounting team if multiple currencies could plausibly be marked base.
- Donor Financial Report's admin/transfer deduction join (`transaction_lines.transaction_id = project_cost_budgets.transaction_id`) assumes each disbursement transaction is created fresh and belongs to exactly one budget row — true today (confirmed by tracing `CreateProjectCostBudgetsPayment`), but not enforced by a DB constraint. Revisit if a batch-disbursement feature is ever added.
- Donor Financial Report's execution-paid grouping trusts `project_cost_budgets_payments.currency_id` to already equal the linked budget's `disbursement_currency_id` (enforced today by `ExecutionPaymentForm::budgetCurrencyId()` at creation time, not by a DB constraint) — same class of implicit-invariant risk as the point above.
